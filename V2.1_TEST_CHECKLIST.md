# ONHARU 2.1 시험 순서

## 2026-08-20 후속 UI 수동 확인

- [ ] 오른쪽 Todo 일정은 체크박스를 눌렀을 때만 완료 상태가 바뀌고, 제목 한 번 클릭으로는 바뀌지 않는다.
- [ ] 오른쪽 일정 본문 더블클릭은 일정 수정창을 연다.
- [ ] 시간표 신규 기본값은 9교시이며 기본 창에 불필요한 회색 빈 공간이 보이지 않는다.
- [ ] 시간표 헤더를 잡아 독립적으로 이동할 수 있고 달력은 고정 상태를 유지한다.
- [ ] 시간표 저장 후 창은 유지되고 설정 패널만 닫히며 설정과 과목·장소가 함께 보존된다.
- [ ] 설정의 RGB 펼치기는 왼쪽, 색상 교환은 오른쪽에 정렬된다.
- [ ] Google 계정 카드를 누르면 연결 또는 동기화되고 상단 별도 동기화 버튼은 표시되지 않는다.
- [ ] 설정의 G 계정 팝업에서 취소는 기존 연결을 유지하며 변경·로그아웃을 선택한 경우에만 후속 동작을 수행한다.
- [ ] 고정 상태에서 투명도 손잡이를 연속으로 움직여도 전체 프레임 깜빡임과 끊김이 이전보다 줄어든다.

2.0 사용자 데이터와 Explorer 상태를 보호하기 위해 아래 순서대로 확인한다. 2.0과 2.1은 같은 단일 실행 mutex와 데이터 폴더를 사용하므로 동시에 실행되지 않는다.

## 0. 자동 검사

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File ".\check-v21.ps1" -Build
```

마지막 줄이 `ONHARU 2.1 quality checks passed.`인지 확인한다.

## 1. 시작과 레이어

1. ONHARU 2.0을 종료한다. xDiarys 동시 실행 여부는 제한하지 않는다.
2. `Tests\LocalTest\ONHARU-2.1-local-test.exe`를 실행한다.
3. 하단 표기가 `ver. 2.1.0`인지 확인한다.
4. 고정 상태에서 달력 위 바탕화면 아이콘 선택·더블클릭·드래그가 된다.
5. 달력 빈 셀 클릭·더블클릭, 오른쪽 상세 체크박스가 한 번에 반응한다.
6. 위치 조정과 고정을 왕복해 위치·크기·투명도가 유지되고 회색 첫 프레임이나 깜빡임이 없다.
7. 위치 조정 상태에서 네 모서리와 상·하·좌·우 가장자리를 각각 끌어 8방향 크기 조절이 된다.
8. 고정→이동과 이동→고정을 각각 반복해 후보 17보다 깜빡임이 악화되지 않고 첫 클릭과 커서가 즉시 동작하는지 확인한다.

### 시간표

1. 설정의 `시간표 기능`을 켜면 `오늘` 왼쪽에 시간표 아이콘이 나타난다.
2. 팝업에서 월~일 표시 요일, 1~12교시, 시작 시각, 수업·쉬는 시간을 설정한다.
3. 과목·장소가 가운데 정렬되고 저장 후 재실행해도 유지된다.
4. 가로·세로 스크롤, 설정 접기, 헤더 저장·닫기가 동작한다.

## 2. 2.1 새 일정 편의

1. 설정에서 새 일정 기본 캘린더, 종일/시간, 시작 시간, 소요 시간, 알림을 바꿔 저장한다.
2. 새 일정에서 기본값이 적용되는지 확인한다.
3. 기존 일정을 열었을 때 기존 시간·기간·알림이 바뀌지 않는지 확인한다.
4. 등록·수정창의 알림 선택은 `없음`, `정시`, `직접 선택`만 표시되고 직접 선택의 분/시간/일 값이 다시 열어도 보존되는지 확인한다.
5. 등록·수정창 상단의 `저장`과 하단의 `일정 저장`이 동일하게 저장되며, 작은 노트북 화면에서는 스크롤로 하단 버튼에 접근할 수 있는지 확인한다.

## 3. 탐색과 표시

1. 달력 제목을 눌러 다른 연·월로 바로 이동한다.
2. 검색에서 과거/미래/전체/사용자 지정 기간을 바꾸고 종료일 일정도 포함되는지 확인한다.
3. 완료 일정을 그대로/흐리게/숨김으로 바꾸고 달력과 오른쪽 상세가 함께 변하는지 확인한다.
4. 시작 화면을 마지막 날짜로 설정해 종료·재실행 후 같은 날짜가 열리는지 확인한다.

## 4. 반복과 D-Day

1. 로컬 반복 일정을 3회 종료로 만들고 정확히 3개만 표시되는지 확인한다.
2. 동일 항목을 Google 캘린더에 저장·수정·삭제해 왕복 동기화한다.
3. D-Day 표시를 켜고 과거·오늘·미래 일정이 각각 D+, D-Day, D-로 보이는지 확인한다.
4. 오른쪽 D-Day 카드가 일반 D-Day 일정만 표시하고 기본 5개·더보기·접기/펼치기가 동작하는지 확인한다.
5. 기념일을 생일/결혼기념일/기타로 등록하고 최초 기준일, 누적 일수, 다음 기념일 및 D-Day 표시 선택이 보존되는지 확인한다.
6. 기념일은 오른쪽 기념일 카드의 `만들기`에서만 등록되고 일반 일정 등록·수정창에는 기념일 전환 항목이 없는지 확인한다.
7. 기념일 수정창에서 `일정으로 변경`을 누르면 일반 로컬 일정이 되고, D-Day 표시가 선택된 경우 오른쪽 D-Day 카드에 나타나는지 확인한다.
8. `온하루 등록`의 D-Day/기념일 체크가 각각 해당 오른쪽 카드 표시를 즉시 전환하고 재실행 후에도 보존되는지 확인한다.

## 5. 알림과 시작 상태

1. 소리 사용을 끄면 알림 팝업만 나오고 소리는 나지 않는다.
2. 현재 시간을 조용한 시간 안/밖으로 설정해 소리만 억제되는지 확인한다.
3. 시작 상태를 이전 상태/항상 고정/항상 위치 조정으로 각각 저장하고 재실행한다.

## 6. 시스템 복구

1. 보조 모니터에서 종료 후 모니터를 분리하고 다시 시작해 창이 주 화면 안으로 복귀한다.
2. Explorer를 다시 시작한 뒤 ONHARU 달력이 자동으로 다시 붙고 클릭이 정상인지 확인한다.
3. 실패 시 `%LOCALAPPDATA%\OnharuV3\logs`의 최신 로그와 화면 캡처를 보관한다.

## 7. 데이터 호환과 내보내기

1. 최초 실행 후 `%LOCALAPPDATA%\OnharuV3\pre-2.1-backup`에 기존 설정·일정과 `completed.txt`가 있고 Google 토큰은 없는지 확인한다.
2. 기존 2.0 일정의 제목·시간·완료·색상·Google 소유 캘린더가 그대로인지 확인한다.
3. 로컬 JSON을 가져와 신규/변경 항목 미리보기와 선택 가져오기가 동작하는지 확인한다.
4. Excel 전체 일정 내보내기에서 D-Day, 반복, 알림, 출처 열이 보이는지 확인한다.

## 합격 조건

- 기존 일정·설정·Google 연결 데이터가 유지된다.
- 2.0에서 안정화한 아이콘 클릭·드래그·선택 해제와 레이어 깜빡임이 악화되지 않는다.
- 새 기능 저장 후 재실행 및 Google 왕복에서 값이 보존된다.
- 화면 잘림, 정렬 이탈, 과한 공백이 없다.
- 시작 직후 회색 임시 일정이나 한 줄로 몰린 프레임이 먼저 보이지 않고, 종료 확인창을 열 때 달력 전체가 번쩍이지 않는다.

## 8. 시간표·계정·색상 편집 최종 확인

1. 시간표 기본 창에서 9교시가 외곽선 안에 맞고 네 모서리 선이 잘리지 않는지 확인한다.
2. 시간표 글자 크기 작게/보통/크게가 과목·교시·시간에 함께 적용되고 재실행 후 유지되는지 확인한다.
3. 시간표 상단의 제목 외 빈 영역을 잡아도 이동되며 설정·저장·닫기 버튼은 정상 클릭되는지 확인한다.
4. Google 계정 카드 클릭 시 `동기화 중`과 완료/오류 상태가 카드에 표시된 뒤 계정명으로 돌아오는지 확인한다.
5. 설정의 `G 계정` 팝업에 현재 계정명이 표시되는지 확인한다.
6. 색상 체크박스 두 개를 고르면 나머지가 비활성화되고 하나를 해제하면 모두 다시 활성화되는지 확인한다.
7. `☑ ⇄ ☑` 버튼으로 선택한 두 색상이 정확히 교환되는지 확인한다.
8. 고정 상태에서 투명도 손잡이를 천천히/빠르게 움직여 달력 밝기가 자연스럽게 바뀌고, 놓은 뒤 손잡이 위치와 저장값이 일치하는지 확인한다.
9. 시간표를 10~12교시로 바꾸고 세로 스크롤해도 요일 헤더와 왼쪽 교시·시간 열이 고정되는지 확인한다.
10. 고정 상태에서 투명도 트랙 위를 누르지 않고 마우스만 움직일 때 값이 변하지 않으며, 손잡이를 잡았을 때만 연속 조절되는지 확인한다.
11. 설정 창을 연 채 메인 종료 버튼에서 `종료`를 선택하면 설정·메인·트레이·LayerHost가 모두 종료되는지 확인한다.
12. 같은 상태에서 `취소` 또는 `최소화`를 선택하면 설정 창과 직전 화면 상태가 의도대로 유지되는지 확인한다.
