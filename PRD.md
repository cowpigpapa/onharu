# 온하루(ONHARU) 제품 요구사항 문서

> 이 문서는 V1 1.2.1의 제품 요구사항 원본을 역사적 기준으로 보존한 파일이다. 현재 2.1의 추가 요구사항과 완료 상태는 `TODO_NEXT.md`, 실제 구조와 시험 기준은 `PROJECT_INDEX.md`, `V2.1_TEST_CHECKLIST.md`를 우선한다.
>
> 현재 개발·배포 상태는 `CURRENT_STATUS.md`를 우선한다. 제품 요구사항을 변경하면 이 문서와 사용자 설명 `README.md`, 현재 상태, TODO 및 HANDOFF를 같은 작업에서 갱신한다.

문서 상태: 1.2.1 배포 후보 문서
기준 빌드: `ONHARU 1.2.1`
정식 배포판 표기: `MADE BY JUAN.HJLEE · ONHARU (ver. 2.1.0)`
대상 운영체제: Windows 전용

## 1. 제품 정의

온하루는 xDiarys/DesktopCal 계열의 바탕화면 월간 달력 경험에 로컬 일정·Todo와 Google Calendar 양방향 동기화를 결합한 Windows 앱이다. 사용자는 다른 업무 창을 가리지 않으면서 바탕화면에서 일정을 상시 확인한다.

### 목표

- 부부가 각자의 Google 계정을 바꿔가며 사용할 수 있는 개인 일정 도구
- 하루 종일 일정과 시간 Todo를 한눈에 구분
- 업무/개인 및 Google 캘린더별 색상·필터·순서 제공
- 별도 서버와 월 운영비 없이 로컬 + Google Calendar로 운영
- 데이터 손실 없이 장기간 사용할 수 있는 Windows 바탕화면 앱

### 비목표

- 현재 단계에서 모바일/PWA, 자체 회원 서버, 실시간 팀 댓글, 담당자 워크플로는 만들지 않는다.
- Google Tasks 전용 API 클라이언트가 아니며 Calendar에 노출된 Tasks의 원문/완료 상태를 Google에 강제 수정하지 않는다.
- 일반 프로그램 위에 떠 있는 always-on-top 앱이 아니다.

## 2. 핵심 사용자 경험

1. 앱은 배경화면과 바탕화면 아이콘 위, 일반 프로그램 창 아래에 위치한다. 아이콘 영역은 클릭 통과로 조작한다.
2. 기본 위치는 잠겨 있으며 위치·크기·잠금 상태를 기억한다.
3. 월간 달력에서 날짜 빈 공간을 더블클릭하면 신규 일정, 일정 자체를 더블클릭하면 수정창을 연다.
4. 시간이 있는 일정과 로컬 업무/개인 하루 종일 일정은 완료 가능한 Todo다.
5. 오른쪽 패널에서 선택 날짜의 일정을 카테고리별로 확인하고 완료 처리한다.
6. Google 연결 시 선택한 캘린더의 일정이 자동으로 합쳐진다.

## 3. 기능 요구사항

### 3.1 바탕화면 창

- 실제 투명 배경과 사용자 설정 투명도를 지원한다.
- 캘린더는 배경화면과 바탕화면 아이콘보다 위, 일반 프로그램 창보다 아래에 유지한다.
- 현재 WPF 안정판은 바탕화면 아이콘을 캘린더 위에 합성하지 못한다. 아이콘은 클릭 통과로 조작하며, 아이콘 아래 렌더링은 별도 네이티브 프로젝트로 분리한다.
- 여러 날짜에 걸친 Google 일정은 시작일부터 종료 직전 날짜까지 각 날짜에 표시한다.
- 월간 달력에서 여러 날짜 일정은 한 주 안에서 하나의 막대가 날짜 칸을 가로지르며, 다음 주에는 새 줄에서 이어진다.
- 일정 막대의 날짜 칸 부분을 한 번 클릭하면 해당 날짜를 선택하고 오른쪽 패널을 갱신하며, 더블클릭은 해당 일정을 수정한다.
- 연속 일정은 `항상 위에 표시`하거나 기존 카테고리·시간 설정 순서를 유지하도록 선택한다.
- 표시 옵션에서 24시간제와 오전·오후 표시를 선택하며, 시간 일정은 오른쪽 패널에서 시간과 제목을 한 줄로 표시한다.
- 일정 등록·수정 팝업을 열고 닫을 때도 메인 캘린더가 일반 프로그램 창 위로 순간 이동하지 않는다.
- Progman 소유 및 Windows 하단 Z-order를 유지한다.
- 클릭 통과/편집 상태를 전환할 수 있어야 한다.
- 위치 고정 상태를 압정 아이콘과 문구로 명확하게 표시한다.
- 이동 가능 상태에서는 상단의 버튼을 제외한 빈 공간으로 이동한다.
- 저장 위치가 현재 연결된 어떤 모니터에도 충분히 보이지 않으면 주 모니터 작업영역 중앙으로 자동 복귀한다.
- 실행 중 모니터가 분리되어 창이 화면 밖으로 나가면 자동 복귀하며, 트레이 메뉴에서 `현재 화면으로 가져오기`를 수동 실행할 수 있다.
- 크기 변경 핸들은 왼쪽 위와 오른쪽 아래의 카드 테두리에 둔다.
- 오른쪽 일정 패널을 접으면 달력 영역이 확장된다.
- 시스템 트레이에서 표시, 모드 전환, 종료가 가능해야 한다.

### 3.2 달력 표시

- 월간 보기, 이전 달/다음 달/오늘 이동을 제공한다.
- 토요일은 파랑, 일요일과 국경일은 빨강이다.
- 오늘은 옅은 핑크 배경과 파란 `오늘` 표기를 사용한다.
- 오늘과 휴일이 겹치면 `오늘 · 광복절` 순서로 표시한다.
- 중요 일정은 `★`, 핫핑크, 굵은 글씨로 표시한다.
- 일정 칩은 살짝 둥근 사각형이며 한 날짜에 최대 6개를 표시한다.
- 주차 표시 여부와 ISO/일반 계산 기준을 설정한다.
- 음력 날짜 표시 여부를 설정한다.
- 24절기 표시 여부를 설정하고 절기명은 날짜 옆의 작은 보조 텍스트로 표시한다.
- 선택 날짜에 일정 칩과 별개인 옅은 배경색을 지정·해제하며 재실행 후에도 유지한다.

### 3.3 일정 등록·수정

- 필수: 제목, 날짜, 카테고리.
- 선택: 시간, 분, 메모, 중요, 알림, 반복, 반복 종료일, 자동 이월.
- 하루 종일 일정은 `여러 날 일정`과 종료 날짜를 선택해 로컬 또는 쓰기 가능한 Google 캘린더에 연속 일정으로 등록·수정할 수 있다.
- 종료 날짜는 화면에서 포함 날짜로 선택하고 Google Calendar에는 API 규칙에 맞는 다음 날 배타 종료일로 저장한다.
- 여러 날 일정은 반복 `없음`과 `매주`만 지원한다. 매주는 시작 요일부터 동일한 기간으로 반복하며 매일·매월·매년은 비활성화한다.
- `★ 중요 일정`은 제목 레이블 오른쪽에 배치하고 반복 종료일은 반복 제목과 같은 행에 표시해 입력창 높이를 줄인다.
- 시간·카테고리 카드의 중복 제목 문구는 표시하지 않는다. 수정창 출처는 본문 카드 대신 헤더 옆의 `온하루 등록` 또는 `Google Calendar` 배지로만 표시한다.
- 제목이 비어 있으면 팝업 내부에 `제목을 입력해 주세요`를 2초간 표시한다.
- 신규 등록 날짜는 사용자가 더블클릭한 날짜다.
- 신규 등록과 수정 창은 상단의 버튼을 제외한 빈 공간 어디서든 드래그하여 이동할 수 있다.
- 수정 시 커스텀 달력으로 날짜를 바꿀 수 있고 바깥 클릭 또는 날짜 더블클릭으로 적용한다.
- Enter로 저장하되 메모 입력 중에는 줄바꿈을 방해하지 않는다.
- 읽기 전용 일정은 수정창을 열지 않고 해당 일정 가까이에 안내한다.

### 3.4 시간과 Todo

- 기본 시간 모드는 `하루 종일`이다.
- 시간 모드는 하루 종일/오전/오후, 시는 1~12, 분은 0/15/30/45다.
- 시간이 있으면 `IsTodo=true`다. 로컬 업무/개인 하루 종일 일정도 자동으로 `IsTodo=true`다.
- Google 하루 종일 일정은 기존 캘린더 일정의 의미를 유지하기 위해 `IsTodo=false`다.
- 설정에서 완료된 Todo를 미완료 일정 뒤로 이동하도록 선택하며 기본값은 켜짐이다.
- Google Tasks 원본의 완료 체크는 로컬 상태로만 유지한다.

### 3.5 카테고리와 색상

- 로컬 카테고리: 업무일정, 개인일정.
- 로컬 업무일정과 개인일정은 Google과 동기화하지 않는다. 사용자가 등록창에서 Google 캘린더를 직접 선택한 경우에만 Google 대상이다.
- Google 카테고리는 계정의 Calendar List에서 동적으로 생성한다.
- 표시 순서는 로컬과 Google 항목 모두 사용자가 바꿀 수 있다.
- 일정 정렬은 `카테고리별·하루 종일 우선` 또는 `전체 시간순`이다.
- 추천 색상 조합, 직접 RGB/HEX 선택, 두 색상 선택 후 교환을 제공한다.
- 기존 `네온 베리` 자리는 `내 설정`으로 사용하며, 현재 로컬·Google 색상과 일정 칩 스타일을 사용자 팔레트로 저장해 재실행 후에도 적용한다.
- 기본 저장 대상은 `내 설정`이고, 다른 추천 라디오를 선택하면 해당 팔레트의 색과 사용자 지정 이름을 함께 덮어쓸 수 있다. `Google 기본`은 덮어쓰지 않는다.
- 팔레트 이름 Enter와 `색상 저장`은 설정창 내부 상태만 바꾸며, 하단 `설정 저장`에서 최종 영구 저장한다. `초기 설정`은 변경이 있을 때만 활성화해 기본 이름과 색으로 되돌린다.
- `Google 기본` 선택 시 이름 입력창은 `Google 기본 (변경 불가)`를 회색으로 표시하고 색상 저장을 막는다.
- 추천 조합은 로컬 2개와 Google 캘린더 최대 8개까지 서로 다른 조화색을 배정하고 휴일은 별도 빨강으로 유지한다.
- 추천 선택지는 선명 5개, 파스텔 4개, 각 캘린더의 Google 원래 색을 복원하는 `Google 기본`까지 총 10개다.
- 달력은 진한색+흰 글씨 또는 파스텔+검정 글씨 스타일을 지원한다.
- 오른쪽 패널은 같은 색 계열의 파스텔 카드로 표시한다.
- 휴일은 팔레트와 무관하게 빨간 계열을 사용한다.

### 3.6 Google 계정과 동기화

- Desktop OAuth, 시스템 브라우저, localhost 임시 콜백, PKCE를 사용한다.
- 로그인 응답 제한은 1분이며 사용자가 앱에서 취소할 수 있다.
- refresh token은 Windows DPAPI CurrentUser로 암호화한다.
- 계정 변경 전 현재 로컬 데이터를 로컬 계정 파일에 보존한다.
- EXE 파일명이 달라도 Windows 사용자당 온하루 인스턴스는 하나만 실행한다.
- 암호화 토큰과 실제 Google 기본 계정 ID를 함께 저장해 토큰·계정 파일 불일치를 방지한다.
- 성공적인 동기화 후 현재 계정의 Calendar List에 없는 외부 일정과 잘못 섞인 표시 순서를 정리한다.
- 로그아웃 상태에서도 로컬 일정을 사용할 수 있다.
- Google 연결 후 로컬 일정 가져오기 목록에서 선택적으로 복사한다.
- 캘린더 목록, 표시 여부, 색상, 읽기/쓰기 설정은 계정마다 다르게 적용한다.
- Google access role이 owner/writer인 경우에만 수정 가능 설정을 허용한다.
- 휴일 캘린더는 항상 읽기 전용이다.
- 수동 동기화 성공은 1초간 인라인으로 표시한다. 실패는 `연결 실패 · 자동으로 다시 시도합니다`로 표시한다.
- 자동 동기화: 사용 안 함/5/15/30/60분.
- 실패한 쓰기는 로컬에 보존하고 실제 GoogleCalendarId가 있는 항목만 `동기화 대기`로 센다.
- 오프라인과 대기 일정이 동시에 존재하면 계정 상태에 `오프라인 (동기화 대기 N건)`으로 함께 표시한다.
- 계정 상태 문구가 영역보다 길면 자동으로 좌우 이동하며, 전체 문구는 툴팁으로도 확인한다.
- HTTP/API 형식·권한 오류를 네트워크 오프라인으로 표시하지 않고 원인별로 구분한다.
- Google 네트워크 요청이 8초 안에 응답하지 않으면 오프라인으로 판정하고 로컬 대기로 전환한다.
- 계정 상태 영역은 대기 건이 있을 때만 상세 목록을 열며, 재시도 실행은 상단 `G 동기화`로 통일한다.
- 대기 건이 없을 때 계정 상태를 누르면 팝업 대신 짧은 동기화 완료 문구를 표시한다.
- 동기화 조회 범위는 오늘 기준 과거 1년~미래 2년이다.
- Calendar List와 일정 목록은 `nextPageToken`이 끝날 때까지 모든 페이지를 읽는다.
- 일정 페이지를 모두 받지 못하면 해당 캘린더의 누락 항목을 삭제로 판단하지 않는다.
- Google 확장 메타데이터의 알림·이월·반복 값은 앱이 지원하는 범위로 검증한 뒤 적용한다.

### 3.7 반복 일정

- 빈도: 없음, 매일, 매주, 매월, 매년.
- 매일은 `매일` 또는 토·일을 제외한 `평일만`을 선택한다.
- 매주는 월~일을 복수 선택하며 최소 한 요일을 사용한다.
- 매월은 `같은 날짜`, `n번째/마지막 같은 요일`, `마지막 날`을 선택한다.
- 매년은 시작일과 같은 월·일에 반복한다.
- 종료일 기본값: 시작일로부터 1년.
- 수정 기본값: 전체 반복 일정. `이번 일정만 변경` 체크 시 그 인스턴스만 변경한다.
- 삭제 시 반드시 범위 선택창을 표시한다.
  - 이번 일정만: 해당 인스턴스만 삭제
  - 이번 일정부터 미래: 과거는 유지하고 선택일 직전으로 반복 종료
  - 과거 포함 전체: 반복 부모와 모든 인스턴스 삭제
- Google의 부모 이벤트 ID와 인스턴스 이벤트 ID를 혼동하지 않는다.
- Google RRULE 종료일은 종일 일정에 `yyyyMMdd`, 시간 일정에 UTC date-time 형식을 사용한다.
- Google 시간 일정은 반복 여부와 관계없이 시작·종료에 `Asia/Seoul` 시간대를 명시한다.
- 로컬 반복 시 전체 수정은 형제 일정의 제목, 메모, 카테고리, 시간, 알림, 중요, 종료일을 일관되게 갱신한다.
- 과거 버전에서 `SeriesId` 없이 저장된 로컬 반복 일정은 시작 시 한 번 복구한다.

### 3.8 자동 이월

- 시간 Todo에서만 활성화한다.
- 기본은 이월 안 함이다.
- 다음 날, 다음 주 같은 요일, 다음 평일을 지원한다.
- 완료되지 않았고 시작일이 오늘 이전인 항목만 한 번 이동한다.
- Google Tasks 원본에는 자동 이월을 적용하지 않는다.

### 3.9 알림

- 없음, 정시, 10분 전, 30분 전, 하루 전.
- 앱 실행 중 30초마다 검사한다.
- 하루 종일 일정 알림 기준 시각은 해당일 오전 9시다.
- 중앙 알림창에서 확인 또는 다시 알림을 선택한다.
- 같은 일정/시각/알림 조합은 중복 표시하지 않는다.
- 잘못된 일정 하나가 다른 알림 검사나 앱 실행을 중단시키지 않도록 일정별·타이머별 예외를 격리한다.
- 앱이 종료되어 있으면 온하루 자체 알림은 동작하지 않는다는 점을 설명서에 명시한다.

### 3.10 검색·백업·복원

- 검색 대상: 제목과 메모.
- 검색 범위: 오늘 기준 ±1년.
- 기본 목록은 날짜 오름차순이며 오늘이 화면 중앙에 보이도록 스크롤한다.
- `오늘` 버튼으로 즉시 돌아온다.
- 일정 JSON은 계정별로 저장한다.
- 원자 저장과 프로세스 간 mutex로 손상을 방지한다.
- Google 일정은 Google에서 관리하며 자동 백업에 포함하지 않는다.
- ONHARU 로컬 일정만 계정별 일일 백업 최대 30개를 보존하고 설정에서 복원한다.
- 사용자가 외부 폴더를 지정하면 `ONHARU-Backups` 하위에 로컬 일정 백업 최대 30개와 최신 설정 파일을 저장한다. 외부 백업 실패는 기본 로컬 저장을 막지 않는다.
- 백업 복원창은 기본 백업 폴더와 지정 백업 폴더를 별도 그룹으로 표시하고 어느 쪽의 날짜 파일이든 복원할 수 있어야 한다.
- 오른쪽 일정 패널은 `선택일 / 이번 주 / 다음 주` 보기를 제공하며 주 시작일은 기존 ISO/일반 주차 기준을 따른다.

## 4. 데이터 모델

### PlannerItem 핵심 필드

| 영역 | 필드 |
|---|---|
| 기본 | Id, Title, Start, End, AllDay, Notes, Category |
| Todo | IsTodo, Completed, RolloverMode, AutoRollover |
| 표현 | Important, ReminderMinutes, ReminderConfigured |
| 반복 | RecurrenceFrequency, RecurrenceUntil, SeriesId |
| Google | GoogleCalendarId/Name/Color, GoogleEventId, GoogleRecurringEventId, GoogleReadOnly, GoogleTaskEvent, PendingGoogleSync |
| 알림 상태 | SnoozeUntil, ReminderDismissedKey |

### 저장 위치

- 기본 폴더: `%LOCALAPPDATA%\FamilyPlanner`
- 일정: 계정 ID를 해시한 `items-*.json`, 로그아웃 시 로컬 파일
- 설정: `settings.json`
- OAuth 토큰: DPAPI 암호화 파일
- 백업: 계정별 날짜 파일, 최근 30개
- 날짜 배경색: `settings.json`의 `yyyyMMdd → 색상` 매핑

계정 파일명과 내부 형식은 마이그레이션 없이 변경하지 않는다. 변경 시 기존 파일을 먼저 복사하고 하위 호환 로더를 제공한다.

## 5. 비기능 요구사항

- 일정 저장 실패가 앱 중단이나 기존 파일 손상으로 이어지지 않아야 한다.
- 네트워크 없이도 로컬 열람·등록·수정이 가능해야 한다.
- 일반 창 활성화 시 온하루가 그 위로 튀어 오르지 않아야 한다.
- 사용자 조작 후 시스템 MessageBox를 띄우지 않는다.
- 빨간색 오류·실패·입력 경고는 다른 메시지가 먼저 발생하지 않는 한 약 5초간 표시한다. 성공 안내는 기존의 짧은 노출 시간을 유지한다.
- 설정/등록/검색/백업/삭제창은 같은 닫기 위치, 둥근 모서리, 버튼 높이와 색상 체계를 사용한다.
- 설정창과 일정 등록·수정창은 현재 모니터 작업영역보다 높아지지 않으며, 내용이 넘을 때만 세로 스크롤을 표시한다.
- 긴 팝업은 제목·닫기 버튼이 있는 고정 헤더 아래에서 내용만 스크롤하며, 스크롤바는 9px 파스텔 스타일을 사용한다.
- 스크롤바는 화살표 없이 둥근 파스텔 Thumb과 투명 트랙을 사용한다.
- 큰 모니터에서도 팝업 내용 영역은 현재 노트북의 새 일정창 높이인 992px을 최대값으로 사용한다.
- 설정의 Google 수정 권한과 표시 옵션은 2열 배치로 세로 공간을 절약한다.
- 별도 런타임 설치가 없는 단일 EXE 개발판을 유지한다.

## 6. 보안 및 개인정보 주의사항

- Desktop OAuth는 PKCE를 사용한다. Desktop client credential은 설치 앱에서 안전한 비밀로 간주할 수 없으므로 공개 저장소와 로그에는 기록하지 않는다.
- 현재 Testing 상태와 등록된 3명의 사용자 범위에서는 기존 Desktop credential을 유지한다. 전체 공개 전 별도 Production 프로젝트와 PKCE-only 동작을 다시 검증한다.
- refresh token은 평문 JSON이나 로그에 기록하지 않는다.
- 오류 기록을 추가할 때 일정 제목/메모와 OAuth 토큰을 기본적으로 마스킹한다.
- 정식 배포 전에 OAuth 동의 화면 게시/검증 여부와 최소 scope를 재검토한다.

## 7. 인수 기준

- 앱 재실행 후 창 위치·크기·잠금·필터·색상·계정별 일정이 동일하다.
- 일반 프로그램 창은 온하루 위에서 정상 동작하고, 바탕화면 아이콘은 클릭 통과 상태에서 조작된다.
- 신규, 수정, 삭제, 완료, 이월, 알림, 반복의 결과가 재실행 후 유지된다.
- Google 신규/수정/삭제가 양방향으로 일치하고 실패 시 로컬 데이터가 사라지지 않는다.
- 반복 일정 세 삭제 범위가 로컬과 Google에서 각각 정확히 동작한다.
- 읽기 전용 캘린더와 Google Tasks 제한이 UI에서 명확하며 잘못된 API 쓰기를 시도하지 않는다.
- 강제 종료 또는 동시 저장 후에도 JSON이 파싱 가능하고 백업 복원이 가능하다.

### 확인된 인수 시험

- 2026-08-08, 남편·와이프 두 Google 테스트 계정으로 다음 항목을 모두 통과했다.
  - 와이프 Google 일정 조회
  - 남편 Google 일정과의 분리
  - 온하루 로컬 일정의 계정별 분리
  - 와이프 Google 캘린더 신규 일정 등록
  - 시간 반복 일정 등록
  - Google 일정 수정 및 삭제
  - 로그아웃 후 와이프 계정 재연결
  - 남편 계정으로 재전환 후 기존 일정 복원

## 8. 향후 로드맵

### 안정화 우선

- 반복 일정 예외, 월말, 윤년, DST/시간대 테스트
- 네트워크 단절·복구, OAuth 취소·만료·계정 전환 장시간 시험
- 동기화 충돌과 API 오류별 사용자 안내 개선
- 구조화 오류 로그 및 개인정보 마스킹

### 3.11 일정 내보내기

- `일정 가져오기 (온하루)`와 `일정 내보내기 (온하루)`는 로컬 일정만 처리하며 Google 일정은 항상 제외한다.
- ONHARU JSON은 로컬 일정 백업·이동용이다.
- `Excel로 내보내기`는 조회용으로 로컬·Google 전체 일정을 CSV에 포함하고 출처를 구분한다.
- CSV는 표준 UTF-8(BOM)과 필드 인용 규칙을 사용해 Excel에서 한글과 줄바꿈이 깨지지 않게 한다.
- CSV의 모든 셀은 선행 공백을 제외한 첫 문자가 `=`, `+`, `-`, `@`이면 작은따옴표를 앞에 붙여 Excel 수식 실행을 차단한다.
- 내보내기는 읽기 전용 작업이며 기존 로컬 및 Google 일정을 수정하지 않는다.
- 실패 안내는 빨간색으로 5초간 표시하고 개인정보를 가린 오류 기록을 남긴다.
- 설치 프로그램과 Windows 시작 시 자동 실행
- OAuth client secret 비포함 구조, Google 검증
- 코드 서명, 버전 정보, 자동 업데이트

### 확장 조건

댓글, 담당자, 팀 초대, 충돌 감지, 장보기 목록처럼 Calendar만으로 부족한 공동 데이터가 실제로 필요해질 때만 별도 서버/Supabase를 검토한다.

## 9. 개발 변경 절차

1. 작업 전 이 PRD와 `DEVELOPMENT_HISTORY.md`를 읽는다.
2. 변경 대상 코드와 저장 데이터 호환성을 확인한다.
3. UI 변경은 메인/팝업 디자인 일관성을 확인한다.
4. Google 변경은 로컬·읽기 전용·오프라인·반복 인스턴스 네 경로를 확인한다.
5. `build.ps1`로 새 step EXE를 만들고 컴파일 결과를 확인한다.
6. 기능 또는 결정이 바뀌면 같은 작업에서 PRD와 개발 기록도 갱신한다.

## 10. 소스 구조 및 리팩토링 주의사항

- 일정·설정·Google 응답 모델의 직렬화 필드명과 타입을 변경하지 않는다. 기존 JSON과의 호환성이 최우선이다.
- 로컬 저장 경로, 계정 키 생성 방식, 백업 보존 개수와 파일 잠금 이름을 변경하지 않는다.
- 로컬 반복 생성과 Google RRULE 생성은 `RecurrenceService`를 단일 기준으로 사용한다.
- Google OAuth와 Calendar API 통신은 `GoogleCalendarService.cs`의 `GoogleCalendar` 클래스에서 담당한다.
- UI 동작을 바꾸는 리팩토링은 구조 분리와 같은 변경에 섞지 않는다.
- 각 팝업은 `*Window.cs`, 메인 달력은 책임별 `MainWindow*.cs` partial 파일, Explorer 고정 레이어는 `MainWindow.ExplorerLayer.cs`·`LayerHostController.cs`·`ExplorerLayer` 네이티브 프로젝트에서 관리한다.
- 창 파일 분리 이후에도 팝업 디자인과 메인 달력의 동작 규칙은 동일하게 유지한다.

## 11. 배포용 Google OAuth

- Windows 데스크톱 앱은 OAuth client secret을 안전한 비밀로 간주할 수 없다. Google 토큰 서버가 요구하는 Desktop client credential은 Git에서 제외한 로컬 빌드 파일로 관리하며 공개 저장소와 진단 로그에는 포함하지 않는다.
- 인증 코드 교환은 매 로그인마다 생성한 PKCE `code_verifier`와 S256 `code_challenge`를 사용한다.
- 로컬 리디렉션 응답의 `state`를 검증한 뒤에만 인증 코드를 교환한다.
- 토큰 교환과 갱신에는 Desktop client credential을 사용하며 사용자 refresh token은 DPAPI로 암호화해 저장한다.
- 기존 DPAPI 토큰 저장, 계정 분리, 로그아웃 및 계정 전환 동작을 유지한다.

## 12. 버전 및 제품 정보

- 현재 배포 버전은 `2.1.0`이며 편의 기능, D-Day·기념일 분리, 알림·반복·탐색 개선과 Explorer 레이어 안정화를 포함한다.
- 화면 하단에는 `MADE BY JUAN.HJLEE · ONHARU (ver. 2.1.0)`을 표시한다.
- Windows 파일 속성의 제품명은 `ONHARU`, 제작자는 `JUAN.HJLEE`, 파일 버전은 `2.1.0.0`으로 기록한다.
- 기능 변경 없이 다시 빌드한 파일은 제품 버전을 임의로 올리지 않는다.
- EXE, Windows 작업표시줄, 트레이 및 설치 프로그램은 앱 헤더의 3×3 컬러 타일을 기반으로 한 동일한 ONHARU 아이콘을 사용한다.

## 13. 오류 기록

- 사용자 화면에는 시스템 오류창이나 원문 코드를 노출하지 않고 기존 빨간 안내를 사용한다.
- 내부 오류는 `%LOCALAPPDATA%\FamilyPlanner\logs\onharu-YYYYMMDD.log`에 날짜별로 기록한다.
- 로그는 최근 14개 파일만 보관한다.
- Google 토큰, Client Secret, 이메일 주소와 일정 제목·메모는 기록하지 않는다.
- 로그인, 동기화, Google 일정 저장·삭제, 로컬 데이터 읽기 및 예상하지 못한 앱 오류를 기록한다.
