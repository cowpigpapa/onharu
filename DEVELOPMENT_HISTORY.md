# 온하루 개발 기록

- step88 반복 UI 종료 수정: 반복 종류를 바꿀 때 재사용 라디오/체크 컨트롤이 이전 행의 자식으로 남아 발생한 WPF 논리 부모 예외를 수정했다. 다시 배치하기 전에 기존 부모에서 안전하게 분리한다.

- step88 일정창 이동 개선: 신규 등록과 수정이 같은 일정창 상단 빈 공간 어디서든 드래그되도록 설정창과 이동 동작을 통일했다. 닫기 버튼은 이동 대상에서 제외했다.

- step88 로그인 재수정: 현재 Google Desktop OAuth 클라이언트가 Client Secret을 요구하므로, 소스/EXE에는 포함하지 않고 Windows 사용자 환경에서 읽어 토큰 발급과 갱신에 사용하도록 복구했다.

- step88 로그인 수정: 환경변수 Client Secret 의존성 때문에 새 EXE에서 Google 로그인이 막힌 문제를 수정했다. Desktop OAuth의 PKCE 흐름에서 토큰 발급과 갱신에 Client Secret을 보내지 않도록 변경했다.

- step88: 하단 크레딧을 `MADE BY JUAN.HJLEE · ONHARU (step88)`로 변경하고, 검정색보다 부드러운 진한 슬레이트 색과 SemiBold 굵기를 적용했다.

- step87 배포 준비: 하단 표기를 `made by Juan.HJ · ONHARU (step87)`로 변경하고, Google OAuth client secret을 소스에서 제거하여 `ONHARU_GOOGLE_CLIENT_SECRET` Windows 환경변수로 분리했다. 정식 배포부터는 `ver. x.x.x` 형식을 사용한다.

> 이 문서는 사용자와 Codex가 나눈 전체 개발 대화를 기능 결정 중심으로 재구성한 기록이다. 세부 구현의 최종 기준은 `PRD.md`와 `Step2-Refactoring\Source`의 현재 소스다.

## 1. 출발점

- 유료 프로그램 xDiarys(CalendarTask)의 바탕화면 달력 경험을 대체하는 개인용 Windows 프로그램으로 시작했다.
- 1차 사용자는 부부 2명이며, 추후 가족이나 5~20명 규모 팀까지 확장할 가능성을 열어 두었다.
- 핵심 요구는 “하루 종일 일정”과 “시간이 있는 Todo”를 바탕화면에서 항상 확인하고 Google Calendar와 연동하는 것이었다.
- 초기 웹/PWA 아이디어는 Windows 바탕화면 레이어 제어가 핵심이 되면서 폐기했고, 최종 구현은 C# WPF/.NET Framework 단일 실행 파일 구조를 선택했다.

## 2. 바탕화면 위젯 기반 구축

1. 투명도는 밝기 조절이 아니라 뒤의 바탕화면이 실제로 보여야 한다는 기준을 확립했다.
2. 창 계층은 `바탕화면 < 온하루 < 폴더/아이콘 < 일반 Windows 창`을 목표로 했다.
3. Progman 소유 창과 `HWND_BOTTOM`, `WS_EX_NOACTIVATE`를 이용해 일반 창 아래에 유지하도록 구현했다.
4. 클릭 통과를 기본 동작으로 하되, 편집 모드에서는 날짜와 일정을 더블클릭할 수 있게 했다.
5. 위치 잠금, 잠금 해제, 상단 빈 영역 드래그, 오른쪽 아래 크기 조절을 추가했다.
6. 위치·크기·잠금 여부를 저장하여 다음 실행에 복원했다.
7. 트레이 아이콘, 종료 버튼, 단일 창 유지 문제를 다듬었다.

## 3. 일정 입력과 디자인 발전

- 날짜를 더블클릭하면 해당 날짜가 고정된 신규 일정 창을 연다.
- 일정 더블클릭은 수정, 날짜 칸 빈 공간 더블클릭은 신규 등록으로 구분했다.
- 제목, 메모, 카테고리, 중요 일정, 알림, 반복, 자동 이월을 한 창에서 관리한다.
- 시간은 기본 `하루 종일`, 또는 오전/오후와 1~12시, 0·15·30·45분으로 선택한다.
- 하루 종일 일정에는 완료 체크를 표시하지 않고 시간 일정은 Todo로 취급한다.
- 신규 등록 날짜는 선택한 날짜로 고정하고, 수정 시에는 동일한 커스텀 달력으로 날짜를 변경한다.
- 제목 누락 등의 오류는 시스템 경고창 대신 해당 팝업 내부의 짧은 빨간 안내로 통일했다.
- 팝업의 둥근 모서리, 닫기 버튼 위치, 여백, 입력 상자, 저장 버튼을 메인 화면과 같은 디자인 언어로 맞췄다.

## 4. 메인 달력과 카테고리

- 제품명은 `온하루 · ONHARU`로 정했다.
- 토요일 날짜는 파랑, 일요일과 국경일은 빨강, 오늘은 옅은 핑크 배경과 `오늘` 표기를 사용한다.
- 중요 일정에는 `★`를 붙이고 핫핑크 굵은 글씨로 강조한다.
- 로컬 카테고리는 `업무일정`, `개인일정` 두 가지다. 초기의 가족/기타 구분은 제거했다.
- Google 캘린더는 내 캘린더, 팀, 가족, 생일, 대한민국 휴일 등 계정별 실제 목록을 동적으로 읽는다.
- Google 휴일 캘린더는 항상 읽기 전용 및 빨간 계열로 표시한다.
- 달력 셀에는 최대 6줄의 일정을 표시하고, 오른쪽 패널은 캘린더별 파스텔 카드로 묶는다.
- 오른쪽 패널은 접고 펼칠 수 있으며 접으면 월간 달력이 그 공간만큼 늘어난다.
- 카테고리별/시간순 정렬, 카테고리 표시 순서, Google 캘린더 순서를 설정할 수 있다.
- 색상 팔레트, 두 색상 교환, 글자 크기, 파스텔 표시, 투명도, 주차, 음력을 설정에 추가했다.

## 5. 로컬 저장과 계정 분리

- 일정은 `%LOCALAPPDATA%\FamilyPlanner` 아래 JSON으로 저장한다.
- Google 계정별 파일과 로그아웃 상태의 로컬 파일을 분리한다.
- 로그아웃 상태에서도 로컬 일정 등록이 가능하며, Google 로그인 후 선택한 로컬 일정을 가져올 수 있다.
- 설정, 위치, 크기, 잠금, 필터, 색상, 정렬 등도 다음 실행에 복원한다.
- 쓰기 도중 JSON 손상을 막기 위해 임시 파일 작성 후 원자적으로 교체하고 프로세스 간 mutex를 사용한다.
- 하루 한 번 자동 백업하고 계정별 최근 30개만 유지한다.
- 한 차례 손상된 계정 JSON을 백업에서 복원했으며, `DateTime.MinValue` 직렬화와 비원자적 쓰기가 원인이었다. 날짜 정규화와 원자 저장으로 근본 수정했다.

## 6. Google Calendar 연동

1. Google Cloud 프로젝트 `Onharu`와 Calendar API를 활성화했다.
2. OAuth 테스트 사용자 문제, client secret 누락, 잘못된 요청 오류를 순서대로 해결했다.
3. 시스템 브라우저 + localhost 임시 콜백 + PKCE로 로그인한다.
4. refresh token은 Windows 사용자 DPAPI로 암호화해 저장한다.
5. 캘린더 목록을 계정마다 동적으로 가져오므로 사람마다 캘린더 이름과 개수가 달라도 동작한다.
6. 동기화 범위는 오늘 기준 과거 1년부터 미래 2년까지다. 검색 UI는 오늘 기준 ±1년이다.
7. 읽기/쓰기 권한을 Google access role과 사용자의 `수정 가능` 설정으로 함께 제한한다.
8. Google Tasks에서 Calendar에 노출된 항목의 설명 수정 제한 문구를 감지하고, 메모를 가져오지 않도록 처리했다.
9. 동기화 성공은 버튼 위치를 흔들지 않고 1초간 인라인 문구로 표시한다.
10. 네트워크 실패 시 변경 내용을 로컬에 남기고 `동기화 대기`로 표시한 뒤 재시도한다.
11. 계정 표시를 누르면 대기 일정명·날짜·대상 캘린더를 확인한다. 재시도는 상단 `G 동기화` 하나로 통일했다.
12. Google 대상 ID가 없는 로컬 일정에 대기 표시가 잘못 남는 문제는 시작 시 자동 정리한다.

## 7. Todo, 이월, 알림

- 시간 지정 일정만 완료 체크 가능한 Todo다.
- 자동 이월 기본값은 `이월 안 함`이다.
- 선택지는 `다음 날`, `다음 주 같은 요일`, `다음 평일`이다.
- Google Tasks 원본 항목은 Google Calendar API로 완료 상태를 수정할 수 없으므로 완료 상태를 온하루 로컬에 보관한다.
- 알림은 없음, 정시, 10분 전, 30분 전, 하루 전을 지원한다.
- 알림은 모니터 중앙의 온하루 스타일 창으로 표시하며 확인 또는 다시 알림을 지원한다.
- 로컬 일정과 Google 일정 모두 알림 대상이다. 온하루가 종료된 동안에는 Windows 자체 알림이 발생하지 않는다.

## 8. 검색, 백업과 편의 기능

- 검색창은 제목/메모를 대상으로 오늘 기준 ±1년을 조회한다.
- 최초 목록은 과거가 위, 오늘이 중앙, 예정이 아래가 되도록 날짜순으로 구성하고 `오늘` 버튼으로 복귀한다.
- 주차 표시는 ISO 월요일 기준 또는 1월 1일/일요일 기준 중 선택한다.
- 음력 날짜 표시를 선택할 수 있다.
- 백업 목록과 복원을 제공하며, 로컬 일정 가져오기는 체크박스로 선택한다.
- 하단에 제작자 표기를 두고 화면 폭이 줄어도 삐져나오지 않도록 위치를 조정했다.

## 9. 반복 일정

- 매일, 매주, 매월, 매년과 종료일을 지원한다.
- 신규 반복 일정은 종료일 기본값을 최초 날짜로부터 1년 뒤로 둔다.
- 수정 기본값은 전체 반복 일정 변경이며 `이번 일정만 변경` 체크 시 선택 인스턴스만 분리 수정한다.
- 반복 삭제는 실수 방지를 위해 다음 세 범위를 묻는다.
  - 이번 일정만
  - 이번 일정부터 미래
  - 과거 포함 전체
- 로컬 반복은 `SeriesId`로 개별 항목을 묶고, Google 반복은 부모 recurring event와 인스턴스 ID를 구분한다.

## 10. 현재 빌드와 남은 과제

- 이 기록 작성 시 기준 실행 파일: `Onharu-step87.exe`
- step72에서 SeriesId가 빠진 기존 로컬 반복 일정을 시작 시 복구하고, Google 반복의 종일/시간 UNTIL 형식을 분리했다. API 형식 오류를 오프라인으로 잘못 표시하던 처리도 `Google 오류`로 구분했다.
- step73에서 Google 시간 반복 등록의 400 오류(`Missing time zone definition`)를 재현해 시작·종료에 `Asia/Seoul`을 명시했다. 원본 API JSON은 UI에 노출하지 않는다.
- step74에서 동기화 대기창을 온하루 공통 디자인으로 다시 구성하고 대기 상태, 일정 날짜·시간·캘린더, 재시도 동작을 한눈에 구분하도록 개선했다.
- step75에서 중복된 팝업 재시도 버튼을 제거했다. 대기창은 문제 확인, 상단 `G 동기화`는 실행이라는 역할로 분리했다.
- step76에서 오프라인 상태가 대기 건수를 가리던 표시를 `오프라인 (동기화 대기 N건)`으로 합쳐 두 상태를 동시에 확인하도록 변경했다.
- step77에서 긴 Google 계정명과 상태 문구가 잘릴 때만 자동으로 왼쪽으로 흐르고 되돌아오는 표시와 전체 문구 툴팁을 추가했다.
- step78에서 계정 문구가 컨테이너 너비로 먼저 잘린 채 이동하던 문제를 수정했다. 전체 글자를 렌더링한 뒤 표시 영역 바깥만 가리는 방식으로 변경했다.
- step79에서 4번째 이후 Google 캘린더에 마지막 빨강이 반복되던 추천 팔레트를 로컬 2색+Google 8색으로 확장했다. 네트워크 응답 제한도 8초로 줄여 오프라인 표시 지연을 개선했다.
- step80에서 추천 조합을 총 10개로 확장했다. 선명한 `네온 베리`를 추가하고 Google에서 받은 캘린더 원본 색상을 별도로 보존해 `Google 기본`으로 복원할 수 있게 했다.
- step81에서 노트북/보조 모니터 대응을 위해 설정 및 일정 등록·수정창에 모니터별 최대 높이와 자동 세로 스크롤을 적용했다. Google 수정 권한은 2열, 주차·음력은 `표시 옵션`의 같은 행으로 정리했다.
- step82에서 큰 모니터에서도 설정창이 끝없이 길어지지 않도록 새 일정창의 노트북 기준 높이 992px을 공통 최대값으로 확정했다.
- step83에서 설정 및 일정 등록·수정창의 헤더를 고정하고 그 아래 내용만 스크롤하도록 재구성했다. 닫기 버튼은 헤더 오른쪽에 정렬하고 스크롤바를 좁은 파스텔 스타일로 정리했다.
- step84에서 일정창의 `★ 중요 일정`을 제목 옆으로 옮기고 반복 종료일을 반복 제목과 같은 행에 배치해 세로 길이를 줄였다. 평일·복수 요일·월별 방식은 다음 고급 반복 단계로 명시했다.
- step85에서 시간·카테고리 카드의 중복 제목을 제거하고 수정 소스 카드를 헤더 옆의 `온하루 등록`/`Google Calendar` 배지로 축약했다. 신규와 수정창 본문 양식을 동일하게 맞췄다.
- step86에서 고급 반복을 구현했다. 매일/평일, 매주 복수 요일, 매월 같은 날짜·n번째 요일·마지막 날, 매년 같은 날짜를 로컬 생성과 Google RRULE에 함께 적용하고 자동 검증 스크립트를 추가했다.
- 서로 다른 이름의 EXE를 동시에 실행한 시험에서 공통 설정·토큰 경쟁으로 남편 파일에 와이프 Google 일정이 섞이는 문제를 확인했다. 원본 세 파일은 `recovery-concurrent-20260808`에 보존했다.
- step87에서 파일명과 무관한 전역 단일 실행, 토큰 계정 ID 암호화 저장, 현재 Calendar List 밖 일정 숨김·동기화 후 정리, 카테고리 순서 정리를 추가했다. 스크롤바도 화살표 없는 둥근 파스텔 형태로 교체했다.
- 2026-08-08 부부 두 계정 실사용 시험에서 Google 일정 조회·신규·시간 반복·수정·삭제, 로그아웃·재연결·계정 재전환, Google/로컬 데이터 계정 분리를 모두 통과했다.
- 컴파일: Windows 내장 .NET Framework C# 컴파일러를 사용하는 `build.ps1`
- 배포 전 필수 과제:
  - 반복 일정의 월말·윤년·시간대·Google 예외 인스턴스 집중 검증
  - 계정 전환, 토큰 만료, 네트워크 단절 장시간 실사용 시험
  - JSON·CSV·ICS 내보내기와 오류 로그
  - 설치 프로그램, Windows 자동 실행, 자동 업데이트
  - OAuth client secret을 실행 파일에 포함하지 않는 배포 구조
  - Google OAuth 게시/검증, Windows 코드 서명

## 11. 개발 원칙

- 사용자에게 보이는 시스템 `MessageBox`는 추가하지 않는다. 오류는 발생 위치 가까이에 짧게 표시하거나 온하루 스타일 확인창을 사용한다.
- 기존 데이터 파일을 변경할 때 계정 분리, 원자 저장, 백업 호환성을 반드시 유지한다.
- 바탕화면 레이어와 클릭 통과 동작은 핵심 제품 기능이므로 일반 WPF 창처럼 단순 변경하지 않는다.
- Google 읽기 전용 일정과 Google Tasks의 API 제한을 우회해 수정하지 않는다.
- 디자인은 둥근 사각형, 파스텔 카드, 일정한 여백, 동일한 닫기 버튼 위치를 유지한다.
- 새 기능은 `PRD.md`의 데이터·동기화 규칙과 충돌하지 않는지 먼저 확인한다.

## 12. step88 안정판 보존 및 1차 리팩토링 (2026-08-08)

- Google 로그인, 반복 옵션 창 안정화, 팝업 이동 기능까지 확인된 소스를 `step88-stable` Git 태그로 보존했다.
- 사용자 기능과 저장 형식을 바꾸지 않고 단일 `Program.cs`에서 다음 책임을 분리했다.
  - `PlannerItem.cs`: 일정 데이터 모델
  - `PlannerSettings.cs`: 앱 및 Google 캘린더 설정 모델
  - `GoogleApiModels.cs`: Google API JSON 응답 모델
  - `RecurrenceService.cs`: 로컬 반복 계산과 Google RRULE 생성
  - `LocalStorage.cs`: 계정별 JSON 저장, 설정, 백업 및 복원
  - `GoogleCalendarService.cs`: OAuth, Calendar API 및 동기화
- `%LOCALAPPDATA%\FamilyPlanner`, `items-{account}.json`, 설정 파일, 백업 폴더 및 파일 잠금 이름은 기존과 동일하게 유지했다.
- 평일, 복수 요일, 매월 마지막 날, 매월 마지막 요일 및 Google RRULE 검사를 자동화해 통과했다.
- UI 창과 `MainWindow` 분리는 결합도가 높고 실사용 회귀 위험이 커서 이번 단계에서는 진행하지 않았다.

## 13. 팝업 및 메인 화면 분리 (2026-08-08)

- step89에서 10개 팝업 창을 각각의 `*Window.cs` 파일로 옮기고 `step89-popups` 태그로 보존했다.
- step90에서 `MainWindow`, `DesktopLayer`, `UiRound`를 각각 별도 파일로 옮겼다.
- `Program.cs`에는 앱 시작과 단일 실행 제어만 남겼다.
- 빌드 스크립트는 프로젝트 폴더의 C# 소스를 자동으로 포함하도록 단순화했다.
- 분리 전후 `MainWindow` 필드 74개·메서드 98개, `DesktopLayer` 필드 14개·메서드 18개가 동일함을 확인했다.
- 최종 컴파일과 고급 반복 일정 자동 검사를 통과했다.

## 14. 개발 결과물 폴더 정리 (2026-08-08)

- 기능 개발 실행 파일은 `Step1-Development`에 보관한다.
- 리팩토링 중간·최종 실행 파일은 `Step2-Refactoring`에 보관한다.
- 설치 프로그램과 최종 배포 파일은 `Step3-Distribution`에 보관한다.
- 현재 기능 비교 기준은 `Step2-Refactoring\Onharu-step90-refactor.exe`이다.
- 리팩토링 소스와 빌드·검사 스크립트는 `Step2-Refactoring\Source`에 함께 보관한다.
- 리팩토링 전 단일 파일 원본은 `Step1-Development\Source\Program.cs`에 별도로 보관하며 `step88-stable` 태그와 동일하다.

## 15. Step3 배포용 OAuth 시작 (2026-08-08)

- Step2 최종 소스를 `Step3-Distribution\Source`로 복사해 배포 작업을 분리했다.
- 기존 PKCE S256과 `state` 검증을 유지하고 OAuth 요청에서 `client_secret` 및 `ONHARU_GOOGLE_CLIENT_SECRET` 환경변수 의존성을 제거했다.
- secret 금지 문자열과 PKCE 필수 항목을 확인하는 `oauth-check.ps1`을 추가했다.
- `ONHARU-step3-oauth.exe` 빌드와 고급 반복 일정 자동 검사를 통과했다.
- 실제 Google 로그인·토큰 갱신·계정 전환은 사용자 실사용 확인 후 안정판으로 확정한다.
- 기존 OAuth 클라이언트가 `client_secret is missing`을 반환해 Google Cloud에서 새 Desktop 앱 클라이언트를 생성했다.
- Step3는 새 Desktop Client ID와 `google-v3.token`/`google-account-v3.dat`를 사용하며 Step2의 기존 v2 토큰은 보존한다.
- 새 Desktop 클라이언트도 실제 토큰 교환에서 `client_secret is missing`을 반환했다. 새 Secret은 `OAuthCredentials.local.cs`에만 저장하고 `.gitignore`로 제외했으며, 공개 소스에는 값을 남기지 않는다.
- 빨간색 오류·실패·입력 경고가 너무 빨리 사라지지 않도록 공통 노출 시간을 5초로 통일하고 성공 안내 시간은 유지했다.
- 동일한 5초 오류 안내를 Step1 단일 소스와 Step2 리팩토링 소스에도 반영하고 각 단계별 확인 EXE를 별도로 생성했다.
- Step3 화면 표기를 `ver. 1.0.0`으로 전환하고 Windows 파일 속성에 제품명, 제작자, 파일·제품 버전을 추가했다.
- `version-check.ps1`로 배포 EXE의 제품명 `ONHARU`, 제작자 `JUAN.HJLEE`, 버전 `1.0.0`을 자동 확인한다.
- 앱 헤더의 3×3 컬러 타일을 기반으로 16·32·48·256px Windows 아이콘을 생성하고 EXE와 트레이 아이콘에 동일하게 적용했다.
- `Assets\create-icon.ps1`로 아이콘을 재생성할 수 있으며 버전 검사에서 EXE 아이콘 포함 여부도 확인한다.
- 날짜별 오류 로그를 `%LOCALAPPDATA%\FamilyPlanner\logs`에 기록하고 최근 14개만 보관하는 `ErrorLog`를 추가했다.
- 오류 로그는 Client Secret, access/refresh token과 이메일 주소를 자동으로 가리며 일정 제목·메모를 기록하지 않는다.
- `error-log-check.ps1`로 민감정보 가림을 자동 검증한다.

## 16. 일정 내보내기 추가 (2026-08-08)

- 설정에 `일정 내보내기`를 추가하고 JSON, CSV, ICS 저장 형식을 제공했다.
- JSON은 온하루 전체 보관, CSV는 Excel 확인, ICS는 다른 캘린더 가져오기 용도로 구분했다.
- 기존 일정은 변경하지 않으며 저장 실패는 5초 안내와 개인정보 보호 오류 기록으로 처리한다.
- `export-check.ps1`로 CSV 인용, ICS 기본 구조, JSON 생성을 자동 검증한다.

## 17. Windows 설치판 준비 (2026-08-08)

- 관리자 권한 없이 `%LOCALAPPDATA%\Programs\ONHARU`에 설치하는 Inno Setup 구성을 추가했다.
- 시작 메뉴 바로가기와 제거 프로그램은 기본 생성하고, 바탕화면 바로가기와 Windows 자동 실행은 설치 시 선택한다.
- 제거 시 사용자 일정·설정·Google 연결 데이터는 보존해 재설치와 복구가 가능하게 한다.

## 18. 바탕화면 아이콘 계층 수정 (2026-08-08)

- 캘린더를 `Progman` 소유 창으로만 내리던 방식에서 바탕화면 아이콘 뒤쪽 `WorkerW`에 연결하는 방식으로 변경했다.
- 바탕화면 아이콘과 폴더는 항상 캘린더 위에, 일반 응용 프로그램 창은 그보다 위에 표시되도록 계층을 고정했다.
- 일정 등록·수정창의 소유 관계를 분리해 팝업을 열 때 캘린더가 일반 창 위로 잠깐 올라오는 현상을 차단했다.
- `desktop-layer-check.ps1`로 WorkerW 연결과 등록·수정창 공통 처리를 검사한다.
- 첫 WorkerW 시험판은 일부 Windows 11 환경에서 캘린더가 배경화면 뒤로 숨는 문제가 있었다. 실제 `SHELLDLL_DefView` 아이콘 호스트를 찾고 그 바로 다음 Z-order에 배치하도록 교정했다.
- 아이콘 호스트 형제 배치와 WorkerW 자식 배치를 추가 시험했지만, 해당 Windows 11의 WPF 투명창 합성에서는 캘린더가 계속 배경화면 뒤로 숨었다. 화면 표시를 최우선으로 기존 `Progman` 소유 창 방식으로 복원했다.
- 일정 등록·수정창의 소유 관계 분리는 유지해 팝업이 캘린더를 순간적으로 끌어올리는 문제는 별도로 해결했다.
- 바탕화면 아이콘 작업영역 자동 재배치도 시험했으나 느려지고 이동 후에도 계층 문제가 남아 전부 제거했다. 배포 소스는 아이콘 실험 전 안정 상태로 복원했다.
- Google 연속 일정이 시작일에만 보이던 조건을 기간 포함 조건으로 수정했다. 하루 종일 일정의 Google 종료일은 제외하고, 시간 일정은 실제 종료 날짜까지 표시한다.
- 캘린더 이동 완료 직후 다시 최하단으로 내려 일반 응용 프로그램 창 위로 남는 현상을 보완했다. 바탕화면 파일 아이콘과의 계층 한계는 별도 과제로 남겼다.
