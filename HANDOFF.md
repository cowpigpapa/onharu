# ONHARU 2.2 HANDOFF

## 2026-09-03 — 웹 검토와 수정 (로컬만, 서버 게시 안 함)

`Publish/`는 Git 제외 영역이라 `git status`에 나타나지 않는다. 아래에 파일명과 크기·해시 앞자리를 남긴다. **서버에는 올리지 않았다.**

### 0. 웹 시험판 파일 삭제

사용자 지시로 지웠다. 앞으로 웹 시험판은 2.2.6으로 다시 올린다.

- `downloads/ONHARU-2.2.5-Test-Setup.exe`, `ONHARU-2.2.5-Test-Portable.zip`
- `downloads/ONHARU-2.2.6-Test-Setup.exe`, `ONHARU-2.2.6-Test-Portable.zip`
- **주의: 공개 서버에는 네 파일이 그대로 있다.** 로컬 원본에서만 지웠다. 실제로 내리려면 게시 작업이 필요하다.

### 1. 사용자가 지적한 버튼 — 원인은 CSS 우선순위

- `다운로드` 페이지의 `2.2.6 시험용 설치판` 버튼이 **보라 그라데이션 배경에 보라 글자**였다. 대비율 **1.15:1**로 사실상 보이지 않았다. 정상 버튼은 6.51:1이다.
- 원인은 `.notice a{color:#6854c9}`가 뒤에 와서 `.button{color:white}`를 덮어쓴 것이다. `.button`은 클래스 하나, `.notice a`는 클래스+요소라 우선순위가 높다.
- `.notice a:not(.button)`으로 좁혀 버튼이 자기 색을 지키게 했다. 앞으로 `.notice` 안에 버튼을 넣어도 같은 일이 생기지 않는다.

### 2. 같은 버튼에 숨어 있던 자바스크립트 오류

- 다운로드 버튼 처리기가 `button.closest('.card').classList.add(...)`를 부른다. 시험판 버튼은 `.card` 밖에 있어 `closest`가 `null`을 돌려주고 **그 자리에서 예외가 났다.** 안내 토스트와 라벨 복원이 실행되지 않았다.
- `null` 검사를 넣었다. 첫 버튼을 눌러 토스트가 뜨는 것까지 확인했다.

### 3. 끊어진 링크

- 시험판 파일을 지워 `다운로드` 페이지의 `출시 예정 · 2.2.6 시험판` 절이 404를 가리키게 되어 절을 통째로 뺐다. 새 시험판을 올릴 때 다시 넣는다.
- 머리말도 실제 내려받을 수 있는 것에 맞췄다. `ONHARU 2.2.6 · 기능 시험판` → `ONHARU 2.2.4 · 정식판`. **2.2.5 정식 파일이 아직 없으므로 2.2.5로 앞서 적지 않았다.**
- **`사용설명서`의 PDF 버튼 두 개가 항상 404였다.** `downloads/ONHARU-User-Guide-2.2.5.pdf`를 가리키는데 공개 서버에 그 파일이 없다. 2.2.0~2.2.6 어느 이름으로도 없다. 두 버튼을 다운로드·FAQ·지원 링크로 바꿨다. **PDF가 있으면 되살릴 수 있다.**

### 4. 글자 대비 — 10개 페이지 전부 WCAG 4.5:1 이상으로 올렸다

색조는 유지하고 명도만 낮췄다. 측정은 브라우저에서 실제 계산색으로 했다.

- `#7a869b` → `#5b6678` 푸터 (3.29 → 5.20). 공통 CSS라 10개 페이지 전부에 적용된다.
- `#667085` → `#616a7e` 본문 회색 (4.45~4.46 → 4.8 이상)
- `#637087` → `#5e6a80`, `#637086` → `#5b6779`(설명서 아이콘 칩), `#647087` → `#5f6a80`(지원)
- `#208566` → `#1d795d` `계속 무료` 배지 (4.17 → 4.87)
- `#7e8796` → `#656c78`, `#7b8390` → `#5f656f` 릴리스 노트의 지난 버전 카드 (3.14~3.35 → 4.8 이상)
- `#68758b` → `#5e6a7e`, `#777087` → `#5c5670` 다운로드 카드의 보조 문구 (4.01~4.32 → 4.5 이상)
- `.button.disabled` 글자 `#89839e` → `#5d5673` (2.86 → 5.46)

### 검증

- 10개 페이지 모두 로컬 정적 서버(`127.0.0.1:8766`)에서 확인했다. **대비 위반 0건, 가로 넘침 0건.**
- 폭 1440·1024·800·390에서 `scrollWidth - clientWidth = 0`. 모바일에서 헤더 메뉴만 의도대로 가로 스크롤한다.
- 페이지 간 내부 링크와 `downloads/` 참조를 전수 검사해 끊긴 링크가 없다.
- 이미지 `alt` 누락 0건, `aria-current="page"` 각 페이지 1개.

### 변경한 웹 파일

`ONHARU-DOWNLOAD.html`(8,538) · `ONHARU-GUIDE.html`(13,878) · `ONHARU-RELEASES.html`(16,035) · `ONHARU-SUPPORT.html`(4,211) · `ONHARU-SITE.css`(2,520) · `index.html`(25,747) · `ONHARU-FEATURES.html`(25,747) · `ONHARU-FAQ.html`(21,202) · `ONHARU-PRIVACY.html`(7,563) · `ONHARU-TERMS.html`(8,803)

- **미해결·다음 작업:**
  - **서버 게시는 하지 않았다.** 사용자가 요청하면 `WEB_OPERATIONS_HANDOFF.md` 절차로 백업 후 진행한다.
  - 게시할 때 서버의 `ONHARU-2.2.5-Test-*`·`ONHARU-2.2.6-Test-*` 네 파일을 함께 내려야 한다.
  - 2.2.5 정식 릴리스가 나오면 다운로드 페이지·릴리스 노트·소개 두 페이지의 버전 문구를 한 번에 갱신한다. `index.html`과 `ONHARU-FEATURES.html`은 내용이 같으므로 함께 고친다.
  - 사용설명서 PDF를 만들 계획이면 파일을 올린 뒤 버튼을 되살린다.


## 2026-09-03 — 정식 버전을 2.2.5로 확정

- 사용자 판단: **2.2.5 시험판을 설치한 사용자가 아직 없다.** 그래서 정식 번호를 2.2.6이 아니라 2.2.5로 되돌린다.
- 되돌리기 전에 알린 위험은 `2.2.5 시험판을 이미 설치한 사용자는 같은 번호라 자동 업데이트를 받지 못한다`였다. 설치자가 없으므로 성립하지 않는다.
- **한 작업에서 전부 맞췄다.** Assembly 세 값, 화면 하단 표기, 제품 정보 창, 메일 백업 `ClientVersion`, `version-check.ps1`의 두 단언, `Installer/ONHARU.iss`의 세 분기, `build-release.ps1`, `finalize-distribution.ps1`, `release-config-check.ps1`, `README.md`.
- 문서도 함께 갱신했다. `CURRENT_STATUS.md`(정식 2.2.5, 공개 중인 것은 2.2.4), `BUILD_AND_RELEASE.md`(머리말과 `자동 검사 17종` → `18종`, 사라진 시험 폴더 참조 정리), `WEB_OPERATIONS_HANDOFF.md`와 `Claude-Web-Handoff.md`의 현재 상태 줄. 과거 게시 기록은 기록이므로 고치지 않았다.
- **주의:** onharu.app에는 지금도 `ONHARU-2.2.5-Test-Setup.exe`와 `ONHARU-2.2.6-Test-Setup.exe`가 올라가 있다. 정식 2.2.5를 게시할 때 같은 번호의 다른 파일이 남지 않도록 함께 정리해야 한다. 두 웹 문서에 적어 두었다.

### 작업 메모 — `sed -i`가 줄바꿈을 LF로 바꾼다

- 버전 치환에 `sed -i`를 썼더니 대상 파일이 전부 **LF 전용**이 됐다. 저장소는 CRLF 기준이라 `feature-pack-check.ps1`의 여러 줄 토큰이 더 이상 맞지 않아 게이트가 깨졌다.
- 아홉 파일을 CRLF로 되돌렸고 BOM은 Git 원본과 대조해 그대로임을 확인했다. **이 저장소에서 `sed -i`로 소스를 고치지 않는다.**

- **변경 파일:** `App/AssemblyInfo.cs`, `App/EmailBackupService.cs`, `App/MainWindow.Layout.cs`, `App/ProductInfoWindow.cs`, `App/version-check.ps1`, `App/release-config-check.ps1`, `Installer/ONHARU.iss`, `build-release.ps1`, `finalize-distribution.ps1`, `README.md`, `CURRENT_STATUS.md`, `BUILD_AND_RELEASE.md`, `WEB_OPERATIONS_HANDOFF.md`, `Claude-Web-Handoff.md`, `HANDOFF.md`.
- **검증:** 빌드 성공. 실행 파일 메타데이터 `FileVersion 2.2.5.0`, `ProductVersion 2.2.5` 실측. `check-v22.ps1` 18개 전부 PASS.
- **미해결·다음 작업:** 릴리스 빌드, GitHub 업로드 형태 결정(정식 Latest / pre-release / 소스만), 남은 리팩토링 둘.


## 2026-09-03 — 정리, 2.2.6 버전 확정, 헤더 기능 아이콘 정렬 회귀 수정

### 1. 불필요 파일 정리

사용자 승인으로 세 묶음을 지웠다. **전부 Git 밖이라 복구되지 않는다.** 지운 목록은 아래와 같다.

- **재생성 가능한 산출물:** 루트 `build-*.log` 9개, `ONHARU-2.2.6-Claude-review-pack.zip`, `ExplorerLayer`의 중간물(obj·lib·exp·dll·exe), `App/ONHARU.exe(.config)`, `Server/EmailBackup/__pycache__`, `Tests/LocalTest`의 옛 시험본 11개와 `.config`·`.pdb`·zip·미리보기 데이터, `Tests/Diagnostics/GitHubUpdateProbe.exe`, `Tests/DpiValidation`의 zip 2개와 생성 복사본(`Runtime`·`Source`).
- **옛 릴리스 스테이징:** `Release` 전체. 2.2.0~2.2.4 스테이징과 설치판, `Test`, `Verify-2.2.3`, `GitHubUpload221`, `GitHubUploadFinal`. **2.2.6만 남긴다**는 사용자 지시에 따라 2.2.4도 지웠다. 공개 자산은 GitHub Release v2.2.4와 `Publish/ONHARU-Web/downloads`에 남아 있다.
- **역할이 끝난 문서·묶음:** `Claude-2.2.6-brief.md`, `Codex-2.2.6-brief.md`, `design-clay.md`, `XDIARY_ONHARU_FEATURE_COMPARISON_20260820.md`, `Publish`의 옛 웹 게시 zip 2개와 `GitHubSource-2.2.3`.
- **함께 지운 죽은 코드:** `PendingSyncWindow.cs`와 `MainWindow.OpenPendingSync`. 부르는 곳이 없었다.
- `Tests`는 15MB에서 810KB로, `Release`는 55MB에서 0으로 줄었다. `Publish/ONHARU-Web`(실제 사이트 원본)은 손대지 않았다.

### 2. 2.2.6 정식 버전 확정

시험판 표기를 모두 정식으로 바꿨다. **한 작업에서 전부 맞췄다.**

- `AssemblyInformationalVersion` `2.2.6-test` → `2.2.6`
- 화면 하단 `ver. 2.2.6 test` → `ver. 2.2.6`, 제품 정보 창 `2.2.6 test` → `2.2.6`
- 메일 백업 `ClientVersion`, `version-check.ps1`의 제품 버전 단언
- `Installer/ONHARU.iss`와 `build-release.ps1`·`finalize-distribution.ps1`의 `2.2.4` → `2.2.6`
- `release-config-check.ps1`의 토큰과 README 버전 단언
- `README.md`의 다운로드 링크·버전 표기·설치 안내

### 3. 헤더 기능 아이콘 정렬 — 내가 만든 회귀를 고쳤다

- 사용자 기준: **아이콘 줄의 오른쪽은 창 조작 버튼 왼쪽에 고정**이고, 아이콘을 하나씩 켤 때마다 왼쪽으로 늘어나며, **전부 켰을 때만** 맨 왼쪽 아이콘이 기간 스위치의 왼쪽 테두리와 같은 선에 선다.
- 이번 세션 앞부분에서 `보이는 첫 아이콘`을 스위치에 맞추게 했더니 **아이콘을 하나만 켜도 줄 전체가 왼쪽으로 끌려갔다.**
- 기준을 `전부 켰을 때의 줄 너비`로 바꿨다. 그 너비는 버튼의 선언 크기와 여백에서 나오므로 숨김 여부와 무관하다.
- 그런데 이 계산만으로는 부족했다. **아래 줄에서 스킨 스위치가 숨겨져 있으면 기준선이 오른쪽으로 크게 밀려 아이콘이 창 조작 버튼 위로 올라탔다.** 실측으로 겹침을 확인했다(설정 아이콘 3763~3806, 최소화 3708).
- 보정 폭을 아이콘 한 칸(38)으로 제한했다. 그보다 큰 요구는 무시하고 고정 위치를 지킨다.
- **확인:** 설정 아이콘 하나만 켠 현재 상태에서 3643~3685, 최소화가 3708이라 23px 여유가 있고 겹치지 않는다. 전부 켰을 때는 줄 너비 232.5px를 기준선에서 빼는 계산이므로 왼쪽 끝이 스위치와 맞는다.

- **변경 파일:** `App/AssemblyInfo.cs`, `App/MainWindow.Layout.cs`, `App/MainWindow.Google.cs`, `App/ProductInfoWindow.cs`, `App/EmailBackupService.cs`, `App/version-check.ps1`, `App/release-config-check.ps1`, `App/feature-pack-check.ps1`, `App/popup-policy-check.ps1`, `Installer/ONHARU.iss`, `build-release.ps1`, `finalize-distribution.ps1`, `README.md`, `HANDOFF.md`. 삭제: `App/PendingSyncWindow.cs` 외 위 목록.
- **검증:** 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 아이콘 위치는 실행 중인 창에서 좌표로 실측하고 캡처로 확인.
- **미해결·다음 작업:**
  - **`ShowThemeSwitch`는 코드가 맞고 문서가 낡았다.** 사용자가 설정에서 직접 언체크한 것이라고 확인해 주었다. `CLAUDE.md`·`AGENTS.md`의 `항상 표시하고 숨김 설정은 두지 않는다`를 `기본으로 표시하되 설정에서 숨길 수 있다`로 같은 문장으로 고쳤고, `Docs/Design/MAIN_HEADER_UI_STANDARD.md`도 맞췄다. 같은 문서에 아이콘 줄 정렬 규칙(오른쪽 고정, 전부 켰을 때만 스위치와 정렬, 보정 한도 38)을 새로 적었다.
  - 남은 리팩토링 둘: `ui-construction-check.ps1`이 저장 같은 실행 경로까지 확인하게 하기, 검사 스크립트 인코딩·BOM 통일.
  - 릴리스 빌드, 문서 버전 동기화(`CURRENT_STATUS.md`·`BUILD_AND_RELEASE.md`·`WEB_OPERATIONS_HANDOFF.md`), GitHub 업로드가 남았다.


## 2026-09-03 — 리팩토링 1단계: 죽은 정의 제거, 중복 통합, 아이콘 버튼 접근성 이름

사용자 지시로 디자인 단계를 마치고 정리·리팩토링·배포 단계로 넘어왔다. `TODO_NEXT.md`의 `리팩토링 단계 정리 대상`을 처리한다.

### 0. 네이티브 빌드 차단 원인을 먼저 풀었다

- `ExplorerLayer/build.ps1`이 부르는 `vcvars64.bat`은 **내부에서 `vswhere.exe`를 PATH에서 찾는다.** Visual Studio 없이 Build Tools만 설치한 이 환경에서는 그 폴더가 PATH에 없어 네이티브 빌드가 통째로 막혀 있었다. 지난 기록의 `vswhere가 PATH에 없어 네이티브를 빌드할 수 없다`가 이것이다.
- 빌드 명령 앞에 설치 폴더를 PATH에 붙였다. **네이티브 DLL과 LayerHost가 실제로 빌드된다.** 따라서 정식 릴리스 빌드가 이 환경에서 가능하다.

### 1. 고정 상태 입력의 죽은 분기 세 개 제거

- `open_pending_sync`, `open_anniversary`, `DiaryDateHitTarget`. 셋 다 처리 코드만 있고 그 표식을 다는 곳이 어디에도 없었다.
- 딸려서 `MainWindow.OpenDiaryEditor`도 지웠다. **일기 쓰기는 읽기 창이 직접 연다**(`DiaryWindows.cs`). 메인의 것은 지운 분기에서만 불리던 죽은 경로였다.
- 검사가 붙잡고 있던 `FamilyPlanner.DiaryDateHitTarget` 타입 단언과 `target as DiaryDateHitTarget` 토큰을 함께 정리했다.

### 2. 죽은 정의 제거

- `OnharuPopupChrome.FeatureTitle` — 팝업 제목을 `FeatureHeading`으로 통일하며 호출부가 사라졌다.
- `OnharuPopupChrome.DisclosureButton`·`SetDisclosure` — 시간표가 유일한 사용처였고 지금은 부르지 않는다.
- `OnharuStateColors.OpacityControl` — 투명도 슬라이더 색을 확정하며 호출부가 없다.
- **`OnharuPopupChrome.ActionButton`은 지우지 않았다.** `TODO_NEXT.md`는 죽었다고 적었지만 KBO 창과 야구 API 창이 여전히 쓴다. 목록을 바로잡았다.
- 각 정의를 붙잡고 있던 검사 토큰을 같은 작업에서 함께 지웠다.

### 3. 중복 통합 — 크기 조절 판정

- 메인 창이 `ResizeEdgeAt`·`ResizeCursor` 사본을 따로 들고 있었다. **두 구현은 문자 그대로 같았다**(`UiCursor.ResizeHorizontal`은 `Cursors.SizeWE`를 돌려준다).
- 팝업 공통 함수를 `internal`로 열고 메인 창 사본을 지웠다. 검사도 메인 창이 아니라 공통 함수를 반사로 확인하게 바꿨다.

### 4. 아이콘 전용 버튼의 접근성 이름

- 메인 헤더의 아이콘 버튼 여덟 개가 **자동화 트리에서 이름이 전부 빈 칸**이었다. 화면 낭독기와 자동화가 무슨 버튼인지 알 수 없었다.
- `OnharuPopupChrome.NameFromToolTip`을 만들어 도구 설명을 이름으로 삼게 했다. 헤더 스타일 함수와 창 조작 버튼 스타일 함수에서 부른다. 개별 호출부를 여덟 군데 고치지 않고 한 곳에서 처리한다.
- **확인:** 실행 중인 시험본의 자동화 트리에서 여덟 개 모두 이름이 나온다. `일정 검색`, `시간표 보기 · 편집`, `알람 · 타이머`, `프로야구 일정`, `색상 및 설정`, `최소화`, `최대화 · 고정 상태에서는 사용할 수 없음`, `끝내기`.

- **변경 파일:** `ExplorerLayer/build.ps1`, `App/MainWindow.DesktopInput.cs`, `App/MainWindow.Diary.cs`, `App/MainWindow.Layout.cs`, `App/MainWindow.Navigation.cs`, `App/MainWindow.Theme.cs`, `App/ExplorerFramePublisher.cs`, `App/OnharuPopupChrome.cs`, `App/OnharuStateColors.cs`, `App/feature-pack-check.ps1`, `App/theme-check.ps1`, `App/popup-policy-check.ps1`, `HANDOFF.md`.
- **검증:** C# 빌드 성공. **네이티브 빌드 성공**(`Onharu.DesktopHook.dll`, `Onharu.LayerHost.exe`). `check-v22.ps1` 18개 전부 PASS. 접근성 이름은 실행 중인 창에서 실측.
- **미해결·다음 작업:**
  - `PendingSyncWindow`와 `MainWindow.OpenPendingSync`도 부르는 곳이 없다. 창 하나를 통째로 지우는 것은 제품 판단이라 남겨 두고 확인을 받는다.
  - 남은 리팩토링 항목 둘: `ui-construction-check.ps1`이 저장 같은 실행 경로까지 확인하게 하기, 검사 스크립트 인코딩·BOM 통일.
  - 2.2.6 버전 확정, 불필요 파일 정리, 릴리스 빌드, GitHub 업로드가 남았다.


## 2026-09-03 — Claude Code용 웹 작업 인수인계 문서 작성

- **요청:** Claude가 ONHARU 웹을 직접 보고 편집하고 검토할 수 있도록 전달 자료를 만든다.
- `Claude-Web-Handoff.md`를 새로 만들었다. 시작 문서 순서, 공개 10개 페이지와 로컬 파일 대응, `Publish/ONHARU-Web` 원본 경계, 공통 UI 계약, 로컬 정적 서버 미리보기, 데스크톱·모바일 검토 항목, 게시 전 백업·검증 절차, 비밀정보 금지, 완료 보고 형식과 Claude 시작 프롬프트를 한 문서에 모았다.
- `Publish/`가 Git 제외 영역이라 변경이 `git status`에 나타나지 않는 점, `Distribution/`이 현재 웹 원본이 아닌 점, `index.html`과 `ONHARU-FEATURES.html`이 같은 소개 페이지인 점을 주요 주의사항으로 명시했다.
- 사용자가 명시할 때만 시안 HTML을 만들고, 게시 요청이 없으면 로컬 미리보기까지만 진행하도록 경계를 적었다.
- **변경 파일:** `Claude-Web-Handoff.md`(신규), `HANDOFF.md`.
- **검증:** 2026-09-03 공개 10개 페이지 HTTP 200 확인. 문서에 적은 로컬 경로·페이지 파일·공통 CSS·현재 공개/시험 버전을 실제 파일과 `WEB_OPERATIONS_HANDOFF.md`에서 대조했다. 코드와 웹 원본은 변경하지 않았다.


## 2026-09-03 — 이동·고정 동작 차이를 코드로 대조한 결과

사용자 질문: "이동과 고정 사이에 동작이 다른 것은 없을지 코드를 비교하면 확인이 가능한가?" — **가능하다.** 방법과 결과를 남긴다.

### 대조 방법

고정 상태에서 어떤 요소가 눌리려면 **세 관문을 모두 통과해야 한다.** 하나라도 빠지면 이동에서는 되고 고정에서는 안 된다.

1. `ExplorerFramePublisher.CollectHitRecords`가 그 요소를 적중 지도에 담는가. 담기는 것은 `Button`·`CheckBox`·`Slider`와 몇몇 태그뿐이다.
2. 담길 때 정해지는 `Kind`에 맞는 처리가 있는가. 네이티브 후크(`ExplorerLayer/DesktopHook.cpp`)가 Kind로 갈라 보낸다. **Kind 1은 100(적중 검사), 2는 104(슬라이더), 3은 107(스크롤), 4는 28(닫기), 5는 108(Google), 6은 20(사이드바 접힘), 7은 위치 모드다.**
3. Kind 1이면 `FindDesktopElement`가 그 요소를 돌려주고 `HitTestDesktop`이 처리하는가.

그래서 **메인 창의 모든 `Tag` 값을 뽑아 세 목록과 맞춰 보면 기계적으로 확인된다.**

### 대조 결과 — 실제 차이 한 건

- **미완료 To-Do 카드의 일정 글자에 표식이 없었다.** 이동에서는 두 번 누르면 수정 창이 열리는데 **고정에서는 아무 일도 없었다.** 같은 카드의 완료 체크박스는 `CheckBox`라 담겨서 잘 됐다.
- 세부 카드의 일정 글자와 같은 `ItemHitTarget`(`DetailCard = true`)을 달았다. `HitTestDesktop`의 해당 분기가 이미 `두 번 누르면 OpenEdit`이라 이동과 같은 동작이 된다.

### 대조 결과 — 문제 없음으로 확인한 것

- Google 계정 카드: 태그 `google_sync` → Kind 5 → 네이티브가 108로 보낸다. `FindDesktopElement`에는 없지만 그 경로를 타지 않아 정상이다.
- 달력 날짜칸·일정 막대·D-Day·기념일 카드·세부 카드 일정: 모두 `DateTime`·`PlannerItem`·`ItemHitTarget` 표식을 갖고 있다.
- 세부 그룹 머리글, 투명도 슬라이더, 세부 스크롤, 닫기, 위치 모드, 사이드바 접힘: 각각 전용 Kind로 처리된다.
- 설정창 색상 카드가 스킨 색을 쓰는 것은 **의도한 미리보기**다. 배경과 글자를 짝으로 쓰므로 대비가 보장된다.
- `완료 체크를 지원하지 않는 일정`의 표시용 체크박스는 눌러도 아무 일이 없는 것이 정상이다.

### 대조 결과 — 죽은 분기 세 개

`MainWindow.DesktopInput.cs`가 처리는 하지만 **그 표식을 다는 곳이 어디에도 없다.** 사용자가 만날 수 있는 버그는 아니지만, 없는 기능이 있는 것처럼 보이게 한다.

- `open_pending_sync`
- `open_anniversary`
- `DiaryDateHitTarget` (클래스는 있으나 `Tag`에 넣는 곳이 없다)

세 가지 모두 지우기 전에 의도를 확인해야 해서 `TODO_NEXT.md`로 넘긴다.

- **변경 파일:** `App/MainWindow.Detail.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** 빌드 성공. `check-v22.ps1` 18개 전부 PASS.
- **미해결·다음 작업:**
  - **미완료 카드 항목의 고정 상태 더블클릭은 실기기 확인이 필요하다.** 합성 클릭이 고정 레이어에 닿지 않는다. 다만 이미 잘 동작하는 세부 카드와 완전히 같은 경로다.
  - 위 죽은 분기 세 개 정리 여부 결정.
  - 메인 헤더 아이콘 버튼 다섯 개의 접근성 이름이 비어 있다.


## 2026-09-03 — 그룹 접힘 화살표가 잘 눌리지 않던 문제

- 사용자 확인: 고정 상태의 그룹 접힘은 잘 된다. 다만 **글자는 잘 눌리는데 화살표는 잡기 어렵다.**
- 화살표는 `ContentControl`이었다. **기본 템플릿에 테두리가 없어 배경이 칠해지지 않는다.** 배경이 없으면 상자 안쪽 빈 곳은 눌리지 않고 **1.6px 획에 정확히 맞아야만** 눌렸다. 고정 상태에서는 적중 지도의 사각형이 12×12라 역시 작았다.
- `Border`로 바꾸고 투명 배경을 칠했다. 그림은 12px 그대로 두고 **누를 상자만 20×20으로** 넓혔다. 넓이·높이 각각 약 1.7배, 면적은 약 2.8배다. 두 상태 모두 좋아진다.
- 두 화살표가 같은 도우미(`SectionToggleIndicator`·`SetSectionToggleIndicator`)를 쓰게 해서 한쪽만 바뀌는 일이 없게 했다.
- **확인:** 화면 캡처로 제목·화살표·전체 선택 체크박스의 간격과 정렬이 그대로인 것을 확인했다.

- **변경 파일:** `App/MainWindow.Layout.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** 빌드 성공. `check-v22.ps1` 18개 전부 PASS. `error.log` 없음.
- **미해결·다음 작업:**
  - 화살표 그림 자체는 12px 그대로다. 잡기가 아니라 **보이기**가 문제라면 크기를 따로 올려야 한다.
  - 메인 헤더 아이콘 버튼 다섯 개의 접근성 이름이 비어 있다.


## 2026-09-03 — 세부 제목 날짜 표기를 아홉 조합 모두 같은 양식으로

- `오늘`·`이번 주`·`다음 주` 세 탭과 `카테고리순`·`시간순`·`미완료` 세 보기를 오갈 때 날짜 표기가 서로 달랐다. 주 단위 제목만 연도 없는 `08/30 ~ 09/05`였다.
- `DetailShortRange`가 `DetailDateValue`를 쓰게 해 **범위 제목을 하나의 양식으로 합쳤다.** `DetailIncompleteRange`는 같은 함수를 부르므로 둘이 어긋날 수 없다.
- 형식 자체는 선택 날짜 제목과 같은 사용자 설정(`yy/MM/dd` 또는 `MM/dd/yy`)을 따른다. 표기를 고정값으로 새로 박지 않았다.
- **확인:** 아홉 조합을 모두 눌러 실측했다. 범위 제목은 전부 `26/08/30 ~ 26/09/05` 형태로 폭 190px, 단일 날짜는 `26/09/03 (목)` 폭 120px, 기준선은 모두 y=436으로 같다. 제목 영역이 236px라 잘리지 않는다.
- 검사에 짧은 형태(`start.ToString("MM/dd"`)가 되살아나면 실패하는 단언을 넣었다.

- **변경 파일:** `App/MainWindow.Detail.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** 빌드 성공. `check-v22.ps1` 18개 전부 PASS. `error.log` 없음.
- **작업 메모:** 첫 번째 탭의 글자는 선택 날짜가 오늘이면 `오늘`, 아니면 `선택 날짜`로 바뀐다. 자동화로 이 탭을 누를 때 이름 하나로 찾으면 조용히 실패한다.


## 2026-09-03 — 세부 머리글 흔들림, 기념일 `+` 자리, 미완료 제목에 연도

### 1. 시간순·카테고리순·미완료를 오갈 때 날짜가 위아래로 움직였다

- 세부 머리글의 `중요한 날` 별 버튼을 범위 제목일 때 `Collapsed`로 감췄다. **자리까지 없어져 머리글 높이가 줄고**, 날짜 제목과 오른쪽 도구 아이콘이 함께 위로 밀렸다.
- **실측:** 날짜 제목 y=436 → 미완료에서 y=434, 도구 아이콘 y=438 → 436. 2px 차이다.
- `Hidden`으로 바꿔 자리는 두고 그림만 감춘다. 클릭은 이미 `IsHitTestVisible`로 따로 막고 있었다.
- **확인:** 네 가지 상태(카테고리순·시간순·미완료·범위 제목) 모두 제목 y=436, 도구 y=438로 같다.

### 2. 기념일 `+` 버튼이 접고 펼 때 움직였다

- 기념일 머리글은 접힘 여부에 따라 높이가 26과 18로 달라진다. `+`가 **가운데 정렬**이라 그 차이만큼 자리가 바뀌었다.
- **실측:** 펼침 y=970 → 접힘 y=975. 제목 글자는 970에 그대로 있어 어긋남이 더 잘 보였다.
- 제목 글자와 같은 윗선에 서도록 `VerticalAlignment.Top`으로 바꿨다. 머리글 높이는 건드리지 않아 접힘 상태의 여백은 그대로다.
- **확인:** 펼침·접힘·되돌림 모두 y=880, x=3706으로 같다.
- 이 버튼에 접근성 이름이 없어 `기념일 만들기`를 달았다. 도구 설명만 있고 이름이 없던 상태였다.

### 3. 미완료 제목에 연도를 넣었다

- 미완료 목록은 몇 달 전 항목까지 거슬러 올라가는데 제목이 `08/03 ~ 09/03`이라 어느 해인지 알 수 없었다.
- `DetailIncompleteRange`만 `DetailDateValue`를 쓰게 했다. **형식은 선택 날짜 제목과 같은 사용자 설정(`yy/MM/dd` 또는 `MM/dd/yy`)을 따른다.**
- 이번 주·다음 주 제목(`DetailShortRange`)은 연도가 필요 없어 짧은 형태로 남겼다.
- **확인:** 미완료 `26/08/03 ~ 26/09/03`(폭 190px), 주간 `08/30 ~ 09/05`(130px). 제목 영역이 236px라 잘리지 않는다.

- **변경 파일:** `App/MainWindow.Detail.cs`, `App/MainWindow.Anniversary.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 세 항목 모두 UI Automation 좌표로 실측. `error.log` 없음.
- **미해결·다음 작업:**
  - 고정 상태의 사이드바 그룹 접힘(`온하루 일정`·`Google`)은 여전히 실기기 확인이 필요하다.
  - 메인 헤더 아이콘 버튼 다섯 개의 접근성 이름이 비어 있다.


## 2026-09-03 — 고정 상태에서 죽어 있던 사이드바 그룹 접힘, 블랙 스킨 더보기 팝업 글씨

### 1. 고정 상태에서 `온하루 일정`·`Google` 접힘이 먹지 않았다

- 고정 화면은 캐시된 그림이라 클릭을 WPF가 직접 받지 않는다. **적중 지도에 담긴 요소만 눌린다.** 지도는 `Button`·`CheckBox`·`Slider`와 몇몇 태그만 담는다.
- 이 머리글들은 `TextBlock`과 `ContentControl`에 `MouseLeftButtonUp`을 단 것이라 **지도에 아예 담기지 않았다.** 그래서 이동에서는 되고 고정에서만 아무 일이 없었다.
- 세 곳을 함께 고쳤다. 한 곳만 고치면 동작하지 않는다.
  - `MainWindow.Layout.cs`: 다섯 요소(`온하루 일정`·`Special Day`·`Google` 글자와 두 화살표)에 `Tag = "toggle_section"`.
  - `ExplorerFramePublisher.cs`: 이 태그를 적중 지도에 담는다.
  - `MainWindow.DesktopInput.cs`: 찾아서 같은 `MouseLeftButtonUp`을 대신 올리고 고정 화면을 다시 그린다.
- 같은 부류의 D-Day·기념일 머리글은 원래 `SectionTitleButton`이라 고정에서도 되고 있었다. 손대지 않았다.

### 2. 블랙 스킨에서 더보기 팝업 글씨가 보이지 않았다

- 더보기 팝업은 다른 팝업과 마찬가지로 **스킨과 무관하게 흰 바탕**이다. 그런데 일정 글자색을 달력용 `EventTextBrush`로 계산했다.
- **블랙 스킨의 일정 글자색은 순백 `#FFFFFF`다.** 흰 팝업 위 대비율이 **1.00**, 곧 글자가 사라진다. 제목은 `T("Text")`, `수정 불가`는 `T("Disabled")`로 같은 문제였다.
- 팝업 전용 색 계산을 `MainWindow.Theme.cs`에 만들고(`PopupThemeId = "classic"`, `PopupEventTextBrush`, `PopupEventBackgroundBrush`, `StylePopupCheckBox`) 팝업 안에서만 쓴다. 제목과 `수정 불가`는 팝업 공통 색으로 바꿨다.
- **수치 확인:** 기본 팔레트 여섯 색에서 블랙 글자색은 전부 `#FFFFFF`, 흰 바탕 대비 1.00이었다. 밝은 스킨 기준으로 계산하면 `#103A93`·`#8B1147` 같은 짙은 색이 되고 대비는 5.37~11.23으로 모두 WCAG 4.5:1을 넘는다.
- **화면 확인:** 블랙 스킨에서 `+ 2개 더보기`를 열어 제목·일정 글자·`수정 불가`가 모두 읽히는 것을 캡처로 확인했다.
- `theme-check.ps1`에 이 팝업 안에서 달력용 색 함수를 쓰면 실패하는 단언을 넣었다.

- **변경 파일:** `App/MainWindow.Layout.cs`, `App/MainWindow.DesktopInput.cs`, `App/ExplorerFramePublisher.cs`, `App/MainWindow.Calendar.cs`, `App/MainWindow.Theme.cs`, `App/feature-pack-check.ps1`, `App/theme-check.ps1`, `HANDOFF.md`.
- **검증:** 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 더보기 팝업은 수치와 화면 양쪽으로 확인. `error.log` 없음.
- **미해결·다음 작업:**
  - **고정 상태 그룹 접힘은 실기기 확인이 필요하다.** 합성 클릭이 고정 레이어에 닿지 않아 자동 확인을 못 했다. 이번에는 이동 상태에서도 합성 클릭이 먹지 않아 시험 장치 자체가 신뢰할 수 없었다. 고정으로 두고 `온하루 일정`·`Google` 글자를 눌러 확인해 달라.
  - 메인 헤더 아이콘 버튼 다섯 개의 접근성 이름이 여전히 비어 있다.
  - `open_anniversary` 태그는 `MainWindow.DesktopInput.cs`가 처리하지만 그 태그를 다는 곳이 없다. 죽은 분기다.


## 2026-09-03 — 시각 입력칸 가운데 정렬, 수기 입력칸 전체 선택, 접힘·펼침 버튼 자리 고정

### 1. 시각 입력칸이 가운데가 아니었다

- 알람의 `타이머 분`·`시각`, 시간표의 `시작 시각`은 **정렬을 지정한 적이 없어 왼쪽 정렬**이었다. 오른쪽 여백만 크게 남아 같은 줄의 라벨·버튼과 눈높이가 어긋나 보였다.
- 세 칸에 `TextAlignment.Center`를 넣었다. 시간표의 `Box()`는 시작 시각 전용이라 함수 자체에 넣었다.
- **실측:** UI Automation으로 각 칸의 실제 사각형을 받아 화면을 떠서 글자 잉크의 좌우 여백을 셌다.
  - 알람 시각 `15:29` 왼 38 / 오른 38, 어긋남 0px.
  - 알람 타이머 `10` 왼 38 / 오른 38, 어긋남 0px.
  - 시간표 시작 시각 `09:00` 왼 34 / 오른 34, 어긋남 0px. 교시 시각 칸 아홉 개도 0~0.5px.
- **일정 창은 원래 정확히 가운데였다.** 알림 `10`은 왼 23 / 오른 23, 반복 횟수 `10`은 왼 20 / 오른 19였다. 이쪽은 손대지 않았다.

### 2. 수기 입력칸을 누르면 값이 통째로 선택되게 했다

- 값을 바꾸려면 지우거나 더블클릭해야 해서 불편하다는 요청이다.
- `UiRound.SelectAllOnFocus`를 만들고 `StyleTextBox` 끝에서 부른다. **스타일을 거치는 모든 한 줄 입력칸이 자동으로 적용된다.** 알람 세 칸, 시간표 시작 시각, 새 일정 제목·알림·반복 횟수, 설정 기본 알림, 기념일 이름·날짜, 연월 이동, 일기 제목이 여기에 든다.
- 스타일을 거치지 않는 칸은 직접 붙였다. 시간표 표 안의 칸(`Editor`)과 검색창 입력칸이다.
- **여러 줄 칸은 글을 쓰는 곳이라 제외한다.** 메모와 일기 본문이 그렇다. `AcceptsReturn`을 나중에 켜는 칸이 있어 판단을 실행 시점으로 미뤘다.
- 시간표의 과목 칸은 줄바꿈을 받지만 값 자체는 짧다. 이런 칸을 위해 `alwaysSelect`를 두고 과목 칸에만 켰다.
- **두 번째 클릭은 커서 옮기기다.** 이미 그 칸에 포커스가 있으면 아무것도 하지 않는다.
- **실측:** 알람의 `알림 이름` 칸에 포커스를 주고 `Z` 한 글자를 보냈더니 `알림` → `Z`로 통째로 바뀌었다.

### 3. 접힘·펼침 버튼이 옛 자리에 서 있던 문제

원인이 둘이었다.

- **접힌 상태로 시작하면 기준 좌표를 잰 적이 없다.** 버튼의 세로 위치 `sidebarToggleTop`은 세부 일정의 기간 스위치가 배치될 때만 갱신되는데, 접혀 있으면 스위치가 배치되지 않아 초기값 `8`이 그대로 쓰인다. 한 번 펼치면 제자리로 내려오고 **다시 접어도 그 자리를 지킨다.** 그래서 같은 접힘 상태인데 자리가 두 가지가 됐다.
  - 실측: 접힘으로 시작 `y=122` → 펼침 `y=390` → 다시 접음 `y=390`.
  - 첫 배치 직후 사이드바를 잠깐 펼쳐 좌표만 재고 곧바로 되돌리는 `EnsureSidebarTogglePlacement`를 넣었다. 같은 디스패처 작업 안에서 끝나 화면에는 펼쳐진 모습이 나타나지 않는다.
  - 확인: 접힘 상태로 다시 시작해 `y=390`. 고친 지점을 직접 겨냥한 시험이다.
- **고정 상태에서 자리를 옮기고 다시 그리지 않았다.** 고정 화면은 캐시된 프레임이라 자리만 바꾸면 옛 그림이 그대로 남는다. 갱신 시 `SchedulePublish()`를 부르게 했다.
- 배치 전 좌표(0)를 받아들이지 않도록 `ActualHeight`와 `top > 0` 가드도 넣었다. 기준 좌표계로 쓰던 지역 변수 `body`는 필드 `bodyGrid`로 올렸다.

- **변경 파일:** `App/AlarmWindow.cs`, `App/TimetableWindow.cs`, `App/SearchWindow.cs`, `App/UiRound.cs`, `App/MainWindow.cs`, `App/MainWindow.Layout.cs`, `App/MainWindow.Startup.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 위 세 항목 모두 실행 중인 시험본에서 실측. `error.log` 없음.
- **미해결·다음 작업:**
  - 사용자 확인 필요: 새 일정 제목처럼 긴 값을 담는 칸도 첫 클릭에 전체 선택되는 것이 편한지. 두 번째 클릭으로 커서를 옮길 수 있게는 해 뒀다.
  - 메인 헤더의 아이콘 버튼 다섯 개에 **접근성 이름이 없다.** 자동화 트리에서 이름이 빈 칸으로 나온다. 이전에 `날짜 변경`·설정 인쇄·정보 버튼에서 고친 것과 같은 종류다.
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다.


## 2026-09-03 — 고정 상태 두 건: 기간 스위치 간헐 무반응, 알람 창이 뜨지 않던 문제

- **요청:** 고정에서 `4주` 토글이 다섯 번에 한 번쯤 안 먹는다. 알람 시계가 고정 상태에서는 안 나타나고 이동으로 옮기면 나타난다.

### 1. 기간 스위치 간헐 무반응 — 더블클릭으로 와서 삼켜졌다

- 고정 상태 입력 처리에는 더블클릭 전용 분기가 있고, 거기서 **`dateColorButton`만 통과시키고 나머지는 아무것도 하지 않고 반환**한다. 색상 팔레트가 더블클릭의 뒷절반에 닫히지 않게 하려고 넣은 처리다.
- 기간 스위치는 요구가 정반대다. **두 번째 클릭이 토글로 닫는 것이 정상 동작**인데, 빠르게 누르면 그 클릭이 `WM_LBUTTONDBLCLK`으로 도착해 여기서 삼켜졌다.
- 천천히 누르면 두 번 다 단일 클릭이라 잘 됐다. **`다섯 번에 한 번`이라는 증상은 클릭 속도에 달린 것**이었다.
- 스위치는 더블클릭의 뒷절반도 보통 클릭으로 흘려보내도록 예외를 뒀다. 앞선 작업에서 넣은 `바깥 클릭으로 닫기` 예외와 짝을 이룬다.

### 2. 알람 창이 고정 상태에서 뜨지 않았다 — `Owner` 때문

- `OpenAlarm`만 `new AlarmWindow { Owner = this }`를 쓰고 있었다. **고정 상태에서는 메인 WPF 창이 cloak되는데 WPF는 소유된 창을 소유자와 함께 숨긴다.** 그래서 창은 만들어졌지만 보이지 않았고, 이동으로 바꿔 소유자가 다시 보이는 순간 그제야 나타났다.
- 같은 부류의 독립 도구 창인 시간표와 KBO는 **Owner 없이** `PlaceCalendarDialog`로 배치한다. 알람만 달랐다.
- `Owner`를 떼고 시간표와 같은 형태로 맞췄다. 최소화 복원과 `Activate()`도 함께 넣었다.

### 검증

- **알람:** 이동 상태에서 알람을 연 뒤 고정으로 전환해 **알람 창이 그대로 남아 있는 것**을 창 목록과 화면 캡처로 확인했다. 고친 지점을 직접 겨냥한 시험이다. 수정 전이라면 이 시점에 창이 사라진다.
- **기간 스위치:** 앞서 고정 상태에서 열기·닫기를 캡처로 확인했다. 이번 더블클릭 예외는 코드 경로가 명확해 사용자 실기기 확인을 남긴다.
- 두 예외 모두 `feature-pack-check.ps1`·`popup-policy-check.ps1`에 사라지면 실패하는 단언을 넣었다.

### 작업 메모 — 합성 클릭은 고정 레이어를 검증할 수 없다

- 사용자가 직접 고정한 상태에서 `mouse_event` 합성 클릭을 여섯 번 보냈으나 `4주`도 `블랙`도 반응이 없었고 `settings.json`도 바뀌지 않았다. 반면 **UI Automation으로 고정한 직후에는 같은 클릭이 동작했다.**
- 즉 합성 입력이 Explorer 레이어에 닿는지가 상황에 따라 달라진다. **고정 상태 조작 검증은 사용자 실기기 확인이 필요하다.** 앞서 `이동` 버튼이 눌리지 않았던 것도 제품 결함이 아니라 이 한계였을 가능성이 크다.

- **변경 파일:** `App/AlarmWindow.cs`, `App/MainWindow.DesktopInput.cs`, `App/feature-pack-check.ps1`, `App/popup-policy-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 알람은 위 시나리오로 실측. `error.log` 없음.
- **미해결·다음 작업:**
  - 실기기 확인 필요: 고정 상태에서 `4주`를 **빠르게** 두 번 눌러도 목록이 닫히는지.
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다.


## 2026-09-03 — 고정 상태에서 기간 스위치 주 수 목록이 닫히지 않던 문제 수정

- **증상:** 이동 상태에서는 `4주`를 다시 누르면 1~6주 목록이 닫히는데, 고정 상태에서는 닫히지 않는다.

### 원인 — 두 번 처리돼 닫았다가 다시 열었다

- 고정 상태 입력은 `MainWindow.DesktopInput.cs`의 히트테스트를 거친다. 거기에 **바깥을 누르면 목록을 닫는** 처리가 있다.
  `if (weekCountOverlay != null && !IsInside(targetElement, weekCountOverlay)) CloseWeekCountOverlay();`
- 기간 스위치 자신은 목록 바깥이므로 **이 줄이 먼저 목록을 닫는다.** 곧이어 같은 함수가 버튼의 `Click`을 올리면
  스위치의 `Clicked`가 `OpenWeekCountPopup()`을 부르는데, `weekCountOverlay`가 이미 `null`이라 **다시 연다.**
- 결과적으로 닫혔다 열려서 사용자에게는 아무 반응이 없는 것처럼 보인다.
- 이동 상태는 이 히트테스트 경로를 타지 않고 WPF가 바로 `Clicked`를 부르므로 토글이 정상이었다.

### 고친 방법

- 바깥 클릭으로 닫는 처리에서 **기간 스위치 자신을 예외**로 뒀다. 스위치를 누른 경우는 스위치의 토글이 처리한다.
- `feature-pack-check.ps1`에 이 예외가 사라지면 실패하는 단언을 넣었다.

### 검증 — 두 상태를 모두 실제로 눌러 확인했다

- **이동:** UI Automation으로 `4주`를 두 번 눌러 주 수 버튼 개수가 `1 → 7 → 1`로 바뀌는 것을 확인했다(7은 스위치 1 + 목록 6).
- **고정:** 실제로 고정으로 전환해 Explorer 레이어 위에 합성 클릭을 보냈다. 첫 클릭에서 1~6주 목록이 열리고 **두 번째 클릭에서 닫히는 것을 화면 캡처로 확인**했다.

- **변경 파일:** `App/MainWindow.DesktopInput.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 위 두 상태 실측. `error.log` 없음.
- **미해결·다음 작업:**
  - **확인 필요:** 고정 상태에서 `이동` 버튼에 합성 클릭을 두 번 보냈으나 이동 상태로 돌아오지 않았다. 같은 방식의 클릭이 `4주`에는 정상 동작했다. 합성 입력의 한계인지 실제 결함인지 판단하지 못했다. **사용자가 실제로 마우스로 눌러 확인해 주어야 한다.** 앱을 다시 시작하면 `StartupPositionMode: editable` 설정에 따라 이동 상태로 돌아온다.
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다.


## 2026-09-03 — 블랙 달력 날짜 칸을 `Card` 역할색으로

- **요청:** 블랙 메인 달력 배경이 너무 밝다. 방금 만든 카드색을 쓰면 어떨까.

### 실측으로 대상을 특정했다

- 캡처에서 픽셀을 뽑아 비교했다. 밝은 것은 달력 바탕이 아니라 **날짜 칸**이었다.
  - 날짜 칸 `#45454D` · 달력 바탕 `#1B1B1B` · 상세 패널 `#191919` · 셸 `#1B1C1C`
- 칸만 주변보다 크게 밝아 격자가 떠 보였다.

### 고친 방법

- `OnharuStateColors.CalendarCell`의 블랙 하드코딩 `#45454D`를 팔레트의 `Card` 역할색으로 바꿨다. 직전 작업에서 넣은 `#FF27272A`다.
- 상세 카드와 달력 칸이 같은 역할색을 공유하므로 앞으로 한 곳만 고치면 함께 따라온다.
- 파스텔은 기존 `CardBorder`를 그대로 쓴다. 값이 바뀌지 않았다.
- 적용 후 다시 픽셀을 뽑아 날짜 칸이 `#27272A`로, 상세 패널과 같은 값이 된 것을 확인했다.

- **변경 파일:** `App/OnharuStateColors.cs`, `App/theme-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 블랙으로 전환해 전체 화면을 캡처하고 픽셀값을 확인한 뒤 파스텔로 되돌리고 `settings.json`의 `ThemeId`가 `classic`인 것까지 확인했다.
- **미해결·다음 작업:**
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다.
  - 웹 게시용 최종 버전 정리가 남았다.


## 2026-09-03 — 블랙 스킨 시간순·미완료 카드의 형광 분홍 수정 (팔레트 키 누락)

- **증상:** 블랙 스킨에서 `시간순`이나 `미완료 일정`으로 보면 상세 카드가 형광 마젠타로 나온다. 사용자는 선택 테두리 색과 같은 값을 쓰는 것 아닌지 의심했다.

### 원인 — 선택 색과 무관했다. 팔레트 키가 없었다

- `OnharuThemePalette`의 인덱서는 **없는 키에 `#FF00FF` 마젠타를 돌려준다.** 누락을 눈에 띄게 하려는 의도된 장치다.
- 상세 패널의 시간순·미완료 카드가 `T("Card")`를 쓰는데 **팔레트에 `Card` 키가 없었다.** 그래서 그 두 카드만 마젠타가 됐다.
- 파스텔에서는 같은 자리를 `settings.ThemeId == "dark" ? T("Card") : Brushes.White`로 분기해 흰색을 직접 썼기 때문에 드러나지 않았다. **블랙에서만, 그것도 두 모드에서만 보이는 조건이라 오래 남아 있었다.**
- 소스가 쓰는 `T("...")` 키 15개를 팔레트의 정의 15개와 전수 대조했다. **`Card` 하나만 없었고 나머지는 모두 있다.**

### 고친 방법

- 팔레트에 `Card`를 넣었다. 파스텔 `#FFFFFF`, 블랙 `#FF27272A`. 블랙 값은 사이드바 `#181818`보다 밝고 격자 `#3F3F46`보다 어두운 중간 단계라 카드가 패널 위에 떠 보인다.
- 두 스킨이 같은 역할색을 쓰게 되어 호출부의 스킨 분기 두 곳을 없앴다.

### 재발 방지 — 문자열이 아니라 실행으로 검사한다

- `theme-check.ps1`에 **소스의 모든 `T("키")`를 정규식으로 모아 팔레트에 실제로 물어보는** 검사를 넣었다. 마젠타가 나오면 실패한다.
- 이 종류는 문자열 단언으로 잡히지 않는다. `T("Card")`라는 표현 자체는 아무 규칙도 어기지 않기 때문이다. 실행해서 값을 확인해야만 잡힌다.
- 검사 정규식이 망가져 0건을 훑는 상황을 막으려고 최소 10개 이상 찾았는지도 함께 단언한다.

- **변경 파일:** `App/OnharuTheme.cs`, `App/MainWindow.Detail.cs`, `App/theme-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 블랙으로 전환해 시간순 모드의 `하루 종일`·`시간 일정` 카드가 차분한 다크 카드로 나오는 것을 캡처로 확인하고 파스텔로 되돌린 뒤 `settings.json`의 `ThemeId`가 `classic`인 것까지 확인했다.
- **미해결·다음 작업:**
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다.


## 2026-09-03 — 범위 탭 블루 통일, 상세 도구 글리프 원복, `+` 아이콘 크기 정렬

- **요청:** 범위 탭은 양쪽 스킨 다 밝은 블루로. 상세 도구 아이콘 12px는 너무 작으니 원복. `+` 버튼을 기념일 카드의 `+`와 같은 사이즈·정렬로 통일하고 상세 도구도 `+`와 맞춘다.

### 범위 탭 — 두 스킨 모두 `#3B82F6`

- `OnharuStateColors.DetailPeriodTab`에서 스킨 분기를 없애고 한 값으로 통일했다.
- **후보를 실제 상세 카드 색과 함께 렌더해 골랐다.** 파스텔 보라는 회보라 카드와, 블랙 피치는 KBO 갈색과 계열이 겹쳤다. 중립 진회색은 선택이 아니라 비활성으로 읽혔다. 청록은 `design-onharu` 3.4가 성공·연결 의미색으로 제한한다.
- `#3B82F6`은 **선택일 테두리 기본값과 같은 값**이다. 이 제품에서 파랑은 이미 `현재 선택`을 뜻하므로 새 색을 들이지 않는다.
- `theme-check.ps1`의 고정 팔레트 토큰 두 곳을 갱신했다.

### 상세 도구 글리프 — 12px 원복

- 앞선 작업에서 12px로 줄였으나 너무 작았다. 기본 크기로 되돌리고 상수 `DetailToolGlyphSize`도 제거했다.

### `+` 아이콘 — 형제 아이콘과 획 넓이를 맞췄다

- **크기 문제가 아니라 도형 문제였다.** 버튼은 넷 다 18×18로 이미 같았는데, `add` 십자만 18칸 안에서 5~13(8칸)을 써서 형제 아이콘(3~15, 12칸)보다 좁았다. 그래서 같은 버튼 안에서 혼자 작아 보였다.
- `add`를 4~14로 넓혔다. 상세 헤더의 도구 셋, 상세 `+`, 기념일 카드의 만들기 버튼이 모두 같은 글리프 넓이를 갖는다.
- 확대 캡처로 상세 `+`와 기념일 `+`가 같은 크기임을 확인했다. 남는 차이는 색뿐이며, 기념일 쪽은 카드 카테고리 색을 따르는 의도된 차이다.

- **변경 파일:** `App/OnharuIcons.cs`, `App/OnharuStateColors.cs`, `App/MainWindow.Theme.cs`, `App/theme-check.ps1`, `Docs/Design/MAIN_HEADER_UI_STANDARD.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 실행 후 상세 패널을 캡처하고 두 `+` 버튼을 4배 확대해 나란히 비교했다.
- **미해결·다음 작업:**
  - 블랙 스킨에서 새 파란 범위 탭을 실기기로 확인해야 한다.
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다.


## 2026-09-03 — 블랙 스킨 검토와 상세 도구 아이콘 크기 수정

- **요청:** 블랙 스킨을 검토한다.

### 검토 결과 — 대부분 양호

- 일정 막대 대비, 헤더 아이콘 흰색 통일, 슬라이딩 버튼 선택 상태, 상세 카드 배경·글자, Google 계정 카드 모두 문제없다.
- 이번에 옮긴 투명도 슬라이더도 어두운 배경에서 중립 회색으로 잘 보인다.
- **처음 훑을 때 의심한 두 가지는 오판이었다.** 완료 일정 취소선과 주차 열(W35~W38)은 확대해 보면 충분히 읽힌다. 전체 화면에서 작게 보고 판단하면 안 된다.
- 설정창과 팝업 달력은 `OnharuPopupChrome` 고정 밝은 팝업이라 스킨과 무관하다. 설정창은 색상 카드만 블랙 색을 미리 보여 준다. 정상이다.

### 고친 것 — 상세 도구 아이콘이 버튼을 꽉 채웠다

- `DetailToolButton`이 버튼을 18×18로 줄이는데 글리프는 기본 크기 17px로 그려져 **여백이 0.5px**이었다. 도형이 버튼 가장자리에 닿는다.
- 특히 가로로 넓은 `detail_category`는 활성 상태에서 배경을 가득 메워 아이콘이 아니라 **덩어리**로 보였다.
- **파스텔도 같은 상태였고** 블랙에서 활성 배경이 진해 드러났을 뿐이다. 스킨 문제가 아니라 크기 문제였다.
- 글리프를 12px로 그리도록 바꿨다. 버튼 크기와 배경은 그대로다. 상수 `DetailToolGlyphSize`에 근거를 남겼다.

### 결정 대기 — 블랙 범위 탭 색

- 후보 다섯을 **실제 블랙 상세 카드 색과 함께** 렌더해 비교했다. 피치는 KBO 갈색과, 보라는 품질관리팀 회보라와 계열이 겹친다. 중립 진회색은 겹치지 않지만 선택보다 비활성으로 읽힌다. 청록은 `design-onharu` 3.4가 성공·연결 의미색으로 제한한다.
- 밝은 블루 `#3B82F6`를 추천했다. ONHARU의 선택일 테두리 기본값과 같은 값이라 이 제품에서 파랑은 이미 `현재 선택`을 뜻한다.

### 작업 메모

- **시험본을 강제 종료하면 마지막 설정 저장이 유실될 수 있다.** 스킨을 파스텔로 되돌린 뒤 `Stop-Process -Force`로 종료했더니 다음 실행에서 블랙으로 돌아와 있었다. 다시 전환하고 `settings.json`의 `ThemeId`가 `classic`으로 바뀐 것을 확인했다. 앞으로 설정을 바꾼 뒤에는 파일로 확인하거나 정상 종료를 쓴다.

- **변경 파일:** `App/MainWindow.Theme.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 블랙·파스텔 두 스킨에서 상세 도구 아이콘을 확대 캡처해 넷 모두 같은 여백을 갖는 것을 확인했다. 검토 후 스킨을 파스텔로 되돌리고 저장까지 확인했다.
- **미해결·다음 작업:**
  - 블랙 범위 탭 색 확정 대기.
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다.


## 2026-09-03 — 일정 창 좌우 여백 대칭과 팝업 달력 색 교체

- **요청:** 새 일정·일정 수정 창의 좌우 여백이 달라 보인다. 팝업 달력이 오렌지에 가까운데 ONHARU는 바이올렛·파랑 계열이니 그쪽이나 중립으로 바꾼다.

### 좌우 여백

- `panel`과 `fixedFooter`가 왼쪽 22, **오른쪽 14**였다. 스크롤바가 생길 자리를 미리 뺀 값으로 보이나, 이 창은 대개 내용이 다 들어와 스크롤바가 없어 오른쪽만 8px 좁아 보인다.
- 양쪽 22로 맞췄다. 실측으로 좌우 여백이 30/30으로 같아진 것을 확인했다.
- 설정창도 같은 방식(26/18)이지만 그쪽은 내용이 길어 스크롤바가 늘 보이므로 지금은 두었다.

### 팝업 달력

- 색이 ONHARU와 무관한 따뜻한 계열이었다. 바탕 `#FFF8F2` 크림, 글자 `#6D3B47`·`#5B3540` 자주빛 갈색, 날짜 칸 테두리 `#F3D4C7` 살구, 오늘 `#34B89A` 민트, 선택 `#E56B6F` 코랄, 월 버튼 `#FDE8E3`·`#B4474D` 연분홍. **일곱 색이 모두 제품 색 체계 밖이었다.**
- 바탕과 날짜 칸은 중립(`#FFFFFF`·`#E2E8F0`·`#334155`)으로, 오늘과 선택만 인터페이스 강조색 계열(`#EDE9FE`·`#6D5CC6`)로 바꿨다. 연보라 `#EDE9FE`는 검색창 범위 버튼과 같은 값이다.
- 이 달력이 뜨는 창(일정 등록·수정, 기념일, 일기, 검색, 메인 날짜 선택기)은 모두 밝은 팝업이라 스킨을 나누지 않는다.
- 검사가 잡고 있던 색은 없어 검사 변경은 필요 없었다.

### 함께 고친 접근성 회귀

- 앞선 작업에서 `날짜 변경` 버튼의 `Content`를 문자열에서 아이콘+텍스트 패널로 바꾸면서 **자동화 이름이 비었다.** 화면 낭독기와 자동 검사가 버튼을 찾지 못한다. `AutomationProperties.SetName`으로 복구했다.
- 같은 이유로 설정창의 인쇄·정보 버튼도 아이콘만 있어 이름이 비어 있었다. 툴팁 문구를 이름으로도 넣었다.
- **아이콘 전용 버튼이나 패널을 Content로 넣는 버튼은 이름을 따로 지정해야 한다.** 앞으로 같은 교체를 할 때 함께 확인한다.

- **변경 파일:** `App/AddItemWindow.cs`, `App/OnharuCalendarStyle.cs`, `App/SettingsWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 실행 후 수정 창 좌우 여백을 UI Automation으로 재고(30/30), 날짜 변경 버튼을 눌러 팝업 달력을 캡처·확대해 확인했다.
- **미해결·다음 작업:**
  - 설정창의 좌우 여백(26/18)도 맞출지 결정 필요. 그쪽은 스크롤바가 늘 보인다.
  - 블랙 스킨 실기기 확인이 아직이다.
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다.


## 2026-09-03 — 상세 카드 왼쪽 강조 막대 원복 (시험 종료)

- **요청:** 그냥 처음으로 원복한다. 코너가 둥글어서 강조색 막대가 이상해 보인다. 직사각형이면 어울릴 것 같다.
- **판단 근거가 명확하다.** 카드가 `CornerRadius(12)`라 왼쪽 변만 그리면 막대의 위아래 끝이 모서리를 따라 좁아진다. 확대해 보면 형태 자체는 깨끗하지만, 직사각형 막대가 주는 `띠` 느낌이 나오지 않는다. 둥근 카드와 세로 띠는 서로 맞지 않는 조합이다.
- `MainWindow.Detail.cs`, `MainWindow.Anniversary.cs`, `CategoryColorSystem.cs` 세 파일을 **막대 도입 전(`d9032d3`)과 완전히 동일한 상태로** 되돌렸다. `git diff`로 세 파일 모두 차이 없음을 확인했다. 쓰이지 않게 된 `DetailAccentBar`와 `MixBlack`도 함께 제거해 죽은 코드를 남기지 않았다.
- `feature-pack-check.ps1`의 상세 카드 테두리 단언도 원래 토큰으로 되돌렸다.
- **함께 진행했던 다른 변경은 유지한다.** 범위 탭 파스텔 선택색(인터페이스 강조색)은 별도 결정이라 그대로 둔다.

### 시험에서 남은 것

- 세 단계를 거치며 확인한 사실을 기록해 둔다. 나중에 같은 아이디어가 다시 나올 때 처음부터 반복하지 않기 위해서다.
  1. **배경을 어둡게 섞는 방식은 못 쓴다.** 카드 배경은 원색을 80% 흰색과 섞은 값이라 채도를 올려도 원색이 살아나지 않고 결과가 탁해져 그림자로 읽힌다.
  2. **색을 뽑는다면 `StrongAccent(배경)`가 맞다.** 옅은 파스텔에서 또렷한 강조색을 뽑는 함수이고 배경 기준이라 카테고리·중요 일정·Google 색을 규칙 하나로 덮는다. 원본 카테고리 색 기준은 중요 일정 카드에 적용할 수 없다. 그 카드 배경은 사용자가 고른 임의의 색이다.
  3. **형태가 문제였다.** 색을 아무리 맞춰도 둥근 모서리 카드에서는 세로 띠가 어울리지 않는다. 다시 시도한다면 카드를 직사각형으로 바꾸는 것이 전제다.
- `TODO_NEXT.md`의 `상세 카드 왼쪽 강조 막대` 후보는 이 결론으로 닫는다.

- **변경 파일:** `App/MainWindow.Detail.cs`, `App/MainWindow.Anniversary.cs`, `App/CategoryColorSystem.cs`, `App/feature-pack-check.ps1`, `TODO_NEXT.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 세 소스 파일이 `d9032d3`과 동일함을 `git diff`로 확인했다. 실행 후 상세 패널 캡처로 원래 모습을 확인했다.
- **미해결·다음 작업:**
  - 블랙 스킨 실기기 확인이 아직이다(범위 탭 피치, 새 아이콘).
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다.


## 2026-09-03 — 상세 카드 테두리를 없애고 왼쪽 막대만 남김

- **요청:** 왼쪽만 두고 테두리는 없앤다. 네 변이 다 강조색이라 촌스럽다.
- `BorderThickness`를 `(4, 1, 1, 1)`에서 `(4, 0, 0, 0)`으로 바꿨다. WPF `Border`는 두께가 0인 변을 그리지 않으므로 왼쪽 막대만 남고 나머지 세 변은 사라진다. **별도 요소를 넣는 구조 변경 없이 해결된다.**
- `CornerRadius(12)`가 그대로 적용돼 막대의 위아래 끝이 카드 모서리를 따라 부드럽게 좁아진다. 확대해 보면 렌더 결함이 아니라 의도한 형태로 읽힌다.
- 일반 상세 카드와 Special Day 카드 양쪽에 같은 값을 적용했다.
- 카드 배경과 강조색은 직전 작업의 `StrongAccent` 기준을 그대로 쓴다.

- **변경 파일:** `App/MainWindow.Detail.cs`, `App/MainWindow.Anniversary.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 실행 후 상세 패널을 캡처하고 확대해 막대 끝 처리를 확인했다.
- **미해결·다음 작업:**
  - 블랙 스킨 실기기 확인이 아직이다. `StrongAccent`는 어두운 배경에서도 채도를 끝까지 올린다.
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다. 현재는 슬라이딩 버튼 줄 왼쪽이다.


## 2026-09-03 — 강조 막대 색을 StrongAccent로 바꾸고 Special Day 카드까지 확대

- **요청:** 막대가 너무 어두워 그림자처럼 보이고 구분이 안 된다. D-Day·기념일 카드는 막대가 없어 깔끔하지만 밋밋하다.

### 왜 그림자처럼 보였나

- 카드 배경은 원색을 **80% 흰색과 섞은** 아주 옅은 색이다(`Background` = `MixWhite(Vivid(color), .80)`).
- 앞선 구현은 그 옅은 배경에서 다시 채도를 올린 뒤 20% 검게 섞었다. **옅은 색은 채도를 올려도 원색이 살아나지 않아** 결과가 탁한 어두운 색이 되고 그림자로 읽힌다.
- 후보 다섯을 실제 색상 함수로 계산해 렌더한 뒤 골랐다. 배경을 어둡게 섞는 안과 체크박스 색(58% 흰색)은 각각 탁하거나 배경과 구분되지 않았다.

### StrongAccent를 쓰는 이유

- `CategoryColorSystem.StrongAccent`는 주석에 적힌 대로 **옅은 파스텔에서 또렷한 강조색을 뽑으려고 만든 함수**다. 지금 필요한 것이 정확히 그 동작이다.
- **배경색에서 뽑으므로 규칙이 하나다.** 카테고리 카드, 중요 일정 카드, Google 색을 모두 덮는다. 원본 카테고리 색을 쓰는 안은 중요 일정 카드에 적용할 수 없다. 그 카드의 배경은 사용자가 고른 임의의 색이라 원본 색이 없기 때문이다. 직전 작업에서 고친 `중요 카드만 기준이 다르다`는 문제를 다시 만들지 않으려면 배경 기준이어야 한다.
- `DetailAccentBar`의 `theme` 인자를 없앴다. 두 스킨이 같은 계산을 쓰므로 쓰이지 않는 인자를 남기지 않는다.

### Special Day 카드

- D-Day·기념일 카드는 `MainWindow.Anniversary.cs`의 `SpecialDetailCard`라는 다른 경로로 만들어져 막대가 없었다. 같은 막대를 넣었다. 막대가 카테고리 신호인데 일부 카드에만 있으면 방금 고친 것과 같은 종류의 불일치가 남는다.

- **변경 파일:** `App/CategoryColorSystem.cs`, `App/MainWindow.Detail.cs`, `App/MainWindow.Anniversary.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 실행 후 상세 패널 캡처로 네 카드 모두 각자 배경 계열의 또렷한 막대를 갖는 것을 확인했다.
- **미해결·다음 작업:**
  - **테두리 전체가 강조색이 됐다.** WPF `Border` 하나로는 변마다 다른 색을 줄 수 없어 `BorderThickness(4,1,1,1)`을 쓰면 나머지 세 변도 같은 색이 된다. 왼쪽만 강조하고 나머지를 옅게 하려면 격자 안에 별도 막대 요소를 두는 구조 변경이 필요하다. 사용자 판단 대기.
  - 블랙 스킨 실기기 확인이 아직이다. `StrongAccent`는 어두운 배경에서도 채도를 끝까지 올리므로 과할 수 있다.


## 2026-09-03 — 상세 카드 강조 막대 색 기준 변경과 범위 탭 선택색 확정

- **요청:** 강조 막대는 글씨색이 아니라 배경색을 진하게. 범위 탭은 주황보다 이전 계열이 나아 보이니 가장 어울리는 색을 추천. 투명도 슬라이더는 일단 둔다.

### 강조 막대 — 배경색 기준으로 통일

- 이전에는 테두리 색이 세 갈래였다. 중요 일정 카드는 `EventTextBrush`(글씨색), 시간순 카드는 중립, 나머지는 `CategoryColorSystem.DetailBorder`. **중요 일정 카드만 글씨색을 기준으로 삼아 혼자 다른 계열로 보였다.**
- `CategoryColorSystem.DetailAccentBar(theme, background)`를 새로 두고 **카드 배경색에서 뽑도록** 통일했다. 파스텔은 같은 색을 어둡게(`MixBlack .20`), 블랙은 배경이 이미 어두우므로 선명하게 올린다(`MixWhite .12`).
- 카드 배경이 흰색·카드색인 시간순 모드는 뽑을 색이 없어 중립 테두리를 그대로 쓴다.
- 색 계산은 `CategoryColorSystem` 한 곳에서만 한다. 검사가 이 함수의 존재와 호출을 단언한다.

### 범위 탭 선택색 — 파스텔만 교체

- 후보 다섯을 실제 스위치 모양으로 렌더해 비교한 뒤 골랐다. 사용자가 파스텔은 주황보다 이전 인디고 계열을, 블랙은 주황을 선호했다.
- **파스텔은 인터페이스 강조색 `OnharuStateColors.ActionAccent`(`#6D5CC6`)**로 정했다. 이전 하드코딩 `#4338CA`는 채도가 높아 탭 전체가 무거웠고, 이 프로젝트가 이미 쓰는 역할색을 쓰면 새 하드코딩이 생기지 않는다.
- **블랙은 피치 `#C2410C`를 유지**한다. 어두운 배경에서 잘 읽힌다.
- `OnharuStateColors.DetailPeriodTab` 한 곳에서 스킨을 나눈다.
- `MAIN_HEADER_UI_STANDARD.md`의 `선택 상태는 현재 프리셋 대표색`은 구현과 맞은 적이 없어 실제 규칙으로 고쳤다.

- **변경 파일:** `App/CategoryColorSystem.cs`, `App/MainWindow.Detail.cs`, `App/OnharuStateColors.cs`, `App/feature-pack-check.ps1`, `Docs/Design/MAIN_HEADER_UI_STANDARD.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 실행 후 상세 패널 캡처로 네 카드(품질관리팀·KBO·D-Day·기념일)의 왼쪽 막대가 각자 배경색 계열로 진해진 것과 범위 탭 보라를 확인했다.
- **미해결·다음 작업:**
  - 투명도 슬라이더 위치는 사용자가 더 고민하기로 했다. 현재는 슬라이딩 버튼 줄 왼쪽이다.
  - 블랙 스킨에서 강조 막대와 범위 탭 피치를 실기기로 확인해야 한다.


## 2026-09-03 — 검증된 커밋 9ba28f5 시험판을 onharu.app에 게시

- **확정 기준:** 버전 `2.2.6-test`, Assembly `2.2.6.0`, Product `2.2.6-test`, 커밋 `9ba28f5`, 파일 `Tests/LocalTest/ONHARU-2.2-local-test.exe`, 크기 735,232 bytes, 빌드 2026-09-03 01:08:23, SHA-256 `427F5F4F36435ACC9FBF12F78DF00BAAC2F5F5C50B324DDE9FCB5892E9E78105`. 게시 시작 시 워킹트리는 깨끗했고 품질 게이트 18개 PASS 기록과 일치했다.
- 지정된 실행 파일을 재빌드하지 않고 그대로 패키징했다. 설치판 2,417,992 bytes, SHA-256 `21A165C4C15BCA86D8F3AD9830B07C92C709C97DBB4E884906AA6BC931F4B942`; 포터블 500,252 bytes, SHA-256 `7370466D2E28428B9296B20833A537B3A3340A333C03C7A00C43D3E8A6A5E1BC`.
- 다운로드 페이지에 커밋 `9ba28f5`, 새 해시와 캐시 키 `v=20260903-1`을 표시했다. 정식판·GitHub Latest·자동 업데이트는 2.2.4를 유지한다.
- **백업:** `/home/ubuntu/onharu-web-backups/site-before-2.2.6-9ba28f5-20260903-0200.tar.gz`.
- **검증:** 서버 해시·크기 일치, 공개 URL 재다운로드 SHA-256 일치, 다운로드 페이지의 커밋·해시·캐시 키 PASS, Nginx active. 전송·검증 임시 폴더는 제거했다.


## 2026-09-03 — 전체 검토 제안 반영 (A·C·D·E 그룹)

- **요청:** 전체 검토에서 낸 제안에 사용자가 항목별로 답을 달았다. 채택한 것만 반영한다.
- **채택:** A1·A2·A4·A5·A6·A7·C1·C2·C3·C6·D1·E1 / **보류:** A3·B1·B2·C5·C7 / **문서화만:** C4

### 보류한 것과 이유 (사용자 판단)

- **A3 하루 종일·오전·오후를 라디오로** — 선택하면 아래 시간 옵션이 또 라디오라 이중 라디오가 어색해서 체크박스를 고른 것. 의도된 선택이므로 두다.
- **B1 달력 막대의 시간 표시 제거** — 세부 일정을 닫거나 창을 키우면 해결되는 문제. 의도됨.
- **B2 오늘·선택일 색 구분** — 설정에서 사용자가 고르는 값이다.
- **C5 D-Day 카드에 `+` 추가** — 기념일은 태어난 날 기준의 별도 일정이고 D-Day는 일반 일정에서 관리한다. 성격이 달라 비대칭이 맞다.
- **C7 `Tasks · Juan.HJLe` 잘림** — 더 긴 이름이 올 수 있고 Task가 여럿일 수 있어 잘려도 무방하다.

### A. 일정 등록·수정 창

- **A1 저장 경로의 `.First()` 8곳** — 설정창을 죽인 것과 같은 모양이라 먼저 처리했다. 재귀 조회 `CheckedRadioTag`·`CheckedRadio`·`RadioByTag`와 비활성 헬퍼 `SetOptionsEnabled`를 **`OnharuPopupChrome`으로 올려** 설정창과 일정 창이 함께 쓴다. 설정창의 사본은 제거했다. 선택이 없으면 예외 대신 대체값을 돌려준다.
- **A2 제목** — `OnharuPopupChrome.FeatureHeading`으로 바꿨다. 새 일정은 `add`, 수정은 `✎` 도형이다. 이 창만 22px 자체 TextBlock에 글꼴 기호를 얹고 있었다. `날짜 변경`의 이모지 `📅`는 `OnharuIcons`에 새로 넣은 `calendar` 도형으로 바꿨다.
- **A4 비활성 표현** — `IsEnabled`만 걸던 8곳을 `SetOptionsEnabled`로 통일했다.
- **A5 왼쪽 기준선** — 카테고리 카드만 좌우 안여백이 14라 안쪽 항목이 2px 밀려 있었다. 다른 카드와 같은 12로 맞췄다.
- **A6 높이** — 종료날짜 버튼 32→34, 알림 숫자·단위와 반복 횟수를 27→28로 맞췄다.
- **A7 역할색** — `온하루 등록` 배지의 평면 인디고를 중립으로 바꾸고 이름도 `온하루 일정`으로 고쳤다. `날짜 변경` 버튼의 파랑도 중립으로 바꿨다. Google 배지의 초록은 연결 완료 의미색이라 유지한다.

### C. 세부 달력

- **C1** `온하루 등록` → `온하루 일정`. 아래는 카테고리 필터이지 등록 동작이 아니다. 전체 선택 툴팁도 함께 고쳤다.
- **C2** `Special Day Card` → `Special Day`. 설정창과 표기를 맞췄다.
- **C3** 범위 탭 선택색의 하드코딩 `#4338CA`를 걷어냈다. 이제 파스텔도 `OnharuStateColors.DetailPeriodTab`을 타며 `design-onharu` 3.4의 `현재 선택은 피치 계열` 규칙을 따른다.
- **C6** 상세 카드 왼쪽 강조 막대를 넣었다. **시험 적용이다.** 새 색을 만들지 않고 `CategoryColorSystem`이 이미 계산한 테두리 색을 왼쪽만 4px로 두껍게 한다. 되돌리려면 `MainWindow.Detail.cs`의 `BorderThickness = new Thickness(4, 1, 1, 1)`을 `new Thickness(1)`로만 바꾸면 된다.

### D·E

- **D1** 필터 그룹 접기 표시 `▲`·`▼`를 `OnharuIcons`의 `chevron_up`·`chevron_down` 벡터로 바꿨다. 상세 카드 앞의 `●`·`◆`·`✦`는 아이콘이 아니라 목록 기호라 두었다.
- **E1** 투명도 슬라이더를 브랜드 줄에서 슬라이딩 버튼 줄 왼쪽으로 옮겼다. 그 줄은 폭 384 중 스위치가 288을 써서 왼쪽 96px이 비어 있다(연·월 제목 줄은 72px뿐이라 78px 슬라이더가 안 들어간다). 색은 같은 줄의 중립 스위치와 맞추도록 `OpacitySliderBrush()` 한 곳에서 정한다. 브랜드 줄은 브랜드 글자만 남아 1열 Grid로 줄였다.

### C4 — 코드는 그대로, 문서만

- Google 계정 카드의 브랜드 그라데이션은 사용자가 현재 모습을 유지하기로 했다. `AGENTS.md`·`CLAUDE.md`·`design-onharu.md`에 **허용 사용처로 명시**했고 새로 늘리지 않는다고 적었다.

- **변경 파일:** `App/AddItemWindow.cs`, `App/MainWindow.Layout.cs`, `App/MainWindow.Theme.cs`, `App/MainWindow.Detail.cs`, `App/OnharuIcons.cs`, `App/OnharuPopupChrome.cs`, `App/SettingsWindow.cs`, `App/feature-pack-check.ps1`, `App/theme-check.ps1`, `App/popup-policy-check.ps1`, `AGENTS.md`, `CLAUDE.md`, `design-onharu.md`, `Docs/Design/MAIN_HEADER_UI_STANDARD.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 실행 후 메인 화면 캡처로 슬라이더 이동·중립색, 필터 라벨, 셰브론, 범위 탭 피치 선택색, 상세 카드 왼쪽 막대를 확인했다.
- **미해결·다음 작업:**
  - **C6는 시험 적용이다.** 사용자가 보고 아니면 되돌린다.
  - 범위 탭 선택색이 인디고에서 피치(`#C2410C`)로 바뀌었다. 채도가 높아 사용자 확인이 필요하다.
  - 옮긴 투명도 슬라이더가 스위치와 떨어져 왼쪽 끝에 서 있다. 스위치 바로 옆에 붙일지 확인이 필요하다.
  - `MAIN_HEADER_UI_STANDARD.md`가 규정하는 `선택 상태는 현재 프리셋 대표색`과 `design-onharu` 3.4의 `현재 선택은 피치 계열`이 서로 다르다. 한쪽으로 정리해야 한다.
  - `MainWindow.Navigation.cs`의 `BindCalendarNavigation`은 호출부가 없다. 리팩토링 정리 대상.


## 2026-09-03 — 일정 수정 창 확인과 헤더 `‹ • ›` 이동 버튼 삭제

- **요청:** 9월 11일 테스트 일정으로 수정 창을 확인하고, 끝나면 연·월 제목 옆 `‹ • ›`를 삭제한다.

### 수정 창 확인

- 오늘 날짜의 일정이 모두 읽기 전용 Google 항목이라 처음에는 수정 창이 열리지 않았다. 달력에서 날짜를 한 번 눌러 선택한 뒤 오른쪽 상세의 라벨을 더블클릭하는 순서로 열었다. 저장하지 않고 닫았다.
- 새 일정과 수정 창을 모두 캡처하고 컨트롤 좌표를 전수 측정했다. 발견 사항은 사용자에게 보고했고 주요 항목은 아래 `다음 작업`에 남긴다.

### `‹ • ›` 삭제

- `HeaderMonthButton`으로 만들던 `‹`·`•`·`›` 셋과 `monthArrows` 묶음을 없앴다. `HeaderMonthButton`은 이 셋이 유일한 사용처라 함께 지웠다.
- **기능 손실이 없음을 먼저 확인했다.** 월·주 이동은 달력 요일 행 양 끝의 `‹ ›`가 담당한다(한 주 이동, 더블클릭 한 달 이동). 임의 날짜 이동은 연·월 제목 클릭으로 열리는 날짜 선택기 `OpenMonthJump`가 담당한다. 오늘로 이동은 오른쪽 상세의 `선택 날짜` 탭 더블클릭이 담당한다.
- `monthNavigation`은 2열 Grid에서 제목만 있는 1열로 줄였다.
- `MainWindow.Navigation.cs`의 `BindCalendarNavigation`은 호출부가 없어졌다. 화살표를 되살릴 가능성이 있어 지우지 않고 리팩토링 정리 대상으로 남긴다.
- `theme-check.ps1`의 관련 토큰 셋을 빼고, 대신 대체 경로(`monthTitle.Click += OpenMonthJump`, 상세 탭 더블클릭)를 단언한다. `HeaderMonthButton`·`monthArrows`가 되살아나면 실패하는 단언도 넣었다.

- **변경 파일:** `App/MainWindow.Layout.cs`, `App/theme-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. 실행 후 헤더 캡처로 세 버튼이 사라지고 연·월 제목만 남은 것을 확인했다.
- **미해결·다음 작업:**
  - `Docs/Design/MAIN_HEADER_UI_STANDARD.md`의 이전·다음 버튼과 오늘 점 버튼 규정을 삭제 사실에 맞게 정정해야 한다.
  - 일정 등록·수정 창의 저장 경로에 `X.Children.OfType<RadioButton>().First(...)`가 8곳 있다. 설정창을 죽인 것과 같은 모양이라 이 창을 정렬 개편하기 전에 먼저 고쳐야 한다.
  - 같은 창의 제목이 `FeatureHeading`을 쓰지 않고 22px 자체 TextBlock이며 `✦`·`✎`·`📅`가 글꼴 기호·이모지다.
  - `하루 종일`·`오전`·`오후`는 코드상 배타 선택인데 체크박스로 그려진다.


## 2026-09-03 — 설정 저장 크래시 수정(내가 만든 회귀)

- **증상:** 사용자가 설정창에서 저장을 누르면 앱이 종료된다. `자꾸 꺼져`.
- **원인은 전날 내가 만든 회귀다.** 정렬 작업에서 `선택일 표시`·`오늘 표시` 행을 가로 StackPanel에서
  `라벨 열 + UniformGrid + 셀 StackPanel` 구조로 바꿨는데, 저장 처리는 여전히
  `selectionOptions.Children.OfType<RadioButton>()`으로 **패널의 직접 자식만** 훑고 있었다.
  라디오가 두 단계 안쪽으로 들어가 결과가 비었고 `First(predicate)`가
  `InvalidOperationException: 시퀀스에 일치하는 요소가 없습니다`를 던져 창이 닫혔다.
- 프로필과 무관하게 항상 재현된다. 빈 프로필에서도 저장만 누르면 죽는다.
- **고친 방법:** 자식을 재귀로 훑는 `CheckedRadioTag(root, fallback)`을 두고 세 곳(`selectionOptions`,
  `todayOptions`, `syncOptions`)을 여기로 돌렸다. 선택된 항목이 없으면 예외 대신 기존 값을 돌려주므로
  배치를 또 바꿔도 저장이 죽지 않는다. `syncOptions`는 지금 구조로는 문제가 없었지만 같은 함정을
  피하려고 함께 옮겼다.
- 구조를 바꾸지 않은 나머지 아홉 곳(`fontOptions`·`orderOptions`·`completedDisplay`·`startView`·
  `startupPosition`·`themeOptions`·`weekRules`·`startDay`·`reminderPositionGroup`)은 라디오가 여전히
  직접 자식임을 하나씩 확인했다.

### 자동 검사가 왜 못 잡았나

- **`check-v22.ps1` 18개는 크래시가 있는 빌드에서도 전부 통과했다.** 이 검사들은 소스 문자열을 비교하는
  방식이라 `selectionOptions.Children.OfType<RadioButton>()`이라는 표현 자체는 아무 규칙도 어기지 않았고,
  저장 경로를 실제로 실행해 보는 검사가 없다.
- 즉 자동 검사는 `무엇이 쓰여 있는가`는 보지만 `실행하면 어떻게 되는가`는 보지 않는다. 배치를 바꾸면서
  그 배치를 읽는 코드를 같이 고치지 않는 이 종류의 회귀는 지금 게이트로는 원리상 잡히지 않는다.
- 이번에도 **사용자가 먼저 발견했다.** 나는 캡처와 좌표 측정으로 배치만 확인했고 저장을 눌러 보지 않았다.
  화면 배치를 바꾼 작업에서는 그 값을 읽어 저장하는 경로까지 실제로 눌러 봐야 한다.
- 재발 방지로 `feature-pack-check.ps1`에 세 패널의 직접 자식 읽기를 금지하고 `CheckedRadioTag` 사용을
  단언하는 검사를 넣었다. 다만 이것도 문자열 검사이므로 같은 함정의 다른 사례는 여전히 못 잡는다.

- **변경 파일:** `App/SettingsWindow.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `check-v22.ps1` 18개 전부 PASS. **수정 전후 A/B로 확인했다.**
  격리 프로필에서 설정창을 열고 저장 버튼을 UI Automation으로 눌렀을 때
  수정 전 빌드는 위 예외와 함께 프로세스가 종료됐고, 수정 후 빌드는 살아남고 오류 로그가 없었다.
- **미해결·다음 작업:**
  - 이 수정은 `cf3a56e` 이후 변경이다. 커밋이 필요하다.
  - 배치를 바꾸는 작업의 검증 절차에 `그 값을 읽는 경로를 실제로 실행해 본다`를 넣을지 검토한다.
    지금은 `ui-construction-check.ps1`이 창을 만들기만 하고 동작은 실행하지 않는다.


## 2026-09-02 — 설정창 전체 검토와 정렬·역할색·순서 개편

- **요청:** 설정 디자인을 전체적으로 검토하고 개선 포인트를 제안한다. 이어서 제안한 1~4단계를 순서대로 모두 적용한다.
- 검토는 인상이 아니라 **실측**으로 했다. 창을 스크롤하며 전 구간을 캡처하고 UI Automation으로 모든 체크박스·라디오·콤보·버튼의 좌표를 뽑아 비교했다. 좌표는 125% DPI 물리 픽셀이다.

### 잘 되어 있던 것

- 카드 내용 왼쪽 기준선이 전 카드 61px로 통일돼 있었다.
- `화면과 동작` 8개 행은 라벨 열 폭이 이미 일관됐다.
- 시작 요일·쉬는 날 라디오 7개 간격이 균등했다(56~58).
- 색상 카드 3열 격자가 정확히 맞았다.
- 순서의 큰 틀(디자인·색 → 무엇을 표시할지 → 동작 → 데이터)이 타당했다.

### 1단계 — 규칙 위반 두 건

- **`✓ 설정 저장`이 평면 인디고 `#4338CA`였다.** 브랜드 그라데이션으로 바꿨다. `design-onharu.md` 3.4가 `설정 저장`을 명시적으로 예로 드는 항목인데, **어제 만든 대표 실행 버튼 검사에 다섯 창만 넣고 `SettingsWindow`를 빠뜨려서 못 잡았다.** 검사의 사각지대였다.
- **`일정 관리` 버튼 다섯이 각각 다른 의미색**이었다(보라·주황·초록·파랑·로즈). 3.4의 역할색 배정에 따라 백업 복원·가져오기·PC 저장·메일 보내기를 중립 `#F1F5F9 / #475569`로 바꾸고 로즈는 `일정 삭제` 하나만 남겼다. 색이 장식이 아니라 의미를 전달하게 된다.

### 2단계 — 정렬

- **라벨 열 폭이 다섯 종류였다.** 디자인 스킨 96, 일정 표시 순서 92, 날짜 표시 78, 화면과 동작 120, 입력 화면은 라벨 없음. 상수 `LabelColumn = 120`을 도입해 통일했다. 컨트롤 시작선이 `176/181/211/229/258` 다섯 갈래에서 **211 하나**로 모였다(12개 행).
- **다열 체크박스가 세로 열을 이루지 않았다.** `WrapPanel` 자유 흐름이라 행마다 시작 위치가 달랐다. 3열 `UniformGrid`로 바꿔 2·3열이 각각 282·502에 선다. **3열인 이유는** 가장 긴 `연속 일정은 항상 위에 표시`가 약 169px이라 4열(132px)에서는 잘리기 때문이다.
- `상단 기능 아이콘`은 아이콘 넷과 스위치 셋을 한 격자에 흘렸다. 두 줄로 나누면 넷과 셋이라 어느 쪽에도 열이 맞지 않고 빈칸이 생긴다.
- **라디오 간격이 글자 길이에 따라 벌어졌다.** `선택일 표시` 124/139, `오늘 표시` 124/152, `자동 동기화` 122/75/82/83. 라벨 열 + 균등 격자 구조로 바꿨다. 자동 동기화는 103/102/102/102로 균등해졌고, 선택일·오늘 표시는 색 스와치를 자기 라디오와 한 칸에 묶어 382/552 같은 기준에 섰다.

### 3단계 — 비활성 표현과 문자열 잘림

- 비활성 처리가 두 갈래였다. 드래그 하위는 `Opacity .45`, 알림 카드는 `IsEnabled`만. WPF 기본 회색이 옅어 알림 카드는 꺼져 있어도 활성처럼 보였다. `SetOptionsEnabled` 하나로 통일했다.
- `새 일정 기본 알림` 행은 아예 연동되지 않아 알림을 꺼도 선명했다. `알림 사용`에 연동했다. 불투명도가 겹쳐 곱해지지 않도록 안쪽 값·단위는 행이 살아 있을 때만 흐리게 한다.
- `파스텔 · 부드럽고 생동감 있는 기본 스킨`이 **말줄임표 없이 하드 클리핑**됐다. 라벨 열을 120으로 넓히자 칸이 205px로 더 좁아져 설명을 넣을 자리가 없었다. **카드에는 스킨 이름만 두고 설명은 툴팁으로 내렸다.** 이 카드의 본래 역할은 스킨의 실제 배경·글자색을 보여 주는 것이고 이름이 그것을 식별한다. 카드가 칸을 채우게 해 색 미리보기 면적도 넓어졌다. 앞으로를 위해 `TextTrimming.CharacterEllipsis`도 남겼다.

### 4단계 — 순서

- **제목 없는 카드**에 `새 버전 자동 확인`과 `드래그로 일정 옮기기`가 함께 있었다. 서로 무관해 제목을 붙일 수도 없었다. `일정 조작`과 `앱` 두 카드로 나눴다.
- **`화면과 동작`이 `메인 달력 표시 옵션` 카드 안의 하위 그룹**이었는데 시작 화면·시작 위치 상태·글자 크기처럼 달력 표시가 아닌 항목이 들어 있어 제목과 내용이 어긋났다. 독립 카드로 분리했다.
- **순서를 매직 인덱스 두 개로 맞추고 있었다.** `Insert(0, ...)`과 `Insert(2, ...)`라서 카드를 하나만 추가해도 `2`가 가리키는 자리가 바뀐다. 카드 열두 개의 순서를 배열 한 곳에서 정의하도록 바꿨다. 흐름은 `디자인 → 표시 → 조작 → 연동 → 입력 → 알림 → 데이터 → 앱`이다.

### 검사

- 대표 실행 버튼 규칙을 설정창까지 확장하고, 일정 관리의 장식 의미색이 되살아나면 실패하게 했다.
- 라벨 열 상수·균등 격자·비활성 헬퍼의 존재를 단언했다.
- **카드 순서를 매직 인덱스로 넣으면 실패**하게 했다. 배열 정의가 사라져도 걸린다.
- `theme-check.ps1`이 고정하던 `panel.Children.Insert(0, SectionCard(themeGroup))`를 배열의 첫 두 줄 단언으로 갱신했다.

- **변경 파일:** `App/SettingsWindow.cs`, `App/feature-pack-check.ps1`, `App/theme-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. **`check-v22.ps1` 18개 전부 PASS.** 시험본을 `LOCALAPPDATA` 격리로 실행해 설정창 전 구간을 다시 캡처하고 좌표를 재측정해 위 표의 값을 확인했다. 사용자 데이터에 쓰지 않았다.
- **미해결·다음 작업:**
  - `주차 (Week) 표시` 뒤의 `ISO 방식`·`일반` 라디오는 체크박스에 딸린 인라인 항목이라 3열 격자 밖에 있다. 별도 패턴으로 두었다.
  - `알림` 카드 첫 줄(`알림 사용`·`소리 사용`·`조용한 시간`)은 라벨 열이 아닌 인라인 배치다. 나머지 두 줄과 기준선이 다르다. 카드 안에서 한 번 더 정리할지 결정 필요.
  - 카드가 열둘로 늘어 스크롤 뷰가 전체의 40.5%다. 자주 쓰지 않는 카드를 접을 수 있게 할지 검토 대상이다.
  - 실기기 수동 확인 필요: 100% DPI에서 3열 격자의 가장 긴 항목이 잘리지 않는지, 블랙 스킨에서 흐린 항목의 대비가 남는지.


## 2026-09-02 — 설정 톱니·인쇄·정보·알람·시간표 아이콘을 다시 그리고 글꼴 대체 경로 제거

- **요청:** 설정창 아이콘이 너무 작고 바깥 설정 아이콘도 마음에 들지 않는다. 톱니바퀴를 제대로 그린다. 인쇄·정보도 같다. 이어서 야구·알람·시간표도 추천한다.

### 원인 — 아이콘이 작았던 이유는 크기가 아니라 글꼴 대체 경로였다

- `SettingsWindow`가 `FeatureHeading("⚙", ...)`를 부르는데 `OnharuIcons.Geometry`에는 `"settings"`만 있고 `"⚙"`가 없었다. **도형을 못 찾으면 `Draw`가 조용히 `Segoe UI Symbol` 글꼴로 떨어진다.** 이 글꼴의 `⚙`는 상자 대비 작고 얇아 제목 아이콘만 혼자 작아 보였다.
- 같은 이유로 시간표·KBO의 톱니 버튼도 `OnharuPopupChrome.Button("⚙", ...)`로 **글꼴 문자**를 그리고 있었다. 벡터를 쓰는 곳은 메인 헤더 하나뿐이었다.
- 인쇄 아이콘은 `SettingsWindow.cs` 안에 경로가 하드코딩돼 있었고 안쪽 사각형이 본체와 겹쳐 형태가 뭉갰다. 정보 아이콘은 Segoe UI 글자 `i`를 `TextBlock`으로 그렸다.
- 즉 `아이콘 도형은 OnharuIcons가 단일 기준`이라는 규칙이 네 곳에서 새고 있었다.

### 형태 — 크기가 아니라 그림이 문제였다

- 후보를 앱과 분리된 미리보기 하니스로 렌더해 **21px·42px·63px을 나란히 놓고** 파스텔·블랙 양쪽에서 비교한 뒤 골랐다. 실제 사용 크기에서 판단하지 않으면 큰 크기에서만 좋은 도형을 고르게 된다.
- **설정 톱니:** 이전 도형은 바깥 링에 가는 선을 방사형으로 붙인 형태라 톱니바퀴가 아니라 **조타륜이나 햇살**로 읽혔다. 실제 이빨을 가진 닫힌 윤곽으로 다시 그렸다. 톱니는 여섯이다. 여덟이면 21px에서 톱니 하나가 2.5px까지 좁아져 1.6px 획에 뭉친다.
- **알람:** 이전에는 종 귀가 가는 대각선이라 종이 아니라 **안테나**처럼 붙어 보였고 다리가 없어 알람시계로 읽히지 않았다. 둥근 종 둘 + 시계 + 바늘 + 다리 둘로 바꿨다.
- **시간표:** 이전에는 머리줄 아래 세로 두 줄뿐이라 시간표가 아니라 **그냥 3칸짜리 표**였다. 머리줄 + 틀을 관통하는 왼쪽 시간열 구분선으로 구조를 드러냈다. 가로줄을 더해 칸을 여섯으로 만드는 안은 21px에서 뭉쳐 쓰지 않았다.
- **야구공:** 현재 도형(원 + 실밥 곡선 둘)을 유지했다. 스티치를 넣으면 42px 이상에서는 야구공다움이 올라가지만 21px에서는 검은 덩어리가 된다. 사용자와 함께 실제 크기 렌더를 보고 유지로 결정했다.
- **인쇄:** 윗종이 + 본체 + 나오는 종이 세 갈래로 다시 그렸다. **정보:** 원 + `i`를 벡터로 그렸고 점은 둥근 끝 획이 그대로 점이 되도록 짧은 선분으로 뒀다.

### 정리한 호출 지점

- `SettingsWindow` 제목을 `FeatureHeading("settings", ...)`로 바꿔 내부 ID로 통일했다. `"⚙"`는 별칭으로 남겨 두어 기존 호출도 벡터로 떨어진다.
- 인쇄·정보 버튼을 `SettingsWindow.HeaderToolButton(glyph, toolTip)` 하나로 묶었다. 30×26 버튼에 21px 도형을 중앙 배치하며 도형을 창 안에 다시 그리지 않는다.
- 시간표·KBO의 톱니 버튼은 글자 대신 `OnharuIcons.Draw("settings", ...)`를 `Content`로 넣는다.

### 검사

- `popup-policy-check.ps1`에 `case "⚙": case "settings":`·`case "print":`·`case "info":` 존재와 각 창의 호출을 단언했다. 글꼴 문자나 하드코딩 경로로 되돌아가면 걸린다.
- **이 검사 파일의 인코딩 함정을 발견했다.** `popup-policy-check.ps1`은 BOM이 없어 Windows PowerShell 5.1이 `.ps1`과 `.cs`를 같은 ANSI 코드페이지로 읽는다. 양쪽이 똑같이 깨져서 한글 토큰 비교는 우연히 성립하지만, 비ASCII 문자가 문자열 끝에 오면 **뒤따르는 따옴표까지 삼켜** 토큰이 어긋난다. 한쪽에만 BOM을 붙이면 오히려 전부 깨지므로 인코딩은 그대로 두고, 새 단언은 `없어야 할 글꼴 문자` 대신 **`있어야 할 도형 호출`을 ASCII 토큰으로** 표현했다.
- `HeaderSize` 단언 토큰도 새 줄에 맞춰 갱신했다.

- **변경 파일:** `App/OnharuIcons.cs`, `App/SettingsWindow.cs`, `App/TimetableWindow.cs`, `App/SportsCalendarWindow.cs`, `App/popup-policy-check.ps1`, `design-onharu.md`, `Docs/Design/MAIN_HEADER_UI_STANDARD.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. **`check-v22.ps1` 18개 전부 PASS.** 시험본을 `LOCALAPPDATA` 격리로 실행해 메인 헤더, 설정창 헤더, 시간표 헤더, KBO 제목줄을 캡처해 네 곳의 톱니가 모두 같은 벡터인지, 인쇄·정보가 벡터인지, 새 알람·시간표 도형이 21px에서 뭉치지 않는지 확인했다. 격리 폴더에 기록 0건, 사용자 인스턴스 영향 없음.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 블랙 스킨과 100% DPI에서 새 톱니 이빨 여섯이 뭉치지 않는지, 알람 다리가 점으로만 보이지 않는지.
  - `popup-policy-check.ps1`의 BOM 없는 인코딩은 그대로 두었다. 리팩토링 단계에서 검사 스크립트의 파일 읽기를 전부 `-Encoding UTF8`로 통일하고 같은 작업에서 BOM을 붙이는 것이 맞다. 지금 한쪽만 고치면 기존 한글 토큰이 전부 깨진다.
  - 다음은 설정창 내부다. 사용자가 `설정을 좀 바꿔보자`로 시작한 작업이며 아이콘 정리가 그 첫 단계였다.


## 2026-09-02 — 현재 공유 소스에서 재빌드한 2.2.6 시험판으로 웹 교체

- **정정 요청:** 앞서 게시한 파일이 최종본이 아니며 Claude Code의 마지막 버전을 다시 올린다.
- 기존 실행 파일을 최신이라고 추정하지 않고, 현재 공유 작업 트리의 최신 C# 소스에서 `App/build.ps1`을 다시 실행했다. 새 표준 시험본 `Tests/LocalTest/ONHARU-2.2-local-test.exe`는 2026-09-02 08:53:55 생성, SHA-256 `CEB2421C26838C2747A22C305635F39682AEB0B3A6DD545646570C77B042DC52`다.
- 새 실행 파일로 설치판과 포터블을 다시 만들고 onharu.app 시험판 파일을 교체했다. 설치판은 2,417,316 bytes, SHA-256 `8848BA7D13B617C2E4E1A6D037BDCF5A49ED338A44894B76151C2CE09583CDF9`; 포터블은 499,397 bytes, SHA-256 `456C9379BD00E01D45BC07C1D42884409B7D87B8716EEED597024E3E9A72B6F1`이다.
- 다운로드 페이지 캐시 키를 `v=20260902-2`로 올리고, 이 파일이 정식 최종본이 아니라 현재 공유 소스에서 재빌드한 시험본임을 명시했다. 정식판·GitHub Latest·자동 업데이트는 2.2.4를 유지한다.
- **백업:** `/home/ubuntu/onharu-web-backups/site-before-2.2.6-rebuilt-test-20260902-0900.tar.gz`.
- **검증:** `check-v22.ps1` 18개 전부 PASS. 서버 해시 일치. 공개 URL에서 두 파일을 실제 재다운로드해 SHA-256 일치. 다운로드 페이지의 새 해시와 캐시 키 PASS. Nginx active.


## 2026-09-02 — Claude Code 마지막 시험 버전을 onharu.app에 게시

- **요청:** 현재 Claude Code가 작업한 마지막 테스트 버전을 웹에 게시한다.
- 오늘 생성된 표준 시험본 `Tests/LocalTest/ONHARU-2.2-local-test.exe`를 기준으로 `Release/Test/ONHARU-2.2.6-Test` 스테이지를 갱신하고 Inno Setup 시험 설치판과 포터블 ZIP을 다시 만들었다. 공개 정식판·GitHub Latest·자동 업데이트는 2.2.4를 유지했다.
- `Publish/ONHARU-Web/ONHARU-DOWNLOAD.html`의 시험판 설명·캐시 키·용량·SHA-256을 최신 값으로 바꾸고, 릴리스 노트의 과거 Clay 시간표 문구를 현재 메인 헤더·검색·시간표·알람·KBO 정돈 및 품질 게이트 18개 통과 내용으로 교체했다.
- 게시 전 `/var/www/html` 전체를 `/home/ubuntu/onharu-web-backups/site-before-2.2.6-latest-test-20260902-0805.tar.gz`로 백업했다.
- 게시 전 확인에서 Nginx가 2026-09-01 06:30 UTC부터 API Gateway 호스트의 일시적 DNS 조회 실패로 중지돼 있었다. 현재 DNS 확인과 `nginx -t`가 통과한 뒤 서비스를 재시작했고 로컬 HTTP 200을 확인했다. Nginx 설정 파일은 변경하지 않았다.
- **게시 파일:** `ONHARU-2.2.6-Test-Setup.exe` 2,417,313 bytes, SHA-256 `6108BA63E52B40FF22E5FEE313F008BD808A83CB324C57CF2178091971C85691`; `ONHARU-2.2.6-Test-Portable.zip` 499,397 bytes, SHA-256 `C5F50E0DA9A66B1D25170B92B7B7212455C205290B2E30CD905D264618624D13`; 다운로드 페이지와 릴리스 노트.
- **변경 파일:** `Release/Test` 시험 산출물, `Publish/ONHARU-Web/ONHARU-DOWNLOAD.html`, `Publish/ONHARU-Web/ONHARU-RELEASES.html`, `CURRENT_STATUS.md`, `WEB_OPERATIONS_HANDOFF.md`, `HANDOFF.md`.
- **검증:** `check-v22.ps1` 18개 전체 PASS. 서버 측 해시·용량과 로컬 원본 일치. onharu.app에서 두 파일을 실제 재다운로드해 SHA-256 일치. 공개 10개 페이지 HTTP 200. 다운로드 페이지의 새 해시와 `v=20260902-1` 캐시 키 표시 PASS. Nginx active.
- **정리:** 첫 백업 명령의 PowerShell 해석 오류로 원격 홈에 생긴 불완전 임시 파일 `/home/ubuntu/-C`, `/home/ubuntu/ -C`는 정확한 경로를 확인한 뒤 제거했다. 게시용 원격 임시 폴더와 로컬 재다운로드 검증 폴더도 검증 후 제거했다.


## 2026-09-02 — 투명도 슬라이더·일정 저장 버튼을 기준값으로 되돌리고 실화면 확인

- **요청:** 투명도 슬라이더와 일정 저장 버튼은 `현재 기준을 적용`한다. 온하루를 닫았으니 시험본을 실행해 확인한다.
- 두 항목 모두 **기준 문서 쪽으로 코드를 맞추는** 방향으로 해석했다. 둘 다 앞선 기록에서 `기준과 어긋난 상태`로 보고한 항목이다.

### 투명도 슬라이더 — 인터페이스 강조색

- `BrandBrush()`(브랜드 그라데이션) → `InterfaceAccentBrush()`(`OnharuStateColors.ActionAccent`, 파스텔 `#6D5CC6` · 블랙 `#7462CF`).
- 슬라이더는 브랜드 표기가 아니라 조작 컨트롤이다. 브랜드 그라데이션은 `온하루 · ONHARU` 글자와 연·월 제목 전용이라는 기존 규칙을 그대로 적용했다. 슬라이더가 브랜드 줄로 옮겨졌다는 배치 사정이 색까지 바꿀 근거는 되지 않는다.
- **색이 두 곳에 있었다.** `MainWindow.Layout.cs`의 생성 시점과 `MainWindow.Theme.cs`의 테마 적용 시점이다. 한쪽만 고치면 첫 화면과 스킨 전환 후 색이 달라진다. 둘 다 고치고 `theme-check.ps1`이 둘 다 검사하게 했다.

### 일정 저장 버튼 — 브랜드 그라데이션

- `AddItemWindow`의 `✓ 일정 저장`이 평면 인디고 `#4338CA`였다. `OnharuPopupChrome.BrandGradientBrush()`로 바꿨다. 알람 `시작`, 시간표 `저장`·`적용`, KBO `선택 경기 등록`, 검색 `오늘`과 같은 브러시다.
- `OnharuPopupChrome.Button` 호출은 유지하고 브러시만 덮어썼다. 다른 창들이 쓰는 방식과 같고 기존 검사 토큰도 그대로 유효하다.
- **검사를 창 단위가 아니라 규칙 단위로 바꿨다.** `feature-pack-check.ps1`이 다섯 창의 대표 실행 버튼을 표로 돌며 한 번에 검사한다. 한 창만 다른 색으로 새는 이번 같은 일이 다시 생기면 여기서 걸린다.
- `design-onharu.md` 3.4의 `대표 실행 버튼은 온하루 블루 #4F46E5` 문장이 실제 구현과 어긋난 채 남아 있었다. 브랜드 그라데이션으로 고치고 평면 인디고를 새로 쓰지 않는다는 문장을 함께 넣었다.
- 같은 규칙을 `AGENTS.md`·`CLAUDE.md` 디자인 원칙에 **같은 문장으로** 추가했다. 공통 절 7개가 여전히 일치함을 확인했다.

### 실화면 확인

- 온하루가 닫힌 상태라 `build-local-test.ps1`로 표준 시험본 `ONHARU-2.2-local-test.exe`를 다시 만들었다. C++ `ExplorerLayer` 빌드는 `vswhere.exe`가 PATH에 없어 실패했으나 이번 변경과 무관하고 기존 `Onharu.LayerHost.exe`·`Onharu.DesktopHook.dll`이 그대로 있어 게이트 산출물 검사는 통과한다.
- 실행 후 확인: 응답 정상, 7초 시점 CPU 1.2초, `error.log` 없음.
- 헤더 아이콘 정렬이 새 빌드에서도 유지된다. 검색 버튼 왼쪽 `3452`, 기간 스위치 캡슐 왼쪽 `3451.5`(125% DPI 물리 좌표).
- 파스텔·블랙 두 스킨에서 브랜드 줄을 캡처해 확인했다. 브랜드 글자는 그라데이션, 슬라이더는 단색 보라로 서로 구분된다. 블랙에서도 슬라이더가 어두운 배경 위에서 읽힌다.
- UI Automation으로 `+` 버튼을 눌러 `새 일정` 창을 열고 저장 버튼을 캡처해 브랜드 그라데이션 적용을 확인했다. 확인 후 창을 닫고 스킨을 파스텔로 되돌렸다.

- **변경 파일:** `App/MainWindow.Layout.cs`, `App/MainWindow.Theme.cs`, `App/AddItemWindow.cs`, `App/feature-pack-check.ps1`, `App/theme-check.ps1`, `design-onharu.md`, `Docs/Design/MAIN_HEADER_UI_STANDARD.md`, `AGENTS.md`, `CLAUDE.md`, `CURRENT_STATUS.md`, `TODO_NEXT.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. **`check-v22.ps1` 18개 전부 PASS**(표준 `ONHARU-2.2-local-test.exe` 기준). 실행·응답 확인, `error.log` 없음. 파스텔·블랙 실화면 캡처로 두 색 변경을 확인했다. `AGENTS.md`·`CLAUDE.md` 공통 절 일치 확인.
- **미해결·다음 작업:**
  - `OnharuStateColors.OpacityControl`은 이 확정으로도 호출부가 없다. 리팩토링 단계 정리 대상이다.
  - C++ `ExplorerLayer` 빌드는 `vswhere.exe`가 PATH에 없어 이 환경에서 돌지 않는다. 고정 레이어 코드를 고칠 때 필요하므로 Visual Studio 빌드 도구 경로를 한 번 잡아 두는 편이 낫다.
  - 일정 수정 화면(삭제 버튼과 나란한 배치)은 같은 `save` 인스턴스를 Grid 1열에 넣을 뿐이라 구조상 같은 브러시다. 별도 확인이 필요하지 않다.
  - 실기기 수동 확인 필요: 고정 상태에서도 슬라이더 색과 저장 버튼이 이동 상태와 같게 보이는지.


## 2026-09-02 — 헤더 아이콘 정렬 수정과 자동 품질 게이트 18개 전체 복구

- **요청:** ① 검색 아이콘이 `4주` 스위치보다 왼쪽으로 튀어나온 것을 고치고 상단 기능 아이콘 위치를 맞춘다. ② 검사 복구를 마무리한다. 이후는 판단에 맡긴다.

### ① 메인 헤더 아이콘 줄 정렬 — 완료

- **원인:** 앞선 작업이 `AlignFeatureIconsToRangeSwitch`의 기준을 `획을 세운다`로 잡고 상수 `FeatureIconInk = (34-21)/2`만큼 안쪽 지점을 스위치 왼쪽에 맞췄다. 코드 자체는 의도대로 수렴하고 있었다.
- 문제는 기준 선택이었다. 헤더 아이콘 버튼은 흰 배경과 옅은 테두리(`#D6DCE8`)가 실제로 보이는 컨트롤이다. 획을 맞추면 **버튼 상자가 스위치보다 6.5px 왼쪽으로 튀어나온다.** 사용자가 본 것이 이 상자다.
- `CLAUDE.md`의 `컨트롤은 테두리를, 글자는 글자를 같은 기준선에 세운다`가 맞는 기준이다. `획을 세운다`는 상자가 없는 팝업 제목 글리프용 규칙이다. 기준을 버튼 테두리로 바꿨다.
- 앞쪽 아이콘을 설정에서 숨기면 `StackPanel`의 왼쪽과 남은 버튼의 테두리가 4px 여백만큼 어긋난다. `FirstVisibleFeatureButton()`을 두어 **처음으로 보이는 버튼**의 왼쪽을 기준점으로 삼는다. 상수 `FeatureIconInk`는 제거했다.
- **측정으로 확인했다.** 판단을 눈대중에 맡기지 않기 위해 실행 중인 창을 UI Automation으로 재고 화면을 캡처해 픽셀로 대조했다(125% DPI, 물리 좌표).
  - 수정 전: 검색 버튼 왼쪽 `3444`, 스위치 캡슐 왼쪽 `3451.5`. 7.5px 어긋남.
  - 수정 후: 검색 버튼 왼쪽 `3452`. 0.5px 이내로 일치.
  - 아이콘 줄 오른쪽 끝은 `3677 → 3685`로 이동했고 창 조작 버튼(`3699`)까지 14px 물리 간격이 남아 겹치지 않는다. 사용자가 맞다고 한 오른쪽 정렬(창 조작 `3807` ↔ `고정` `3806`)은 건드리지 않았다.
- 검증용 시험본은 `LOCALAPPDATA`를 임시 폴더로 돌려 격리 실행했다. 사용자 인스턴스의 설정·데이터에 쓰지 않았고 실행 후 정리했다.

### ② 검사 복구 — 완료. `check-v22.ps1` 18개 전부 PASS

**게이트 자체의 실행 환경 문제**

- `check-v22.ps1`이 `feature-pack-check.ps1`과 `theme-check.ps1`만 `pwsh`(PowerShell 7)로 호출하고 있었다. 이 PC에 `pwsh`가 없어 게이트가 두 번째 항목에서 멈췄다. **개별 검사를 아무리 고쳐도 게이트가 통과할 수 없는 상태였다.**
- 두 파일은 UTF-8 BOM이 있어 Windows PowerShell 5.1도 한국어 문자열을 그대로 읽는다. `pwsh`가 필요하지 않다. 나머지 검사와 같은 셸에서 실행하도록 바꾸고, 스크립트마다 다른 인자 이름(`-Exe` / `-ExePath`)만 표로 구분했다.

**`feature-pack-check.ps1` — 4건**

- `OnharuDetailCard:mode:*`, `IsWithinDetailCard(control)`, `ddayColorChanged`, `classic|automation:...` 단언 제거. **`git log -S`로 저장소 전체 이력을 확인한 결과 이 토큰들은 어떤 커밋의 C# 소스에도 존재한 적이 없다.** 커밋 `8902881`에서 검사 파일에만 추가됐다. 회귀를 잡던 검사가 낡은 것이 아니라 구현되지 않은 계획을 단언으로 적어 둔 것이었다. 요소별 색상 오버라이드 맵 자체가 이 설계에 없어 되돌릴 오버라이드도 없다. 대신 상세 카드 색·입력 판정이 공통 모듈을 벗어나지 않는지 단언한다. 아이디어는 `TODO_NEXT.md`에 남겼다.
- `$temporaryPaletteSource`는 정의된 적 없는 변수였다. 해당 블록과 함께 사라졌다.
- `ActionButton("✓  일정 저장"` → `OnharuPopupChrome.Button("✓  일정 저장"`. 저장 버튼이 공통 헬퍼를 쓴다는 목적은 유지된다.
- `SettingsGlyph(Brush foreground)` → `HeaderGlyph("settings"`. 이 단언은 `popup-policy-check.ps1`의 `SettingsGlyph가 되살아나면 실패` 단언과 **서로 모순이었다.** 2026-09-01 통합 작업의 의도가 명확하므로 통합된 쪽을 기준으로 잡았다.
- 시간표 `Height = 568` → `ApplyPreferredSize`의 계산식. 교시 수와 요일 수가 사용자 설정이라 고정 높이는 애초에 유지될 수 없는 단언이었다. `settingsPanel.Visibility` → `new TimetableSettingsWindow(data) { Owner = this }`. 인라인 접이식 패널이 POPUP_POLICY 3장 자식 차단 팝업으로 바뀐 변경이며 소스 주석에 근거가 있다.

**`theme-check.ps1` — 11건**

- 하나씩 실패를 좇는 대신 `throw`를 출력으로 바꾼 진단 사본으로 남은 실패 11개를 한 번에 뽑아 각각 판정했다.
- 헤더 개편 반영: `Grid.SetRow(opacitySlider, 0)` → `brandNavigation.Children.Add(opacitySlider)`, `Width = 83, Height = 18` → `78`, `monthTitle.Width = 306` → `compactHeaderWidth`(245) 기준 `monthNavigation`.
- 아이콘 통합 반영: `StrokeThickness = ... 1.8` → `OnharuIcons.cs`의 `DefaultThickness = 1.6`. 검사가 새 파일도 읽도록 `$iconSource`를 추가했다.
- 오늘 버튼 형태 변경 반영: `UpdateTodayButtonStyle()`·`todayButton.Background`·`todayButton.Foreground` → 점 버튼 생성과 더블클릭 이동 단언.
- 알람 기능 반영: `StyleLightHeaderActionButton(diaryButton, "✎")` → `"◴"`.
- 상세 추가 버튼: 고정 `Margin(8,0,13,0)` → 구조로 보장되는 `DockPanel.SetDock(detailAddButton, Dock.Right)`. 같은 줄의 기념일 추가 버튼은 현재 없다.
- **투명도 슬라이더 색은 코드에 맞추지 않고 판단을 보류했다.** 아래 결정 필요 항목 참고.

- **변경 파일:** `App/MainWindow.Layout.cs`, `App/feature-pack-check.ps1`, `App/theme-check.ps1`, `check-v22.ps1`, `Docs/Design/MAIN_HEADER_UI_STANDARD.md`, `TODO_NEXT.md`, `CURRENT_STATUS.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. **`check-v22.ps1` 18개 전부 PASS**(`Tests\LocalTest\ONHARU-2.2-gate.exe` 기준). 헤더 정렬은 UI Automation 좌표와 화면 캡처 픽셀로 수정 전후를 대조해 확인했다. `AGENTS.md`와 `CLAUDE.md`의 공통 절 7개는 자기 참조 문구 한 줄을 빼고 완전히 일치함을 확인했다(동기화 작업 불필요).
- **미해결·다음 작업:**
  - **결정 필요 — 투명도 슬라이더 색:** `MAIN_HEADER_UI_STANDARD.md`는 프리셋 대표색을 규정하는데 현재 코드는 `BrandBrush()`다. 2.2.5 기준선은 `InterfaceAccentBrush()`였고, 검사는 세 번째 형태인 `OnharuStateColors.OpacityControl(...)`을 요구하고 있었다. 셋이 서로 다르고 근거 기록이 없다. 문서에 `확인 대기`로 표시했고 검사는 `색을 한 곳에서만 정한다`는 실제 불변식만 지킨다.
  - **결정 필요 — 일정 저장 버튼 색:** `AddItemWindow`의 대표 실행 버튼이 평면 인디고 `#4338CA`다. 2.2.5에서는 그라데이션이었고, KBO 작업(2026-09-01)은 창의 대표 실행 버튼을 브랜드 그라데이션으로 통일한다고 정했다. 두 창의 규칙이 어긋난다.
  - `OnharuStateColors.OpacityControl`은 정의만 남고 호출부가 없다. 위 결정이 `유지`로 나면 리팩토링 단계 정리 대상이다.
  - `Tests\LocalTest\ONHARU-2.2-local-test.exe`는 실행 중이라 덮어쓸 수 없어 이번 변경이 반영되지 않았다. 인스턴스를 닫은 뒤 `build-local-test.ps1`로 다시 만든다. 게이트 검증에 쓴 최신 빌드는 같은 폴더의 `ONHARU-2.2-gate.exe`다.
  - **실기기 수동 확인 필요:** 검색 아이콘 버튼 테두리가 `4주` 캡슐 왼쪽과 한 선에 서는지, 설정에서 검색·시간표·알람·야구 아이콘을 하나씩 숨겼다 켜도 유지되는지, 아이콘 줄 오른쪽이 창 조작 버튼과 겹치지 않는지, 100% DPI에서도 맞는지.
  - `MAIN_HEADER_UI_STANDARD.md`를 현재 코드에 맞게 정정했다(아이콘 크기·획, 버튼 34×28, 오늘 점 버튼, actionArea 390, 행 간격 1px, 슬라이딩 버튼 24/22, 상세 범위 탭 슬라이딩 스위치, 아이콘 줄 정렬 기준). 앞선 기록의 `문서 정정 대기` 항목이 해소됐다.


## 2026-09-02 — 검사 복구(진행 중)와 체크포인트 커밋

- **요청:** 리팩토링 전에 ① 자동 검사 복구, ② 체크포인트 커밋을 진행한다. 디자인은 며칠 더 진행한다.

### ② 체크포인트 커밋 — 완료

- 8/28 이후 미커밋으로 쌓여 있던 93개 파일을 `8902881`로 커밋했다. 되돌릴 지점이 하나도 없던 상태가 해소됐다.
- `.gitignore`에 `*.exp`·`*.lib`를 추가했다. 링커 산출물이 추적 대상에 들어오고 있었다. `Tests/LocalTest`의 실행 파일은 기존 `*.exe`·`*.dll`·`*.pdb` 규칙으로 이미 제외되고, 그 안의 `.ps1` 도우미 스크립트 세 개만 소스로 추적한다.
- 작성자는 `juan.hjlee@gmail.com`으로 지정했다. 전역 `user.email`이 비어 있어 커밋 단위로 넘겼다. 앞으로도 커밋할 계획이면 전역 설정을 한 번 잡아 두는 편이 낫다.

### ① 검사 복구 — 부분 완료

**고친 것**

- `ui-construction-check.ps1` — `SettingsWindow` 생성자 인자 83개는 개수가 맞았고 **마지막 두 개의 순서만 어긋나** 있었다. 생성자는 `detailDateFormat(string), showFullColorPaletteValue(bool)` 순인데 검사는 반대로 넘겨 형변환에서 멈췄다. 순서를 바로잡았다. 이어서 설정창 폭 기대값을 개편에 맞춰 620에서 640으로 갱신했다.
- `theme-check.ps1` — 오늘 버튼이 없어진 게 아니라 형태가 바뀐 것이었다. `periodNavigation`의 42×23 텍스트 버튼에서 `monthArrows` 안의 점 버튼(더블클릭으로 오늘 이동)이 됐다. 토큰을 `monthArrows.Children.Add(todayNavigationButton)`으로 갱신하고, 같은 개편에서 사라진 `Margin = new Thickness(-8, 0, 0, 0)`도 뺐다.
- `feature-pack-check.ps1` — 일기장 기능 제외로 사라진 단언 다섯 개를 뺐다. 달력 날짜 숫자·점의 일기 열기 연결, 달력의 일기 표시, 리더 자동 닫기, 설정창의 일기장 항목이다. 바탕화면 더블클릭 경로와 히트 타깃 판정은 남아 있어 계속 검사한다. `BrandGradient()`를 공통 헬퍼로 올리며 바뀐 토큰도 갱신했다.
- `popup-policy-check.ps1` — 앞선 작업에서 이미 PASS.

**남은 것**

- `feature-pack-check.ps1`이 `OnharuDetailCard:mode:time`에서 계속 실패한다. 오른쪽 상세 카드 영역의 토큰이며 이번 디자인 작업 범위 밖이다.
- 전수 대조 결과 `theme-check.ps1`에 소스에 없는 토큰이 십수 개 더 남아 있다. 설정창 팔레트, 투명도 슬라이더, 메인 헤더 세부가 주된 영역이다.
- **이 토큰들은 기계적으로 지우면 안 된다.** 각각 `기능이 의도적으로 바뀐 것`인지 `회귀`인지 판단이 필요한데, 해당 작업의 의도가 `HANDOFF.md`에 기록돼 있지 않다. 검사를 코드에 무조건 맞추면 게이트가 회귀를 잡는 능력을 잃는다.
- 권장: 설정창·메인 헤더 개편을 진행한 작업자가 남은 토큰을 확인하는 것이 맞다. 판단 근거를 모르는 쪽이 정리하면 검사가 형식만 통과하는 껍데기가 된다.

- **변경 파일:** `App/ui-construction-check.ps1`, `App/theme-check.ps1`, `App/feature-pack-check.ps1`, `.gitignore`, `HANDOFF.md`.
- **검증:** 네 검사 중 `popup-policy-check.ps1` PASS, `search-check.ps1` PASS. `ui-construction-check.ps1`은 생성자 문제를 넘어 폭 단언까지 진행했다. `check-v22.ps1`은 `feature-pack-check.ps1`의 상세 카드 토큰에서 여전히 멈춘다. 커밋 후 워킹트리는 깨끗하다.
- **미해결·다음 작업:**
  - 위 `남은 것` 항목. 특히 `theme-check.ps1`의 남은 토큰은 의도 확인이 먼저다.
  - 디자인 작업은 계속 진행한다. 커밋 기준선이 생겼으므로 이후 변경은 `git diff`로 범위를 확인할 수 있다.
  - 리팩토링 단계 정리 대상은 `Codex-2.2.6-brief.md` 7장에 정리돼 있다.


## 2026-09-02 — 이틀간 디자인 작업 확정과 Codex 인수인계 문서 작성

- **요청:** 현재 상태로 확정하고, 오늘 변경 내용과 그동안의 문제를 정리한 문서를 Codex용으로 작성한다.
- `Codex-2.2.6-brief.md`를 새로 만들었다. 기존 `Claude-2.2.6-brief.md`와 짝이 되는 이름이다. 내용은 새로 생긴 단일 기준 파일, 화면별 결과, 확정한 창 규격 일곱 가지, 발견한 문제 세 갈래, 재발 방지로 검사에 고정한 항목, 환경 메모, 남은 결정 사항이다.
- 문제를 세 갈래로 나눠 적었다. 동작이 실제로 틀린 것 아홉 가지, 문서가 정한 규격과 다른 것 일곱 가지, 문서·검사 관리 문제 다섯 가지다. 각 항목에 근거가 되는 파일과 증상을 함께 남겼다.
- **저자 귀속은 하지 않았다.** 이 저장소는 로컬 `main` 하나이고 마지막 커밋이 2026-08-28이라 그 뒤 변경이 전부 미커밋으로 섞여 있다. 어떤 변경을 누가 만들었는지 구분할 수 없으므로 `코덱스가 한 잘못`이 아니라 `이 시점의 코드에서 발견한 문제`로 읽도록 문서 앞머리에 명시했다. 목적은 재발 방지이지 책임 규명이 아니다.
- 문서에 검사 실패를 다루는 판단 기준을 넣었다. 검사가 낡았는지 코드가 기준에서 벗어났는지 먼저 구분하고, 운영 코드를 과거 검사 문자열에 맞추지 않는다. `FeatureTitle` 건을 그 예로 들었다. 문서 다섯 곳이 호출되지 않는 죽은 API를 기준으로 삼고 있었고, 그래서 코드가 아니라 문서와 검사를 고쳤다.
- **변경 파일:** `Codex-2.2.6-brief.md`(신규), `HANDOFF.md`.
- **검증:** 문서 작업이라 빌드와 자동 검사는 실행하지 않았다. 직전 기록의 빌드 성공과 `popup-policy-check.ps1` PASS가 현재 기준이다.
- **미해결·다음 작업:**
  - 사용자가 현재 상태를 확정했다. 다음 단계는 리팩토링이며 그 뒤에 코드를 올린다.
  - 리팩토링 단계 정리 대상: 죽은 정의 `OnharuPopupChrome.FeatureTitle`·`DisclosureButton`·`SetDisclosure`, 메인 창 `MainWindow.Navigation.cs`의 `ResizeEdgeAt`·`ResizeCursor` 사본을 `OnharuPopupChrome.EnableResize`로 합치기.
  - 문서 정정 대기: `CURRENT_STATUS.md`의 `자동 품질 게이트 17개: PASS`, `MAIN_HEADER_UI_STANDARD.md`의 아이콘 줄 정렬 기준과 상단 오늘 버튼 규정.
  - 검사 정리 대기: `feature-pack-check.ps1` 일기 단언 14곳, `ui-construction-check.ps1`의 `PlannerSettings` 생성자 인자, `theme-check.ps1`의 `periodNavigation.Children.Add(todayButton)`.


## 2026-09-02 — KBO 보기 설정 팝오버 상시 표시·이동 지원과 메인 헤더 아이콘 왼쪽 정렬

- **요청 1:** KBO 설정 팝오버를 계속 띄워 두고 X를 눌렀을 때만 닫히게 한다. 위치도 옮길 수 있게 한다. 슬라이딩 버튼의 남는 여백을 조절한다.
- 팝오버를 `StaysOpen = true`로 바꿨다. 바깥을 눌러도 닫히지 않아 달력을 보면서 설정을 계속 열어 둘 수 있다. 팝오버 안에 `보기 설정` 제목 줄과 24px X 버튼을 넣어 이 버튼으로만 닫는다. 톱니 버튼으로 여닫는 토글은 그대로 둔다.
- 항목을 눌러도 팝오버가 닫히지 않게 바꿨다. 앞선 구현은 동작 후 닫았는데, 계속 띄워 두는 것이 이제 이 창의 기본 동작이므로 유지가 맞다.
- 제목 줄을 끌어 팝오버를 옮길 수 있게 했다. `Popup`은 창이 아니라 `DragMove`를 쓸 수 없으므로 `EnablePopupDrag`가 화면 좌표의 이동량을 `HorizontalOffset`·`VerticalOffset`에 그대로 더한다. 커서는 `SizeAll`로 바꿔 잡을 수 있음을 알린다.
- `StaysOpen`이라 창을 닫아도 팝오버가 남을 수 있어 `Closed`에서 팝오버 두 개를 함께 닫는다.
- **슬라이딩 버튼 여백:** 칸 폭이 74·74와 49·49·49라 팝오버 안쪽 폭보다 좁아 오른쪽에 빈 자리가 남았다. 아래 도구 버튼들은 폭을 꽉 채우고 있어 차이가 더 드러났다. 팝오버 214에서 테두리 2와 안여백 24를 빼면 188, 스위치 자신의 테두리·안여백 4를 더 빼면 칸에 쓸 수 있는 폭이 184다. 두 칸은 92씩, 세 칸은 61.34·61.33·61.33으로 맞춰 꽉 채웠다. 계산 근거를 주석에 남겼다.
- **요청 2:** 메인 헤더의 검색 아이콘을 `4주` 스위치와 왼쪽 정렬한다.
- 두 줄이 서로 다른 기준에 맞춰져 있었다. 아이콘 줄 `featureArea`는 폭 202에 오른쪽 여백 111, 스위치 줄 `lowerActions`는 폭 384에 오른쪽 여백 6이다. `Docs/Design/MAIN_HEADER_UI_STANDARD.md`가 스위치 줄을 오른쪽 상세 패널의 좌우 선에 맞추도록 규정하고 있어 생긴 차이다.
- 아이콘 줄이 오른쪽 정렬이라 설정에서 아이콘을 숨기면 왼쪽 끝이 움직인다. 고정 여백으로는 한 가지 조합에서만 맞으므로, 배치가 끝난 뒤 실제 위치를 재서 보정하는 `AlignFeatureIconsToRangeSwitch`를 넣었다. `featureActions.LayoutUpdated`에서 호출하고 차이가 0.5px 미만이면 아무것도 하지 않아 반복 보정으로 들어가지 않는다.
- 맞추는 기준은 `획을 세운다`이다. 아이콘은 34px 버튼 안에 21px로 가운데 놓이므로 버튼 왼쪽에서 6.5px 안쪽이 실제 획의 시작이고, 그 점을 `4주` 스위치의 왼쪽 테두리에 맞춘다. 상수 `FeatureIconInk`로 근거를 남겼다.
- **변경 파일:** `App/SportsCalendarWindow.cs`, `App/MainWindow.Layout.cs`, `App/MainWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. 실행 7초 시점 CPU 1.8초로 `LayoutUpdated` 보정이 반복 루프로 들어가지 않음을 확인했다.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 팝오버가 바깥을 눌러도 닫히지 않는지, X로만 닫히는지, 제목 줄을 끌어 옮겨지는지, 옮긴 뒤 톱니로 다시 열면 어디에 뜨는지, 슬라이딩 버튼이 팝오버 폭을 꽉 채우는지.
  - 실기기 수동 확인 필요: 메인 헤더에서 검색 아이콘 획과 `4주` 왼쪽 테두리가 한 선에 서는지, 설정에서 검색·알람·야구 아이콘을 숨겼다 켰을 때도 유지되는지, 125% DPI에서도 맞는지.
  - 팝오버를 옮긴 뒤 `HorizontalOffset`·`VerticalOffset`이 유지되므로 다음에 열 때도 옮긴 자리에 뜬다. 톱니 옆으로 되돌리는 초기화가 필요한지 확인 필요.
  - `MAIN_HEADER_UI_STANDARD.md`는 아직 갱신하지 않았다. 아이콘 줄 정렬 기준이 바뀌었으므로 사용자 확인 후 문서에 반영한다.


## 2026-09-02 — KBO 제목줄을 한 줄로 줄이고 보기 설정을 톱니 팝오버로 이동

- **요청:** 시안 중 `C · 보기 옵션 접기`를 선택했고 설정은 플로팅으로 띄운다.
- 제목줄을 2행 `Grid`에서 1행 `DockPanel`로 바꿨다. 왼쪽은 `⚾ KBO 경기 일정 · ‹ 2026년 9월 › · 오늘`, 오른쪽은 `응원팀 선택 · ⚙ · ✕`만 남는다. 조작 10개가 6개로 줄고 달력에 세로 공간이 한 줄만큼 더 간다.
- 보기 설정과 도구를 톱니 버튼에 붙는 플로팅 팝오버로 옮겼다. 팝오버는 `보기`(전체 경기·응원팀 / 4주·월 전체 / 작게·중간·크게)와 `도구`(응원팀 경기 선택 · 선택 해제 · 새로고침 · API 설정)로 나뉜다. 기존 `favoritePopup`과 같은 `Popup` 방식이고 `StaysOpen = false`라 바깥을 누르면 닫힌다.
- 팝오버 항목을 누르면 팝오버를 먼저 닫고 동작을 실행한다. 팝오버가 열린 채로 달력이 바뀌면 결과가 가려지기 때문이다.
- 슬라이딩 버튼 라벨을 팝오버 폭에 맞춰 손봤다. `응원팀 경기`는 `응원팀`으로, `월`은 메인 달력과 같은 `월 전체`로 맞췄다. 세 스위치의 칸 폭을 74·74·49로 통일해 팝오버 안에서 좌우 끝선이 맞는다.
- 하단의 `API 설정` 버튼을 팝오버로 옮겨 하단에는 상태 문구와 등록 조작만 남겼다.
- 팝오버 전용 헬퍼 `OptionLabel`·`OptionButton`을 두어 항목 높이 30px과 아래 여백 6px을 한 곳에서 관리한다.
- **변경 파일:** `App/SportsCalendarWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS, `search-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 톱니를 눌러 팝오버가 열리고 바깥을 누르면 닫히는지, 팝오버 항목이 동작한 뒤 팝오버가 닫히는지, 톱니를 다시 눌러 토글이 되는지, 제목줄이 한 줄로 줄어 달력이 넓어졌는지, 좁은 폭에서 제목줄 요소가 겹치지 않는지.
  - `4주`를 다시 눌렀을 때 1~6주 드롭다운을 여는 기능은 아직 넣지 않았다. 메인 달력은 `calendarRangeSwitch.Clicked`로 처리한다. KBO의 `visibleWeeks`가 4와 0만 다루므로 1~6주를 지원하려면 `RenderCalendar`의 주 수 처리를 함께 확인해야 한다.
  - 제목 아이콘 광학 정렬(`⚾`)은 아직 적용하지 않았다.
  - `OnharuPopupChrome.ActionButton`을 쓰는 하위 팝업 네 곳(`SportsApiWindows` 두 곳, 응원팀 팝업 `완료`, 경기 선택 `선택 적용`)의 역할색은 별도 검토 대상이다.


## 2026-09-02 — KBO 이동 버튼을 메인 달력과 같은 규칙으로 통합하고 역할색 정리

- **요청:** `< << > >>` 네 개를 `< >` 두 개로 줄이고 메인 달력과 같은 규칙을 쓴다. 클릭은 1주, 더블클릭은 다음 기간, 월 보기에서는 클릭만 있다. 버튼 배치와 로즈 버튼은 판단에 맡겼다.
- **이동 버튼:** `previousPeriodButton`·`nextPeriodButton`(`<<`·`>>`)을 없애고 좌우 하나씩만 남겼다. 4주 이동은 별도 버튼 대신 더블클릭으로 처리한다. 툴팁에 `이전 · 더블클릭은 4주`로 동작을 적었다.
- 판정 방식은 메인 달력의 `BindCalendarNavigation`을 그대로 따랐다. `PreviewMouseLeftButtonDown`에서 `e.ClickCount`로 구분하므로 더블클릭 대기 시간이 없고 한 번 클릭의 반응이 느려지지 않는다. 월 보기는 첫 클릭이 이미 한 달을 옮겼으므로 두 번째 클릭에서 아무것도 하지 않는다.
- **비동기 경합을 함께 고쳤다.** 메인 달력의 이동은 동기지만 이 창의 이동은 매번 네트워크 조회를 한다. 첫 클릭의 조회가 끝나기 전에 두 번째 클릭이 들어오면 두 요청이 서로 다른 `month`를 기준으로 계산해 결과가 어긋난다. `Navigate`를 `NavigateFrom(origin, direction)`으로 바꿔 두 클릭이 같은 원점에서 계산하게 하고, `navigationVersion` 요청 번호로 최신 요청만 결과를 반영하게 했다. 검색창의 `renderRequestVersion`과 같은 방식이다.
- **역할색 정리(위임받아 판단):**
  - `↻ 새로고침`은 조회 동작인데 로즈 `#FFF1F2 / #BE123C`를 쓰고 있었다. `design-onharu.md` 3.4가 이 계열을 삭제·경고에 배정하므로 중립 `#F1F5F9 / #475569`로 바꿨다.
  - `응원팀 경기 선택`도 같은 로즈였다. 파괴적 동작이 아니라 선택을 더하는 동작이므로 선택 의미색인 피치 `SelectionSurfaceColor / SelectionTextColor`로 바꿨다. 옆의 `전체 선택 취소`는 중립색이라 두 버튼이 선택·해제 짝으로 읽힌다.
- **변경 파일:** `App/SportsCalendarWindow.cs`, `README.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS, `search-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인. `feature-pack-check.ps1`은 일기장 단언으로 계속 실패하며 이번 변경과 무관하다.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 4주 보기에서 한 번 클릭이 1주, 더블클릭이 4주로 이동하는지, 월 보기에서 더블클릭이 두 달을 넘어가지 않는지, 빠르게 여러 번 눌렀을 때 마지막 위치가 어긋나지 않는지, 로딩 표시가 남지 않는지.
  - 제목 아이콘 광학 정렬은 아직 적용하지 않았다. `titleActions`의 왼쪽 여백이 2라 `⚾` 획이 기준선보다 오른쪽에서 시작한다.
  - 달력 본문과 하단 등록 줄의 좌우 기준선, 셀 밀도는 아직 검토하지 않았다.
  - `OnharuPopupChrome.ActionButton`은 앞선 기록에서 사용처가 없다고 적었으나 사실과 다르다. `SportsApiWindows`의 API 키 연결·안내 링크, KBO 응원팀 팝업의 `완료`, 경기 선택 팝업의 `선택 적용` 네 곳이 아직 청녹을 쓴다. 하위 팝업의 역할색은 별도로 검토한다.


## 2026-09-01 — KBO 창에 공통 규격 적용

- **요청:** 알람 다음 작업으로 야구 화면을 손본다.
- **그림자 여백:** `Shell`에 사방 12px 여백을 주고 창 크기 계산에 24를 더했다. 최소 크기도 `940×780`에서 `964×804`로 맞췄다. 모서리 검은 자국이 사라진다. 이 창은 `uiScale`에 따라 크기가 변하므로 여백을 계산식 안에 넣었다.
- **크기 조절:** `ResizeMode`를 지정하지 않아 기본값에 기대고 있었다. `NoResize`로 두고 `OnharuPopupChrome.EnableResize`를 붙여 메인 창·검색·시간표·알람과 같은 방식으로 통일했다.
- **오늘 버튼:** `#DCE7F6`·`#334155`·`#9FB1C7`이 하드코딩돼 있었다. 이 값들은 브랜드 그라데이션 적용 때 `TodaySurfaceColor`·`TodayTextColor`·`TodayBorderColor`로 갱신했는데 이 창만 옛 값을 그대로 들고 있어 검색창의 오늘 버튼과 색이 달랐다. 공통 토큰 참조로 바꿨다.
- **슬라이딩 버튼 선택색:** `#4B5563` 진회색에 흰 글자를 쓰고 있어 검색창 범위 버튼·알람 모드 버튼의 `#EDE9FE / #6D28D9`와 달랐다. 세 슬라이딩 버튼 모두 공통 값으로 맞췄다. 창마다 다른 선택색을 만들지 않는다.
- **대표 실행 버튼:** `선택 경기 등록`이 청녹 `OnharuPopupChrome.ActionButton`을 쓰고 있었다. `design-onharu.md` 3.4의 청녹 사용 제한 위반이며 알람에서 고친 것과 같은 문제다. 이 창의 대표 실행 버튼이므로 브랜드 그라데이션으로 바꿨다.
- 이 변경으로 `OnharuPopupChrome.ActionButton`은 호출하는 곳이 없어졌다. `FeatureTitle`·`DisclosureButton`과 함께 리팩토링 단계 정리 대상이다.
- **변경 파일:** `App/SportsCalendarWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 모서리 검은 자국이 사라졌는지, 테두리로 크기 조절이 되는지, `작게·중간·크게` 세 배율에서 창이 화면을 벗어나지 않는지, 오늘 버튼과 슬라이딩 버튼 색이 검색·알람과 같은지, 등록 버튼 그라데이션이 다른 창과 같은지.
  - **결정 필요 — 새로고침 버튼 역할색:** `↻ 새로고침`이 `#FFF1F2 / #BE123C` 로즈를 쓴다. `design-onharu.md` 3.4는 이 계열을 삭제·경고에 배정하고 있어 단순 조회 동작에 쓰는 것은 역할색 오용이다. 중립 `#F1F5F9 / #475569`로 바꿀지 확인 필요.
  - **결정 필요 — 응원팀 경기 선택 버튼:** 같은 로즈 계열을 쓴다. 파괴적 동작이 아니므로 같은 검토 대상이다.
  - 제목 아이콘 광학 정렬은 아직 적용하지 않았다. `titleActions`의 왼쪽 여백이 2라 `⚾` 획이 기준선보다 약 5px 오른쪽에서 시작한다. 헤더 구조가 다른 창들과 달라 별도로 확인이 필요하다.
  - 달력 본문과 하단 등록 줄의 좌우 기준선, 셀 밀도는 아직 검토하지 않았다.


## 2026-09-01 — 설정 아이콘 크기를 나머지 헤더 아이콘과 통일

- **요청:** 설정 아이콘이 너무 작다.
- 두 가지가 겹쳐 작아 보였다.
  - `HeaderSize`가 `⌕ ▦ ◴ ⚾`만 21px로 두고 설정은 17px을 줬다. 헤더에 나란히 서는 다섯 중 설정만 작았다.
  - 이전 채움 도형은 그린 면이 상자를 거의 채웠지만, 선 도형은 실제 획이 18×18 상자 안에서 2.4~15.6 범위만 써서 같은 크기에서도 더 작아 보였다.
- 설정을 21px로 올려 헤더 다섯 아이콘의 크기를 모두 같게 했다. `HeaderSize` 주석에 설정만 17px이던 때가 있었다는 사실과 이동·달력 화살표 같은 보조 기호는 17px이라는 구분을 남겼다.
- 톱니 도형도 상자를 더 채우도록 다시 잡았다. 바깥 링 반지름을 5.3에서 5.8로 키우고 톱니 끝을 1.6과 16.4까지 늘렸다. 안쪽 원도 2.2에서 2.4로 키웠다.
- `popup-policy-check.ps1`의 크기 규칙 토큰을 새 표현으로 갱신했다.
- **변경 파일:** `App/OnharuIcons.cs`, `App/popup-policy-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 설정 톱니가 나머지 넷과 같은 크기로 보이는지, 21px에서 톱니 여덟 개가 뭉치지 않는지. 뭉치면 톱니를 여섯으로 줄이거나 바깥 링을 뺀다.
  - 다음 화면은 야구장이다.


## 2026-09-01 — 설정 톱니 아이콘도 OnharuIcons로 통합

- **요청:** 앞선 아이콘 통합에서 빠진 설정 아이콘도 합친다.
- 설정 톱니는 `MainWindow.Layout.cs`의 `SettingsGlyph`가 **24×24 좌표계의 채워진 도형**으로 따로 그리고 있었다. 나머지 아이콘은 모두 18×18 좌표계의 선 도형이라, 설정만 면으로 채워져 헤더에서 혼자 무겁게 보였다. 위치가 다른 것뿐 아니라 표현 방식 자체가 달랐다.
- 같은 18×18 선 도형으로 다시 그려 `OnharuIcons`에 넣었다. 안쪽 원 + 바깥 링 + 여덟 방향 톱니로 구성하고 획은 다른 아이콘과 같은 1.6px을 쓴다.
- `SettingsGlyph` 함수를 제거하고 호출 지점 세 곳을 공통 경로로 바꿨다. `MainWindow.Theme.cs`의 `glyph == "settings" ? SettingsGlyph(...) : HeaderGlyph(...)` 분기 두 곳은 `OnharuIcons`가 `settings` 도형을 갖게 되어 특수 처리가 필요 없어졌으므로 통째로 없앴다. `MainWindow.Layout.cs`의 직접 호출도 `HeaderGlyph("settings", ...)`로 바꿨다.
- 이제 메인 헤더의 다섯 아이콘(`⌕ ▦ ◴ ⚾ settings`)이 모두 같은 파일, 같은 좌표계, 같은 획 두께를 쓴다.
- `popup-policy-check.ps1`에 두 가지를 추가했다. `settings` 도형이 `OnharuIcons`에 있는지 확인하고, `SettingsGlyph`라는 별도 함수가 되살아나면 실패시킨다. 같은 분기가 다시 생기는 것을 막는다.
- **변경 파일:** `App/OnharuIcons.cs`, `App/MainWindow.Layout.cs`, `App/MainWindow.Theme.cs`, `App/popup-policy-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. `theme-check.ps1`은 메인 헤더의 `periodNavigation.Children.Add(todayButton)` 기존 문제로 계속 실패하며 이번 변경과 무관함을 오류 메시지로 확인했다.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 설정 톱니가 나머지 네 아이콘과 같은 무게로 보이는지, 채움에서 선으로 바뀌어 작은 크기나 블랙 스킨에서 형태가 뭉치지 않는지. 선 도형이 오히려 읽기 어려우면 톱니 수를 여덟에서 여섯으로 줄이거나 바깥 링을 빼는 방향으로 조정한다.
  - 다음 화면은 야구장이다.


## 2026-09-01 — 아이콘 도형을 OnharuIcons 한 곳으로 모으고 메인·팝업 그림 통일

- **요청:** 검색·시간표·알람·야구 아이콘이 선이 두껍고, 각 창 제목의 아이콘이 메인과 제각각이다. 같게 맞춘다.
- **원인:** 메인 헤더는 `MainWindow.Layout.cs`의 `HeaderGlyph`가 벡터 경로를 그리는데, 팝업 제목의 `OnharuPopupChrome.FeatureHeading`은 `Segoe UI Symbol` 글꼴 기호를 그리고 있었다. 같은 기능인데 메인과 팝업의 그림이 서로 다른 이유가 이것이다. `AGENTS.md`가 `같은 기능은 메인과 팝업에서 같은 아이콘을 사용한다`고 정해 두었지만 구현이 두 갈래였다.
- `App/OnharuIcons.cs`를 새로 만들어 아이콘 도형의 단일 기준으로 삼았다. `HeaderGlyph`는 `OnharuIcons.Draw`로 위임하고 `FeatureHeading`도 같은 도형을 21px로 받아 그린다. 이제 한 곳만 고치면 메인과 모든 팝업 제목이 같이 바뀐다.
- **획을 1.8px에서 1.6px로 줄였다.** 두꺼워 보이던 원인이다.
- **부가기능 아이콘 네 개를 다시 그렸다.**
  - `⚾` 야구: 이전에는 벡터가 아예 없어 글꼴 기호로 떨어졌다. 혼자 다른 그림이던 이유다. 원 + 좌우 실밥 벡터를 새로 넣었다.
  - `▦` 시간표: 안쪽 선이 넷이라 작은 크기에서 뭉쳤다. 바깥 틀 + 머리줄 하나 + 세로 두 줄로 줄였다.
  - `◴` 알람: 아래쪽 다리 둘이 작은 크기에서 점으로만 보여 뺐다. 시계 + 위쪽 종 귀 둘 + 바늘만 남겼다.
  - `⌕` 검색: 형태는 유지하고 손잡이 끝점만 다듬었다.
- `✎` 일기 도형도 함께 정의했다. 일기장은 기능에서 제외했지만 도형 기준은 남겨 둔다.
- `HeaderGlyph`에 `⌕` 분기가 두 번 있었고 두 번째는 도달할 수 없는 죽은 코드였다. 정리하면서 제거했다.
- **문서 동기화:** `AGENTS.md`·`CLAUDE.md`에 아이콘 도형 단일 기준 규칙을 같은 문장으로 추가했다. 두 파일에 남아 있던 죽은 `OnharuPopupChrome.FeatureTitle` 참조도 `FeatureHeading`으로 함께 바로잡았다. `design-onharu.md` 8.1의 `stroke: 1.8px`을 `1.6px`로, 9장의 `글꼴 기호를 쓰는 시간표·일기·야구` 문장을 `글꼴 기호로 남겨 두지 않는다`로 고쳤다. 공통 절 81줄 일치를 확인했다.
- `popup-policy-check.ps1`은 아이콘 규칙이 옮겨간 새 위치를 보도록 갱신했다. 부가기능 21px 규칙 토큰을 `OnharuIcons.cs`에서 확인하고, 메인이 `OnharuIcons.Draw`로 위임하는지, 팝업 제목이 같은 도형을 쓰는지, 부가기능 네 개가 모두 도형을 갖는지를 새로 검사한다. 글꼴 기호로 떨어지는 아이콘이 다시 생기면 여기서 걸린다.
- **변경 파일:** `App/OnharuIcons.cs`(신규), `App/MainWindow.Layout.cs`, `App/OnharuPopupChrome.cs`, `App/popup-policy-check.ps1`, `AGENTS.md`, `CLAUDE.md`, `design-onharu.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS, `search-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. `theme-check.ps1`은 메인 헤더의 `periodNavigation.Children.Add(todayButton)` 기존 문제로 계속 실패하며 이번 변경과 무관함을 오류 메시지로 확인했다. `ui-construction-check.ps1`은 `PlannerSettings` 생성자 문제로 22행에서 멈춘다.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 메인 헤더의 네 아이콘이 얇아졌는지, 검색·시간표·알람 창 제목의 아이콘이 메인과 같은 그림인지, 파스텔·블랙 두 스킨과 125% DPI에서 형태가 뭉치지 않는지.
  - 설정 `⚙`은 `SettingsGlyph`라는 별도 함수로 그려지며 이번 통합 대상에 넣지 않았다. 같은 방식으로 `OnharuIcons`에 합칠지 결정 필요.
  - 다음 화면은 야구장이다.


## 2026-09-01 — 알람 목록의 울릴 시각 표기 오류 수정

- **요청:** `실행 중` 목록의 날짜가 맞지 않는다.
- 표기 형식이 `yy/MM`이었다. 연도와 월을 찍는 형식이라 2026년 9월이 `26/09`로 나왔고, 정작 필요한 **날짜가 표시되지 않았다.** 내일 10시에 울릴 알람도 `26/09 10:00`으로 보여 오늘 것인지 내일 것인지 구분할 수 없었다. 알람 동작 자체는 정상이며 표시만 잘못됐다.
- 알람은 대부분 오늘 아니면 내일이므로 날짜를 숫자로 읽게 하지 않고 말로 적는다. 오늘은 `오늘 10:00`, 내일은 `내일 10:00`, 그 이후는 `09/05 10:00`으로 표시한다. 남은 시간 카운트다운은 그대로 옆에 붙는다.
- `README.md`의 알람 절에도 표기 규칙을 적었다.
- **변경 파일:** `App/AlarmWindow.cs`, `README.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. 새 시험본 실행 및 응답 상태 확인.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 오늘 울릴 알람과 내일 울릴 알람을 각각 만들어 `오늘`·`내일` 표기가 맞는지, 이틀 뒤 이후는 `MM/dd`로 나오는지.
  - 앞선 기록의 확인 항목(모드 전환, 목록 스크롤, 타이머 분 입력 정리 규칙)은 그대로 남아 있다.
  - 다음 화면은 야구장이다.


## 2026-09-01 — 알람 실행 버튼을 모드 슬라이딩 방식으로 통합

- **요청:** 시안 중 `A · 모드 슬라이딩`을 선택했다.
- 청녹 `ActionButton` 두 개(`분 타이머 시작`, `시각 알람 설정`)를 없애고 `타이머 | 시각 알람` 슬라이딩 버튼 하나와 실행 버튼 하나로 바꿨다. 선택한 모드에 따라 분 입력칸과 시각 입력칸이 교대로 보이고 버튼 글자가 `시작`·`설정`으로 바뀐다.
- 이로써 `design-onharu.md` 3.4의 청녹 사용 제한 위반이 해소된다. 청녹은 성공·연결 완료 같은 상태 전달에만 쓰고, 이 창의 대표 실행 버튼은 검색·시간표와 같은 브랜드 그라데이션을 쓴다.
- 슬라이딩 버튼 팔레트는 검색창 범위 버튼과 같은 `#EDE9FE / #6D28D9`, 테두리 `#C4B5FD`다. 창마다 다른 선택색을 만들지 않는다.
- 입력칸과 버튼의 연결도 분명해졌다. 이전에는 한 줄에 입력칸 두 개와 버튼 두 개가 나란히 있어 어느 쌍인지 읽어야 알 수 있었다.
- `README.md`에 `9-2. 알람 · 타이머` 절을 새로 추가했다. 기존에 알람 사용법이 없었다.
- **변경 파일:** `App/AlarmWindow.cs`, `README.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 모드를 바꿀 때 입력칸이 교대로 나타나는지, 버튼 글자가 `시작`·`설정`으로 바뀌는지, 슬라이딩 손잡이가 부드럽게 움직이는지. 알람 창의 렌더는 가벼워 검색창처럼 애니메이션 중 지연을 넣지 않았다. 끊겨 보이면 같은 방식으로 미룬다.
  - 알람을 여러 개 만들어 `실행 중` 목록이 창 안에서 스크롤되는지 확인한다. 직전 작업에서 바깥을 `Grid`로 바꾸고 목록을 `*` 행에 넣어 고친 부분이다.
  - 타이머 분 입력은 여전히 자유 입력이며 `1~1440` 범위를 벗어나면 안내 팝업을 띄운다. 시각 입력처럼 칸을 벗어날 때 정리하는 규칙이 필요한지 검토한다.
  - `OnharuPopupChrome.ActionButton`은 알람이 마지막 사용처였을 수 있다. 남은 사용처를 확인하고 없으면 `FeatureTitle`·`DisclosureButton`과 함께 리팩토링 단계에서 정리한다.
  - 다음 화면은 야구장이다.


## 2026-09-01 — 알람 창에 공통 규격 적용과 시각 입력 공통화

- **요청:** 시간표 다음 작업으로 알람 화면을 손본다.
- **목록이 창을 넘치던 문제:** 바깥이 `StackPanel`이라 `실행 중` 목록의 `ScrollViewer`가 무한 높이를 받았다. 스크롤이 생기지 않고 알람이 늘어날수록 목록이 창 밖으로 넘쳐 잘렸다. 검색창에서 고친 것과 같은 종류다. 바깥을 `Grid` 4행으로 바꾸고 목록을 `*` 행에 넣어 남은 공간을 채우게 했다.
- **그림자 여백:** `Shell`에 사방 12px 여백을 주고 창을 `500×420`에서 `524×444`로, 최소 크기를 `484×384`로 키웠다. 모서리 검은 자국이 사라진다.
- **크기 조절:** `ResizeMode.CanResize`를 `NoResize`로 바꾸고 `OnharuPopupChrome.EnableResize`를 붙였다.
- **기준선:** 바깥 여백 24를 없애고 요소별로 나눴다. 제목줄 `15, 9, 14, 9`, 입력 카드와 목록 카드 `12, 0, 12, 12`, `실행 중` 제목 `20, 0, 20, 8`. 목록 행의 좌우 여백 2를 없애 카드 12 + 테두리 1 + 안여백 7 = 20px 기준선에 맞췄다.
- 제목 아이콘 광학 정렬로 헤더 왼쪽 여백을 15로 두었다. `◴`의 획이 24px 상자보다 약 5px 안쪽에서 시작한다. 창별 값은 검색 `⌕` 14, 시간표 `▦` 17, 알람 `◴` 15다.
- **시각 입력 공통화:** 시간표 설정과 알람이 각자 시각 파서를 갖고 있었고 동작이 달랐다. 알람의 `TryAlarmTime`은 네 자리 숫자만 받아 `900`이나 `9`를 거부했다. 두 화면에서 같은 문제가 생겼으므로 `App/OnharuTimeInput.cs`로 공통화했다.
- `OnharuTimeInput.TryParse`는 콜론이 있으면 그대로 파싱하고 숫자만 있으면 자리수로 시·분을 나눈다. `Attach(box, fallback)` 한 줄이면 숫자·콜론만 입력, 포커스 이탈과 Enter에서 `HH:mm` 정리, 도움말까지 붙는다. 시간표와 알람이 같은 정의를 쓴다.
- 알람의 입력 오류 안내 문구도 `900, 0900, 9:00 모두 됩니다`로 바꿔 실제 허용 범위를 알렸다.
- **변경 파일:** `App/AlarmWindow.cs`, `App/TimetableWindow.cs`, `App/OnharuTimeInput.cs`(신규), `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS, `search-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. `feature-pack-check.ps1`은 일기장 단언으로 계속 실패하며 이번 변경과 무관하다.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 알람을 여러 개 만들었을 때 목록이 창 안에서 스크롤되는지, 모서리 검은 자국이 사라졌는지, 테두리로 크기 조절이 되는지, 시각 알람 칸에 `900`·`9`를 넣고 칸을 벗어나면 `HH:mm`으로 정리되는지, 제목·카드·목록의 왼쪽 선이 한 줄로 보이는지.
  - **결정 필요 — 버튼 역할색:** `분 타이머 시작`과 `시각 알람 설정`이 모두 `OnharuPopupChrome.ActionButton`의 청녹 `#147D75`를 쓴다. `design-onharu.md` 3.4는 `청녹은 성공·연결 완료·동기화 정상처럼 긍정 상태를 알리는 보조 의미색으로 제한한다. 저장 버튼의 기본색으로 사용하지 않는다`고 정하고 있어 규격 위반이다. 다만 두 버튼이 동등한 실행이라 검색·시간표처럼 대표 버튼 하나에만 그라데이션을 주는 규칙을 그대로 적용할 수 없다. 배치를 바꿔 `타이머 | 시각 알람` 슬라이딩 버튼으로 모드를 고르고 실행 버튼을 하나로 만드는 안을 검토할 수 있다. 시안 제작 여부는 사용자 확인 대기.
  - 타이머 분 입력은 아직 자유 입력이다. 시각 입력처럼 정리 규칙이 필요한지 검토한다.


## 2026-09-01 — 시간표 창 크기 계산을 실측 기반으로 교체 (스크롤 발생 수정)

- **요청:** 창 크기를 표에 맞췄는데 스크롤이 생긴다.
- 원인 두 가지가 겹쳐 계산이 몇 px 모자랐다.
  - `OnharuPopupChrome.Shell()`은 셸 테두리를 `1.5`로 만들지만 그 직후 `UiRound.EmphasizePopup()`이 `2`로 덮어쓴다. 1.5로 계산해 사방 1px씩 부족했다.
  - 제목줄 높이를 48px로 추정했다. 실제로는 저장 버튼 30, 닫기 버튼 26, `FeatureHeading`의 21px 글자가 섞여 결정되며 글꼴과 DPI에 따라 달라진다.
- 제목줄 높이를 추정하지 않고 `headerPanel.Measure()`의 `DesiredSize`로 실측한다. 나머지 고정 요소는 `ShellMargin`·`ShellBorder`·`TableCardMargin`·`TableCardBorder` 상수로 이름을 붙여 근거를 남겼다. DPI 반올림으로 1px 스크롤이 뜨는 것을 막으려고 `SizeSlack` 2px을 더한다. 팝업 면이 2px 더 보이는 정도라 눈에 띄지 않는다.
- 작업 중 문자열 치환이 설정 창의 헤더 생성 코드까지 잡아 `headerPanel = header;`가 잘못 들어갔다. 두 창의 해당 줄이 완전히 같아서 생긴 일이며 빌드 전에 제거했다. 같은 형태의 일괄 치환은 클래스 경계를 확인하고 써야 한다.
- **변경 파일:** `App/TimetableWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. 사용자가 실기기에서 창 크기가 표에 맞는 것을 확인했다.
- **미해결·다음 작업:**
  - 시간표 화면은 여기서 마무리한다. 표 안쪽(교시·시간 2단 표기, 셀 여백, 요일 배경 색조)과 요일당 기본 폭 132px은 사용할수록 걸리는 점이 나오면 그때 조정한다.
  - 다음 화면은 알람이다.


## 2026-09-01 — 시간표 설정 창 보정과 창 크기 자동 맞춤

- **요청:** 설정 창에서 일요일 칸이 잘린다. 토·일 색을 정한다. 시작 시각을 `1000`처럼 입력해도 되게 하고 입력 편의성을 검토한다. 창 기본 크기를 시간표에 딱 맞춘다.
- **일요일 잘림:** 설정 창 본체가 430px이라 표시 요일 7칸이 넘쳤다. 본체를 470px로 넓혀 창을 `454×322`에서 `494×322`로 키우고, 마지막 칸의 오른쪽 여백을 없애 남는 공백도 정리했다.
- **토·일 색:** 메인 달력과 같은 규칙으로 정했다. `README.md` 2장이 `토요일 파란색 / 일요일 및 국경일 빨간색`으로 이미 정의하고 있어 그대로 따른다. 설정 창의 요일 이름은 토요일 `#2563EB`, 일요일 `#DC2626`만 색을 쓰고 월~금은 공통 글자색 `#334155`으로 둔다. 일곱 요일을 모두 다른 색으로 칠하면 색이 주말 구분이 아니라 장식이 되어 `design-onharu.md` 13장에 어긋난다. 표 머리글의 요일별 색조는 열을 훑을 때 도움이 되므로 그대로 유지했다.
- **시작 시각 입력:** 기존에는 `TimeSpan.TryParse`만 써서 `1000`을 1000일로 읽고 실패 처리한 뒤 **조용히 09:00으로 되돌렸다.** 사용자는 되돌아간 사실을 `적용` 이후에야 알 수 있었다.
- `TryParseTime`을 새로 만들었다. 콜론이 있으면 기존대로 파싱하고, 숫자만 있으면 자리수로 시·분을 나눈다. `9`→09:00, `900`→09:00, `1000`→10:00, `0930`→09:30. 콜론 없는 입력에 `TimeSpan.TryParse`를 쓰지 않는 이유를 주석에 남겼다.
- 입력 편의를 함께 손봤다. 숫자와 콜론만 입력되게 막고, 칸을 벗어나거나 Enter를 누르면 즉시 `HH:mm`으로 정리해 결과를 눈으로 확인할 수 있게 했다. 도움말로 `900, 0900, 9:00, 9 모두 09:00으로 읽습니다`를 붙였다.
- **창 크기 자동 맞춤:** 고정값 `900×568`을 없애고 `ApplyPreferredSize()`가 표 내용에서 계산한다. 가로는 시간 열 92 + 요일 수 × 132, 세로는 머리글 38 + 교시 수 × 48에 고정 요소를 더한다. 작업 영역보다 커지지 않게 제한하고 최소 크기는 `520×360`이다. 창을 처음 열 때와 설정에서 `적용`한 뒤에 다시 맞춘다.
- 표 치수를 상수 `TimeColumnWidth`·`PreferredDayWidth`·`MinDayWidth`·`HeaderRowHeight`·`PeriodRowHeight`로 모아 창 크기 계산과 표 생성이 같은 값을 쓰게 했다. 이전에는 92·105·38·48이 두 곳에 흩어져 있었다.
- **변경 파일:** `App/TimetableWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 설정 창에서 일요일 칸이 온전히 보이는지, 시작 시각에 `1000`·`900`·`9`를 넣고 칸을 벗어났을 때 `HH:mm`으로 정리되는지, 교시 수와 요일 수를 바꾼 뒤 창이 표에 맞게 다시 잡히는지, 창을 직접 늘렸다가 설정을 적용하면 크기가 다시 계산되는데 거슬리지 않는지.
  - 요일당 기본 폭 132px은 한글 6~8자 기준으로 잡은 값이다. 실제 과목명 길이를 보고 조정한다.
  - 표 안쪽(교시·시간 2단 표기, 셀 여백, 요일 배경 색조)은 아직 검토하지 않았다.
  - 다음 화면은 알람, 그다음 야구장이다.


## 2026-09-01 — 시간표 설정을 자식 차단 창으로 분리하고 대표 버튼에 브랜드 그라데이션 적용

- **요청:** 시안 중 `C · 설정 별도 창`을 선택했다. 버튼도 검색 기준으로 맞춘다.
- **설정 분리:** 본 창의 접이식 설정 패널을 없애고 `TimetableSettingsWindow`를 새로 만들었다. `POPUP_POLICY.md` 3장 자식 차단 팝업에 해당하므로 부모 창의 `Owner`를 지정하고 일반 `ShowDialog()`를 쓴다. 정책 문서의 자식 차단 팝업 목록에도 추가했다.
- 본 창은 표만 남았다. `root`의 행이 3개에서 2개로 줄고 표가 남은 세로 공간을 모두 쓴다. 설정을 열고 닫을 때 표가 밀리지 않는다.
- 제목줄의 `시간표 설정` 접이식 버튼을 36px 톱니 버튼으로 바꿨다. 설정 창에서 `적용`을 누르면 본 창이 `Rebuild()`로 표를 다시 그린다. 창을 열기 전에 `Capture()`로 입력 중인 칸을 먼저 데이터에 담아 잃지 않게 했다.
- **버튼 규격 통일:** 검색창의 오늘 버튼과 같은 방식으로, 각 창의 대표 실행 버튼 하나에만 브랜드 그라데이션을 쓴다. 본 창은 `시간표 저장`, 설정 창은 `적용`이다. 높이 30~32px, 반지름 8px, 테두리 없음. 설정 창의 `취소`는 흰 면에 `#CBD5E1` 테두리로 두어 대표 버튼과 구분한다.
- `BrandGradient()`를 `SearchWindow`의 private static에서 `OnharuPopupChrome.BrandGradientBrush()`로 올렸다. 검색과 시간표가 같은 정의를 쓴다. 시작색 `#0369A1`은 흰 글자 대비 5.3:1을 확보하기 위한 값으로 그대로다.
- 설정 창은 검색·시간표와 같은 규격을 따른다. 본체 `430×298`에 그림자 여백 12px을 더해 창은 `454×322`, 제목은 `FeatureHeading("▦", "시간표 설정")`으로 부모 도구의 글리프를 그대로 쓴다. 라벨 폭 76px, 컨트롤 높이 30px, 행 높이 36px로 통일했다.
- `README.md`에 `9-1. 시간표` 절을 새로 추가했다. 기존에 시간표 사용법이 없었다.
- **변경 파일:** `App/TimetableWindow.cs`, `App/OnharuPopupChrome.cs`, `App/SearchWindow.cs`, `Docs/Architecture/POPUP_POLICY.md`, `README.md`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `popup-policy-check.ps1` PASS, `search-check.ps1` PASS. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. `theme-check.ps1`은 메인 헤더의 기존 문제로 계속 실패하며 이번 변경과 무관하다.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 톱니 버튼으로 설정 창이 열리고 `적용` 후 표가 다시 그려지는지, `취소`가 아무것도 바꾸지 않는지, 설정 창을 열기 전 입력 중이던 칸이 보존되는지, 표가 7교시까지 한 화면에 들어오는지, 두 창의 그라데이션 버튼이 같은 색으로 보이는지.
  - `OnharuPopupChrome.DisclosureButton`·`SetDisclosure`는 시간표가 유일한 사용처였고 이제 호출되지 않는다. `FeatureTitle`과 함께 리팩토링 단계에서 제거 여부를 정한다.
  - 표 안쪽(셀 높이 48px, 시간 열 92px, 요일 색, 교시·시간 2단 표기)은 아직 검토하지 않았다.
  - 다음 화면은 알람, 그다음 야구장이다.


## 2026-09-01 — 팝업 제목을 FeatureHeading 하나로 통일하고 기준 문서·검사 갱신

- **요청:** 검색을 기준으로 통일한다.
- 확인 결과 `OnharuPopupChrome.FeatureTitle`은 **어느 창에서도 쓰지 않는 죽은 정의**였다. 검색 `⌕`, 설정 `⚙`, 시간표 `▦`, KBO `⚾` 네 창이 모두 `FeatureHeading`을 쓴다. 코드가 이미 하나로 통일돼 있었고 기준 문서 네 곳만 옛 규격을 붙들고 있었다. 따라서 코드를 되돌리지 않고 문서와 검사를 현재 화면 기준으로 갱신했다.
- `AGENTS.md`와 `CLAUDE.md`의 아이콘 규칙에서 `팝업의 34px 제목 박스에서 17px`를 빼고, 메인 버튼과 팝업 제목 모두 21px 기준임을 명시했다. 두 파일에 같은 문장을 함께 넣었고 공통 절 80줄 일치를 확인했다.
- 같은 자리에 이번 작업에서 얻은 규칙 두 개를 추가했다. 팝업 제목은 `FeatureHeading`으로 통일한다는 것과, 아이콘은 상자가 아니라 획을 왼쪽 기준선에 세우고 글리프 폭에 따라 헤더 여백을 보정한다는 것이다(`⌕` 14, `▦` 17).
- `design-onharu.md` 8.6의 `34px 아이콘 박스 + 20px 제목`을 `FeatureHeading` 규격으로 고쳤다. 8.7 `창 외곽과 기준선` 절을 새로 만들어 검색에서 확정한 여섯 항목을 정리했다. 기준선 20px, 그림자 여백 12px, `EnableResize`, 목록·표 카드 12px 들임, 브랜드 그라데이션 시작색 `#0369A1`, 애니메이션 중 렌더 지연이다.
- `Docs/Architecture/POPUP_POLICY.md`의 독립 도구 창 제목 규칙도 같은 내용으로 고쳤다. 일기장은 기능 제외에 맞춰 문장에서 뺐다.
- `popup-policy-check.ps1`을 현재 구조에 맞게 갱신했다. `FeatureTitle` 요구를 `FeatureHeading`으로 바꾸고, 일기장 창 검사에서 `DiaryTitle()`을 뺐으며, KBO의 글자형 `«/»` 버튼 요구를 지웠다. 마지막으로 메인 헤더의 부가기능 아이콘 규칙 토큰을 현재 표현으로 맞췄다. 21px 규칙 자체는 그대로이고 알람 `◴`가 목록에 들어왔으며 크기 계산이 `Width`·`Height` 각각에서 `iconSize` 하나로 바뀌었다. 갱신 이유는 모두 주석으로 남겼다.
- **변경 파일:** `AGENTS.md`, `CLAUDE.md`, `design-onharu.md`, `Docs/Architecture/POPUP_POLICY.md`, `App/popup-policy-check.ps1`, `HANDOFF.md`.
- **검증:** `popup-policy-check.ps1` PASS. 자동 검사 18종 중 15종 PASS로 이전 14종에서 하나 늘었다. `AGENTS.md`·`CLAUDE.md` 공통 절 80줄 완전 일치 확인. 코드 변경이 없어 빌드는 실행하지 않았다.
- **미해결·다음 작업:**
  - 남은 실패 3종은 모두 이번 작업 범위 밖의 기존 문제다. `feature-pack-check.ps1`은 일기장 제외에 맞춘 일기 단언 14곳 정리가 필요하고, `theme-check.ps1`은 메인 헤더에서 사라진 `periodNavigation.Children.Add(todayButton)`을 요구하며 `Docs/Design/MAIN_HEADER_UI_STANDARD.md`와도 어긋난 상태다. `ui-construction-check.ps1`은 `PlannerSettings` 생성자 서명 불일치로 22행에서 멈춘다.
  - `FeatureTitle`은 이제 호출되지 않는 죽은 정의다. 리팩토링 단계에서 제거 여부를 정한다.
  - 시간표 표 내부 배치, 설정 패널 구성, 요일 색, 셀 밀도는 아직 검토하지 않았다.


## 2026-09-01 — 시간표 창에 검색에서 확정한 공통 규격 적용 (1차: 창 껍데기)

- **요청:** 검색 다음 작업으로 시간표 화면을 손본다.
- 검색에서 확정한 규격 중 창 껍데기에 해당하는 네 가지를 먼저 적용했다. 표 내부 배치와 설정 패널 구성은 아직 손대지 않았다.
- **그림자 여백:** `Shell`에 사방 12px 여백을 주어 `UiRound.EmphasizePopup`의 그림자가 창 경계에서 잘리지 않게 했다. 팝업 본체 크기를 유지하려고 창을 `900×568`에서 `924×592`로, 최소 크기를 `720×500`에서 `744×524`로 키웠다.
- **크기 조절:** `ResizeMode.CanResize`를 `NoResize`로 바꾸고 `OnharuPopupChrome.EnableResize(this, shell)`를 붙였다. 메인 창·검색창과 같은 판정(모서리 18px, 변 10px)과 같은 네이티브 호출을 쓴다.
- **기준선:** 바깥 `root`의 여백 `22, 16, 18, 18`을 없애고 요소별로 나눴다. 헤더 `17, 9, 14, 9`, 설정 패널 `20, 0, 20, 10`, 표 카드 `12, 0, 12, 12`. 좌우가 22/18로 비대칭이던 것을 20/20 기준선 하나로 맞췄다.
- **제목 아이콘 광학 정렬:** 헤더 왼쪽 여백을 17로 두었다. `FeatureHeading`의 글리프는 24px 상자에 가운데 정렬돼 있고 `▦`의 획은 상자보다 약 3px 안쪽에서 시작한다. 검색창의 `⌕`는 획이 더 좁아 6px이라 여백이 14였다. 글리프마다 보정값이 다르므로 창별로 값을 두고 근거를 주석에 남긴다.
- **표 카드 여백:** 표를 좌우·아래로 12px 들여 팝업 면이 테두리처럼 남게 했다. 이 띠가 없으면 테두리 판정 영역 10px이 표 안 입력칸 클릭과 겹친다.
- **변경 파일:** `App/TimetableWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. 시간표 전용 자동 검사는 없다. `popup-policy-check.ps1`은 아래 미해결 항목 때문에 여전히 실패한다.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 모서리 검은 자국이 사라졌는지, 네 변·네 모서리에서 크기 조절이 되는지, 제목·설정 패널·표 카드의 왼쪽 선이 한 줄로 보이는지, 표 헤더 고정과 가로 스크롤이 그대로 동작하는지.
  - `popup-policy-check.ps1`이 요구하는 `FeatureTitle("▦", "나의 시간표")`와 현재 코드의 `FeatureHeading`이 어긋난 상태 그대로다. `design-onharu.md` 8.6·`AGENTS.md`·`CLAUDE.md`·2026-08-23 기록이 모두 `34px 아이콘 박스 + 20px 제목`을 규정하므로 검사가 맞고 코드가 벗어난 쪽이다. 이 창을 손보는 김에 결정 필요.
  - 표 내부 배치, 설정 패널 구성, 요일 색, 셀 밀도는 아직 검토하지 않았다.
  - `README.md`에 시간표 사용법 절이 없다. 화면 변경이 확정되면 함께 추가한다.


## 2026-09-01 — 검색 화면 작업 마무리와 README 갱신

- **요청:** 지금까지의 검색 작업을 기록하고 시간표로 넘어간다.
- 건수 표기를 더 내리는 건은 사용자가 현 상태로 두기로 결정했다. 아래 여백 3px이 남아 있어 그만큼은 더 내릴 수 있지만 그 이상은 결과 카드 영역을 침범한다.
- `README.md` 9장 검색 사용법을 현재 화면에 맞게 갱신했다. 없어진 `검색` 버튼 대신 Enter로 검색한다는 점, 기간을 드롭다운이 아니라 `±1년 · 과거 · 앞으로 · 지정 · 전체` 버튼으로 고른다는 점, 결과 건수와 500건 상한 표기, 오늘 띠, 창 크기 조절을 추가했다.
- **이번 검색 작업에서 확정한 규격** — 시간표·알람·KBO에 이어서 적용한다.
  - 글자 기준선은 왼쪽·오른쪽 모두 20px. 아이콘은 상자가 아니라 획을, 컨트롤은 테두리를, 글자는 글자를 이 선에 세운다.
  - 팝업 본체 바깥에 그림자 여백 12px을 두고 창 크기를 그만큼 키운다. 여백이 없으면 `UiRound.EmphasizePopup`의 그림자가 창 경계에서 잘려 모서리에 검은 자국이 남는다.
  - 크기 조절은 `OnharuPopupChrome.EnableResize(window, surface)`. 판정 기준은 메인 창과 같은 모서리 18px·변 10px이며 `ResizeMode`는 `NoResize`로 둔다.
  - 목록은 좌우·아래 12px 들여 카드로 두고 안쪽 행 여백은 7px. 바깥 띠가 크기 조절 영역과 결과 행 클릭이 겹치지 않게 한다.
  - 둘 이상의 보기 옵션은 `OnharuSegmentedSwitch`. 선택색은 `#EDE9FE / #6D28D9`, 테두리 `#C4B5FD`.
  - 대표 강조 버튼은 온하루 브랜드 그라데이션. 흰 글자 대비 4.5:1을 지키려고 시작색은 `#0369A1`을 쓴다(`OnharuPopupChrome.BrandGradientStartColor`).
  - 슬라이딩 버튼 전환처럼 애니메이션이 있는 조작은 무거운 렌더를 애니메이션 종료 뒤로 미룬다. 검색은 170ms `DispatcherTimer`를 쓴다.
- **변경 파일:** `README.md`, `HANDOFF.md`.
- **검증:** 코드 변경이 없어 빌드와 자동 검사는 실행하지 않았다. 직전 기록의 빌드·`search-check.ps1` PASS 결과가 현재 기준이다.
- **미해결·다음 작업:**
  - 다음 작업은 시간표 화면이다. 위 규격을 그대로 적용한다.
  - 검색 실행 버튼 추가 여부 결정.
  - 시간표·KBO 창의 제목 아이콘 광학 어긋남 확인.
  - 다른 팝업의 모서리 그림자 자국 공통 처리 여부 결정.
  - `popup-policy-check.ps1`·`theme-check.ps1`이 요구하는 `FeatureTitle`·메인 `todayButton`은 검사가 맞고 코드가 기준 문서에서 벗어난 건이다. 되돌릴지 문서를 바꿀지 결정 필요.
  - 일기장 기능 제외에 맞춘 `feature-pack-check.ps1` 일기 단언 14곳 정리.

## 2026-09-01 — 검색 건수 표기 아래쪽 맞춤 정렬

- **요청:** `500(최대검색)/696건`을 슬라이딩 버튼과 같은 여백으로 아래쪽 맞춤 정렬한다.
- 건수를 세로 가운데 정렬에서 아래쪽 정렬로 바꿔 범위 슬라이딩 버튼의 아래선에 세웠다. 가운데 정렬은 26px 버튼 안에서 11px 글자가 떠 보였다. 글자가 아래선에 닿지 않도록 3px만 띄웠다.
- 오른쪽 여백은 기존 20px을 유지했다. 결과 목록 글자의 끝선과 같은 기준선이라 세로로 한 줄이 된다.
- **변경 파일:** `App/SearchWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `search-check.ps1` PASS(exit 0). 새 시험본 실행 및 응답 상태 확인.
- **미해결·다음 작업:**
  - `검색결과창에`라는 표현을 범위 줄 안에서의 아래쪽 정렬로 해석했다. 건수를 결과 목록 카드 안쪽으로 옮기라는 뜻이었다면 다시 조정한다. 사용자 확인 대기.
  - 검색 실행 버튼 추가 여부 결정.
  - 시간표·KBO 창의 제목 아이콘 광학 어긋남 확인.
  - 다른 팝업의 모서리 그림자 자국 공통 처리 여부 결정.


## 2026-09-01 — 검색창 제목 아이콘 광학 정렬

- **요청:** 검색 제목과 슬라이딩 버튼이 결과 목록보다 오른쪽으로 밀려 보인다. 더 왼쪽으로 가야 하는지 확인한다.
- 세 요소의 상자 왼쪽 모서리는 이미 같은 22px 선에 있었다. 헤더 여백 20 + 셸 테두리 2, 범위 줄 여백 20 + 2, 목록 카드 12 + 테두리 1 + 행 여백 7 + 2다. 어긋나 보인 것은 상자가 아니라 글자와 획의 시작점이었다.
- `OnharuPopupChrome.FeatureHeading`의 글리프는 24px 상자에 가운데 정렬돼 있어 `⌕`의 실제 획이 상자보다 약 6px 안쪽에서 시작한다. 상자는 선에 맞아 있는데 눈에는 아이콘만 들여쓰기된 것처럼 보였다.
- 헤더의 왼쪽 여백을 20에서 14로 줄여 글리프의 획이 20px 기준선에 서게 했다. 공통 `FeatureHeading`은 시간표·KBO도 함께 쓰므로 건드리지 않고, 이 창의 여백만 조정했다. 값의 근거를 소스에 주석으로 남겼다.
- 범위 슬라이딩 버튼은 그대로 두었다. 테두리가 이미 기준선에 있고, 안쪽 여백 1 + 테두리 1로 알약이 2px 들어가는 것은 컴포넌트 자체의 구조다. 라벨 글자를 기준선에 맞추려면 컨트롤 전체를 왼쪽으로 밀어야 하는데 그러면 오른쪽 끝선이 대신 어긋난다. 테두리를 살리는 쪽이 균형에 맞다.
- **변경 파일:** `App/SearchWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `search-check.ps1` PASS(exit 0). 새 시험본 실행 및 응답 상태 확인.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: `⌕` 획과 결과 날짜 글자가 한 줄로 보이는지, 헤더 왼쪽 여백을 줄인 뒤에도 제목과 검색 입력칸 간격이 좁아 보이지 않는지.
  - 같은 광학 어긋남이 시간표·KBO 창에도 있다. 두 창은 `FeatureHeading`을 쓰면서 헤더 여백을 20 계열로 두고 있어 아이콘이 같은 만큼 밀려 있을 가능성이 크다. 배치 개선 작업에서 함께 확인한다.
  - 검색 실행 버튼 추가 여부 결정.
  - 다른 팝업의 모서리 그림자 자국 공통 처리 여부 결정.


## 2026-09-01 — 검색창 왼쪽 기준선 20px 통일

- **요청:** 결과 창과 헤더·슬라이딩 버튼의 선이 맞지 않는다. 오른쪽 X 버튼 정도의 여백을 두면서 맞춘다. 균형을 우선한다.
- 왼쪽 기준선이 셋으로 갈라져 있었다. 제목과 범위 슬라이딩 버튼은 여백 20px, 결과 목록 카드의 모서리는 6px, 카드 안 날짜 글자는 6 + 테두리 1 + 행 여백 20 = 27px에서 시작했다. 카드 모서리가 버튼보다 14px 왼쪽으로 튀어나오고, 그 안의 글자는 다시 7px 오른쪽으로 밀려 세 개의 선이 보였다.
- 글자가 서는 선을 20px 하나로 통일했다. 목록 카드 여백을 6px에서 12px로 키우고 행 여백을 20px에서 7px로 줄여 `12 + 1 + 7 = 20`을 맞췄다. 제목·범위 버튼·사용자 지정 날짜 행의 여백 20px과 같은 선이다.
- 오른쪽도 같은 선에서 끝난다. 범위 줄의 건수는 여백 20px, 목록 글자는 카드 12 + 테두리 1 + 행 7 = 20px, 헤더 닫기 버튼은 여백 14px에 26px 버튼이라 X 글리프의 바깥선이 20px에 온다.
- 목록 카드 바깥 띠가 6px에서 12px로 넓어져 테두리 판정 영역 10px이 완전히 그 안에 들어간다. 결과 행 클릭과 겹치던 문제가 구조적으로 해소된다.
- 오늘 구분 띠와 빈 결과·로딩 문구의 여백도 같은 7px 기준으로 맞췄다.
- **변경 파일:** `App/SearchWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `search-check.ps1` PASS(exit 0). 새 시험본 실행 및 응답 상태 확인.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 제목·범위 버튼·결과 날짜의 왼쪽 선이 한 줄로 보이는지, 오른쪽 끝선도 맞는지, 넓힌 12px 띠에서 테두리가 잘 잡히는지.
  - 검색 실행 버튼 추가 여부 결정.
  - 다른 팝업의 모서리 그림자 자국 공통 처리 여부 결정.


## 2026-09-01 — 검색 건수 표기 변경과 목록 여백으로 테두리 잡기 개선

- **요청:** 최대 검색 건수가 500건이 맞는지 확인하고 `500(최대검색)/862건` 형식으로 표시한다. 아래와 왼쪽 테두리를 잡기 어려우니 여백을 줘서 쉽게 만든다.
- 최대 건수는 500이 맞다. `Render()`가 오늘과의 거리로 정렬한 뒤 `.Take(500)`으로 자른다. 이 정렬·절단 표현은 `search-check.ps1`이 문자열로 고정하고 있어 그대로 두었다.
- 건수 표기를 요청한 형식으로 바꿨다. 잘린 경우 `500(최대검색)/862건`, 잘리지 않은 경우 `862건`. 전체 문장은 마우스를 올렸을 때 도움말로 보여 준다.
- 표기가 길어져 자리를 확보했다. 범위 슬라이딩 버튼 한 칸을 74px에서 70px로 줄이고(5칸 370 → 350), 건수를 범위 줄의 오른쪽 끝으로 옮겼다. 창을 좁혀도 글자가 잘리기만 하고 배치가 무너지지 않도록 `TextTrimming`을 넣었다.
- **아래·왼쪽 테두리 잡기:** 결과 목록이 팝업 면 끝까지 차 있어서, 테두리 판정 영역 10px이 결과 행의 클릭 영역과 겹쳤다. 커서도 결과 행의 손 모양이 이겨서 잡을 수 있다는 신호가 보이지 않았다.
- 결과 목록을 좌우·아래로 6px 들여 팝업 면이 테두리처럼 남게 했다. 목록은 반지름 10px 카드가 되고, 남은 띠 위에서는 결과 행과 겹치지 않아 크기 조절 커서가 제대로 뜬다. 제목줄은 헤더 띠이므로 위쪽은 그대로 끝까지 채운다.
- **변경 파일:** `App/SearchWindow.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `search-check.ps1` PASS(exit 0). 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. 목록을 카드로 감싸며 `Grid.SetRow(resultScroller, 3)`이 `Grid.SetRow(listShell, 3)`으로 바뀌어 `feature-pack-check.ps1`의 해당 토큰을 함께 갱신했다.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 아래·왼쪽·오른쪽 테두리에서 크기 조절 커서가 뜨고 실제로 늘어나는지, `500(최대검색)/862건`이 기본 크기와 최소 크기에서 잘리지 않는지, 목록 카드의 둥근 모서리가 매끄러운지.
  - 최소 폭 464px에서는 범위 버튼 350px과 건수 표기가 함께 들어가기 빠듯하다. 실기기에서 잘리면 최소 폭을 올리거나 좁을 때 `(최대검색)`을 생략하는 방식을 검토한다.
  - 검색 실행 버튼 추가 여부 결정.
  - 다른 팝업의 모서리 그림자 자국 공통 처리 여부 결정.


## 2026-09-01 — 검색창 테두리 크기 조절·모서리 그림자 자국·건수 문구 잘림 보정

- **요청:** 창 테두리가 잡히지 않는다. 메인 창에 적용된 방식과 같은 것으로 맞춘다. 네 모서리에 검은색이 비친다. `±1년`과 `전체`에서 건수 문구가 잘린다. 건수만 보여도 된다.
- **테두리 크기 조절:** `ResizeMode.CanResize`만으로는 `WindowStyle.None` + `AllowsTransparency` 조합에서 잡을 수 있는 영역이 보이지 않았다. 메인 창은 `ResizeMode.NoResize`로 두고 `MainWindow.Navigation.cs`의 `ResizeEdgeAt`·`ResizeCursor` 판정과 `DesktopLayer.BeginResize` 네이티브 호출로 직접 처리하고 있었다. 검색창도 같은 방식으로 바꿨다.
- 판정 기준을 메인 창과 동일하게 맞췄다. 모서리 18px, 변 10px, 커서는 `UiCursor.ResizeNwSe`·`ResizeNeSw`·`SizeWE`·`SizeNS`. 공통 함수 `OnharuPopupChrome.EnableResize(window, surface)`로 넣어 다른 팝업도 같은 한 줄로 켤 수 있게 했다. 메인 창은 자기 사본을 그대로 두었고, 합치는 것은 리팩토링 단계 과제로 남긴다.
- **모서리 검은 자국:** `UiRound.EmphasizePopup`이 Shell에 `DropShadowEffect`(BlurRadius 18, ShadowDepth 5, Opacity .38, `#1E293B`)를 건다. Shell이 창 경계에 딱 붙어 있어 그림자가 경계에서 잘리고, 둥근 모서리 바깥의 남는 사각 영역에 짙은 슬레이트가 그대로 남아 검게 보였다. Shell에 사방 12px 여백을 주어 그림자가 자연스럽게 흩어지게 했고, 팝업 본체 크기를 유지하려고 창 크기를 `520×540`에서 `544×564`로 키웠다. 최소 크기도 `464×404`로 맞췄다. 이 여백은 테두리를 잡기도 쉽게 만든다.
- **건수 문구:** `696건 중 오늘과 가까운 500건`은 범위 슬라이딩 버튼 옆의 좁은 자리에서 잘렸다. `500 / 696건`으로 줄이고, 전체 문장은 마우스를 올렸을 때 도움말로 보여 준다. 잘리지 않은 경우는 `696건`처럼 건수만 표시한다. 표시 건수와 전체 건수를 함께 두어 조용한 절단이 되지 않게 유지했다.
- **변경 파일:** `App/SearchWindow.cs`, `App/OnharuPopupChrome.cs`, `App/ui-construction-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `search-check.ps1` PASS(exit 0). 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. `ui-construction-check.ps1`의 검색창 크기 기대값을 `544×564`로 갱신하고 이유를 주석으로 남겼다. 이 검사는 여전히 `PlannerSettings` 생성자 서명 불일치로 22행에서 멈춰 검색창 단언까지 도달하지 못한다.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 네 변과 네 모서리에서 크기 조절 커서가 뜨고 실제로 늘어나는지, 모서리 검은 자국이 사라졌는지, 늘렸을 때 결과 목록만 늘어나는지, `500 / 696건` 표시와 도움말이 잘리지 않는지.
  - 검색 실행 버튼 추가 여부 결정.
  - 가로 조절을 막을지 여부 결정.
  - 다른 팝업에도 같은 모서리 그림자 자국이 있을 수 있다. `OnharuPopupChrome.Shell`을 쓰면서 여백이 없는 창은 모두 해당한다. 공통 처리 여부는 사용자 확인 후 별도 작업으로 다룬다.

## 2026-09-01 — 검색 범위 전환 애니메이션 끊김 해소와 창 크기 조절

- **요청:** 슬라이딩 버튼이 움직이는 동안 검색이 즉시 시작해 손잡이 움직임이 끊긴다. 슬라이딩이 끝난 뒤에 검색을 시작하게 한다. 창 크기 조절도 추가한다.
- 원인은 `OnharuSegmentedSwitch`의 손잡이 애니메이션이 130ms인데 `changed` 콜백이 즉시 `ScheduleRender()`를 호출해, 애니메이션이 도는 동안 결과 목록 전체를 다시 그리며 UI 스레드를 붙잡은 것이다. `Dispatcher` 우선순위를 낮추는 것만으로는 렌더 자체의 길이가 줄지 않아 해결되지 않는다.
- 170ms `DispatcherTimer`를 두어 범위 전환 시 렌더를 애니메이션 종료 뒤로 미룬다. 전환할 때마다 타이머를 다시 시작하므로 여러 칸을 연속으로 눌러도 마지막 한 번만 그린다. 사용자 지정 날짜 행의 표시·숨김은 이전처럼 즉시 반응한다.
- `ResizeMode = ResizeMode.CanResize`를 명시했다. 이전에는 지정하지 않아 WPF 기본값에 의존했고 잡을 수 있는 테두리가 눈에 띄지 않았다. 결과 목록이 이미 `Grid`의 `*` 행이라 창을 늘리면 목록이 그만큼 늘어난다. `MinWidth 440`·`MinHeight 380`이 하한이다.
- 가로 조절도 함께 열려 있다. 제목 열이 `*`라 창을 넓히면 긴 일정 제목이 덜 잘린다. 세로만 허용하려면 별도로 폭을 고정해야 하며, 현재는 제한하지 않았다.
- **변경 파일:** `App/SearchWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. `search-check.ps1` PASS(exit 0). 새 시험본 실행 및 응답 상태 확인.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 범위 전환 시 손잡이가 부드럽게 미끄러지는지, 170ms 지연이 검색이 느려진 것처럼 느껴지지 않는지, 창 테두리를 실제로 잡아 늘릴 수 있는지, 늘렸을 때 결과 목록만 늘고 제목줄·범위 줄 높이는 그대로인지.
  - 테두리를 잡기 어렵다면 오른쪽 아래에 눈에 보이는 크기 조절 손잡이를 추가한다.
  - 가로 조절을 막을지 여부 결정.
  - 검색 실행 버튼 추가 여부 결정.

## 2026-09-01 — 검색 화면 '목록 밀도' 배치 확정과 브랜드 그라데이션 적용

- **요청:** 검색창 배치 시안 중 사용자가 `목록 밀도`안을 고르고, 버튼 색은 더 화사한 온하루 스타일인 `브랜드 그라데이션`안을 골랐다.
- 제목·검색 입력·오늘·닫기를 제목줄 한 행에 모았다. 별도 검색 행이 사라져 결과 목록에 세로 공간을 넘긴다. 제목은 공통 `OnharuPopupChrome.FeatureHeading("⌕", "검색")`을 그대로 쓴다.
- 범위 선택을 `ComboBox`에서 공통 `OnharuSegmentedSwitch`로 바꿨다. `design-onharu.md` 8.6의 `둘 이상의 보기 옵션은 슬라이딩 버튼` 규칙에 맞추고, 사용자가 지적한 `드롭다운 옵션이 너무 작다`·`아래 화살표가 너무 작다`를 원인째 없앤다. 범위가 항상 펼쳐져 있어 클릭 한 번에 전환된다. 라벨은 `±1년·과거·앞으로·지정·전체`.
- 결과를 54px 카드에서 42px 목록 행으로 바꿨다. 날짜를 왼쪽 92px 고정 열로 정렬하고 상태 점·제목·시각을 한 줄에 둔다. 같은 창 높이에서 보이는 건수가 5건에서 10건 이상으로 늘었다.
- 오늘 구분은 32px 둥근 블록에서 목록 폭을 가로지르는 26px 띠로 바꿔 42px 행 리듬을 끊지 않게 했다.
- 오늘 버튼에 온하루 브랜드 그라데이션을 적용했다. 다만 브랜드 원본 `#0EA5E9 → #7C3AED`에 흰 글자를 올리면 밝은 쪽 끝의 대비가 2.9:1로 `AGENTS.md`의 4.5:1 기준에 미달한다. 시작색을 `#0369A1`로 한 단계 낮춰 5.3:1을 확보했고, `OnharuPopupChrome.BrandGradientStartColor`·`BrandGradientEndColor` 토큰으로 고정했다. 브랜드 그라데이션은 원래 로고 글자용 정의이므로 버튼 면으로 쓸 때의 값은 이 토큰을 기준으로 한다.
- 오늘 상태색 토큰을 선택한 팔레트에 맞춰 갱신했다. `TodaySurfaceColor #E0F2FE`, `TodayTextColor #0369A1`, `TodayBorderColor #7DD3FC`. 범위 슬라이딩 버튼의 선택색은 `#EDE9FE / #6D28D9`, 테두리는 `#C4B5FD`를 쓴다. 새 색은 만들지 않고 브랜드·바이올렛 계열 안에서만 골랐다.
- 검색 버튼은 두지 않았다. 제목줄 입력칸에서 Enter로 검색한다. `버튼 높이가 너무 크다`는 지적에 따라 뺀 배치이며, 눈에 보이는 실행 버튼이 필요하면 오늘 버튼 왼쪽에 30px로 추가한다. 사용자 확인 대기.
- 이번 배치 변경으로 낡아진 검사 토큰을 갱신했다. 운영 코드를 과거 검사 문자열에 맞추지 않았다. `SettingsWindow.StyleComboBox(range)`는 콤보가 사라져 `new OnharuSegmentedSwitch(labels`로, 결과 행 여백 `Thickness(12, 5, 12, 5)`는 42px 행이 되어 `Height = 42, HorizontalContentAlignment`로, `Grid.SetColumn(range, 1)`·`Grid.SetColumn(search, 2)`·`titleGroup.Children.Add(todayButton)`·`Grid.SetRow(resultScroller, 4)`는 새 구조 토큰으로 교체했다. 갱신 이유는 해당 파일에 주석으로 남겼다.
- **변경 파일:** `App/SearchWindow.cs`, `App/OnharuPopupChrome.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. `search-check.ps1` PASS. 갱신한 검사 토큰 7개가 모두 실제 소스에 존재함을 확인. `ui-construction-check.ps1`은 `PlannerSettings` 생성자 서명 불일치로 22행에서 멈추므로 검색창 `520×540` 단언까지 도달하지 못한다. 창 크기 값 자체는 소스에서 유지했다.
- **미해결·다음 작업:**
  - 실기기 수동 확인 필요: 슬라이딩 버튼 5칸이 좁은 폭·125% DPI에서 잘리지 않는지, 42px 행에서 제목이 길 때 말줄임이 제대로 되는지, 오늘 띠와 오늘 버튼의 색이 한 조합으로 읽히는지, 그라데이션 위 흰 글자가 실제 화면에서 읽히는지.
  - 검색 실행 버튼 추가 여부 결정.
  - 창 세로 확장은 아직 막혀 있다. 본문은 이미 `Grid`의 `*` 행이라 `ResizeMode`만 열면 결과 목록이 따라 늘어난다.
  - 앞선 기록의 분류 4(코드가 기준 문서에서 벗어난 `FeatureTitle`·메인 `todayButton`)는 그대로 남아 있다.


## 2026-09-01 — 검색 화면 레이아웃 구조·끝선·상태색 토큰화

- **요청:** 검색 기능을 온하루 스타일로 검토하고 배치를 사용자 관점에서 개선한다. 2.2.6-test 기준.
- 결과 목록이 창 크기를 따라가지 않던 문제를 고쳤다. 바깥 `panel`이 `StackPanel`이라 `resultScroller.Height`를 400으로 고정하고 있었고, `사용자 지정` 범위를 고를 때만 366으로 줄였다. 실제로 추가되는 날짜 행은 높이 34 + 위 여백 7 = 41px여서 매번 7px이 초과 계산됐다. `panel`을 5행 `Grid`로 바꾸고 결과 목록을 `*` 행에 넣어 400·366·34 고정값을 모두 제거했다. 창 크기와 사용자 지정 행 표시 여부에 관계없이 목록이 남은 공간을 채운다.
- 검색 입력 행과 사용자 지정 날짜 행에만 붙어 있던 오른쪽 여백 20px을 제거해 결과 목록과 오른쪽 끝선을 맞췄다. 이 값은 HEAD의 82px에서 오늘 20px로 줄어든 상태였다.
- `#DCE7F6`·`#9FB1C7` 오늘 강조색이 오늘 버튼과 오늘 마커 두 곳에 각각 하드코딩돼 있었다. `OnharuPopupChrome`에 `TodaySurfaceColor`·`TodayTextColor`·`TodayBorderColor`를 추가하고 두 곳이 같은 토큰을 쓰게 했다. 팝업은 파스텔·블랙 공통 톤이므로 테마별 `OnharuStateColors`가 아니라 팝업 크롬에 둔다.
- `.Take(500)` 절단을 사용자에게 알리지 않던 문제를 고쳤다. 결과 목록 위에 건수를 표시하고, 잘린 경우 `N건 중 오늘과 가까운 500건`으로 명시한다. 결과가 없으면 이 줄은 숨긴다. `search-check.ps1`이 요구하는 정렬 문자열은 그대로 보존했다.
- 검색 입력칸에 `일정 제목·메모 검색` 안내 문구를 넣고, 40px 칸에서 위쪽에 붙어 있던 입력 글자를 세로 중앙으로 맞췄다.
- `range` 콤보의 `Width`·`Height` 중복 설정을 정리하고, 빈 결과 메시지 여백을 로딩 메시지와 통일했다. 오늘 버튼에 툴팁을 추가했다. 창에 `MinWidth 440`·`MinHeight 380`을 지정했다. `Width 520`·`Height 540`은 `ui-construction-check.ps1` 기준대로 유지했다.
- 낡은 검사 토큰 두 개를 현재 구조에 맞게 갱신했다. 운영 코드를 과거 검사 문자열에 맞추지 않았다. ① `feature-pack-check.ps1`의 일기 진입점 목록에서 `OpenDiaryEditor(date)`를 뺐다. 일기장 기능 제외로 두 번째 호출 지점이 사라졌고 남은 `OpenDiaryEditor(selectedDate)`는 그대로 검사한다. ② 검색 배치 토큰 `Margin = new Thickness(0, 7, 70, 0)`은 HEAD에서도 82였던 낡은 값이라, 끝선 정렬 의도를 담은 `Height = 34, Margin = new Thickness(0, 7, 0, 0)`과 새 Grid 구조 토큰 두 개로 교체했다.
- **변경 파일:** `App/SearchWindow.cs`, `App/OnharuPopupChrome.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공. 새 시험본 실행 및 응답 상태 확인, `error.log` 없음. `search-check.ps1` PASS. 자동 검사 18종 중 14종 PASS. `feature-pack-check.ps1`이 SearchWindow에 요구하는 배치·기능 토큰은 이 변경 뒤에도 모두 남아 있고, `ui-construction-check.ps1`이 고정한 검색창 `520×540`도 유지했다.
- **PowerShell 7 경로:** 일반 PATH에는 `pwsh`가 없지만 Codex 번들 런타임에 7.6.4가 있다. `C:\Users\HOME\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\powershell\pwsh.exe`. 이 실행기의 폴더를 PATH 앞에 붙이면 `check-v22.ps1`을 정식으로 돌릴 수 있다. 아래 실패는 PS7에서도 동일하게 재현했으므로 실행 환경 문제가 아니다.
- **실패 4종 분류 (모두 이번 검색창 변경과 무관):**
  - **분류 1 — SearchWindow.cs 변경과 직접 관련: 없음.** `search-check.ps1` PASS, 빌드 PASS, `feature-pack`·`ui-construction`의 검색 관련 단언 모두 유지.
  - **분류 2 — 기존 코드와 검사 문자열이 어긋난 낡은 검사:** `feature-pack-check.ps1`의 일기 단언 14곳(일기장 기능 제외 결정 반영 안 됨), `popup-policy-check.ps1`의 KBO 글자형 `Button("«", 23`·`Button("»", 23`(2026-09-01 KBO 적용에서 벡터 이동 버튼으로 교체 완료, HANDOFF에 기록됨), `ui-construction-check.ps1`의 `PlannerSettings` 고정 인자 목록(생성자 서명 변경).
  - **분류 3 — PowerShell 5.1/7 실행 환경 차이:** `popup-policy-check.ps1`과 `search-check.ps1`은 BOM 없는 UTF-8이라 PS 5.1에서 한글 리터럴이 깨진다. PS7에서는 정상. 단 PS7로 다시 돌려도 아래 분류 4의 실패는 그대로 남는다.
  - **분류 4 — 검사가 맞고 코드가 기준에서 벗어난 경우 (검사를 고치면 안 되는 항목):**
    - `popup-policy-check.ps1`이 요구하는 `FeatureTitle("▦", "나의 시간표")`·`FeatureTitle("⚾", "KBO 경기 일정")`은 낡은 토큰이 아니다. `design-onharu.md` 8.6 독립 도구 창은 `34px 아이콘 박스 + 20px 제목`, `AGENTS.md`·`CLAUDE.md`는 `팝업의 34px 제목 박스에서 17px`, 2026-08-23 HANDOFF는 `팝업 제목은 34px 박스 안 17px을 기준으로 하고 규칙을 AGENTS.md·POPUP_POLICY.md·popup-policy-check.ps1에 고정했다`고 적고 있다. 이는 `FeatureTitle`(34px Border·17px 글리프·20px 제목)의 규격이다. 현재 두 창은 `FeatureHeading`(박스 없음·24px 폭·21px 글리프)을 쓰고 있고, 전환 근거가 어느 문서에도 없다. 기준 문서 4곳과 어긋나므로 검사를 고칠 것이 아니라 코드를 되돌리거나 기준 문서를 바꿔야 한다. 사용자 확인 필요.
    - `theme-check.ps1`이 요구하는 `periodNavigation.Children.Add(todayButton)`도 마찬가지다. `todayButton`은 현재 `App/SearchWindow.cs`에만 있고 메인 헤더에는 없다. 그런데 `Docs/Design/MAIN_HEADER_UI_STANDARD.md`는 여전히 `상단 오늘 버튼: 42×23, 글자 12.5px SemiBold`와 `<, 오늘, > 사이 간격 각각 3px`를 규정한다. `TODO_NEXT.md`에 오늘 버튼 이동·제거 검토 항목이 있으나 미완료 상태다. 코드와 기준 문서 중 어느 쪽이 맞는지 사용자 확인 필요.
  - `CURRENT_STATUS.md`의 `check-v22.ps1 자동 품질 게이트 17개: PASS`는 현재 사실과 다르다. 갱신 여부는 위 분류 4 결정 이후로 미룬다.
  - 실기기 수동 확인 필요: 검색창을 세로로 늘였을 때 결과 목록이 따라 늘어나는지, `사용자 지정` 범위 선택 시 날짜 행과 결과 목록이 겹치거나 잘리지 않는지, 100%·125% DPI에서 안내 문구와 건수 표시가 잘리지 않는지, 오늘 버튼과 오늘 마커 색이 같은지.


## 2026-09-01 — Claude 지침 파일 신설과 AGENTS.md 동기화 규칙

- Claude 작업용 진입 문서 `CLAUDE.md`를 새로 만들었다. `AGENTS.md`의 제품 기준·코드 책임 분리·디자인 원칙·팝업 규칙·검증과 기록 본문을 같은 문장으로 담고, 마지막에 Claude 고유 절 `Claude 세션 운영`을 추가했다.
- `Claude 세션 운영`에는 작업 폴더와 PowerShell 5.1 제약, `check-v22.ps1` 게이트, 현재가 디자인 단계이며 디자인 마무리 → 리팩토링 → 업로드 순서라는 점, 그 전까지 원격 push·공개 배포 금지, Codex와 워킹트리를 공유하므로 남의 미커밋 변경을 되돌리지 않는다는 규칙, `HANDOFF.md` 기록 형식을 명시했다.
- `AGENTS.md` 앞부분에 `두 지침 파일의 역할과 동기화` 절을 추가했다. 공통 규칙을 한쪽에서 고치면 같은 작업에서 다른 쪽도 같은 문장으로 고치고, 한쪽만 갱신한 작업은 완료로 보지 않는다.
- `START_HERE.md` 1항 읽는 순서를 `AGENTS.md`(Codex) 또는 `CLAUDE.md`(Claude)로 바꾸고, 2항 문서 동기화 목록에 두 지침 파일을 함께 갱신하라는 항목을 추가했다.
- **변경 파일:** `CLAUDE.md`(신규), `AGENTS.md`, `START_HERE.md`, `HANDOFF.md`.
- **검증:** 두 지침 파일의 공통 5개 절 본문 일치 확인. 코드 변경이 없어 빌드·`check-v22.ps1`은 실행하지 않았다.
- **미해결·다음 작업:** 워킹트리에 미커밋 변경 84개가 남아 있어 작업자별 변경 구분이 어렵다. 에이전트 교대 지점의 체크포인트 커밋 여부는 사용자 판단 대기 중이다.

## 2026-09-01 — 검색창 반경·역할색·날짜행 정렬 보정

- 검색 입력과 DatePicker 반경을 8px로, 오늘 버튼 반경을 10px로 맞췄다.
- 검색 버튼을 공통 selection 토큰 `#FBE8DE/#9A3412/#E4AA8D`로 통일하고 날짜 팝업의 임시 크림색을 공통 흰 작업면으로 교체했다.
- 사용자 지정 날짜 행의 오른쪽 여백을 70px에서 20px로 줄여 기본 검색 행과 같은 우측 정렬선에 맞췄다.
- **변경 파일:** `App/SearchWindow.cs`, `HANDOFF.md`.
- **검증:** 검색 범위 검사 통과, C# 시험본 빌드 성공, 새 시험본 실행 및 응답 상태 확인.

## 2026-09-01 — 시간표 역할색·요일색·헤더 정렬 보정

- 시간표 저장을 온하루 블루 `#4F46E5/#FFFFFF/#4338CA`, 설정 적용을 소프트 블루 `#EEF2FF/#4338CA/#C7D2FE`로 변경해 청녹·로즈의 역할 혼동을 제거했다. 다른 팝업의 공통 버튼색은 변경하지 않았다.
- 요일 배경/글자 배열을 시간표 내부 단일 소스로 만들고 설정 체크박스와 표 헤더가 같은 글자색을 사용하도록 통일했다.
- 헤더 하단 여백을 11px로 맞추고 닫기·저장·설정 버튼에 명시적인 세로 중앙 정렬을 적용했다.
- **변경 파일:** `App/TimetableWindow.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공, 새 시험본 실행 및 응답 상태 확인.

## 2026-09-01 — 요일 이동 클릭면·세부 패널 토글 위치 보정

- 일·토요일 양끝 이동 버튼을 28×34px 투명 버튼으로 바꿔 화살표 선을 정확히 가리키지 않아도 전체 면에서 손 모양과 이동 동작이 작동한다.
- 세부 패널 토글을 오늘/이번 주/다음 주 행의 높이에 맞추고, 온하루·Google 필터를 접거나 펼쳐도 해당 행을 따라 이동하게 했다.
- 접기 버튼은 마우스를 올리면 오른쪽으로 32px까지 넓어지는 기존 동작을 복구했고, Google 계정 카드는 원래 폭으로 되돌렸다. 펼치기 버튼도 마지막으로 계산된 같은 세로 위치를 유지한다.
- **변경 파일:** `App/MainWindow.cs`, `App/MainWindow.Layout.cs`, `App/MainWindow.Calendar.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공, 새 시험본 실행 및 응답 상태 확인.

## 2026-09-01 — 4주 보기 월 시작일 인지 개선

- 달력에서 매월 1일을 `8/1`, `9/1`처럼 월/일로 표시해 4주 이동 중 월 경계를 바로 알 수 있게 했다.
- 월 시작일이 오늘 표시와 겹쳐도 글자가 잘리지 않도록 오늘 날짜 배지를 기본 25px, 월 시작일은 34px 폭으로 조정했다.
- **변경 파일:** `App/MainWindow.Calendar.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공, 새 시험본 실행 및 응답 상태 확인.

## 2026-09-01 — 세부일정 접기·요일 양끝 이동 버튼 배치

- 세부일정 접기 버튼을 기존 경계 바깥에서 패널 안쪽 약 20px 지점의 Google 계정 카드 옆으로 옮겼다. 패널을 접은 뒤에는 기존처럼 달력 오른쪽 경계에 남아 다시 펼칠 수 있다.
- 일요일 왼쪽과 토요일 오른쪽에 기존 온하루 벡터 `‹ / ›` 버튼을 추가했다. 한 번 클릭은 한 주, 더블클릭은 같은 방향 한 달 이동하며 기존 상단 이동 버튼은 변경하지 않았다.
- **변경 파일:** `App/MainWindow.Layout.cs`, `App/MainWindow.Calendar.cs`, `App/MainWindow.Navigation.cs`, `App/MainWindow.DesktopInput.cs`, `HANDOFF.md`.
- **검증:** C# 시험본 빌드 성공, 새 시험본 실행 및 응답 상태 확인.

## 2026-09-01 — design-onharu 대표 실행색 수정

- 일정 저장·설정 저장·등록·적용 등 확정 버튼의 기준색을 청녹에서 온하루 블루 `#4F46E5`로 바로잡았다.
- 청녹은 성공·연결·동기화 정상 상태에만 쓰도록 역할을 분리했다.
- 추가 기능 창의 배치는 아직 공통 규격으로 확정하지 않고 향후 별도 개선 대상으로 명시했다.
- **변경 파일:** `design-onharu.md`, `HANDOFF.md`.

## 2026-09-01 — design-onharu KBO 화면 적용

- **순서 4/4:** 야구공 아이콘과 경기 기능은 유지하고 등록·완료·API 연결을 공통 틸 액션으로 통일했다. 오늘·이동 벡터·로딩·오늘 날짜 셀·응원팀 팝업은 온하루의 중립/블루/틸 역할색으로 정리했다.
- **보존:** 야구 슬라이딩 선택은 기존 진한 슬레이트 스타일과 무여백 높이를 유지했다.
- **변경 파일:** `App/SportsCalendarWindow.cs`, `App/SportsApiWindows.cs`, `HANDOFF.md`.
- **검증:** 새 C# 시험본 빌드 성공.

## 2026-09-01 — design-onharu 알람·타이머 화면 적용

- **순서 3/4:** 실행 버튼을 공통 틸 액션 버튼으로 통일하고 입력·실행 목록을 흰 작업면과 중립 테두리 카드로 정리했다. 스크롤 영역은 공통 보조 배경을 사용한다.
- **보존:** 취소의 부드러운 경고색과 알람 계산·HHmm 입력·카운트다운 동작은 변경하지 않았다.
- **변경 파일:** `App/AlarmWindow.cs`, `HANDOFF.md`.
- **검증:** 새 C# 시험본 빌드 성공.

## 2026-09-01 — design-onharu 시간표 화면 적용

- **순서 2/4:** Clay의 크림 캔버스·원색 요일 면을 제거하고 공통 중립 팝업, 흰 편집면, 저채도 온하루 7색 요일 헤더, 슬레이트 표 구분선으로 재구성했다. 시간표 저장은 공통 틸 실행 버튼, 적용은 소프트 로즈를 사용한다.
- **보존:** 창 크기, 교시·요일·시간 설정, 스크롤, 첫 행·첫 열 고정과 저장 동작은 변경하지 않았다.
- **변경 파일:** `App/TimetableWindow.cs`, `HANDOFF.md`.
- **검증:** Clay 토큰 제거와 공통 제목·저장·7색 헤더 소스 검사 통과, 새 C# 시험본 빌드 성공.

## 2026-09-01 — design-onharu 검색 화면 적용

- **순서 1/4:** 검색 화면의 기능·크기·배치는 유지하고 강한 보라색을 제거했다. 오늘은 온하루 블루그레이, 검색 실행은 소프트 로즈, 입력·결과·날짜 선택은 공통 흰 작업면과 슬레이트 테두리로 통일했다.
- **변경 파일:** `App/SearchWindow.cs`, `HANDOFF.md`.
- **검증:** `search-check.ps1` 통과, 새 C# 시험본 빌드 성공.

## 2026-09-01 — ONHARU 자체 디자인 시스템 문서 작성

- **요청:** 외부 Clay 디자인이 제품과 어울리지 않는 점을 반영해 ONHARU 고유 스타일의 `design-onharu.md`를 만든다.
- **정리:** 현재 메인 달력·상세 카드·팝업·아이콘·슬라이딩 버튼의 실제 코드와 공식 디자인 문서를 기준으로 색상, 글자, 간격, 모서리, 컴포넌트, 상태, DPI 및 금지 규칙을 하나의 재사용 문서로 통합했다.
- **핵심 원칙:** 넓은 면은 중립 작업면으로 유지하고 카테고리색은 일정 구분에 집중한다. 외부 마케팅 디자인의 강한 원색 면·큰 카드·과장된 여백을 앱 UI에 그대로 이식하지 않는다.
- **변경 파일:** `design-onharu.md`, `HANDOFF.md`.
- **검증:** 문서가 `CategoryColorSystem`, `OnharuStateColors`, `OnharuPopupChrome`, 메인 헤더·테두리·팝업 공식 문서를 구현 기준으로 가리키는지 확인했다.

## 2026-09-01 — Clay 디자인을 시간표에 한정 적용

- **요청:** `design-clay.md`의 디자인 체계를 나의 시간표에 적용한다.
- **수정:** 시간표에만 크림 캔버스·따뜻한 카드 표면·헤어라인 테두리·16px 외곽 모서리를 적용하고 요일 머리글은 Clay의 핑크·딥틸·라벤더·피치·오커·민트·크림 팔레트로 구성했다. 저장은 잉크색 주 실행, 적용은 핑크로 구분했다.
- **보존:** 시간표 창 크기, 교시·요일·시간 설정, 저장 방식, 스크롤 및 첫 행·첫 열 고정 동작과 다른 팝업의 공통 디자인은 변경하지 않았다.
- **웹 게시:** 새 `ONHARU-2.2.6-Test-Setup.exe`와 `ONHARU-2.2.6-Test-Portable.zip`, 다운로드 페이지와 릴리스 노트를 onharu.app에 게시했다. 정식판과 자동 업데이트는 2.2.4를 유지한다.
- **변경 파일:** `App/TimetableWindow.cs`, `Publish/ONHARU-Web/ONHARU-DOWNLOAD.html`, `Publish/ONHARU-Web/ONHARU-RELEASES.html`, `WEB_OPERATIONS_HANDOFF.md`, `HANDOFF.md`.
- **검증:** C# 시험본과 Inno Setup 설치판 빌드 성공. 설치판 SHA-256은 `60A0D2E8A27F8BD5E50F2CE85F4E810C4F1167124591EA48E3D2E90D934E25E1`, 포터블은 `5C3EABDFE9EBA619126345973F16125E7D275DA1AADFB92143D39005D6B297D3`이며 공개 재다운로드 해시가 일치했다. 공개 10개 페이지 HTTP 200, Nginx active를 확인했다. 사용자 요청에 따라 게시 전 화면 확인은 생략했다.
- **서버 백업:** `/home/ubuntu/onharu-web-backups/site-before-2.2.6-clay-timetable-20260901-1330.tar.gz`.

## 2026-09-01 — 기념일 펼침 상태의 추가 버튼 크기 고정

- **요청:** 세부 달력의 기념일 카드를 펼쳐도 제목 오른쪽 `+` 버튼이 닫힌 상태와 같은 모양을 유지하게 한다.
- **수정:** 기념일의 펼침·닫힘 상태에 따라 `18px/26px`로 달라지던 추가 버튼을 항상 `18px`로 고정했다.
- **웹 게시:** 새 `ONHARU-2.2.6-Test-Setup.exe`와 `ONHARU-2.2.6-Test-Portable.zip`, 다운로드 페이지와 릴리스 노트를 onharu.app에 게시했다. 정식판과 자동 업데이트는 2.2.4를 유지한다.
- **변경 파일:** `App/MainWindow.Anniversary.cs`, `Publish/ONHARU-Web/ONHARU-DOWNLOAD.html`, `Publish/ONHARU-Web/ONHARU-RELEASES.html`, `CURRENT_STATUS.md`, `WEB_OPERATIONS_HANDOFF.md`, `BUILD_AND_RELEASE.md`, `HANDOFF.md`.
- **검증:** 새 `2.2.6-test` C# EXE와 Inno Setup 설치판 빌드 성공. 설치판 SHA-256은 `1BE0DA5A30AF11425E43B46F2DD9156C9D22F393D8CFA3D08ED7CE84FD7EB98C`, 포터블은 `4C27F6FC563C112A3FD538A1A123DBF313FC0AA11A94C4D3021197308912BBE4`이며 공개 재다운로드 해시가 일치했다. 공개 10개 페이지 HTTP 200, Nginx active를 확인했다. 전체 품질 게이트는 기존 일기 기능 검사 문자열 `OpenDiaryEditor(date)` 누락에서 중단됐다.
- **서버 백업:** `/home/ubuntu/onharu-web-backups/site-before-2.2.6-test-refresh-20260901-0812.tar.gz`.

## 2026-08-31 — 2.2.6-test 웹·공개 소스 게시

- HTML 시안을 기준으로 일기 읽기 화면을 종이형 2열 레이아웃으로 재구성하고 목록 접기, 정렬 방향 연동 탐색, 선택·전체 선택·일괄 삭제를 추가했다.
- 일기 작성 화면에 날짜·기분·제목·한 줄 요약·본문을 분리하고 빈 일기도 저장할 수 있게 했다. 기존 일기 JSON은 새 필드가 없어도 그대로 읽힌다.
- `ONHARU-2.2.6-Test-Setup.exe`와 `ONHARU-2.2.6-Test-Portable.zip`을 생성해 onharu.app 시험판 구역에 게시했다. 정식판과 자동 업데이트는 2.2.4를 유지한다.
- 설치판 SHA-256: `FEABAA7065713FD4A1D888709B83D6F43F49D264C2DFE900B5882F35209C653B`, 포터블 SHA-256: `F92E73AF607D7001590BB8127FAA8903DAB192FCFA9F8A2FE5934018B7DAF3A4`.
- 서버 게시 전 백업: `/home/ubuntu/onharu-web-backups/site-before-2.2.6-test-20260831.tar.gz`. 공개 재다운로드 해시가 로컬 원본과 일치하고 9개 공개 페이지 HTTP 200을 확인했다.
- `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS.

## 2026-08-30 — 팝업 슬레이트 헤더·글자·버튼 재통일

- **피드백:** 기능별 파스텔 헤더는 아이디어가 분산되어 버튼과 제목·본문의 색 체계가 맞지 않았다. 설정의 슬레이트 헤더 방향은 유지 가치가 있었다.
- **수정:** 모든 팝업 헤더를 `#E9EDF3`, 제목·아이콘 글자를 `#334155`로 통일했다. 일반 탐색·분할 선택은 `#DCE7F6/#334155/#9FB1C7`, 주요 실행만 옅은 블루를 유지한다. 기존의 보라 일반 버튼은 블루그레이 선택색으로 자동 정규화한다.
- **보존:** 삭제·경고·완료 의미색, 메인 달력과 오른쪽 상세 패널은 변경하지 않았다.
- **변경 파일:** `App/OnharuPopupChrome.cs`, `App/SettingsWindow.cs`, `App/SearchWindow.cs`, `App/TimetableWindow.cs`, `App/DiaryWindows.cs`, `App/SportsCalendarWindow.cs`, `App/popup-policy-check.ps1`, `Docs/Design/COLOR_PALETTE_STANDARD.md`, `HANDOFF.md`.
- **검증:** `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS, 새 `2.2.6-test` C# EXE 빌드 성공. 네이티브 Hook DLL 최종 복사는 Explorer 파일 잠금으로 기존 파일을 유지했다.

## 2026-08-30 — 기능별 저채도 파스텔 헤더 조화

- **피드백:** 팝업 전체가 블루 계열로 몰려 통일감은 있으나 온하루다운 색 조화가 부족했다.
- **수정:** 공통 바탕을 중립 쿨그레이 `#F7F7FA`, 보조 카드를 `#F2F4F7`, 외곽선을 `#AEB4C0`로 중립화했다. 헤더만 동일한 명도·채도로 일정 인디고, 검색 로즈, 시간표 라벤더, 일기 피치, 야구 민트, 설정 슬레이트를 배정했다.
- **보존:** 실행·선택·성공·주의·삭제 버튼은 역할 기반 공통색을 유지해 기능별 헤더가 산만하게 번지지 않도록 했다. 메인 달력과 오른쪽 상세 패널은 변경하지 않았다.
- **변경 파일:** `App/OnharuPopupChrome.cs`, `App/SettingsWindow.cs`, `App/SearchWindow.cs`, `App/TimetableWindow.cs`, `App/DiaryWindows.cs`, `App/SportsCalendarWindow.cs`, `App/popup-policy-check.ps1`, `Docs/Design/COLOR_PALETTE_STANDARD.md`, `HANDOFF.md`.
- **검증:** `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS, 새 `2.2.6-test` C# EXE 빌드 성공. 네이티브 Hook DLL 최종 복사는 Explorer 파일 잠금으로 기존 파일을 유지했다.

## 2026-08-30 — 팝업 블루화이트 4단계 표면 계층

- **피드백:** 중립 팝업이 아이보리 한 면처럼 보여 내부 구조와 색 리듬이 부족했다.
- **수정:** 팝업 바탕 `#F3F7FC`, 헤더 `#EAF2FF`, 내용 카드 `#FFFFFF`, 보조 카드 `#EEF3F8`의 네 단계로 분리하고 외곽선을 블루그레이 `#94A3B8`로 조정했다. 새 일정·수정·설정·검색·시간표·일기·야구의 실제 헤더 및 주요 내용/보조 카드에 적용했다.
- **보존:** 메인 파스텔 달력·오른쪽 상세 패널·실행/선택/의미 버튼 역할색은 변경하지 않았다.
- **변경 파일:** `App/OnharuPopupChrome.cs`, `App/AddItemWindow.cs`, `App/SettingsWindow.cs`, `App/SearchWindow.cs`, `App/TimetableWindow.cs`, `App/DiaryWindows.cs`, `App/SportsCalendarWindow.cs`, `App/popup-policy-check.ps1`, `Docs/Design/COLOR_PALETTE_STANDARD.md`, `HANDOFF.md`.
- **검증:** `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS, 새 `2.2.6-test` C# EXE 빌드 성공. 네이티브 Hook DLL 최종 복사는 Explorer 파일 잠금으로 기존 파일을 유지했다.

## 2026-08-30 — 공식 가이드 기반 온하루 팝업 톤 체계

- **기준:** Fluent의 중립·공유·브랜드 팔레트 분리, Material의 surface/primary/secondary 의미 역할, WCAG 2.2 대비 기준을 온하루에 적용했다. 메인 파스텔 달력과 오른쪽 상세 패널은 변경하지 않는다.
- **수정:** 팝업은 쿨화이트 `#F8FAFC`와 중간톤 블루그레이 테두리 `#A8B3C5`, 주요 실행은 옅은 블루 `#DBEAFE/#1D4ED8/#93C5FD`, 탭 선택만 라벤더 `#EDE9FE/#6D28D9/#C4B5FD`로 역할을 분리했다. 민트·오렌지·코랄은 연결·주의·삭제 의미에만 유지한다.
- **적용:** 새 일정·수정·설정·검색·시간표·일기·야구 및 공통 팝업 버튼·분할 선택 조작부.
- **변경 파일:** `App/OnharuPopupChrome.cs`, `App/AddItemWindow.cs`, `App/SettingsWindow.cs`, `App/SearchWindow.cs`, `App/DiaryWindows.cs`, `App/SportsCalendarWindow.cs`, `App/popup-policy-check.ps1`, `Docs/Design/COLOR_PALETTE_STANDARD.md`, `HANDOFF.md`.
- **검증:** 실행·선택·일반 버튼의 텍스트 대비는 각각 `5.49:1`, `5.98:1`, `6.92:1`. `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS, 새 `2.2.6-test` C# EXE 빌드 성공. 네이티브 Hook DLL 최종 복사는 Explorer 파일 잠금으로 기존 파일을 유지했다.

## 2026-08-30 — 새 일정·설정·도구 팝업 파스텔 조작부 통일

- **요청:** 새 일정·수정과 검색·시간표·일기·야구 등 팝업을 메인 달력·상세 패널의 파스텔 분위기에 맞추되 팝업임은 분명히 구분한다.
- **수정:** 공통 팝업 표면은 `#FFF9F8FF`, 외곽선은 중간톤 `#A9A1C7`로 통일했다. 진한 인디고 실행 버튼은 밝은 라벤더 `#E4E7FF`·인디고 글자 `#4338CA`·`#A5B4FC` 테두리로 변환하고, 모든 공통 버튼 템플릿이 실제 테두리를 렌더링하도록 했다. 새 일정·수정·설정 저장, 검색 실행, 시간표·일기·야구 버튼과 일기·야구 분할 선택 조작부가 같은 규칙을 사용한다.
- **변경 파일:** `App/OnharuPopupChrome.cs`, `App/AddItemWindow.cs`, `App/SettingsWindow.cs`, `App/SearchWindow.cs`, `App/DiaryWindows.cs`, `App/SportsCalendarWindow.cs`, `App/popup-policy-check.ps1`, `HANDOFF.md`.
- **검증:** `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS, 새 `2.2.6-test` C# EXE 빌드 성공. 네이티브 Hook DLL 최종 복사는 Explorer 파일 잠금으로 기존 파일을 유지했다.

## 2026-08-30 — 시간순·미완료 카드 색상 보호와 × 체크박스 정렬

- **문제:** 동적으로 생성되는 시간순·미완료 상세 카드가 과거의 위치 기반 임시 팔레트 저장값을 잘못 받아 네온색으로 표시될 수 있었다. 완료를 지원하지 않는 일정의 `×` 체크박스는 테두리가 배경과 섞이고 글리프가 미세하게 치우쳐 보였다.
- **수정:** 두 모드 카드에 안정적인 상세 카드 ID를 부여하고 카드 내부 자식에는 위치 기반 색상 오버라이드가 침투하지 않게 했다. `×` 상태에는 흰색 1px 테두리를 적용하고 14px 줄 높이·가운데 텍스트 정렬로 중앙을 맞췄다.
- **변경 파일:** `App/TemporarySegmentPaletteTool.cs`, `App/MainWindow.Detail.cs`, `App/MainWindow.Theme.cs`, `App/feature-pack-check.ps1`, `App/theme-check.ps1`, `HANDOFF.md`.
- **검증:** `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS, 새 `2.2.6-test` C# EXE 빌드 성공. 네이티브 Hook DLL 최종 복사는 Explorer 파일 잠금으로 기존 파일을 유지했다.

## 2026-08-30 — 블랙 달력 셀 한 단계 어두운 차콜

- **요청:** 블랙 테마에서 넓게 보이는 달력 셀의 중간 회색을 한 단계 어둡게 한다.
- **수정:** 블랙 달력 셀 전용 배경을 `#52525B→#45454D`로 조정했다. 공용 카드 테두리 역할과 오른쪽 패널·일정 카드·파스텔 테마는 유지한다.
- **변경 파일:** `App/OnharuStateColors.cs`, `App/MainWindow.Calendar.cs`, `App/theme-check.ps1`, `HANDOFF.md`.
- **검증:** `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS, 새 `2.2.6-test` C# EXE 빌드 성공. 네이티브 Hook DLL 최종 복사는 Explorer 파일 잠금으로 기존 파일을 유지했다.

## 2026-08-30 — 옅은 라벤더 스크롤바·추가 버튼 기준선 정렬

- **요청:** 파스텔 스크롤바의 존재감을 더 낮추고 날짜 제목 옆 `+`와 기념일 카드 옆 `+`의 세로 기준선을 맞춘다.
- **수정:** 파스텔 손잡이를 옅은 라벤더 `#B7ACE8`로 되돌리되 폭은 `9px`로 유지했다. 날짜 추가 버튼 오른쪽 여백을 카드 내부 버튼의 실제 우측 여백과 같은 `13px`로 맞췄다.
- **변경 파일:** `App/OnharuStateColors.cs`, `App/MainWindow.Layout.cs`, `App/theme-check.ps1`, `Docs/Design/COLOR_PALETTE_STANDARD.md`, `HANDOFF.md`.
- **검증:** `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS, 새 `2.2.6-test` C# EXE 빌드 성공. 네이티브 Hook DLL 최종 복사는 Explorer 파일 잠금으로 기존 파일을 유지했다.

## 2026-08-30 — 상단 선택 버튼·스크롤바 무게 완화

- **요청:** 파스텔 화면에서 회색 스크롤바와 검정에 가까운 상단 선택 버튼이 무겁게 보이는 문제를 완화한다.
- **수정:** 파스텔 상세 스크롤 손잡이를 저채도 블루라벤더 `#9AA6C4`로 바꾸고 폭을 `11→9px`로 줄였다. 상단 범위·테마·이동/고정 선택 버튼은 차콜 대신 블루슬레이트 `#475569`, 테두리는 `#64748B`를 사용한다. 블랙 테마 값과 스크롤 트랙은 유지한다.
- **변경 파일:** `App/OnharuStateColors.cs`, `App/UiRound.cs`, `App/theme-check.ps1`, `Docs/Design/COLOR_PALETTE_STANDARD.md`, `HANDOFF.md`.
- **검증:** `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS, 새 `2.2.6-test` C# EXE 빌드 성공. 네이티브 Hook DLL 최종 복사는 Explorer 파일 잠금으로 기존 파일을 유지했다.

## 2026-08-30 — 상세 스크롤바 저장색 우회 제거와 2.2.6 전체 검증

- **원인:** 상세 스크롤바의 기본 라벤더색이 임시 색상 편집 도구의 저장 오버라이드로 남아 있어, 새 중립 슬레이트 상태색보다 우선 적용되고 있었다.
- **수정:** 상세 스크롤바를 임시 팔레트 편집 대상에서 제외하고 설정 버전 `48` 마이그레이션에서 기존 `automation:OnharuDetailScroll` 저장값을 제거한다. 따라서 파스텔은 `#8794A8` 손잡이와 `#F1F5F9` 트랙, 블랙은 기존 중립 회색 손잡이와 투명 트랙을 일관되게 사용한다.
- **검사 정리:** 2.2.6에서 이미 확정된 선명한 선택색·오른쪽 체크박스·카드 라우팅·설정 창 생성자와 일치하도록 오래된 정적 검사 기대값을 갱신했다. 앱 색이나 일정 레인 수는 검사 때문에 변경하지 않았다.
- **변경 파일:** `App/PlannerSettings.cs`, `App/LocalStorage.cs`, `App/MainWindow.Layout.cs`, `App/MainWindow.Detail.cs`, `App/feature-pack-check.ps1`, `App/theme-check.ps1`, `App/ui-construction-check.ps1`, `CURRENT_STATUS.md`, `HANDOFF.md`.
- **검증:** `check-v22.ps1` 자동 품질 게이트 17개 전체 PASS. 새 `2.2.6-test` C# EXE는 빌드됐으며 네이티브 Hook DLL 최종 복사만 Explorer 파일 잠금 때문에 기존 파일을 유지했다.

## 2026-08-30 — 상세 스크롤바 중립 슬레이트

- **요청:** 파스텔 상세 스크롤바가 카드색처럼 보이지 않고 스크롤 가능한 UI 요소로 분명히 보이게 한다.
- **수정:** 파스텔 상세 손잡이를 라벤더 `#B7ACE8`에서 중립 블루그레이 `#8794A8`로 바꾸고 트랙에 옅은 슬레이트 `#F1F5F9`를 적용했다. 세로 스크롤바 폭은 `10→11px`로 확대했다. 블랙 손잡이 `#8C8C96`과 투명 트랙은 유지한다.
- **변경 파일:** `App/OnharuStateColors.cs`, `App/UiRound.cs`, `App/MainWindow.Theme.cs`, `App/TemporarySegmentPaletteTool.cs`, `App/theme-check.ps1`, `Docs/Design/COLOR_PALETTE_STANDARD.md`, `HANDOFF.md`.
- **검증:** 새 `2.2.6-test` C# EXE 빌드와 파스텔·블랙 손잡이/트랙 역할값 및 동적 리소스 템플릿 검사를 통과했다. 네이티브 Hook DLL 최종 복사는 Explorer 잠금으로 기존 파일을 유지했다. 실제 가시성은 사용자 화면 확인이 필요하다.

## 2026-08-30 — 오른쪽 상세 카드 간격 리듬 정리

- **요청:** 날짜·일정 카드·Special Day 사이의 구분과 간격을 자연스럽게 만들고, 카드 내부와 카드 사이의 간격 계층을 명확히 한다.
- **수정:** 날짜 제목 아래 선을 제거하고 첫 카드까지 실제 12px 간격을 만들었다. 일반 카드 헤더→첫 항목은 `9→7px`, 카드 경계 사이는 `8→11px`로 조정했다. 일반 일정과 Special Day 사이에는 옅은 선을 유지하되 카드 자체 마진을 포함한 실제 간격을 약 18px로 축소했다. D-Day·기념일의 마지막 항목 아래 불필요한 선은 제거했다.
- **보존:** 카드 내부 패딩, 좌우 시작선, 단일 항목 카드 높이와 카드 색상은 유지했다.
- **변경 파일:** `App/MainWindow.Layout.cs`, `App/MainWindow.Detail.cs`, `App/MainWindow.Anniversary.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** 새 `2.2.6-test` C# EXE 빌드와 날짜·헤더·카드·그룹 간격 및 마지막 행 구분선 분기 검사를 통과했다. 네이티브 Hook DLL 최종 복사는 Explorer 잠금으로 기존 파일을 유지했다. 실제 리듬은 사용자 화면 확인이 필요하다.

## 2026-08-30 — 접힌 상단 필터 여백 축소

- **요청:** 오른쪽의 `온하루 등록·Special Day Card`와 `Google` 목록을 접었을 때 선택 버튼 위가 지나치게 비어 보이지 않게 한다.
- **수정:** 펼침 상태의 기존 간격은 유지한다. 로컬·Special Day 목록을 접으면 제목 아래 여백을 제거하고 묶음 아래를 `14→6px`, Google 목록을 접으면 선택 스위치 위를 `18→10px`로 줄인다.
- **보존:** 가로 배치, 펼친 목록 간격, 선택 스위치와 날짜·일정 카드 간격은 변경하지 않았다.
- **변경 파일:** `App/MainWindow.Layout.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.
- **검증:** 새 `2.2.6-test` C# EXE 빌드와 접힘·펼침 간격 분기 소스 검사를 통과했다. 네이티브 Hook DLL 최종 복사는 Explorer 잠금으로 기존 파일을 유지했다. 실제 간격은 사용자 화면 확인이 필요하다.

## 2026-08-30 — 오른쪽 조작부 선명한 고정 강조색

- **요청:** 왼쪽 일정은 선택 프리셋을 따르되 오른쪽 체크박스와 `오늘·카테고리` 계열 선택 버튼은 항상 `맑고 선명한 조합`을 사용한다.
- **수정:** 오른쪽 필터·상세 일정 체크박스는 현재 역할색과 같은 열의 선명한 색으로 표시한다. 기간 선택은 오렌지, 정렬 선택은 블루 고정 강조색으로 통일했다. 미선택 버튼과 오른쪽 일정 카드 배경은 기존 중립색·선택 프리셋을 유지한다.
- **보존:** 왼쪽 달력 일정·Todo 체크박스, 사용자 저장색, Google 원본색과 카드 배경은 변경하지 않았다.
- **변경 파일:** `App/OnharuColorPresets.cs`, `App/OnharuStateColors.cs`, `App/MainWindow.Theme.cs`, `App/MainWindow.Display.cs`, `App/MainWindow.Detail.cs`, `App/theme-check.ps1`, `Docs/Design/COLOR_PALETTE_STANDARD.md`, `HANDOFF.md`.
- **검증:** 새 `2.2.6-test` C# EXE 빌드 성공. 세 프리셋 36색의 선명한 열 매핑, 파스텔·블랙 선택 버튼 대비 4.5:1 이상, 왼쪽 일반 체크/오른쪽 선명 체크 분리 검사를 통과했다. 네이티브 Hook DLL 최종 복사는 Explorer 잠금으로 기존 파일을 유지했다. 최종 색감은 사용자 실기기 확인이 필요하다.

## 2026-08-30 — 2.2.6-test 대중형 역할 색상 조합

- **요청:** 전체 구성은 유지하고 다수 사용자가 무난하게 받아들일 수 있도록 색상 조합 완성도를 높인다. 이상하면 이전 상태로 원복할 수 있어야 한다.
- **수정:** 대비가 검증된 기존 36개 배경·글씨 쌍은 바꾸지 않고 배열 순서만 재조정했다. 세 프리셋 모두 로컬 일정은 `업무 블루·개인 로즈·야구 그린·D-Day 골드·기념일 바이올렛·휴일 레드`, Google 일정은 남은 청록·인디고·오렌지·시안·라임·마젠타 계열로 분산된다.
- **호환:** 팔레트 정의 버전을 4로 올렸다. 기존 추천 프리셋 사용자의 로컬 색이 과거 기본값과 정확히 일치할 때만 새 역할색으로 옮기며, 사용자가 직접 바꾼 색·내설정·Google 원본색은 보존한다.
- **복구:** 변경 전 파일은 `Tests/LocalTest/ONHARU-2.2.6-before-mainstream-palette.zip`에 보관했다.
- **변경 파일:** `App/OnharuColorPresets.cs`, `App/PlannerSettings.cs`, `App/LocalStorage.cs`, `App/theme-check.ps1`, `Docs/Design/COLOR_PALETTE_STANDARD.md`, `CURRENT_STATUS.md`, `HANDOFF.md`.
- **검증:** 새 `2.2.6-test` C# EXE 빌드 성공. 36개 색상은 프리셋별 중복 없음, 역할 순서, 파스텔·블랙 WCAG 4.5:1 이상 대비를 모두 통과했다. 전체 품질 게이트의 기존 토큰 불일치 2건과 Explorer에 로드된 Hook DLL 잠금은 앞선 기록과 동일하다. 새 시험본을 실행했으며 최종 색감은 사용자 실기기 확인이 필요하다.

## 2026-08-30 — 2.2.6-test 색상·가독성 개선

- **요청:** Claude의 읽기 전용 검토 명세에서 확정한 변경을 적용하고 개발 버전을 `2.2.6-test`로 올린다.
- **수정:** 추천 프리셋의 국경일 색이 업무일정과 같아지던 인덱스를 여섯 번째 휴일 색으로 바로잡았다. 완료 일정 흐림은 `0.48`에서 `0.66`으로 높였고, 달력의 `+ N개 더보기`는 기존 크기·위치·동작을 유지한 채 파스텔·블랙 전용 배경과 테두리를 적용했다.
- **보존:** 사용자 저장 색상과 Google 원본색은 마이그레이션하지 않았고, 12색 상위군 재배치와 공개 배포는 수행하지 않았다.
- **버전:** Assembly·화면·메일 백업 클라이언트·검사 기준을 `2.2.6-test`로 통일했다. 기존 웹 2.2.5 시험판과 공개 정식판 2.2.4는 유지한다.
- **변경 파일:** `App/OnharuColorPresets.cs`, `App/OnharuStateColors.cs`, `App/MainWindow.Calendar.cs`, `App/MainWindow.Detail.cs`, `App/AssemblyInfo.cs`, `App/MainWindow.Layout.cs`, `App/ProductInfoWindow.cs`, `App/EmailBackupService.cs`, `App/version-check.ps1`, `App/theme-check.ps1`, `README.md`, `CURRENT_STATUS.md`, `BUILD_AND_RELEASE.md`, `WEB_OPERATIONS_HANDOFF.md`, `HANDOFF.md`.
- **검증:** C# `2.2.6-test` EXE 빌드·버전 메타데이터와 변경 항목 소스 검사는 통과했다. 전체 `check-v22.ps1`은 기존 상세 카드 테두리 토큰 `timeMode ? Brush("#CBD5E1")` 불일치에서, `theme-check.ps1`은 기존 아이콘 토큰 `StrokeThickness = 1.8` 불일치에서 중단됐다. 네이티브 재빌드의 최종 복사는 Explorer에 로드된 기존 Hook DLL 잠금으로 생략했다. 파스텔·블랙의 완료 일정과 더보기 버튼은 실기기 확인이 필요하다.

## 2026-08-30 — Claude 2.2.6 디자인 검토 패키지

- **목적:** Claude가 로컬 프로젝트 접근 없이 현재 색상·디자인 구현을 읽기 전용으로 검토하고, `2.2.6-test`용 파일별 코드 diff를 제안할 수 있도록 전달 자료를 묶었다.
- **구성:** 실행 지시서와 관련 소스·디자인 표준·검증 스크립트 등 19개 파일을 `ONHARU-2.2.6-Claude-review-pack.zip`으로 생성했다.
- **제외:** OAuth 비밀값, 사용자 일정 데이터, 실행 파일과 빌드 산출물은 포함하지 않았다. 앱 소스 및 런타임 동작 변경은 없다.
- **변경 파일:** `Claude-2.2.6-brief.md`, `ONHARU-2.2.6-Claude-review-pack.zip`, `HANDOFF.md`.

## 2026-08-30 — 과거 KBO Google 일정 이중 표시 복구

- **원인:** 현재 계정 데이터에 같은 날짜·대진의 Google KBO 일정이 두 개씩 있고, 과거 형식으로 생성된 한쪽에는 `SportsGameId`가 비어 있었다. 기존 중복 제거는 ID가 있는 항목끼리만 비교해 과거 항목을 합치지 못했다.
- **수정:** `KBO 경기 일정` 메모가 있는 과거 항목은 `날짜+대진`으로 안정 ID를 복구한 뒤 기존 항목과 같은 그룹으로 합친다. Google 이벤트 ID가 있는 온하루 등록 항목을 우선 보존하며 시작·Google 동기화·KBO 창 진입 시 같은 경기는 한 건만 표시·저장한다. Google Calendar 원본 이벤트는 자동 삭제하지 않는다.
- **변경 파일:** `App/SportsApi.cs`, `App/SportsApiWindows.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.

## 2026-08-30 — 오늘·이번 주 기간 선택 파스텔 강조

- **요청:** 세부 달력의 `오늘·이번 주·다음 주` 선택 상태를 파스텔 톤의 강조색으로 표시한다.
- **수정:** 사용자 캡처 기준으로 위쪽 기간 선택 스위치의 활성 항목은 파스텔 스킨에서 `#FFF1E8` 배경·`#C2410C` 글씨·`#C6BBB4` 테두리를 사용한다. 블랙 스킨에서는 같은 계열의 진한 강조색인 `#C2410C` 배경·흰 글씨·`#FB923C` 테두리를 사용한다. 아래쪽 카테고리·시간순·미완료 일정 선택색은 유지한다.
- **변경 파일:** `App/OnharuStateColors.cs`, `App/MainWindow.Theme.cs`, `App/theme-check.ps1`, `HANDOFF.md`.

## 2026-08-30 — 미완료 일정 조회 기간 글씨 잘림 수정

- **원인:** 설정의 조회 기간 콤보박스 폭이 105px로 고정돼 `최근 12개월 이후`가 고해상도 배율에서 잘렸다.
- **수정:** 해당 콤보박스 폭만 128px로 넓혀 1·3·6·12개월 옵션을 모두 완전히 표시한다. 행 높이와 주변 배치는 유지한다.
- **변경 파일:** `App/SettingsWindow.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.

## 2026-08-30 — 설정의 파스텔 스킨 미리보기 톤 변경

- **요청:** 설정의 파스텔 배경이 품질관리팀 색처럼 보이므로 `밝고 산뜻한 조합` 톤으로 변경한다.
- **수정:** 사용자 캡처의 실제 KBO 카드를 기준으로 디자인 스킨의 파스텔 미리보기를 `#F6F0FF` 배경·`#70429B` 글씨·`#B49CCB` 테두리로 변경했다. 실제 사용자 카테고리색은 건드리지 않는다.
- **변경 파일:** `App/SettingsWindow.cs`, `App/theme-check.ps1`, `HANDOFF.md`.

## 2026-08-30 — 추천 프리셋 표시 순서 변경

- **요청:** 추천색을 `차분한 중간톤 → 밝고 산뜻한 조합 → 맑고 선명한 조합` 순서로 표시한다.
- **수정:** 프리셋 이름·배경·글씨 배열을 같은 순서로 재배치했다. 팔레트 정의 버전을 3으로 올리고 기존 사용자가 수정한 프리셋 값과 현재 선택값도 새 위치로 함께 이동해 이름과 색이 뒤바뀌지 않게 했다. 내설정의 기존 Soft Workspace 기본색은 유지한다.
- **변경 파일:** `App/OnharuColorPresets.cs`, `App/PlannerSettings.cs`, `App/LocalStorage.cs`, `App/theme-check.ps1`, `HANDOFF.md`.

## 2026-08-30 — 블랙 스킨 세부 일정 글씨 대비

- **원인:** 시간순 카드와 미완료 일정 카드는 배경이 흰색으로 고정돼 있었지만 일정 글씨는 블랙 스킨 규칙에 따라 흰색이 되어 내용을 볼 수 없었다.
- **수정:** 블랙 스킨의 시간순·미완료 카드 배경과 테두리를 테마의 `Card`·`Grid` 색으로 사용하고 제목·일정 글씨는 흰색을 유지한다. 파스텔 스킨의 기존 흰 카드 표현은 보존한다.
- **변경 파일:** `App/MainWindow.Detail.cs`, `App/theme-check.ps1`, `HANDOFF.md`.

## 2026-08-30 — 미완료 일정 기간 제목 `▷` 표시

- **원인:** 미완료 일정의 선택 날짜 화면은 실제로 과거 기준일부터 선택일까지의 기간을 보여주지만, 날짜 아이콘 로직은 단일 선택 날짜로 판단해 기간 아이콘을 숨겼다.
- **수정:** 미완료 일정 모드는 선택 날짜·이번 주·다음 주 모두 기간 제목으로 취급해 다른 기간 화면과 같은 `▷` 아이콘을 표시한다.
- **변경 파일:** `App/MainWindow.Detail.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.

## 2026-08-30 — 설정 재진입 시 선택 프리셋 유지

- **원인:** 설정창이 저장된 `SelectedPaletteIndex`를 생성자 인자로 받고도 선택값을 항상 `내설정` 인덱스로 초기화했다. 프리셋 색 자체는 적용·저장됐지만 라디오 선택만 잘못 표시됐다.
- **수정:** 저장된 인덱스가 현재 3개 추천 프리셋 또는 내설정 범위이면 그대로 복원하고, 잘못된 과거 값만 내설정으로 보정한다. `차분한 중간톤`을 선택해 저장하면 다음 설정 진입에서도 같은 항목이 선택된다.
- **변경 파일:** `App/SettingsWindow.cs`, `App/theme-check.ps1`, `HANDOFF.md`.

## 2026-08-30 — 설정의 D-Day 색상 즉시 반영

- **원인:** 설정에서 새 D-Day 색을 정상 저장해도 오른쪽 D-Day 카드에 과거 개별 색상 오버라이드가 남아 있으면 그 값이 우선 적용됐다.
- **수정:** 설정 저장 시 D-Day 색이 실제로 바뀐 경우 파스텔·블랙의 D-Day 카드 개별 오버라이드만 제거한다. 새 카테고리 색이 달력·필터·오른쪽 카드에 즉시 같은 값으로 반영된다.
- **변경 파일:** `App/MainWindow.Settings.cs`, `App/feature-pack-check.ps1`, `HANDOFF.md`.

## 2026-08-30 — Google 계정 영역·로그아웃 버튼 그라데이션

- **요청:** 메인 Google 계정 영역과 로그아웃 버튼을 온하루 스타일의 그라데이션으로 정리한다.
- **변경:** 기존 크기·여백·동작은 유지하고 Google 계정 카드에 `온하루 · ONHARU` 글씨 로고와 정확히 같은 `#0EA5E9 → #7C3AED` 그라데이션을 공통 브러시로 적용했다. 카드 글씨는 흰색으로 맞췄고 Login/Logout·계정 변경은 보라 단색, 로그아웃 경고 버튼은 옅은 로즈 또는 진한 로즈 단색으로 정리했다. 로그아웃 버튼에는 그라데이션을 사용하지 않는다. 사용자 화면에서 카드가 거의 보라 단색으로 보이는 것을 확인해 임시 팔레트 오버라이드 대상에서 `google_sync` 카드를 제외하고 로고 브러시가 항상 그대로 렌더링되게 고정했다.
- **변경 파일:** `App/OnharuStateColors.cs`, `App/MainWindow.Layout.cs`, `App/MainWindow.Theme.cs`, `App/MainWindow.Google.cs`, `App/GoogleAccountActionWindow.cs`, `README.md`, `HANDOFF.md`.
- **검증:** 별도 `ONHARU-2.2-gradient-preview.exe` 컴파일에 성공했다. 전체 `check-v22.ps1`은 기존 상세 카드 팔레트 검사 토큰 `AttachDetailCard(liftSurface, groupHeader, groupKey)` 누락에서 중단됐고, `theme-check.ps1`도 기존 아이콘 토큰 `StrokeThickness = 1.8` 불일치에서 중단됐다. 이번 변경과 무관한 선행 작업 상태이므로 되돌리지 않았다.

## 2026-08-29 — 3개 톤 프리셋·36개 확정 배경/글씨 쌍

- 추천 프리셋을 `맑고 선명한 조합·차분한 중간톤·밝고 산뜻한 조합` 3개로 정리했다.
- 각 프리셋은 빨강부터 로즈까지 색상환에 고르게 배치한 서로 다른 12색을 제공한다.
- 파스텔 배경과 동계열 글씨를 한 쌍으로 저장한다. 36개 조합은 작성 단계에서 대비 4.5:1 이상을 통과했으며 실행 중 자동 보정으로 HEX를 바꾸지 않는다.
- 설정 미리보기·메인 달력·오른쪽 상세 카드가 동일한 배경과 글씨를 사용한다. 체크박스만 같은 계열에서 한 단계 선명하게 표시한다.
- `시작할 때 색상 무작위 배치`는 선택 사항이며, 끄면 저장 배열을 유지한다. 전체 17개 품질 게이트를 통과했다.
- 육안 검토 후 `밝고 산뜻한 조합`을 더 밝고 가벼운 배경과 부드러운 글씨로 재조정해 `맑고 선명한 조합`과 구분했다. 블랙 추천색에는 `Vivid` 재증폭을 적용하지 않아 네온처럼 보이는 현상을 줄였다.

## 2026-08-29 — 추천 팔레트 12색·선택형 시작 무작위 배치

- 추천 프리셋을 `중요일정 색조합·맑고 선명한 조합·차분한 중간톤 조합·밝고 산뜻한 조합·깊고 안정적인 조합`으로 확정했다.
- 각 조합은 분홍·오렌지·골드·그린·블루·라벤더와 시안·틸·인디고·코랄·라임·브라운을 포함한 서로 다른 12색이다.
- 특정 대표색 개념을 제거하고 설정의 프리셋 관련 UI 강조는 고정 중립 인터페이스 색을 사용한다.
- `시작할 때 색상 무작위 배치` 옵션을 추가했다. 기본은 꺼짐이며, 켜면 시작할 때 선택 조합 안에서 카테고리 배치만 섞고 끄면 마지막 저장 상태를 유지한다.
- `중요일정 색조합` 첫 6색은 팔레트랩의 확정 파스텔 배경과 글씨 쌍을 보정 없이 그대로 렌더링한다. 사용자가 고른 색과 실제 화면이 달라지지 않도록 이 6쌍은 공통 4.5:1 자동 보정의 예외로 명시했다.

## 2026-08-29 — 파스텔 추천 조합을 톤 중심 고정 팔레트로 전환

- 추천 조합을 단일 대표색 이름이 아닌 `맑은균형·생기포인트·산뜻자연·차분감성·따뜻노을`의 분위기 중심 5종으로 변경했다.
- 각 조합은 블루·핑크·그린·바이올렛·오렌지·옐로 계열을 섞어 업무·개인·야구·D-Day·기념일과 여러 Google 캘린더가 서로 구분되도록 한다.
- 실행 및 Google 로그인 뒤 추천색을 난수로 다시 배치하던 로직과 `현재 색상 배치 고정` 옵션을 제거했다. 추천 조합은 항상 사용자가 확인한 고정 순서로 적용된다.
- 파스텔 메인 달력과 오른쪽 상세 카드는 동일한 밝은 배경과 동계열 진한 글씨를 사용한다. 오른쪽 체크박스만 같은 계열에서 한 단계 선명하게 표시한다.
- 옅은 노랑 배경+파란 글씨처럼 다른 계열을 섞는 조합은 자동 카테고리색이 아니라 사용자가 명시적으로 고르는 중요 일정 강조색으로 한정한다.
- 실행 중인 기본 시험본을 종료하지 않고 별도 출력으로 컴파일했으며, 전체 17개 품질 게이트를 통과했다.

## 2026-08-28 — 기능별 팔레트 랩 프로젝트 편입

- 기존 시각화의 기능별 팔레트 랩을 `Docs/Design/PaletteLab`의 독립 오프라인 HTML로 정리했다.
- 현재 5개 프리셋 계열(블루·핑크·그린·바이올렛·오렌지), HSL 설명과 근접 배열, 실제 코드의 전체 적용 위치, 단일/복수 필요 색상 수를 포함한다.
- 프리셋 12색, 중요 일정 7쌍, 선택일·오늘 각 7색, 날짜 빠른색 6색, 로고 9색, 시간표 7색 등 기존 랩에서 빠진 임시 팔레트까지 기록했다.
- 이 도구는 결정안만 브라우저 로컬 저장소에 보관하며 아직 앱 색상 코드를 변경하지 않는다.
- 각 후보 카드에 기준색과 함께 흰 배경의 파스텔 실제 표시, 검은 배경의 블랙 실제 표시를 나란히 추가했다. 파스텔 배경·같은 계열 진한 글씨, 블랙 대비 보정·흰 글씨는 `CategoryColorSystem` 공식을 JavaScript로 동일하게 옮겼다.

## 2026-08-28 — 고정 상태 Ctrl+Z 입력 귀속

- 고정 상태의 Explorer 합성 화면을 마지막으로 클릭한 경우에만 `Ctrl+Z`를 ONHARU 실행 취소로 전달한다.
- 바탕화면 아이콘·ONHARU 바깥 영역이나 다른 프로그램을 클릭하면 ONHARU가 키를 가로채지 않는다. 키 자동 반복은 소비하되 최초 입력 한 번만 전달한다.
- 전역 키보드 훅이나 시스템 전역 단축키 등록은 사용하지 않았다.
- 초기 구현에서 동일한 좌표 판정 구문 때문에 입력 소유권 갱신이 마우스 휠 분기에 잘못 들어간 문제를 수정했다. 실제 왼쪽 클릭 분기에서 소유권을 기록하고 Explorer 달력 창에 포커스를 부여한다.
- Explorer가 `Ctrl+Z`를 바탕화면 가속키로 먼저 소비해 경고음만 내는 경로를 막기 위해, 전역 훅이 아닌 Explorer 바탕화면 UI 스레드 전용 `WH_KEYBOARD` 훅에서 ONHARU 소유 상태일 때만 키를 전달한다. 포커스를 잃으면 소유 상태를 즉시 해제한다.

## 2026-08-28 — Google Tasks 권한 설명 문구 명확화

- 설정의 Google Tasks 설명을 `읽기 및 완료`에서 `읽기 전용, 완료 체크만 가능`으로 변경해 실제 지원 범위를 명확히 표시한다.
- 권한과 동기화 동작은 변경하지 않았으며, 해당 문구를 확인하는 기능 회귀검사도 함께 갱신했다.

## 2026-08-28 — Ctrl 복사 커서와 일정 실행 취소

- **요청:** 메인 달력에서 `Ctrl` 드래그 복사를 알아볼 수 있게 하고, 이동·삭제를 `Ctrl+Z`로 되돌린다.
- **변경:** `UiCursor`에 기존 이동 카드 커서를 재사용한 흰 배지·초록 `+` 복사 커서를 추가했다. WPF 드래그 루프가 키 변경 뒤 커서를 다시 덮는 환경을 고려해 Windows `GetAsyncKeyState`로 실제 Ctrl 상태를 읽고, 드래그 동안 25ms 주기로 이동·복사 커서를 유지한다. 가장 최근 한 건의 등록·수정·이동·복사·삭제를 `Ctrl+Z`로 되돌리며 등록·수정은 확인 팝업을 거친다. 반복 일정 생성·수정·삭제는 이전 기록을 대체하는 미지원 항목으로 기록해 `Ctrl+Z` 시 안내 팝업을 표시한다.
- **변경 파일:** `UiCursor.cs`, `MainWindow.ItemDragDrop.cs`, `MainWindow.Items.cs`, `MainWindow.Settings.cs`, `MainWindow.Startup.cs`, `MainWindow.Undo.cs`, `MainWindow.cs`, `PlannerItem.cs`, `feature-pack-check.ps1`, `README.md`.
- **검증:** 별도 잠금 없는 시험 실행 파일을 빌드했고 전체 17개 품질 게이트를 통과했다. 실제 마우스 커서 전환과 Google 왕복은 사용자 화면 시험이 필요하다.
- **문구 보정:** `일정 삭제을`처럼 고정 조사 연결로 어색해지지 않도록 모든 결과 문구를 `일정 삭제 작업을 되돌렸습니다` 형식으로 통일하고 회귀검사를 추가했다.

## 2026-08-28 01:08:00 +09:00 — 카테고리 설정 반영 시 메인 창 유지

- **확인한 변경:** 동시 외부 작업에서 `MainWindow.Layout.cs`, `MainWindow.Settings.cs` 변경이 들어온 것을 감지해 보존했다.
- **동작:** 온하루 등록 카테고리의 사용 여부가 바뀌면 메인 창 전체를 재생성하지 않고 기존 필터의 표시·선택 상태만 동기화한다. 비활성 카테고리 필터 객체는 유지하되 화면에서 접는다.
- **문서 동기화:** 사용자 동작을 `README.md`에 반영하고 이 기록을 추가했다.
- **검증:** `check-v22.ps1` 17개 통합 품질 게이트를 다시 실행한다.

## 2026-08-28 01:06:04 +09:00 — 숨긴 카테고리 색상 보존 변경 동기화

- **확인한 변경:** Git 기준점 커밋 직후 외부 작업에서 `SettingsWindow.cs`, `feature-pack-check.ps1` 변경이 들어온 것을 감지해 사용자 변경으로 보존했다.
- **동작:** 설정 화면에서 표시를 꺼 색상 슬라이더가 생성되지 않은 업무·개인·야구·D-Day·기념일·국경일 카테고리도 설정 저장 시 기존 색상을 유지한다.
- **문서 동기화:** 사용자 동작을 `README.md`에 반영하고 이 기록을 추가했다.
- **검증:** 숨긴 카테고리 색상 보존 회귀검사가 17개 통합 품질 게이트에 포함되어 PASS했다.

## 2026-08-28 01:01:35 +09:00 — 루트 Git·현재 상태·문서 갱신 규칙 정리

- **요청받은 작업:** 현재 ONHARU 2.2.5 작업 폴더의 Git과 누적 메모를 정리하고, 시작 문서에 기능 변경 시 관련 문서를 항상 함께 갱신하도록 명시한다.
- **변경한 파일:** `.gitignore`, `.gitattributes`, `START_HERE.md`, `CURRENT_STATUS.md`, `AGENTS.md`, `TODO_NEXT.md`, `README.md`, `PRD.md`, `PROJECT_INDEX.md`, `BUILD_AND_RELEASE.md`, `HANDOFF.md`, 루트 `.git`.
- **주요 변경 내용:** 실제 최신 작업 루트를 독립 로컬 Git `main` 저장소로 초기화하고 OAuth 로컬 자격 증명·사용자 데이터·바이너리·Release·Publish를 추적에서 제외했다. DPI 검증 묶음 안의 중복 `Source`·`Runtime`, 로컬 테스트 설정 복사본, Python 캐시도 추적하지 않아 기준 소스를 `App`·`ExplorerLayer`·`Installer`로 단일화했다. 파일은 삭제하지 않았다. `START_HERE.md`를 모든 새 채팅과 Hermes/Codex의 첫 문서로 만들고, 변경 종류별 필수 문서 갱신 규칙과 판단 우선순위를 명시했다. `CURRENT_STATUS.md`에는 2.2.5-test와 공개 2.2.4를 구분하고 사용자가 확인한 고정 주차, 실제 인쇄, 125% DPI, 혼합 DPI, LayerHost 자동 종료 실기기 PASS를 현재 기준으로 기록했다. README·TODO·프로젝트 인덱스·빌드 문서의 현재 버전과 품질 게이트 수를 정리했다.
- **실행한 테스트와 결과:** `check-v22.ps1` 17개 품질 게이트 PASS. Git 추적 후보를 검사했으며 실제 Google/AWS 키와 개인키는 발견되지 않았다. `error-log-check.ps1` 두 사본의 `GOCSPX-*` 문자열은 개인정보 마스킹 검증용 가짜 시험값임을 확인했다.
- **해결하지 못한 문제:** 2.2.5는 아직 기능 시험판이며 공개 GitHub Latest는 2.2.4다. 과거 역사 문서와 버전별 릴리스 노트의 당시 버전 표기는 의도적으로 보존했다.
- **다음 작업 권장사항:** 정식 2.2.5 확정 시 `START_HERE.md`의 버전 동기화 목록에 따라 Assembly·화면·설치·README·CURRENT_STATUS·배포 문서를 한 번에 갱신하고, 사용자 승인 후에만 공개 GitHub 스테이지와 웹을 게시한다.

- 2026-08-27: 사용자의 실기기 설명에 따라 고정 주차 문제를 `팝업 닫힘`이 아니라 `팝업은 보이나 항목 선택이 전달되지 않음`으로 정정했다. 고정 레이어 클릭 시 주차 버튼의 실제 화면 좌표를 먼저 직접 판정해 `ApplyWeekCount()`를 호출한다. 고정 인쇄는 Windows 스풀러가 UI 스레드를 소유한 동안 Explorer/WPF 표면을 다시 교대하던 코드를 제거했다. 빌드와 기능 검사를 통과한 2.2.5 시험 설치판을 onharu.app에 재게시했으며 설치판 SHA-256은 `B399A170672D450BF4C3893BDA6DF03DD53ABE464B38A7AA3D5E63F57E4EEB72`, 포터블판은 `A2CA37E8EA4722DD4532337F45F68024F8A2DCB0F1F295A4DFB4B548457D95BA`이다. 공개 재다운로드 해시가 일치하며 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-2.2.5-fixed-week-print-20260827.tar.gz`이다.

- 2026-08-27: KBO 중복, 고정 주차 목록, 특수 카드 기준선, 야구 카테고리 결합을 수정했다. KBO 경기 ID는 경기 시간 대신 `날짜+대진`으로 고정하고 Google private extended property `onharuSportsGameId`로 왕복하며, 시작·동기화·등록 시 동일 경기 중복을 하나로 합친다. 고정 입력의 선행 `action 29`가 주차 목록을 먼저 닫지 않도록 처리해 1~6주 선택이 실제 반영된다. D-Day·기념일 제목 버튼은 접힘·펼침 모두 18px 기준선과 같은 위쪽 정렬을 사용한다. 프로야구 기능 아이콘과 로컬 야구 카테고리 표시를 분리하고 설정의 `온하루 등록 카테고리`에서 업무·개인·야구를 독립 관리한다. 빌드와 기능 검사를 통과했으며 최신 로컬 시험본을 실행했다.

- 2026-08-27: KBO 수정 허용 캘린더 필터와 온하루 선택 UI, 상세 카드 비작동 `×` 체크박스, 고정 상태 주차 선택, D-Day·기념일 간격 보정을 포함한 2.2.5 기능 시험용 설치판을 재게시했다. 설치판 SHA-256은 `34B71B7D0505EA09E00BB98FE1654BFB962B249FD0F729A0E101F00C25C604F6`, 포터블판은 `7E683D40EE27DAEC50C545BD0354C80B37426C04031BAE3B45DFABD3B5800DCE`이며 공개 웹 재다운로드 해시가 일치한다. 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-2.2.5-ui-fixes-20260827.tar.gz`이다.

- 2026-08-27: 고정 상태의 1~6주 임시 목록이 보이지만 선택값이 바뀌지 않던 문제를 수정했다. 각 주차 버튼에 `week_count:N` 값을 부여하고 네이티브 고정 입력 경로에서 `ApplyWeekCount()`를 직접 호출하므로 WPF Click 재전달에 의존하지 않는다. 오른쪽 D-Day·기념일 카드는 일반 상세 카드와 같은 위 3px·아래 5px 외부 간격을 사용하고, 접힌 제목은 영문 소문자 하단이 잘리지 않도록 제목줄 18px과 내부 상하 여백을 재배분했다. 공통 제목 크기도 일반 카드와 같은 12로 맞췄다. 기능 검사와 빌드를 통과했으며 최신 로컬 시험본을 실행했다.

- 2026-08-27: 오른쪽 상세 일정에서 완료 기능이 없는 Google 하루 종일 일정과 야구 일정에 동일한 비작동 체크박스를 표시하고, 체크박스 내부 `×`로 완료 체크 불가 상태를 구분한다. 야구는 Todo 속성이 있더라도 완료 토글 대신 비작동 표시를 우선한다. 기능 검사와 빌드를 통과했으며 최신 로컬 시험본을 재실행했다.

- 2026-08-27: KBO 경기 등록 대상이 Google의 원래 쓰기 권한만 확인해 설정에서 `수정 가능`을 체크하지 않은 캘린더까지 노출하던 오류를 수정했다. 이제 Google `owner/writer`이면서 ONHARU 설정의 `Editable`이 선택된 캘린더만 표시한다. 기본 대상 문구는 `ONHARU 로컬 일정`으로 변경하고 공통 온하루 ComboBox 스타일을 적용했다. 기능 검사와 빌드를 통과했으며 최신 로컬 시험본을 실행했다.

- 2026-08-27: ONHARU 본창을 닫아도 `Onharu.LayerHost.exe`가 남아 설치 자동 종료와 파일 교체가 실패하던 문제를 수정했다. `LayerHostController.Stop()`은 현재 프로세스가 직접 시작한 호스트인지와 무관하게 실제 실행 중인 LayerHost 전체에 종료를 요청하고, 4초 대기 후 남은 프로세스만 강제 종료한다. 기능 시험용 설치기는 설치 전 기존 호스트에 `--stop`을 보내고 `taskkill`을 안전망으로 사용하며, 자동 업데이트 설치 실행은 앱 종료·레이어 정리 시간을 위해 2초 지연한다. 2.2.5 시험 설치판 SHA-256은 `36FA3DE1C7BE5CBD1A1CC5525790342CAF1BF728719CFC93A42E7B1BAD4EF5BE`, 포터블판은 `4FBD2E514E1681ACC8C334C270588499E647A76C29F41BBC4F991FA147163E7E`이며 서버 재다운로드 해시가 일치한다. 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-2.2.5-layer-stop-20260827-011607.tar.gz`이다. 실제 설치 중 자동 종료 동작은 사용자 화면에서 최종 확인한다.

- 2026-08-27: 업데이트 위치 보존을 실제 설치 흐름으로 시험할 수 있도록 운영 설치 경로와 AppId를 그대로 사용하는 `ONHARU-2.2.5-Test-Setup.exe`를 추가했다. 공식 Latest/자동 업데이트에는 올리지 않고 onharu.app의 기능 시험용 구역에만 게시했다. 설치판 SHA-256은 `741560BBA460D14877CC08627F1B61AF59BE301B6671EFDA999151F92F156F2F`이며 서버 원본과 공개 재다운로드 해시가 일치한다. 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-2.2.5-test-installer-20260827-010626.tar.gz`이다.

- 2026-08-27: 2.2.5 테스트판을 갱신했다. 고정 상태에서 실제 인쇄 직전에 Explorer 합성 레이어를 일시 중단하고 인쇄 완료·실패 후 복귀시켜 프린터 전송 시 멈춤을 방지한다. 자동 업데이트는 설치 UI를 열기 전에 현재 물리 좌표를 저장하고 업데이트 종료 시 임시 표시 좌표가 이를 덮어쓰지 않도록 했다. KBO 저장 대상은 이름과 무관하게 Google 소유자/수정 권한 캘린더 전체다. 갱신 ZIP SHA-256은 `5DD5598D4FDFFE087F5A1F321A0B98D072A9EB4F5E3D68C8AF91B42EE7CDEC78`, 서버 백업은 `/home/ubuntu/onharu-web-backups/site-before-2.2.5-test-refresh-20260827-005710.tar.gz`이다. 웹 캐시 회피 링크와 재다운로드 해시를 확인했다.

- 2026-08-27: 2.2.5 테스트 후보에 KBO 경기 저장 위치 선택을 추가했다. KBO 창에서 `PC 로컬 일정` 또는 소유자/수정 권한이 있는 Google Calendar를 선택하며, Google 등록은 기존 저장·재시도 경로를 사용한다. `onharuCategory` 비공개 확장 속성으로 동기화 후에도 `야구` 카테고리를 유지한다. 기능 검사와 빌드를 통과했고, 공식 2.2.4와 분리된 `ONHARU-2.2.5-Test-Portable.zip`을 onharu.app 다운로드 페이지에 게시했다. SHA-256은 `C64984457578D77D363E464DE98DCCCBA28E797BB89383C2FB0D25F754BEB9B6`이며 웹 재다운로드 검증을 통과했다. 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-2.2.5-test-20260827-005128.tar.gz`이다.

- 2026-08-27: ONHARU 2.2.4를 GitHub와 onharu.app에 정식 게시했다. 공개 소스 커밋은 `2a44786`, 릴리스는 `https://github.com/cowpigpapa/onharu/releases/tag/v2.2.4`이다. 설치판 SHA-256은 `A488994AD1579D69E10B738623C2DA5D2ADC428AFEB217BBF505E564B77C6CF6`, 포터블판은 `CFD51D4C62FB238BF060704E57ADD36334A8ECB4B4917B8A95032E731DB22C65`이다. 웹 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-2.2.4-20260826-213048.tar.gz`이며, 공개 10개 페이지 HTTP 200, Nginx active, 웹 재다운로드 해시 일치를 확인했다.

- 2026-08-26: 2.2.4 로컬 후보에서 상세 스크롤, 고정 레이어의 4주 목록 입력, 패널 명칭, 중요한 날 표시, 상단 기능 표시 옵션과 Windows 제어 버튼, ONHARU/Google 필터 접기·전체 선택, 중요 일정 자동 글자 대비, 등록 카테고리 위치, 기본 10분 알림 및 동적 문구를 정리했다. 공통 팝업 테두리는 프리셋 대표색을 사용한다. 인쇄에는 완료·실패 상태와 예외 처리를 추가했다. 전체 빌드와 17개 품질 게이트를 통과했으며 최신 로컬 시험본을 실행했다. 고정 상태 4주 목록과 실제 프린터 인쇄는 사용자 화면 시험이 남아 있다.

- 2026-08-26: ONHARU 2.2.3을 GitHub와 onharu.app에 정식 게시했다. 달력 기간 선택을 상단 1~6주 직접 선택과 월 전체 4~6주 자동 표시로 단순화하고, 알림 사용 여부·알림 위치를 통합했다. 중요한 날 표시, 오른쪽 상세 정렬, 주 범위 임시 팝업의 배치와 테마, 신규 설치 샘플 일정 및 프리셋 대표색 회귀를 보완했다. `check-v22.ps1 -Build` 17개 품질 게이트를 통과했다. 공개 소스 커밋은 `18d8a6604769c659d78fa22f6008f5501f16779b`, 릴리스는 `https://github.com/cowpigpapa/onharu/releases/tag/v2.2.3`이다. 설치판 SHA-256은 `5F7754F379D50B40D13C04A593F7EC9B6BE7EC071B3F7956D5830F7542178172`, 포터블판은 `D9DC4C3A8ABD6D3EF47209BE66567C83901F5F21789676EA8C4AD80B44A93736`이다. 웹 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-2.2.3-20260826-005922.tar.gz`이며, 공개 10개 페이지 HTTP 200과 재다운로드 해시 일치를 확인했다. 혼합 DPI 다중 모니터 이동은 실제 장비 검증을 계속한다.

- 2026-08-25: 상세 날짜 옆 중요한 날 아이콘을 폰트 `★`에서 전용 벡터 `important_day`(4방향 sparkle)로 교체했다. `IconButton`의 내부 여백 0, 23×23 컨테이너, Viewbox 정중앙 규칙을 사용해 글꼴 기준선에 따른 상하 치우침을 제거했다. 선택 상태의 전경색도 벡터에 즉시 반영한다. 전체 17개 품질 게이트 PASS.

- 2026-08-25: 2.2.3 후보 01 실기기 피드백을 반영했다. `< 오늘 >`은 브랜드에서 6px 더 떨어뜨렸다. 고정 레이어의 패널 접기 버튼은 음수 여백 때문에 WPF 좌표 재탐색이 실패하던 구조를 폐기하고 hit-map kind 6 / action 20 전용 네이티브 경로로 직접 토글한다. 상세 헤더는 `카테고리/시간순`을 기존 중요한 날 자리로 이동하고 중요한 날은 날짜 옆 `★` 아이콘과 툴팁으로 축소했다. 중요 일정 색상 컨트롤은 24px 고정 제목 행의 아래쪽에 맞춰 펼쳐져도 창 높이가 늘지 않는다. 설정 저장 직후 야구 필터 표시 여부를 갱신한다. 알림 위치는 `주 모니터 중앙` 또는 `온하루 위`로 명확히 했으며 둘 다 명시적인 대상 영역 중앙에 배치한다. 전체 17개 품질 게이트 PASS, 최신 로컬 후보 실행 완료.

- 2026-08-25: 2.2.3 안정화 후보에 혼합 DPI 드래그 권위 분리를 적용했다. 네이티브 이동 중 `WM_DPICHANGED`에서는 Windows suggested RECT를 허용하고 기존 모니터 물리 좌표 복원·저장은 `WM_EXITSIZEMOVE` 뒤 한 번만 수행한다. 고정 레이어의 상세 패널 토글도 실제 WPF 토글 경로를 호출하도록 수정했다. 최소 논리 폭 720, 상단 간격·접기 폭, 오른쪽 공통 버튼 높이, Google 2열, Special Day 여백을 정리했다. 2.1 백업 호환, 일기 클릭 영역, 중요 일정 색상, 상세 독립 정렬, 카테고리 연속 이동, 야구 노출 조건을 자동 검사에 고정했다. 알림 위치는 설정에서 `사용 화면 중앙`과 `온하루 위` 중 선택한다. 전체 17개 품질 게이트 PASS. 실제 혼합 DPI 모니터 왕복 시험은 사용 환경에서 마지막 확인이 필요하다.

- 2026-08-25: 사용자 지시에 따라 프리셋 대표색 미반영과 신규 설치 샘플 일정 4개 생성 오류를 수정한 2.2.2 재배포본을 게시했다. GitHub main·`v2.2.2` 태그는 `0eb27afe3b46f487e773dbd225a2ff0b5feb2e94`이다. 설치판 SHA-256은 `191BA2BA67437EE9E2E63115DA83DE9F2E8F822C4C84DC1AE36B299C36C96A7A`, 포터블은 `825F29B20D5C6DFA1FFC6551811259881965CF4857B3E892480666A940FFD4E4`이다. onharu.app 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-2.2.2-republish-20260825-002651.tar.gz`이며 외부 HTTP 다운로드와 GitHub Release 파일의 해시 일치를 확인했다.

- 2026-08-23: 2.1 설치본의 자동 업데이트 창이 나타나지 않는 실제 원인을 확정했다. 로그의 `Check update / HttpRequestException`을 동일 .NET Framework 4.x `HttpClient`로 재현한 결과 기본 보안 프로토콜이 `Ssl3, Tls`라 GitHub와 TLS 채널을 만들지 못했다. `ServicePointManager.SecurityProtocol |= SecurityProtocolType.Tls12` 적용 후 같은 요청이 HTTP 200으로 성공했다. 2.2 `UpdateService`에 TLS 1.2를 명시하고 `update-check.ps1` 회귀검사를 추가했다. 이미 배포된 2.1 EXE는 이 수정 자체를 자동으로 받을 수 없어 2.2 최초 전환은 수동 설치가 필요하다.

- 2026-08-23: 2.1→2.2 자동 업데이트 실기기 시험 중 Google 연결 실패가 시작 동기화에서 업데이트 확인까지 막는 흐름을 발견했다. 같은 프로세스에서 로그아웃·재로그인 후 동기화가 성공해도 이미 중단된 `Loaded` 시작 절차는 재개되지 않아 업데이트 확인 시각이 2000년으로 남았다. 다음 2.2 수정에서는 시작 단계를 서로 독립된 안전 실행으로 분리해 Google 오류가 업데이트·타이머·알림·트레이 초기화를 끊지 못하게 하고, 업데이트 확인을 Google 동기화와 독립 실행한다. Google 실패는 `재로그인이 필요한 인증 오류`와 `재시도해야 하는 네트워크·서버 오류`로 분류하며, 인증 오류에만 다시 로그인 안내를 표시하고 민감한 토큰·응답 본문은 화면과 로그에 남기지 않는다.

- 2026-08-23: ONHARU 2.2.0 공개 소스를 GitHub `main`에 커밋(`5af00bd`, 웹 문서 후속 `fe51e1e`)하고 `v2.2.0` 정식 Release를 게시했다. 설치판 SHA-256은 `3EAFC011975A30FF922B82A92D30624B7EA72AEE3F382AFCE64245DF716C01C0`이며 Setup·Portable·Source·SHA 목록을 제공한다. GitHub `releases/latest`가 2.2.0과 필수 자산을 정상 반환하므로 앱의 사용자 승인형 자동 업데이트 배포 경로도 준비됐다. `Publish/ONHARU-Web-Publish-2.2.0.zip` 웹 배포 묶음을 만들었으나 현재 작업 환경에 onharu.app 서버 SSH/AWS 로그인 세션이 없어 실제 사이트는 아직 2.1이다. Google OAuth 앱은 외부·프로덕션 상태지만 `calendar.events`, `calendar.calendarlist.readonly`, `tasks` 민감 범위의 공개 검증이 승인되지 않아 무경고 일반 공개 상태가 아니며, 검증 전에는 경고 화면과 프로젝트 수명 전체 100명 신규 사용자 상한이 적용될 수 있다.

- 2026-08-23: 메인 상단과 시간표·일기장·KBO 팝업의 아이콘을 같은 글리프로 통일했다. 메인은 기존 버튼 크기 안에서 21px, 팝업 제목은 34px 박스 안 17px을 기준으로 하고 새 기능은 두 위치의 아이콘을 동시에 정의한다. 규칙은 `AGENTS.md`, `Docs/Architecture/POPUP_POLICY.md`, `popup-policy-check.ps1`에 고정했다.

- 2026-08-23: 설정의 파스텔 스킨 선택 카드가 중립 흰색·일반 남색으로 표시돼 실제 `옅은 파스텔 배경 + 짙은 동계열 글자` 원칙을 보여주지 못하던 문제를 수정했다. 파스텔 미리보기는 연한 라벤더 `#E7E9FF`와 짙은 보라 `#4338CA`를 사용하며, 실제 색상 편집 카드는 계속 `CategoryColorSystem`의 공통 파스텔 계산을 사용한다.

- 2026-08-23: 스킨 빠른 전환을 날짜 옆에서 `4주·월 전체` 보기 스위치 옆으로 이동했다. 파스텔 선택은 연한 파스텔 면과 보라 글자, 블랙 선택은 검정 면과 흰 글자를 사용한다. 설정의 표시 옵션은 `디자인 스킨` 제목 행 오른쪽 끝으로 옮겼고, 파스텔·블랙 라디오는 색상 미리보기 카드 안에 한 줄 설명으로 복원했다. 블랙 오른쪽 카드의 별도 어둡힘을 제거해 설정 색상 카드와 동일한 기준색·흰 글자를 사용한다.

- 2026-08-23: 스킨 표현 기준을 다시 확정했다. 파스텔은 메인·오른쪽 카드 모두 파스텔 배경과 짙은 동계열 글자, 블랙은 오른쪽 상세 카드에 어두운 카테고리색과 흰 글자를 사용한다. 블랙의 선택 날짜·이번 주·다음 주·중요한 날 버튼은 보라 상태색으로 통일했다. 오늘 버튼 옆에 임시 `파스텔·블랙` 슬라이더를 추가하고 설정의 `상단 스킨 전환 버튼 표시`로 숨길 수 있게 했다. 설정의 스킨 선택은 두 줄 미리보기 카드에서 한 줄 라디오 설명으로 압축했다. 설정 버전 38, 전체 17개 품질 게이트 PASS.

- 2026-08-23: 정식 스킨을 `파스텔·블랙` 두 개로 축소하고 시험용 `colorful` 팔레트와 실행 분기를 삭제했다. 저장된 `colorful` 값은 파스텔로 정규화한다. 파스텔은 기존 컬러의 생동감을 흡수해 흰색 혼합률을 68%로 조정하고 모든 일정 글자를 짙은 동계열로 통일했다. 오른쪽 일반 일정·D-Day·기념일 카드는 메인보다 진한 중간톤으로 변경했다. 설정 버전은 37이며 전체 17개 품질 게이트가 통과했다.

- 2026-08-23: 컬러 체크박스를 파스텔 스킨까지 확장해 세 스킨의 체크박스 표현을 통일했다. 블랙에서는 체크 면 기준으로 계산한 어두운 글자색이 사이드바 라벨에 전파되어 보이지 않던 문제를 수정하고, 라벨은 블랙 스킨의 밝은 공통 글자색을 사용하도록 역할을 분리했다.

- 2026-08-23: 블랙 스킨 실기기 피드백에 따라 오른쪽 `중요한 날` 버튼과 선택 날짜의 배경을 더 진한 중간 명도로 보정했다. 블랙의 기본 검정 체크박스는 카테고리 설정색 면과 흰 체크를 쓰는 공통 컬러 체크박스로 교체했다. 선택 날짜 및 모든 기본 팔레트의 글자 대비율 4.5:1 검사를 추가했고 전체 17개 품질 게이트가 통과했다.

- 2026-08-23: 오른쪽 상세 카드의 색상 경로를 통일했다. 일반 일정은 업무·개인·Google·야구의 실제 설정색을, D-Day와 기념일은 각 Special Day 설정색을 기준으로 사용한다. 파스텔·블랙·컬러 어느 스킨에서도 상세 카드는 밝은 파스텔 배경과 안정적인 진한 동계열 글자를 유지하며, 모든 기본 프리셋에서 글자 대비율 4.5:1 이상을 자동 검증한다.

- 2026-08-23: 독립 도구 창과 메인 차단 팝업이 동시에 보일 때 메인 입력의 포커스 복구 대상을 차단 팝업 및 그 Owner 자식 체인으로 제한했다. 야구·시간표·일기장 읽기 창이 설정·가져오기 등의 차단 팝업 대신 활성화되는 것을 방지한다.
- 2026-08-23: 전체 팝업 생성 경로를 전수 조사해 `Docs/Architecture/POPUP_POLICY.md`를 작성했다. 메인 차단 팝업은 공통 `ShowBlockingDialog`, 파일 선택·저장은 `ShowBlockingFileDialog`로 관리하며 차단 중 Explorer 레이어의 모든 입력을 기존 팝업 활성화로 돌린다. 시간표·일기장 읽기·KBO는 단일 인스턴스 독립 창으로 확정했고, KBO는 모달에서 modeless 창으로 전환해 선택 경기 등록을 이벤트로 메인에 전달한다.
- 2026-08-23: 슬라이딩 버튼의 비활성 영역에 마우스를 올리면 Windows 기본 Button hover 반전이 선택 손잡이 위에 겹쳐, 전환 후에도 이전 상태가 활성화된 것처럼 보이던 문제를 공통 컨트롤에서 해결했다. 각 세그먼트의 버튼은 hover 시각효과가 없는 투명 클릭 영역만 담당하고 활성 상태는 이동 손잡이만 표시한다.
- 2026-08-23: 설정창에서도 이동 전환이 실행된 원인은 중앙 배치 팝업이 `Owner=null`이라 메인 창의 `IsEnabled`가 유지되기 때문이었다. `IsEnabled` 조건을 폐기하고 같은 Dispatcher에 보이는 다른 ONHARU 창이 있으면 전환을 취소해 해당 창을 앞으로 가져오며, 먼저 움직인 세그먼트도 실제 모드 위치로 되돌린다.
- 2026-08-23: 로컬 일정 가져오기 등 모달 팝업이 메인 창을 비활성화한 상태에서 고정 레이어의 이동 명령만 실행되어, 비활성 메인 창이 위로 올라오고 경고음과 함께 조작 불능이 되던 문제를 수정했다. 이동·고정 공통 진입점에서 모달 상태를 검사하고 전환을 중단한 뒤 같은 UI 스레드의 열린 팝업을 앞으로 복구한다.
- 2026-08-23: KBO 상단의 `보기·기간·크기` 캡션과 외곽 그룹은 슬라이딩 버튼의 의미를 중복해 제거했다. `전체 경기/응원팀 경기`, `4주/월`, `작게/중간/크게` 세 슬라이더만 7px 간격으로 정돈했다.
- 2026-08-23: 이동/고정 세그먼트가 4주/전체월처럼 끝까지 움직이지 않던 원인은 130ms 애니메이션 완료 전에 Explorer 고정 레이어로 전환되던 순서였다. 아이콘을 제거하고, 이동→고정은 140ms 슬라이딩 완료 후 레이어를 전환하며 고정→이동은 WPF 화면 복귀 직후 오른쪽에서 왼쪽으로 이동하게 수정했다. 이미 선택된 상태를 다시 누르면 재전환하지 않는다.
- 2026-08-23: 이동/고정 세그먼트는 보기 선택과 달리 상태·동작을 함께 보여주므로 `↔ 이동 | 고정 📌`로 보완했다. 선택 손잡이는 이동에서 노란색 왼쪽, 고정에서 녹색 오른쪽으로 즉시 확정되며, 위 상태 문구의 중복 아이콘은 제거했다.
- 2026-08-23: 공통 `OnharuSegmentedSwitch`를 추가해 메인의 이동/고정 및 사용자 주수/전체월, KBO 팝업의 전체/응원팀 경기·4주/월·작게/중간/크게를 동일한 슬라이딩 세그먼트로 통일했다. 이동/고정은 Explorer 고정 프레임이 이전 위치의 선택 배경을 캡처하지 않도록 모드 전환 전에 선택 위치를 즉시 확정한다.
- 2026-08-23: 위치 모드 버튼을 단일 글리프에서 `이동 | 고정` 슬라이딩 세그먼트로 교체했다. 기존 이동·Explorer 고정 로직은 변경하지 않고, 현재 상태 영역으로 130ms 선택 배경이 이동하도록 해 조작 의미를 직관적으로 보여준다.
- 2026-08-23: `전체월` 버튼의 가로 폭을 42px에서 50px로 늘려 글자 잘림을 제거하고, 오른쪽에서 계산되는 `4주` 시작선을 보조 패널 왼쪽 선에 맞췄다. 시간표·일기·야구 추가 아이콘과 보기 전환 사이에는 버튼 한 칸 수준의 36px 간격을 두어 기능군을 분리했다.
- 2026-08-23: 상단 오늘 버튼을 다음 기간 `»` 오른쪽으로 이동했다. 기간 이동은 기존 23×27 크기를 유지하면서 `≪ ≫`을 단일 주 화살표와 조화되는 `« »`로 바꾸고 글자 굵기·크기·도움말을 보완했다. 보기 전환은 모호한 `기간/월간` 대신 설정의 사용자 지정 주 수를 반영한 `4주` 형식과 `전체월`로 표시한다.
- 2026-08-23: 설정의 `오늘 표시`에서 셀 테두리 선택을 제거하고 `색상`, `날짜 원형`, `색상 + 날짜 원형`, `표시 안 함`으로 변경했다. 사용자가 말한 아이콘은 실제 모양을 바로 알 수 있도록 `날짜 원형`으로 표기했다. 기존 `border` 설정은 `icon`, `both`는 `fill_icon`으로 자동 해석하며 직렬화 호환을 위해 기존 `TodayBorderColor` 저장 필드는 날짜 원형 색상 용도로 유지한다.
- 2026-08-23: 홈페이지 시안을 기준으로 컬러 스킨을 더 밝게 조정했다. 컬러 카테고리는 원색의 채도와 밝기를 소폭 높인 뒤 60% 백색 혼합 파스텔 배경과 43% 진한 동색 글씨를 만든다. 컬러 스킨의 오늘 `색상` 표시는 셀 전체 채움 대신 파란 원형 날짜로 표현하고 기존 오늘 `테두리` 설정은 유지한다.
- 2026-08-23: 기간 이동 `≪ ≫`을 단일 주 이동 `‹ ›`과 함께 날짜 양옆으로 옮겼다. 네 버튼의 글자를 키우고 내부 여백과 폭을 줄였으며, 브랜드 텍스트 시작선을 화살표를 제외한 연도 시작선에 맞췄다. 기본 버튼은 기존 8px 패딩을 유지하도록 템플릿을 Padding 기반으로 바꿨다.
- 2026-08-23: 2.2 추가 개발의 기준선을 완성된 `Onharu_v2.1`로 확정했다. 특히 디자인·배치는 변경 전에 대응하는 2.1 소스와 실제 화면을 먼저 비교하고, 별도 요청이 없는 크기·간격·정렬·클릭 영역을 보존한다. 이 규칙을 프로젝트 `AGENTS.md`와 스킨 아키텍처 문서에 반영했다.
- 2026-08-23: 컬러 체크박스 축소 후 Special Day 필터가 업무·개인 행과 야구 행의 중간 높이에 놓이는 문제를 수정했다. 왼쪽 `온하루 등록`의 2행 전체 높이로 오른쪽 체크박스가 늘어나 중앙 배치되던 것이 원인이며, Special Day 패널과 항목을 첫째 행 위쪽에 명시적으로 정렬했다.
- 2026-08-23: 컬러 스킨에서 Special Day Card의 마지막 `기념일` 필터가 잘리던 원인을 2.1과 비교했다. 사이드바 폭은 동일했지만 새 컬러 체크박스가 기본 체크박스보다 체크 영역과 글자 간격을 합쳐 약 4px 더 사용했다. 사이드바 배치를 바꾸지 않고 컬러 체크 영역을 14px, 내용 간격을 4px로 맞춰 2.1과 같은 점유 폭을 유지했다.
- 2026-08-23: 컬러 스킨의 체크박스 적용을 로컬 일부 항목에서 모든 오른쪽 필터로 확장했다. 업무·개인·야구·D-Day·기념일뿐 아니라 각 Google Calendar와 Google Tasks도 자신의 실제 카테고리 색상을 체크박스에 전달한다. 달력 일정은 같은 색상의 파스텔 배경과 진한 동색 글씨를 사용하며, 오른쪽 Todo 체크도 동일한 컬러 규칙을 따른다.
- 2026-08-23: 블랙에서 컬러 스킨으로 전환한 뒤 앱이 종료되고 재시작하지 못한 회귀를 수정했다. 컬러 체크박스 XAML 템플릿이 `x:Type`, `x:Name`을 사용하면서 `xmlns:x`를 선언하지 않아 저장된 `colorful` 스킨을 시작 시 다시 적용할 때 `XamlParseException`이 발생했다. 네임스페이스를 추가하고 `theme-check.ps1`이 실제 템플릿 생성까지 수행하도록 회귀 검사를 강화했다.
- 2026-08-23: 기능과 Explorer 레이어를 그대로 유지하고 외형만 교체하는 2.2 스킨 시스템을 시작했다. `classic`, `dark`, `colorful`의 언어 독립 ID와 의미 기반 팔레트를 `OnharuTheme.cs`로 분리했다.
- 2026-08-23: 메인 외곽·달력·사이드바·상세 카드·일정 막대·체크박스를 팔레트 역할에 연결했다. 기존 스킨은 이전 색을 보존하고, 블랙은 고대비 어두운 면, 컬러는 파스텔 일정과 컬러 체크를 사용한다.
- 2026-08-23: 설정 버전을 28로 올리고 `ThemeId`를 저장한다. 누락·손상·알 수 없는 값은 `classic`으로 안전 복구한다. 설정에 세 스킨의 미리보기 선택 카드를 추가했다.
- 2026-08-23: 2.2 로컬 빌드와 기존 15개 게이트 및 신규 `theme-check.ps1`을 합친 16개 품질 게이트가 PASS했다. 구조와 확장 규칙은 `Docs/Architecture/THEME_SYSTEM_2.2.md`에 기록했다.

- 2026-08-23: 일반 배포 다음 순서를 `메일 발송 → Google OAuth 공개 검증 → 사용자 승인형 자동 업데이트 → 한국어·영어 다국어`로 확정했다. 홈페이지 자체 수정·배포는 별도 작업에서 관리한다.
- 2026-08-23: `Docs/Release/OAUTH_VERIFICATION_SUBMISSION_KO.md`에 현재 앱이 실제 요청하는 Calendar 목록 읽기, Calendar 일정 수정, Tasks 범위의 제출 사유·검증 영상·체크리스트를 작성했다.
- 2026-08-23: `Docs/Architecture/MAIL_DELIVERY_ARCHITECTURE.md`에 SMTP/API 키를 공개 EXE에 넣지 않는 원칙과 메일 전송 데이터 경계를 기록했다. JSON/ICS는 로컬 일정만, CSV는 사용자 결정에 따라 Google을 포함한 전체 일정을 전송한다.
- 2026-08-23: GitHub Release 기반 사용자 승인형 자동 업데이트를 독립 `UpdateService`, `UpdateAvailableWindow`, `MainWindow.Updates`로 추가했다. 하루 1회 확인하고 새 버전이 있을 때만 사용자에게 묻는다. Setup EXE를 받은 뒤 Release의 `SHA256SUMS.txt`와 일치할 때만 실행하며 자동 강제 설치는 하지 않는다. 현재 `v2.1.0` 자산 이름 정합성과 전체 품질 게이트 13개 PASS를 확인했다.
- 2026-08-23: Inno Setup에 한국어와 영어 설치 언어를 추가하고 바로가기·자동 시작·실행 문구를 언어별 `CustomMessages`로 분리했다. 앱 본문 다국어는 별도 Localization 모듈로 화면 단위 전환한다.
- 2026-08-23: Google Cloud의 실제 ONHARU 프로젝트를 점검하고 사용자 승인 후 브랜딩을 저장·게시했다. 홈페이지 `https://onharu.app/`, 개인정보처리방침 `https://onharu.app/ONHARU-PRIVACY.html`, 승인 도메인 `onharu.app`을 등록했다. 대상은 외부이며 게시 상태는 `프로덕션 단계`로 전환됐다. `calendar.calendarlist.readonly`, `calendar.events`, `tasks` 3개 범위와 앱 코드는 일치한다. Google은 현재 `앱을 인증해야 합니다`를 표시하므로 인증 센터의 공개 OAuth 검증 제출은 다음 단계다.
- 2026-08-23: 앱의 256px 원본 ICO를 기준으로 Google 권장 규격 120×120 PNG `Distribution/onharu-oauth-logo-120.png`를 생성하고 시각 확인 후 OAuth 브랜딩 로고로 저장했다. Google Cloud에서 `현재 앱 로고`와 `브랜딩 확인` 버튼이 표시되는 것을 확인했다.
- 2026-08-23: 메일 공급자를 Amazon SES로 확정했다. 서울 `ap-northeast-2`, 표시 발신자 `ONHARU <noreply@onharu.app>`, 권장 Custom MAIL FROM `mail.onharu.app`을 기준으로 한다. 공개 EXE에는 AWS 자격증명을 넣지 않고 Lambda/API Gateway만 SES raw attachment 발송을 호출한다.

- 2026-08-22: 현재 시즌을 제공하지 않는 API-SPORTS 무료 연동을 Parse.bot KBO Schedule API BYOK로 교체했다. 키는 별도 `parse-kbo-api-key-v1.dat`에 DPAPI 저장하여 이전 공급자 키와 혼동하지 않는다. 월별 POST 조회 결과를 `sports-kbo-parse-YYYYMM.json`으로 영구 캐시하고 사용자가 `새로고침`을 눌렀을 때만 1크레딧을 다시 사용한다. 키 연결 검증에서 받은 현재 월 데이터도 바로 캐시한다. Parse 응답의 날짜·시간·원정팀·홈팀·구장을 읽고 안정적인 합성 경기 ID로 중복 등록을 막는다. 발급 안내와 링크도 Parse.bot KBO API로 변경했다. 샘플 2026 응답 파싱, 빌드, 13개 품질 게이트 PASS. 실제 Parse 키 연결 시험은 사용자 확인 대기다.

- 2026-08-22: 사용자가 발급한 API-SPORTS 키를 노출하지 않고 실측했다. `/status` 정상, `leagues?search=KBO`에서 KBO 리그 ID 5 확인, 2024 시즌은 786경기 정상 조회됐다. 그러나 2026 시즌은 HTTP 성공 응답 안의 `errors.plan`으로 무료 플랜의 허용 시즌이 2022~2024뿐임을 확인했다. 현재 시즌 실패 원인은 키나 ONHARU 코드가 아니라 공급자 무료 플랜 제한이다. 앱은 이 오류를 한국어로 정확히 표시하고 발급 안내에 현재 무료 시즌 제한과 클릭 가능한 `https://dashboard.api-football.com/` 주소를 명시한다. 표시 옵션의 Google Tasks 앞 불필요한 18px 여백도 제거했다. 빌드·13개 게이트 PASS.

- 2026-08-22: 상단 프로야구 아이콘을 독립 KBO 월간 달력 팝업으로 연결했다. API-SPORTS의 KBO 리그와 시즌 경기 목록을 조회하고 12시간 로컬 캐시하여 무료 호출량을 절약한다. 경기별 체크 후 하단 확정 버튼으로만 ONHARU 로컬 `야구` 일정에 등록하며 `SportsGameId`로 중복을 막는다. 오른쪽 온하루 등록 필터는 업무·개인·야구를 2열 2행으로 배치했고 야구 표시 상태를 설정 버전 25에 저장한다. 실제 API 키 기반 응답 스키마·KBO 시즌 조회는 사용자 키 발급 후 실기기 확인이 필요하다. 빌드 및 품질 게이트 13개 PASS.

- 2026-08-22: 프로야구 일정은 서버 공용 키 대신 사용자가 자신의 API-SPORTS Baseball 키를 연결하는 선택형 BYOK 구조로 확정했다. `SportsApi.cs`는 키를 `%LOCALAPPDATA%\OnharuV3\sports-api-key-v1.dat`에 Windows DPAPI(CurrentUser)로 암호화 저장하고 `/status`로 연결을 검증한다. `SportsApiWindows.cs`는 ONHARU 스타일 발급 안내·키 입력·변경·삭제 화면을 제공한다. 설정 버전 24의 `UseProBaseball`은 기본 꺼짐이며 표시 옵션을 `달력 내 표시`와 `상단 기능 아이콘`으로 분리했다. 빌드와 품질 게이트 13개 전체 PASS. 실제 키 연결과 KBO 리그·팀·경기 조회는 다음 단계다.

- 2026-08-22: CSV 기념일 기준일이 `Important && ShowDday`일 때만 기록되던 잘못된 조건을 제거했다. 이제 기념일 기준일은 중요한 날 여부와 무관하게 항상 내보낸다. 이 수정 전 CSV도 카테고리가 기념일이면 일정 날짜를 기준일로 복원하여 동일 기념일을 변경 일정으로 오인하지 않는다.

- 2026-08-22: CSV 가져오기에서 Google 행을 선택하면 Google과 연결되지 않은 로컬 복사본이 된다는 안내를 추가했다. CSV 왕복 시 기념일이 변경 일정으로 오인되지 않도록 기념일 종류, 반복 방식, 반복 요일과 내부 기본값을 보존한다. `LocalImportWindow`와 `PendingSyncWindow`에 누락됐던 ONHARU 공통 스크롤바 스타일을 적용했으며, 코드로 생성하는 모든 ScrollViewer의 공통 스타일 적용 여부를 재점검했다.

- 2026-08-22: 내보낸 ICS 기념일을 다시 가져올 때 동일 ID임에도 내부 기본값(null/빈 문자열, 비활성 알림 값, snooze 기본값 등) 차이 때문에 신규·변경 후보로 잘못 표시되는 비교를 수정했다. 가져오기 비교는 사용자에게 의미 있는 일정 필드만 비교하며, 기념일은 ID가 달라도 이름+기념일 종류+기준일이 같으면 동일 기념일로 연결하고 기존 ID를 유지한다.

- 2026-08-22: 일정 관리의 ICS 가져오기·내보내기와 Excel CSV 가져오기를 구현했다. JSON/ICS는 로컬 일정만 내보내고, CSV는 Google 포함 전체 일정을 내보낸다. ICS/CSV 가져오기는 모두 선택 목록을 거쳐 로컬 일정으로만 저장되며 Google 서버를 변경하지 않는다. ICS는 표준 VEVENT, 종일/시간 일정, RRULE 및 ONHARU 전용 Todo·완료·중요·D-Day·기념일·알림 필드를 지원한다. CSV에는 왕복 식별용 `ONHARU ID`를 추가했다. `export-check.ps1`에 JSON·ICS·CSV 왕복 검증을 포함했다.
- 2026-08-22: Loading Simulator 메일 설정을 읽기 전용으로 조사했다. 프로젝트에는 메일 발송 코드나 Supabase Edge Function이 없고 별도 텍스트 파일에 Brevo 계열 SMTP 키로 보이는 비밀값만 존재한다. 해당 키를 ONHARU EXE에 내장해 재사용하면 유출되므로 사용하지 않는다. 향후 onharu.net 서버 또는 Supabase Edge Function에 비밀값을 보관하고 ONHARU는 인증된 HTTPS 요청만 보내는 구조로 구현해야 한다.

- 2026-08-22: 일정 관리 팝업 UI를 공통 `OnharuPopupChrome` 규칙으로 통일했다. 백업 복원, 로컬 일정 가져오기, 일정 삭제/삭제 확인, 안내, 데이터 형식/복원 경로 선택 팝업이 동일한 둥근 외곽선, 드래그 가능한 제목줄, 우측 상단 닫기 `×`, 하단 취소·확인 버튼의 크기·간격·색상 체계를 공유한다. 선택만 하는 작은 팝업은 `×`로 취소하고, 데이터 변경이 발생하는 팝업은 하단 `취소`도 함께 제공한다.

## 2026-08-22 — 데이터 작업 분기·선택 삭제·기념일 단일 저장

- 해상도/DPI 변경 뒤 고정 모드와 이동 모드가 서로 다른 위치·크기로 전환되던 원인을 수정했다. 고정 시 논리 좌표뿐 아니라 물리 픽셀 좌표를 항상 저장하고, 고정→이동 직전에 Explorer에 실제 게시된 프레임 사각형을 WPF 창에 그대로 적용한다.
- 상단 위치 버튼과 트레이 위치 메뉴가 동일한 `EnterEditMode` 전환 경로를 사용하도록 통일했다. 디스플레이 변경 이벤트는 Windows의 연속 변경 통지가 끝난 뒤 350ms 안정화 후 화면 보정·물리 위치 저장·고정 프레임 재게시를 수행한다.
- 1차 보완 뒤에도 해상도 변경 직후 차이가 남아, 고정 프레임 좌표를 WPF 언클로킹 전뿐 아니라 언클로킹 완료 다음 렌더 턴에도 다시 적용한다. `WM_DPICHANGED` 권장 사각형이 첫 적용을 뒤늦게 덮는 경합을 막고, 두 번째 적용 직후에만 Explorer 프레임을 제거한다.
- 추가 실기기 피드백에서 고정 상태는 글씨와 전체 달력이 작아지고 이동 모드에서만 원래 크기로 복구되는 점을 확인했다. 숨겨진 WPF가 새 해상도/DPI 정보를 반영하기 전에 고정 비트맵을 만들던 것이 핵심이었다.
- 디스플레이 변경 시 기존 Explorer 프레임은 계속 표시하고 WPF 창은 Opacity 0 상태로만 언클로킹한다. 두 렌더 턴 뒤 새 DPI·레이아웃으로 프레임을 교체한 후 다시 cloak하므로, 이동 모드로 들어가지 않아도 고정 달력의 크기·글씨·위치가 갱신된다.
- 위 투명 언클로킹 실험은 이동 상태에서 해상도를 바꾼 뒤 고정해도 모드 간 크기가 달라 실기기 실패로 판정하고 제거했다. XDiary도 해상도 변경 자체로 달력이 확대·축소되며, 별도 위치 이동을 시작할 때 현재 화면 위치를 기준으로 삼는다는 관찰을 새 기준으로 채택했다.
- 최종 후보는 고정 프레임의 물리 HWND 위치만 바꾸지 않고 `TransformFromDevice`로 현재 DPI의 WPF 논리 `Left/Top/Width/Height`까지 함께 갱신한다. 이전에는 다음 WPF 레이아웃 패스가 옛 Width/Height를 다시 적용해 위치·크기가 튀었다.
- 방향별 실기기 결과에서 이동→고정이 별도로 어긋나는 점을 반영했다. 이동 중 해상도가 바뀌면 WPF `TransformToDevice`가 늦게 갱신될 수 있으므로 고정 프레임 생성 시 이 배율을 위치·크기 기준으로 사용하지 않는다.
- 이동→고정은 Win32 `GetWindowRect`로 사용자가 실제 보고 있는 WPF HWND 물리 사각형을 읽고, 그 픽셀 크기에서 유효 렌더 DPI와 히트맵 배율을 역산한다. 따라서 XDiary 위치 이동처럼 현재 보이는 사각형 자체가 고정 결과의 기준이다.
- `before-delete-20260822-015043` 안전 백업을 끝 8자리 일일 날짜로 잘못 잘라 `2-01년 50월 43일`로 표시하던 문제를 수정했다. 일일 백업과 삭제 전 안전 백업을 각각 엄격한 날짜 형식으로 파싱하고, 안전 백업은 `2026년 08월 22일 01:50 삭제 전 안전 백업`으로 표시한다.
- 일정 관리 5개 버튼의 두 줄 글자를 한 단계 키우고 내부 Padding을 1px로 통일했다.
- 공통 안내창의 성공 제목이 모든 호출에서 `일정 가져오기`로 고정돼 Excel 저장과 야구 준비 안내까지 잘못 표시되던 진짜 원인을 수정했다. 안내창은 호출 목적별 제목을 받으며 미지정 시 중립적인 `안내`를 쓴다.
- 일정 관리 버튼 5개를 같은 한 줄·같은 높이·같은 2행 캡션 구조로 맞췄다. 문구를 압축해 잘림을 막고 일정 삭제에는 쓰레기통 아이콘을 적용했다.
- 일정 삭제창을 540px 고정 외곽 안에서 헤더 닫기, 관리 개수, 카드형 목록, 실제 스크롤, 하단 작업 버튼이 모두 보이도록 재배치했다.
- `일정 가져오기`, `일정 내보내기`, `Excel로 내보내기`를 서로 다른 `SettingsDataAction` 값으로 재검증하고 품질 게이트에 고정했다. 최신 시험본에서 Excel은 CSV 저장 경로만 실행한다.
- 파일 열기·JSON 저장·CSV 저장은 마지막으로 성공한 폴더를 기억하며 최초 1회만 바탕화면을 사용한다.
- 일정 관리에 `일정 선택 삭제`를 추가했다. 로컬 일정·반복 시리즈·기념일을 선택 삭제할 수 있고 Google D-Day는 Google 원본을 남긴 채 ONHARU D-Day 설정만 제거한다.
- 삭제는 목록 선택 → 삭제 버튼 → 2차 확인 순서이며 실행 직전에 `before-delete-yyyyMMdd-HHmmss` 복구 백업을 별도로 만든다.
- 기념일은 100년치 `PlannerItem`을 만들지 않고 기준 레코드 하나만 저장한다. 달력 발생일은 표시 연도별로 계산하며 기존 물질화 시리즈도 로드 시 하나로 축약한다.
- 앱 빌드 및 13개 품질 게이트를 모두 통과했다.

## 2026-08-20 — 일정 카드·시간표·색상 도구·Google 흐름·고정 투명도 보완

- 오른쪽 일정 카드에서 Todo 제목이나 본문을 한 번 클릭해 완료 상태가 바뀌던 지연 토글을 제거했다. 완료 변경은 체크박스 클릭으로만 수행하며 일정 본문 더블클릭은 기존처럼 수정창을 연다. Explorer 고정 레이어의 상세 카드도 동일하게 체크박스만 완료를 변경한다.
- 시간표 신규 기본값을 10교시에서 9교시로 바꾸고 창 기본 높이를 9교시에 맞춘 570px로 조정했다. 표 아래 남는 영역은 회색 대신 옅은 ONHARU 배경색을 사용한다. 기존 사용자가 저장한 교시 수는 유지한다.
- 시간표를 `ShowDialog` 모달 창에서 단일 비모달 팝업으로 변경했다. 달력이 고정된 상태를 유지하면서 별도로 이동할 수 있고, 이미 열려 있으면 새 창을 만들지 않고 기존 창을 활성화한다.
- 시간표 저장은 창을 닫지 않는다. 설정 패널이 열려 있으면 교시·요일·시간 설정을 적용한 뒤 과목·장소와 함께 저장하고 설정 패널만 닫으며 1.2초 동안 저장 완료 상태를 표시한다.
- 설정 색상 영역에서 `RGB 조절 펼치기`를 왼쪽으로 옮기고 기존 오른쪽 자리에 `색상 교환`을 배치했다. `Special Day` 제목은 `Special Day Card`로 변경했다.
- 메인 상단의 별도 `G 동기화` 버튼을 숨기고 오른쪽 Google 계정 카드를 클릭하면 연결 또는 동기화되도록 변경했다. 설정의 `G 변경/G 로그아웃`은 `G 계정` 하나로 통합하고 ONHARU 스타일 선택 팝업에서 계정 변경 또는 로그아웃을 명시적으로 선택한 뒤에만 실행한다.
- 고정 상태 투명도 조절은 매 포인터 이동마다 WPF 전체 프레임을 다시 렌더링·복사하지 않는다. `ExplorerFramePublisher.UpdateOpacity`가 기존 캐시 비트맵의 알파값만 갱신하고 필요한 영역만 다시 합성해 깜빡임과 지연을 줄인다. 공식 후보 17 전환 순서는 변경하지 않았다.
- 새 동작을 `feature-pack-check.ps1`에 추가하고 `check-v21.ps1 -Build`로 App, DesktopHook, LayerHost를 재빌드했다. 13개 품질 게이트가 모두 PASS했으며 `Tests/LocalTest` 시험본을 실행했다.

## 2026-08-20 — MainWindow 후속 분리와 프로젝트 작업 원칙

- 1차 리팩토링 이후 491줄로 늘어난 `MainWindow.cs`를 다시 감사했다. 공유 필드와 생성자 조립은 본체에 유지하고 메인 UI를 `MainWindow.Layout.cs`, 시작·보정·수명주기를 `MainWindow.Startup.cs`, 팝업 배치를 `MainWindow.Dialogs.cs`, 위치 상태를 `MainWindow.PositionMode.cs`, 색상·크기 도우미를 `MainWindow.Theme.cs`로 분리했다.
- 생성자는 설정·일정 로드 후 `RestoreConnectedGoogleAccount`, `RepairLoadedData`, `ConfigureInitialWindow`, `AttachWindowLifecycle`을 조립하는 구조로 줄였다. 데이터 보정 순서와 타이머·Loaded/Closing 동작은 유지했다.
- 프로젝트 루트 `AGENTS.md`를 추가했다. 새 기능을 `MainWindow.cs`에 몰아넣지 않는 원칙, 기능 변화 없는 기계적 분리 후 검증, ONHARU 파스텔·둥근 모서리·공통 버튼·정렬·간격·작은 화면 디자인 원칙을 이후 모든 작업에 적용한다.
- 구조 설명은 `Docs/Architecture/MAINWINDOW_REFACTOR_20260820.md`에 기록했다.
- `MainWindow.cs`는 491줄에서 111줄로 줄었고 생성자 외 메서드는 남기지 않았다. `check-v21.ps1 -Build`로 App과 네이티브 레이어를 재빌드한 뒤 13개 품질 게이트가 모두 PASS했다.

## 2026-08-20 — onharu.net 및 향후 제품 방향 확정

- 사용자가 공식 도메인 `onharu.net`을 확보했다. 향후 제품 소개, 다운로드, 설명서, 개인정보처리방침, 업데이트 manifest와 Google OAuth 공개 정보를 이 도메인 중심으로 구성한다.
- `TODO_NEXT.md`를 새로 만들고 한국어·영어 다국어, 사용자 승인형 자동 업데이트, 일반 공개 배포, 향후 Google Calendar 연동의 ONHARU Plus 유료 전환을 확정 과제로 기록했다.
- 독립 메모는 제품 범위에서 제외하고 기본 로컬 기능은 프리웨어로 유지한다. Google 유료화 전에는 권한 경계를 코드로 먼저 분리하되 현재 사용자의 Google 기능은 그대로 허용한다.
- 중간 디자인·기능 개선은 계속 진행한다. 새 기능을 `MainWindow.cs`에 다시 집중시키지 않고 Localization, Updates, Licensing, Google, ExplorerLayer를 독립 책임으로 유지한다.

## 2026-08-20 — xDiarys 기능·완성도 재비교

- 오늘 추가된 쉬는 날·월~일 시작 요일·달력 인쇄·제품 정보·기능 소개 HTML·독립 시간표와 기존 Google·Todo·D-Day·기념일 기능을 반영해 `XDIARY_ONHARU_FEATURE_COMPARISON_20260820.md`를 작성했다.
- ONHARU 우위, xDiarys 우위, 동등·성격 차이, 추가 확인 필요를 분리하고 폐쇄형 xDiarys에서 확인할 수 없는 범위는 단정하지 않았다.
- 종합 판단은 일정·동기화·개인화에서 ONHARU 우위, 고정→이동 레이어 전환의 마지막 미세 안정성·독립 메모·자동 업데이트·다국어에서는 xDiarys 우위다.
- 두 앱을 동시에 실행했을 때 ONHARU가 위에 보이는 현상은 같은 Explorer 페인트 경로의 합성 순서이며 제품 우열로 해석하지 않도록 기록했다.
- 제품 목표와 무관한 독립 스티커 메모는 사용자 결정에 따라 경쟁 기능 비교와 향후 우선순위에서 제외했다. 다음 제품 과제는 자동 업데이트, 한국어·영어 다국어, 일반 공개 배포와 향후 Google 유료 기능 분리다.

## 2026-08-19 — 일정 제목·상단 저장 및 알림 직접 선택 정리

- 압축 과정에서 글자가 잘리던 제목 입력칸 높이를 40px에서 기존 46px로 복원했다.
- 신규·수정 팝업 헤더의 닫기 버튼 왼쪽에 `✓ 저장` 버튼을 추가했다. 스크롤 하단의 기존 일정 저장·수정 저장 버튼도 유지한다.
- 알림 빠른 선택을 `없음`, `정시`, `직접 선택`으로 단순화하고 `10분 전`, `30분 전`, `하루 전` 라디오 버튼을 제거했다. 기존 기본 알림값이나 저장된 알림값이 해당 값이면 직접 선택 숫자·단위로 자동 표시된다.
- 직접 선택 단위 드롭다운을 74×27px로 조정하고 설정창과 같은 둥근 온하루 ComboBox 템플릿, 화살표, hover/선택 색상을 적용했다.
- 회귀 검사에 상단 저장, 제목 높이, 직접 선택 드롭다운과 제거된 빠른 옵션 검사를 추가했다. 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-19 — 노트북용 일정·설정 팝업 압축 및 기념일 경로 분리

- 새 일정·일정 수정 팝업의 스크롤 영역을 작업 화면 높이의 약 70%로 제한했다. 최대 620px, 최소 340px 범위에서 계산해 작은 노트북에서도 팝업 하단이 화면 밖으로 나가지 않는다.
- 설정 팝업도 작업 화면 높이의 약 70%로 제한하고 기존 상단 저장 버튼 자동 표시와 온하루 스타일 스크롤바를 유지했다.
- 일정창의 바깥 여백, 날짜 카드, 제목 입력, 시간 카드, 카테고리 카드, 반복, 메모 및 하단 버튼의 상하 간격을 단계적으로 축소했다. 저장·삭제 버튼 높이는 44px에서 40px로 줄였다.
- 분 선택은 `분 + 0/15/30/45분`이 같은 한 줄에 표시되도록 재배치했다.
- 일반 일정 신규·수정창의 `기념일로 등록`, 기념일 기준일, 기념일 변환 저장 분기를 완전히 제거했다. 기념일은 오른쪽 기념일 카드의 `만들기` 및 기념일 전용 수정창에서만 관리한다. 따라서 Google 일정 선택과 기념일 변환이 섞이지 않는다.
- 일반 D-Day 기능은 유지하고 체크박스 글자색을 오른쪽 D-Day 카드와 같은 `#0369A1` 하늘색 계열로 변경했다.
- 회귀 검사에서 일반 일정창에 기념일 컨트롤이 다시 들어오지 않는지, D-Day 팔레트와 70% 높이 제한이 유지되는지 검증한다. 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-19 — 투명도 슬라이더 세로 여백 추가

- 투명도 슬라이더의 클릭 영역은 18px로 유지하고 실제 보이는 트랙·손잡이만 세로 74%로 축소했다.
- 시각 요소를 중앙 기준으로 축소해 위아래 여백을 균등하게 만들고 아래 날짜 제목과 붙어 보이던 문제를 완화했다.
- 클릭·드래그 히트 영역, 투명도 범위, 저장 및 고정 레이어 반영은 변경하지 않았다.
- 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-19 — 투명도와 날짜 제목 사이 여백 조정

- 날짜 제목을 아래로 내려 헤더 전체 높이를 바꾸지 않고, 투명도 슬라이더에 적용했던 2px 하향 이동만 제거했다.
- 슬라이더의 18px 높이는 유지해 아래 잘림을 방지하면서 날짜 제목과의 간격을 확보했다.
- 오른쪽 버튼 아래 정렬과 달력 시작 위치는 변경하지 않았다.
- 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-19 — 제목줄 투명도 슬라이더 정렬

- `온하루 · ONHARU`보다 위로 떠 보이던 투명도 슬라이더를 시각적으로 2px 아래로 이동했다.
- 슬라이더 폭을 70px에서 64px, 높이를 22px에서 18px로 줄여 제목과 같은 한 줄의 중앙선에 맞췄다.
- 조작 범위·저장·고정 레이어 반영 및 툴팁은 유지했다.
- 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-19 — 위치 동작 아이콘 및 투명도 슬라이더 교정

- 위치 위쪽 문구는 현재 상태를 나타내고 버튼은 다음 동작을 나타내도록 분리했다. `📌 고정됨` 상태의 버튼은 `↔`, `↔ 이동 가능` 상태의 버튼은 `📌`으로 표시한다.
- 왼쪽 제목 영역의 `투명도` 캡션을 제거하고 슬라이더만 배치해 시각적 복잡도를 줄였다. 마우스 툴팁 `달력 투명도`는 유지한다.
- 슬라이더 높이를 17px에서 22px로 늘리고 폭과 왼쪽 여백을 조정해 아래쪽 트랙·손잡이가 잘리지 않게 했다.
- 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-18 — 상단 아이콘 균등 간격 및 투명도 이동

- 이전 버튼부터 닫기 버튼까지 모든 작업 컨트롤의 왼쪽 간격을 5px로 통일하고 설정–닫기 사이의 43px 공백을 제거했다.
- 상단 작업 버튼 높이를 28px로 낮추고 이전·다음·검색·설정·닫기 아이콘 글자와 버튼 폭을 약 20% 축소했다.
- 투명도 캡션과 슬라이더를 오른쪽 작업 영역에서 제거해 왼쪽 `온하루 · ONHARU`와 연·월·기간 제목 사이의 윗줄에 배치했다.
- 위치 버튼은 Google 버튼과 비슷한 78px 폭으로 넓히고 고정 상태 `📌`, 이동 상태 `↔` 아이콘을 사용한다. 상태 문구도 `📌 고정됨`/`↔ 이동 가능`으로 통일해 작은 화면에서 잘리지 않도록 했다.
- 닫기 버튼의 기존 최소화·확인·우클릭 메뉴 안전장치는 유지했다.
- 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-18 — 상단 제목·버튼 세로 기준 재정렬

- 연도·월·기간 제목은 화면 폭별 반응형 크기를 폐기하고 작은 화면 기준 `17px`로 고정했다. 사용자 글자 크기 배율은 기존대로 적용한다.
- `고정됨/이동 가능` 상태와 `투명도` 캡션을 동일한 10.5px SemiBold로 맞췄다.
- 이전·오늘·다음·위치·Google·검색·설정·닫기 버튼 높이를 32px로 통일해 기존보다 2px 낮췄다.
- 투명도 컨트롤도 32px 높이 안에서 캡션과 슬라이더를 재배치해 주변 버튼의 아래 기준선과 일치시켰다.
- 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-18 — 작은 화면 상단 작업 버튼 압축

- 이전·오늘·다음 버튼 폭을 42/62/42px에서 36/52/36px로 줄였다.
- 위치 버튼의 긴 문구를 제거하고 고정 상태에서는 `↔`, 이동 상태에서는 `✓` 아이콘으로 표시한다. 위쪽 `고정됨/이동 가능` 상태 문구와 툴팁은 유지한다.
- 투명도 캡션을 슬라이더 왼쪽에서 위로 옮겨 가로 폭을 줄였으며 슬라이더 조작 범위와 값은 유지했다.
- Google 연결·동기화 버튼 폭을 92px에서 78px로 줄였다. 로그인 취소·동기화 중 상태도 같은 버튼에서 유지한다.
- 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-18 — 작은 화면 날짜·기간 제목 반응형 축소

- 작은 해상도에서 상단 `연도·월·기간` 제목이 오른쪽 작업 버튼 영역에 잘리던 문제를 수정했다.
- 기본 폭에서는 기존 24px 디자인을 유지하고, 창 폭이 1160/1020/900px 아래로 내려갈 때 제목만 22/19/17px로 단계적으로 축소한다.
- 창 크기를 조절하는 동안 `SizeChanged`에서 즉시 적용하며, 월·주 범위 변경 및 글자 크기 설정 변경 후에도 반응형 크기를 다시 계산한다.
- 달력 셀, 일정 글자 및 사용자 글자 크기 설정은 변경하지 않았다.
- 회귀 게이트를 추가하고 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-18 — 고DPI 노트북 고정 위치 수평 이동 수정

- 작은 해상도·고DPI 노트북에서 이동 가능 화면을 고정할 때 달력이 오른쪽으로 어긋나는 치명적 좌표 문제를 수정했다.
- 기존에는 WPF `PointToScreen` 결과에서 WinForms `VirtualScreen` 원점을 빼 고정 프레임 위치를 추정했다. 이 값은 DPI 가상화 및 Explorer 데스크톱 뷰의 실제 클라이언트 원점 차이 때문에 일치하지 않을 수 있었다.
- 이제 WPF 창의 실제 화면 픽셀 좌표를 Explorer `SysListView32`에 `ScreenToClient`로 직접 변환해 프레임의 `panelLeft/panelTop`으로 사용한다. 따라서 이동·고정 양쪽이 동일한 물리 픽셀 원점을 사용한다.
- `SysListView32` 탐색 실패 시에만 기존 가상 화면 계산을 안전 폴백으로 유지한다. 저장된 WPF 논리 위치·크기는 전환 때 다시 쓰지 않아 반복 고정에 따른 누적 이동을 막는다.
- 좌표 변환을 `window-position-check.ps1` 회귀 게이트에 추가했고 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-18 — 온하루 등록 필터 한 줄 정리

- 오른쪽 `온하루 등록`의 화면 문구를 `업무일정`→`업무`, `개인일정`→`개인`으로 줄였다.
- 내부 카테고리 키와 기존 일정 데이터는 변경하지 않았다.
- 업무·개인·D-Day·기념일이 줄바꿈 없이 한 줄을 유지하도록 필터 컨테이너를 가로 StackPanel로 정리했다.
- 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-18 — D-Day·기념일 헤더 토글 정리

- D-Day와 기념일 헤더의 별도 `접기/펼치기` 버튼을 제거했다.
- 각 섹션의 제목 영역(`D-Day (n개)`, `기념일 (n개)`) 전체를 클릭하면 접기/펼치기가 토글되도록 통일했다.
- 기념일 `만들기` 버튼은 헤더 오른쪽의 기존 접기 버튼 위치에 배치했다. D-Day는 일반 일정 등록에서 D-Day를 지정하는 기존 흐름을 유지한다.
- 제목을 실제 WPF `Button`으로 구성해 이동 모드뿐 아니라 Explorer 고정 모드의 자동 히트맵에도 포함되도록 했다.
- 전체 자동 품질 검사 13개를 통과했다.

## 2026-08-18 — 이동 전환 투명 중간 프레임 제거 후보 15

- 후보 14 사용자 실측에서 고정 전환은 안정됐고 고정→이동 전환에만 전체 깜빡임이 남아 이동 경로만 수정했다.
- 이동 전환에서 WPF HWND를 `Opacity=0`으로 먼저 uncloak하던 단계를 제거했다. cloak 상태에서 최신 visual을 준비한 뒤 하나의 Render 작업에서 정상 투명도 적용 → uncloak → Explorer 프레임 해제를 연속 처리한다.
- 고정 전환과 날짜 선택 보존 로직은 변경하지 않았다. 13개 품질 검사 및 60fps 전환 contact sheet에서 빈 화면이나 투명 달력 프레임이 없음을 확인했다.
- 후보와 영상은 `Tests/LocalTest/TransitionCandidates/15-cloaked-wpf-preparation`에 보존했다.

## 2026-08-18 — 전환 중 강제 DWM 합성 제거 후보 14

- 사용자 실측에서 날짜 선택 깜빡임은 해결됐으나 달력 전체의 고정/이동 전환 깜빡임이 남아 전환 경로를 다시 분리 점검했다.
- cloak을 제거하고 투명 WPF 창으로 Explorer 입력을 통과시키는 후보 13은 실제 고정 상태 클릭이 전달되지 않아 전면 폐기하고 후보 12 소스로 원복했다.
- 안정 상태의 이동/고정 화면 픽셀은 거의 동일했으므로 잔여 문제를 전환 중간 프레임의 강제 제출로 좁혔다. 고정 진입과 이동 진입에 남아 있던 명시적 `DwmFlush()` 두 곳을 제거해 `Opacity=0`, cloak, 공유 프레임 교대를 DWM이 한 합성 흐름으로 묶도록 했다.
- 날짜 선택 보존, DWM cloak, ACK 없는 전환, `RDW_ERASE` 제거는 그대로 유지했다. 13개 품질 검사와 60fps 전환 녹화를 통과했으며 후보는 `Tests/LocalTest/TransitionCandidates/14-no-forced-dwm-flush`에 보존했다.
- 자동 영상에는 달력 전체가 사라지는 프레임이 없었다. 실제 마우스 체감은 최신 `Tests/LocalTest` 실행본으로 최종 확인한다.

## 2026-08-18 — 과거 안정 기록 재검증 및 전환 회귀 롤백

- 사용자 실기기에서 후보 07도 전체 달력 깜빡임을 제거하지 못했고, 클릭 지연과 잠깐 멈춘 듯한 반응까지 추가된 것을 확인했다.
- 공식 2.0 `HANDOFF.md`의 2026-08-13 기록과 당시 실제 `MainWindow.cs`를 다시 대조했다. 안정판은 WPF 표면을 먼저 `Show → UpdateLayout → Activate`한 뒤 Explorer 프레임을 내렸으며 DWM cloak, 네이티브 ACK 대기, 투명도 0 준비 표면을 사용하지 않았다.
- `MainWindow.ExplorerLayer.cs`를 해당 순서로 복원하고, `ExplorerFramePublisher.cs`와 `DesktopHook.cpp`에서 최근 추가한 그리기 ACK 이벤트를 제거했다. 2.1의 LayerHost 실패 WPF 폴백, 닫기 우클릭 및 신규 기능은 유지했다.
- 새 시험본과 소스 스냅샷을 `Tests/LocalTest/TransitionCandidates/09-restore-20260813-stable-handoff`에 보존했다.
- App/Native 빌드와 13개 품질 검사를 모두 통과했다. 사용자 데이터는 수정하지 않았다.

## 2026-08-18 — 날짜 선택 보존 및 강제 배경 erase 깜빡임 제거

- 후보 09 사용자 실측에서 과거 선택일과 전체 달력 깜빡임이 모두 재발해 단순 2.0 전환 복원을 폐기했다.
- 날짜 선택은 DWM cloak 상태의 live visual과 투명 준비 Render turn으로 보존하되, 클릭 지연을 만든 네이티브 ACK/250ms 타임아웃은 다시 넣지 않았다.
- 60fps 프레임을 전환 구간별 contact sheet로 검사해, `ExplorerFramePublisher.RedrawDesktop`의 `RDW_ERASE`가 달력을 합성하기 전 바탕화면-only 프레임을 노출하는 직접 원인임을 확인했다.
- `RDW_ERASE`를 제거하고 `RDW_INVALIDATE | RDW_UPDATENOW`만 사용하도록 변경했다. 고정/이동 전환 프레임에서 달력이 연속 유지됐으며 서로 다른 날짜 좌표 3회의 스트레스 녹화도 완료했다.
- 후보 11 전체 산출물과 근거 영상은 `Tests/LocalTest/TransitionCandidates/11-no-forced-erase`에 보존했다. 품질 검사 13종 PASS, 앱 응답 정상, 사용자 데이터 변경 없음.

## 2026-08-18 — 고정 상태 클릭 경로 DWM 대기 제거

- 고정 상태에서 날짜·일정 클릭으로 프레임을 갱신할 때 이미 cloak된 WPF HWND에 대해 `DwmFlush()`를 반복하던 불필요한 동기 대기를 제거했다.
- `DwmFlush()`는 WPF HWND가 실제로 uncloaked 상태에서 cloak 상태로 바뀌는 고정 진입에만 실행한다. 날짜 선택 보존, `RDW_ERASE` 제거, ACK 없는 교대 구조는 유지한다.
- 후보 12의 60fps lock/unlock contact sheet에서 달력 표면이 전 구간 연속 유지됐고 이전 선택일·바탕화면-only·이중 밝기 프레임이 관찰되지 않았다.
- 최종 시험본/소스/영상은 `Tests/LocalTest/TransitionCandidates/12-transition-only-dwm-flush`에 보존했다. 13개 품질 검사 PASS.

## 2026-08-18 — 고정/이동 전환을 2.0 안정 구현으로 복원

- 2·3차 DWM 보정에서도 사용자 실측 증상이 동일해 새 보정 코드를 전부 제거했다.
- 안정적이었던 `Onharu_v2/App/MainWindow.cs`의 최종 전환 경로와 2.1을 직접 비교했다.
- 2.1에서 이동 모드를 일반 창처럼 보이게 하려고 추가한 `ShowInTaskbar=false ↔ true` 전환이 창 스타일과 DWM 표면을 재구성해 이전 선택 프레임과 전체 깜빡임을 유발하는 핵심 차이로 확인됐다.
- 이동·고정 모두 `ShowInTaskbar=false`를 유지하고, 고정 해제는 2.0과 동일하게 `explorerFrame.Disable(); Show(); Activate();`, 고정은 `SchedulePublish()`만 수행하도록 복원했다.
- 이동 모드는 `Activate()`로 일반 창처럼 앞으로 오지만 다른 앱의 최상위로 고정되지는 않는다.
- 자동 품질 검사 13종을 통과한 새 `Tests/LocalTest` 시험본을 빌드하고 실행했다.

## 2026-08-18 — 선택 테두리 전환 보완 3차

- 사용자 실측에서 2차 수정도 이동→고정의 전체 깜빡임과 고정→이동의 이전 선택 테두리 노출을 완전히 제거하지 못해 전환을 DWM 합성 단위로 다시 구성했다.
- 고정→이동 시 WPF 창을 `Opacity=0`으로 먼저 깨워 현재 시각 트리를 렌더하고 `DwmFlush()`로 숨겨진 DWM 표면 갱신을 완료한다. 이후 Explorer 프레임 비활성화와 원래 투명도 복원을 같은 합성 흐름에서 처리한다.
- 이동→고정 시 Explorer 프레임 게시와 WPF 숨김 후 `DwmFlush()`로 합성 완료를 기다려 중간 빈 프레임을 줄였다.
- 전체 달력 `RenderAll()`은 사용하지 않으며 선택일 상태도 변경하지 않는다.
- 자동 품질 검사 13종을 통과한 최신 `Tests/LocalTest` 시험본을 다시 빌드하고 실행했다.

## 2026-08-18 — 선택 테두리 전환 보완 2차

- 1차의 `RenderAll()` 재구성 방식은 사용자 실측에서 이전 선택 테두리 깜빡임이 남고 전체 화면 깜빡임까지 추가되어 철회했다.
- 고정→이동 전환 시 전체 달력을 다시 만들지 않는다. 현재 WPF 시각 트리만 `InvalidateVisual()`하고 숨겨진 창의 Render 우선순위 작업을 완료한 다음 Explorer 프레임을 비활성화한다.
- 이동→고정 및 트레이 고정 경로에 추가했던 전체 재렌더링도 제거해 기존 안정적인 프레임 게시 방식으로 복귀했다.
- 새 전환 순서를 회귀 검사에 반영했고 전체 자동 품질 검사 13종을 통과했다. 최신 시험본은 `Tests/LocalTest`에 생성했다.

## 2026-08-18 — 고정/이동 전환 시 선택 날짜 테두리 깜빡임 보완

- 고정→이동 전환에서 Explorer 프레임을 먼저 비활성화해 숨겨진 WPF 창의 이전 선택 테두리가 한 프레임 노출되는 순서 문제를 수정했다.
- 최신 `selectedDate`로 WPF 달력을 다시 구성하고 레이아웃을 확정한 뒤 창을 표시하며, 준비된 WPF 화면이 나타난 다음 Explorer 캐시를 비활성화하도록 순서를 변경했다.
- 이동→고정 및 트레이 위치 고정 경로도 `RenderAll()`로 최신 선택 상태를 확정한 다음 Explorer 프레임을 게시한다.
- 해당 전환 순서를 `feature-pack-check.ps1` 회귀 검사에 추가했고 전체 자동 품질 검사 13종을 통과했다.
- 최신 시험본은 `Tests/LocalTest`에 다시 빌드했다. 설치본이 실행 중이므로 실행 중인 프로그램은 종료하거나 교체하지 않았다.

## 2026-08-18 — GitHub 2.1 소스·사용자 설명서 갱신

- 공개 저장소의 기존 V1 단일 파일 소스를 현재 2.1의 `App`, `ExplorerLayer`, `Installer` 구조와 제품·빌드 문서로 교체하는 커밋 `8297d8b`를 게시했다.
- README 상단에 2.1.0 다운로드 링크, 지원 환경과 미서명 SmartScreen 안내를 추가하고, 현재 Explorer 레이어·8방향 크기 조절·종료 동작에 맞게 오래된 설명을 수정했다.
- 실제 `OAuthCredentials.local.cs`, Client Secret, Release/Tests, 실행 바이너리와 사용자 데이터는 공개 커밋에서 제외했다.
- PR: `https://github.com/cowpigpapa/onharu/pull/2`.
- GitHub API와 웹 UI 모두 `503 / Merge status cannot be loaded` 상태여서 커밋은 원격 브랜치에 안전하게 보존하고 병합만 대기한다.

## 2026-08-18 — GitHub 2.1 정식 릴리스 게시

- 공개 저장소 `cowpigpapa/onharu`에 최신 정식 릴리스 `v2.1.0`을 게시했다.
- 릴리스 URL: `https://github.com/cowpigpapa/onharu/releases/tag/v2.1.0`
- 첨부 파일: `ONHARU-2.1.0-Setup.exe`, `ONHARU-2.1.0-Setup.zip`, `SHA256SUMS.txt`.
- 설치 EXE SHA-256: `638C1117362E20A1ED14CFC8A686743D6E3B1294DD6754E0026E7FC186713409`.
- ZIP SHA-256: `F114108BE5146F2B362F8EE9265E4F0A506E781D2DCAD034055877ABA4F5F80F`.
- OAuth 로컬 자격 증명과 개발 소스는 이번 릴리스 자산에 포함하지 않았다.

## 2026-08-18 — 프로젝트 시험 산출물 정리

- 시험 산출물을 `Tests/LocalTest`, 외부 검토 패키지를 `Tests/Review`로 통합했다.
- `Release`는 정식 배포 스테이지와 설치 파일만 남기는 구조로 정리했다.
- 재생성 가능한 `Release/BuildTemp` 후보 59개, 구버전 `Release/FeatureTest` 후보 전체, 중복 압축 해제된 Claude 검토 폴더를 삭제했다.
- 로컬 빌드와 자동 검사 13종의 기본 경로를 `Tests/LocalTest`로 변경했다. 사용자 일정·설정과 정식 설치 파일은 수정하지 않았다.

## 2026-08-16 — 2.1 개발 시작

- ONHARU 2.0을 동결하고 `Onharu_v2.1`을 별도 개발 작업공간으로 생성했다.
- 복제 범위: App, ExplorerLayer, Installer, Docs, 핵심 제품/빌드 문서.
- 제외 범위: Release 생성물, Archive, Diagnostics, Promo. 2.0 배포본과 소스는 수정하지 않는다.
- 2.1 우선순위와 완료 조건은 `V2.1_ROADMAP.md`에 정리했다.
- 현재 네이티브 IPC·사용자 데이터 폴더의 내부 `V3` 명칭은 2.0 데이터 호환을 위해 유지한다. 2.0과 2.1을 동시에 실행하지 않는다.

## 2026-08-16 — 2.1 1차 기능 구현

- 제품·파일·설치 버전을 2.1.0으로 변경하고 설정 스키마를 12까지 순차 마이그레이션했다.
- 맞춤 알림: 분/시간/일 단위 직접 입력을 추가하고 기존 `ReminderMinutes`를 그대로 사용해 알림·Google 경로를 유지했다.
- 새 일정 기본값: 기본 캘린더, 하루종일/시간 지정, 시작 시간, 소요 시간, 기본 알림을 설정에 추가했다. 기존 일정 수정은 원래 시간과 소요 시간을 유지한다.
- 달력 제목을 클릭하면 연도·월을 직접 선택해 이동하는 ONHARU 스타일 팝업을 추가했다.
- 완료 일정 표시를 그대로/흐리게/숨김 중 선택하며 달력과 오른쪽 상세에 함께 적용한다.
- 반복 일정에 N회 종료를 추가하고 로컬 생성 횟수와 Google RRULE `COUNT`를 일치시켰다.
- D-Day 표시를 추가했다. 기존 매년 반복과 조합할 수 있고 달력·상세에 D-Day/D-n/D+n으로 표시한다.
- 검색 기간을 오늘 기준 ±1년/과거 1년/앞으로 1년/사용자 지정/전체 일정으로 선택할 수 있다.
- 시작 화면은 오늘/마지막 날짜, 시작 위치 상태는 이전 상태/항상 고정/항상 위치 조정 중 선택한다.
- 알림 소리와 조용한 시간(기본 22~07시)을 추가했다. 조용한 시간에도 팝업은 유지하고 소리만 끈다.

## 2026-08-16 — 2.1 안정화 및 검증 자동화

- xDiarys 실측 프로세스 `desktopcal.exe`, `dkdockhost.exe`가 실행 중이면 시작을 막지 않고 트레이 풍선으로 동시 실행 위험을 안내한다.
- 검색 기간에 사용자 지정 시작일·종료일을 추가했다. 날짜를 역순으로 선택해도 내부에서 정렬하며 종료일을 포함한다.
- 오래되거나 손상된 설정 파일의 열거값, 시간, 주 수, 일정 기본 시간을 안전한 범위로 정규화해 설정 창의 선택 항목 누락 예외를 방지했다.
- Google JSON DTO 필드에 명시적 기본값을 지정해 동작 변경 없이 배포 빌드의 CS0649 경고를 제거했다.
- 각 검사 스크립트의 기본 실행 파일을 `Release\LocalTest\ONHARU-2.1-local-test.exe`로 통일했다.
- 루트 `check-v21.ps1`을 추가했다. `-Build` 옵션으로 앱·x64 레이어를 빌드한 뒤 버전, 기능팩, 2.0→2.1 백업, 반복, 다일 일정, 내보내기, 동기화 보안, 창 위치, 오류 로그, OAuth 검사를 연속 실행한다.
- 2.1 README, 프로젝트 인덱스, 빌드 문서의 활성 버전·산출물 경로를 2.1.0 기준으로 갱신했다. 내부 `V3` 명칭은 2.0 데이터·IPC 호환을 위해 유지한다.
- 2.1 최초 실행 직전에 기존 `settings.json`과 `items-*.json`을 `%LOCALAPPDATA%\OnharuV3\pre-2.1-backup`에 한 번만 보존한다. Google 토큰은 복사하지 않는다. 임시 폴더 자동 시험으로 복사·토큰 제외·재실행 시 미덮어쓰기를 확인했다.
- 로컬 반복 시리즈 전체 수정에서 종일 여부, Todo 여부, 자동 넘김 방식이 형제 일정에 누락되던 부분을 보완했다. 회차별 완료 여부는 유지한다.
- Google에서 일정을 다시 읽을 때 직접 알림(예: 2시간 전, 2일 전)이 기존 프리셋 정규화 때문에 10분/없음으로 바뀌던 문제를 수정했다. 0~999일 범위를 보존하며 시간·종일 직접 알림 왕복 검사를 추가했다.
- 직접 알림과 반복 횟수 입력을 숫자 3자리로 제한했다. 알림 소리를 끄면 조용한 시간 선택도 함께 비활성화해 설정 관계를 직관적으로 표시한다.
- 연·월 이동 팝업에 연도 오류 안내와 Enter 이동/Esc 취소를 추가했다.
- 연·월 이동 상한을 9998년으로 제한하고 이전/다음 이동도 1900~9998 범위에서 멈추게 해 6주 렌더링의 `DateTime` 범위 초과를 막았다.
- 이전 형식·수동 편집 JSON을 읽을 때 잘못된 종료 시간을 30분/1일로 복구하고 반복 횟수와 직접 알림을 정규화한다. 날짜 계산 안전 범위 밖의 항목은 전체 파일을 실패시키지 않고 제외한다.
- README의 과거 `Step3-Distribution`, 고정 ±1년 검색, `%LOCALAPPDATA%\FamilyPlanner` 설명을 현재 2.1 구조와 기능으로 교정했다. V1 PRD에는 역사적 기준 문서라는 안내를 추가했다.
- 설치 설정과 정식 빌드 스크립트의 2.1 버전·AppId·SHA256 경로를 확인하는 자동 검사를 추가해 전체 검사 수는 11종이다.
- 검색의 `과거 1년`이 미래 전체를, `앞으로 1년`이 과거 전체를 포함하던 범위 조건 오류를 수정했다. 과거/미래/±1년/역순 사용자 지정의 시작·종료 경계를 독립 자동 검사로 검증하며 전체 검사 수는 12종이다.
- 사용자 지정 기간이 오늘을 포함하지 않으면 결과 목록에 범위 밖의 `오늘` 구분선을 삽입하지 않는다.
- xDiarys 충돌 감지는 시작 시뿐 아니라 1분 간격으로 확인하며, 실행 중 새로 감지됐을 때 한 번만 안내하고 종료 후 상태를 초기화한다.
- 2.0 동결본과 해시 비교 결과 `ExplorerLayer`의 C++/헤더/빌드·중지 스크립트 6개, App의 입력·프레임 합성 핵심 파일 5개가 모두 동일하다. 2.1 기능 작업은 안정화된 아이콘 합성·깜빡임 경로를 변경하지 않았다.
- Excel 전체 일정 내보내기에 D-Day 여부, 반복 방식·종료, 알림 분 값을 추가했다. JSON 가져오기용 형식은 그대로 유지하고 CSV 수식 주입 방어도 회귀 검사했다.
- 사용자 화면이나 데이터를 열지 않는 STA UI 구성 검사를 추가했다. 설정, 새 일정, 빈 검색, 연·월 이동 창을 실제 생성·닫아 생성자 예외와 고정 크기 회귀를 확인하고 반복 없음/매주 상태의 종료 컨트롤 활성화도 검증한다.
- `반복 없음` 상태에서 횟수 종료만 선택해 의미 없는 `RecurrenceCount`가 저장될 수 있던 조합을 막았다. 반복 종류가 있을 때만 횟수·종료일을 활성화하고 저장 시에도 빈 반복은 횟수를 0으로 강제한다.
- 설정 파일의 글자 크기, 자동 동기화 간격, 일정 정렬, 주차 규칙, 투명도가 UI 지원값을 벗어나면 안전한 기본값으로 복구한다. `jan1` 일요일 시작은 정상값으로 보존한다.
- 최신 검증 결과: 경고 없는 C# 빌드, x64 네이티브 레이어 빌드, 자동 검사 13종 모두 통과.
- 최신 로컬 시험본: `Release\LocalTest\ONHARU-2.1-local-test.exe`, 파일 버전 2.1.0.0, SHA-256 `9987F0D53FBA38BE849A1750EA68251B1030BCF3B42BADEA76E7693AD926024B`.
- 수동 확인 필요: 실제 Explorer 고정/이동 전환, 아이콘 클릭·드래그, Google 로그인·왕복 동기화, 신규 설정 화면의 정렬과 잘림. 2.0과 2.1은 동시에 실행하지 않는다.

## 2026-08-16 — 직접 시험 준비 보완

- 실제 동시 실행에서 충돌이 관찰되지 않았다는 사용자 확인에 따라 xDiarys 프로세스 감지, 트레이 경고, 1분 주기 검사를 제거했다.
- 위치 조정 상태의 네 모서리와 상·하·좌·우 가장자리 크기 조절 코드를 재확인하고 자동 회귀검사에 8방향 매핑을 추가했다.
- 최신 소스로 로컬 시험본을 다시 빌드한 뒤 자동 검사와 실제 UI 시험을 진행한다.
- 최신 빌드에서 자동 검사 13종과 팝업 실제 생성·종료 검사를 모두 통과했다.
- 200×120 WPF 표면의 네 모서리 및 상·하·좌·우 좌표를 `ResizeEdgeAt`에 직접 넣어 1~8 방향 판정을 검증했다. 해당 구현 1,600자는 2.0 최종본과 완전히 동일하다.
- 실제 프로세스를 8초 유지한 시작 스모크 테스트에서 앱이 응답 상태로 생존함을 확인하고 시험 프로세스를 종료했다.
- 고정 모드의 메인 달력은 Explorer 내부 합성 레이어라 일반 창 자동화 목록에 노출되지 않는다. 아이콘 클릭·드래그, 실제 가장자리 드래그, Google 왕복은 화면에서 수동 확인이 필요하다.
- 최신 로컬 시험본 SHA-256: `F03463B2491063AC2D2BCB0D8FBDF787079396CA8A70863E49AE3ED5E28927D0`.
- 실제 실행에서 스키마 12 설정 파일에 새 `LastShownDate` 필드가 없으면 `DateTime.MinValue`가 되어 UTC JSON 직렬화가 실패하는 시작 종료 문제를 확인했다. 로드·저장 경계에서 오늘 날짜로 복구하고 누락 필드 회귀검사를 추가했다.

## 2026-08-17 — Google 생일 D-Day 동기화 복구

- Google `birthday` 특수 일정은 private extended property를 허용하지 않아 D-Day 저장 시 `eventTypeRestriction`이 반복되던 원인을 확인했다.
- Google `eventType`을 일정에 보존하고, 생일 일정의 D-Day 등 ONHARU 표시 속성은 로컬에만 유지한다. Google 생일 원본은 수정하지 않는다.
- 기존 실패 대기 항목은 다음 동기화에서 Google 오류 응답을 생일 일정으로 식별해 대기 상태를 해제하고 나머지 동기화를 계속한다.
- 실제 계정은 Google 캘린더 3개를 각각 순차 다운로드하고 있어 대기 복구 후에도 동기화가 느렸다. 캘린더별 다운로드를 동시에 시작하고 결과 병합·삭제 판정은 기존 순서대로 유지하도록 최적화했다.
- 고정 모드에서 동기화 완료 후 WPF 버튼만 복원되고 Explorer 프레임은 `동기화 중…`으로 남아, 달력을 클릭해야 갱신되던 문제를 수정했다. 모든 동기화 종료 경로에서 버튼 복원 후 고정 프레임을 즉시 다시 게시한다.

## 2026-08-17 — 기념일 카드 1차 시안

- `★ 중요 일정`과 `D-Day 표시`를 함께 선택한 일정만 오른쪽 상세 하단의 보라색 파스텔 `기념일` 카드에 모아 표시한다.
- 일정 편집창에 두 옵션을 모두 선택했을 때만 `기념일 기준일` 선택란을 표시한다. Google 생일처럼 현재 연도 발생일만 내려오는 일정도 실제 출생일·입사일을 로컬 기준일로 지정할 수 있다.
- 카드에는 제목에 따라 `태어난 지`, `입사한 지`, `함께한 지` 누적 일수와 다음 생일·입사기념일·결혼기념일까지 남은 일수를 함께 표시한다.
- 윤년 2월 29일은 비윤년에 2월 28일을 기념일로 계산한다. 중복 반복 회차는 시리즈별 카드 하나로 합친다.
- 로컬 JSON·Excel 내보내기와 반복 시리즈 생성/전체 수정에서 `기념일 기준일`이 보존되도록 연결했다.
- 일반 Google 일정은 `onharuAnniversaryDate` 비공개 메타데이터로 기준일을 왕복한다. Google `birthday` 특수 일정은 비공개 속성을 금지하므로 기존 원칙대로 ONHARU 로컬 값만 유지한다.
- 수동 Google 동기화 완료 문구에 실제 소요시간(초)을 함께 표시해 병렬 다운로드 개선 효과와 지연 재발을 사용자가 바로 판단할 수 있게 했다.
- 기념일 날짜 계산, 윤년, UI 조건부 표시, 내보내기, Google 메타데이터를 자동 검사에 포함했으며 분리 후보 `Release\BuildTemp\ONHARU-2.1-anniversary-card-test.exe`로 13종 검사를 모두 통과했다.
- 실행 중인 기본 시험본은 강제 종료하거나 덮어쓰지 않았다. 사용자 종료 후 검증된 후보를 `Release\LocalTest\ONHARU-2.1-local-test.exe`로 승격해야 한다.
- 사용자 종료 확인 후 검증 후보를 기본 시험본으로 승격했다. 승격본에서 자동 검사 13종을 다시 통과했으며 SHA-256은 `9CE7AEE691521539E49EBF5554F4E4BA93BDC6F965EB740338537024B1D6B344`이다.
- Codex 터미널에서 직접 시작한 시험본은 사용자 Explorer와 다른 Windows 데스크톱 컨텍스트를 상속할 수 있어 앱만 살아 있고 LayerHost가 `Progman`을 찾지 못하는 현상을 재확인했다. 이것은 배포 실행 결함이 아니라 개발 도구 실행 컨텍스트 문제이므로 화면 시험본은 사용자가 Explorer에서 직접 실행한다.
- 해당 현상을 Explorer 계층 변경으로 오인해 시도한 `LayerHost.cpp` 수정은 즉시 전부 철회했다. 2.1 파일 SHA-256이 2.0 안정본과 동일함을 확인하고 x64 레이어를 다시 빌드·배치했다.
- 위치 조정 모드에서는 Explorer 프레임을 끄고 WPF 일반 창으로 표시하며 작업 표시줄에도 노출한다. ONHARU를 클릭하면 앞으로 오고 다른 앱을 클릭하면 그 아래로 내려가는 표준 Z-order를 사용한다. 고정 시 작업 표시줄 노출을 해제한 뒤 기존 Explorer 아이콘 아래 프레임으로 복귀한다. 상단 버튼·Explorer 액션·트레이 메뉴·시작 설정의 모든 진입 경로를 같은 동작으로 통일했다.
- 검증 후보를 기본 `Release\LocalTest\ONHARU-2.1-local-test.exe`로 승격하고 자동 검사 13종을 재통과했다. SHA-256은 `4623A762ADBD2BF19AC163CFF171434D24E4307BBCDCC72121381018EA10E756`이다.
- 이동 가능한 상단 영역의 `DragMove()` 직후 창을 강제로 바닥에 내리던 `DesktopLayer.Lower(this)` 호출을 제거했다. 이동 모드의 `Topmost`가 드래그 후에도 유지되어 바탕화면을 다시 클릭해야 올라오던 불안정성을 해소한다.
- 수정본을 기본 LocalTest로 승격하고 자동 검사 13종을 재통과했다. SHA-256은 `8ED1388AD3F2FFF2375F22ECAD784468C90CB922F171F95A9153467DB5588225`이다.
- 상단 및 고정 레이어의 `×`는 즉시 트레이로 최소화하도록 변경하고 툴팁을 `트레이로 최소화`로 명확히 했다. 실제 종료는 트레이 메뉴의 `종료`에서만 기존 확인창을 거쳐 수행한다.
- 일반 창 Z-order와 트레이 우선 닫기 수정본을 기본 LocalTest로 승격하고 후보·승격본 모두 자동 검사 13종을 통과했다. SHA-256은 `62A715924F58419FE7F34E4A5EF866D58813B9F36623DBAB3749BA5A45E02762`이다.
- 트레이/고정/이동 상태와 종료 확인창을 위해 잠시 보인 WPF 창 상태를 분리했다. 종료 취소 시 트레이에서 시작했으면 트레이로, 고정 화면에서 시작했으면 Explorer 고정 화면으로, 이동 화면에서 시작했으면 일반 창으로 정확히 복귀한다. 종료창 표시 중 `calendarMinimized`를 임시 해제해 이후 고정 동작이 화면 표시 상태를 최소화로 오인하지 않게 했다.
- 종료 상태 복원 수정본을 기본 LocalTest로 승격하고 기능팩 검사를 재통과했다. SHA-256은 `3CB2E83E999A01F1997AC1FCCCFC98773A5E960531B5C3DD95BAF068925BBFCB`이다.
- D-Day 데이터 등록 수는 제한하지 않고 오른쪽 기념일 카드만 다음 기념일이 가까운 순서로 기본 5개 표시한다. 초과분은 `+ N개 더보기`로 펼치고 `접기`로 다시 5개 상태로 돌아온다.

## 2026-08-17 — 팝업 강조 및 × 버튼 동작 정리

- 메인 배경과 팝업이 섞여 보이지 않도록 설정, 일정 등록, 검색, 가져오기, 백업, 동기화 대기, 알림, 종료 등 주요 팝업에 2px 인디고 테두리와 절제된 그림자를 공통 적용했다. 팝업 내부 여백과 크기는 바꾸지 않았다.
- 설정 스키마를 13으로 올리고 `× 버튼 동작`을 추가했다. 기본값은 `트레이로 최소화`이며 사용자가 `종료 확인`으로 변경할 수 있다. 기존 설정은 자동으로 기본값을 받는다.
- 이동 모드와 Explorer 고정 모드 모두 `×` 왼쪽 클릭은 설정값을 따르고, 오른쪽 클릭은 `취소 / 트레이로 최소화 / 종료` 메뉴를 연다. 우클릭 메뉴의 종료는 즉시 종료한다.
- 트레이 메뉴의 `종료`는 확인 팝업 없이 즉시 종료하도록 단순화했다. 종료 확인 팝업은 `× 버튼 동작`을 `종료 확인`으로 선택한 경우에만 사용한다.
- Explorer 고정 레이어에 닫기 버튼 전용 우클릭 hit kind와 action 28을 추가했으며, 네이티브 x64 레이어를 다시 빌드했다.
- 기념일 카드 기본 5개 표시 변경도 이번 시험본에 함께 포함했다.
- 앱 UI 구성, 기능팩, 2.0→2.1 설정 마이그레이션 검사를 통과한 뒤 LocalTest로 승격했다.
- 최신 LocalTest SHA-256: 앱 `D29FE9C8FFBE338CBE1DFF7759A406F0146013FB8D8897B7697CB1A4FEA91E5F`, 훅 DLL `22FCFBE1A2CA409517D03FB8C720ED7A0FD1D66F7E1167E92B1C763C83B44D88`, LayerHost `AA83941EBB6D62AD6D339A5B4C4E23C33FED402C38E3EE0796100A44396258C7`.
- `×` 우클릭 메뉴를 둥근 인디고 테두리, 파스텔 선택 배경, 손 모양 커서로 다듬었다. 일반 WPF 메뉴의 외부 클릭 자동 닫힘 동작은 유지한다.
- Explorer 고정 모드에서 `WM_RBUTTONDOWN`뿐 아니라 `WM_RBUTTONUP`과 `WM_CONTEXTMENU`까지 닫기 버튼 영역에서 소비해 ONHARU 메뉴와 바탕화면 메뉴가 동시에 열리지 않도록 수정했다.
- 우클릭 메뉴 수정 LocalTest SHA-256: 앱 `58A84C1D985B8E2F599FFE88069823E1462576675AAB5A7E2B13840C464871AA`, 훅 DLL `0E3B4C4856384EF2C0714DB3D876131B5386E25679B753EB92CBFA7FC05F9388`.
- `중요한 날` 색상 팝업을 추적하고 Explorer 고정 화면의 다른 곳이나 바탕화면 아이콘을 클릭할 때 action 29로 닫도록 보완했다. 원래의 `StaysOpen=false`도 유지해 이동 모드 외부 클릭과 동일하게 동작한다.
- 기념일 카드는 `★ 중요 일정` 여부와 무관하게 `D-Day 표시` 자체를 기준으로 표시한다. 중요 체크는 달력 강조 표현에만 사용한다.
- 과거 D-Day 일정인데 기념일 기준일이 오늘 이후로 잘못 남은 데이터는 시작일을 기준일로 안전하게 복구한다. 실제 `서아 태어난 날`의 시작일 2008-01-18 / 잘못된 기준일 2026-08-19 사례를 대상으로 한 보완이다.
- 기념일 기준일 입력란은 D-Day를 켜는 즉시 표시되도록 단순화했다.
- 앱 UI 구성, 기능팩, 마이그레이션 검사를 통과했다. 최신 LocalTest SHA-256: 앱 `82FA10897B19333852AEF67BABBA989BFAC043A03B100FD03C54554E7A42DDF5`, 훅 DLL `2A035DFDB808EE18685EB10B3F3F496A1FEF2669AC813949C2380EFDAF18DE18`.
- 오른쪽 기념일 영역에 `＋ 기념일 등록`을 추가하고 생일/결혼기념일/입사기념일/기타, 이름, 최초 기준일, 중요 강조를 입력하는 전용 팝업을 구현했다.
- 전용 기념일은 Google이 아닌 ONHARU 로컬 개인 일정으로 저장하며 최초 기준일부터 100년 범위의 매년 반복 일정을 구성한다. 일반 종일 로컬 일정을 Todo로 바꾸는 기존 정규화에서 전용 기념일은 제외한다.
- 기념일 카드 한 번 클릭은 올해 아직 남아 있는 날짜 또는 다음 해 발생일로 달력을 이동한다. 전용 기념일 카드 더블클릭은 전용 수정창을 열며 기존 일반 D-Day 카드는 기존 일정 수정창을 유지한다.
- 기념일 요약은 줄바꿈을 허용해 `다음 기념일까지` 문구가 오른쪽 패널에서 잘리지 않게 했다.
- 앱 UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `D8B4E497B28910F29CC3F762B21322DF189F1A4895C64FD96F5E0088B84C537C`.
- 기념일 전용 등록에서 `중요한 날로 강조` 선택을 제거하고 새 기념일은 `Important=false`로 저장해 기념일과 중요 일정을 분리했다.
- 기념일 목록의 카드 안 카드 구조를 없애고 구분선이 있는 한 줄 목록으로 압축했다. `제목 · 출생 +N일 · 생일 D-N` 형식과 말줄임/툴팁으로 좁은 사이드바에 대응한다.
- 오른쪽 일반 일정 카드의 항목 상하 간격, 카드 패딩, 카드 사이 간격을 소폭 줄여 표시 밀도를 높였다.
- 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `2080B988A04D837E6F51DBC3B095908596183281D0CD1D2E4E263A57D8E7AB9D`.
- 기념일 등록창을 ONHARU 공통 팝업 스타일로 다시 구성했다. 로컬 저장 안내, 카드형 종류/이름/기준일 입력, 파스텔 라디오 선택, 둥근 날짜 버튼·달력 팝업, 정돈된 취소/저장 버튼을 적용했다.
- 큰 `기념일 등록` 버튼을 제거하고 기념일 제목 오른쪽의 작은 `＋` 버튼과 툴팁으로 교체했다.
- 기념일 한 번 클릭의 달력 날짜 이동을 제거했다. 더블클릭 수정만 유지해 기념일 영역을 등록·조회 중심으로 단순화했다.
- 앱 UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `465174BE11E8F5F679FB29754153929956B7F40D5BF534EF2C9D3A3E4E826B1A`.
- 기념일 목록에서 생일/결혼/입사 종류 명칭을 반복 출력하지 않고 `제목 · +누적일 · D-남은일`로 통일했다. 당일은 `D-Day`로 표시한다.
- 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `FB794053203886066F5F75D7479586806B1B85FCF95D6C7527BED27B36133E74`.
- 기념일 제목 오른쪽 `＋`가 잘리던 문제를 피하기 위해 24×24 보라색 `+` 버튼으로 다시 설계하고 오른쪽 안전 여백을 늘렸다.
- 기념일 등록창의 중첩 입력 카드를 제거하고 하나의 정돈된 폼 패널로 재구성했다. 종류는 파스텔 pill 선택, 이름은 전체 폭 입력칸, 기준일은 둥근 날짜 버튼으로 표시하며 각 항목에 짧은 안내를 추가했다.
- 팝업 폭과 좌우 안전 여백을 늘려 이름 입력칸과 오른쪽 테두리가 잘리지 않도록 했다.
- 앱 UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `CFC234FB52F8DCF1C5AE339EA01A10C51C528C2BCA41D1E9EF9F89BBFC4EA3F8`.
- 기념일 종류에서 입사를 제거하고 생일/결혼기념일/기타만 유지했다. 기존 `employment` 전용 항목을 수정할 때는 기타로 안전하게 매핑한다.
- 작은 `+` 버튼의 둥근 템플릿에 가로·세로 중앙 정렬과 `Segoe UI Symbol`을 명시해 기호가 우측으로 밀리거나 잘리던 원인을 제거했다.
- 기념일 팝업의 닫기 버튼을 32×32 연분홍 원형 계열 버튼으로 바꾸고 중앙 정렬, 테두리, 툴팁을 적용했다. 모든 팝업 하단 버튼도 중앙 정렬을 명시했다.
- 앱 UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `E84170C21B66C6DD4A57D8431254AC2728310DF0F4F74B40BF35D4494C083C2A`.
- 기념일 전용 등록창에 `남은 날짜를 D-Day로 표시` 체크를 추가했다. 해제한 기념일은 누적 `+N일`만 표시한다.
- 기념일은 기본 5개만 표시하고 더보기로 펼치되 등록 자체는 최대 10개로 제한했다. 팝업 상단에 `최대 10개 · ONHARU 로컬 저장`을 표시하고, 초과 추가 시 기존 기념일을 더블클릭해 삭제하라는 안내를 띄운다.
- 전용 수정창에 `삭제` 버튼을 추가했다. 시리즈로 생성된 로컬 기념일은 전체 시리즈를 함께 삭제한다.
- 기념일 한 번 클릭 이벤트를 항상 소비해 위쪽 선택일·일정 카드가 바뀌지 않도록 했다. 더블클릭 수정만 유지한다.
- 일반 일정 등록·수정에서는 `기념일 기준일` 입력 영역을 제거했다. 기념일 기준일과 종류는 전용 기념일 팝업에서만 관리한다.
- 등록창 강조색을 보라 단색에서 연분홍 안내, 라벤더 테두리와 핑크-라벤더 선택 배경으로 분산했다.
- 앱 UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `AC0B4734B08907F560F7D537308CE0DD66AFB0A40E2D10BC76042A6CD47FB184`.
- 일반 일정 D-Day와 기념일을 분리했다. 일반 일정의 D-Day는 시험·마감 등 일정일까지 표시하는 기능으로 남고 기념일 목록에는 `AnniversaryType`이 있는 전용 기념일만 표시한다.
- 전용 기념일을 달력 격자, 오른쪽 일정, 검색 결과 등 어느 경로에서 더블클릭해도 일반 일정 수정창이 아니라 동일한 `기념일 수정` 팝업을 연다. 여기서 종류, 최초 기준일, D-Day 표시와 삭제를 관리한다.
- 기존 버전에서 일반 D-Day 방식으로 저장한 생일·결혼·기타 기념일은 최초 1회 생일/결혼/기타로 분류해 기존 기념일 카드가 사라지지 않게 했다. 이후 새 일반 D-Day 일정은 자동 분류하지 않는다.
- Google 반복 기념일의 기존 로컬 회차를 전용 기념일로 수정할 때 동일 Google 반복 ID의 캐시 회차를 함께 정리하고 ONHARU 로컬 기념일로 재생성한다.
- 앱 UI 구성, 기능팩, 마이그레이션 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `746859DCE4F5B1A397F8A44A378E3C888487BDCECBA03B58140929FB68D1C55F`.
- 달력에서 기념일이 있는 날짜를 선택해도 전용 기념일 항목은 오른쪽 일반 일정 카드에 표시하지 않는다. 기념일은 하단 기념일 목록에만 유지하고 같은 날짜의 일반 일정만 상세 카드에 표시한다.
- 기념일을 `개인일정`과 분리된 내부 카테고리 `기념일`로 저장하고 라벤더 색상 `#A78BFA`를 사용한다. 별도 사이드바 필터는 추가하지 않아 화면 복잡도는 늘리지 않았다.
- 기존 전용 기념일의 카테고리도 로드 시 `기념일`로 정규화한다.
- 헤더 추가 컨트롤을 Button에서 투명 Border + 중앙 정렬 `＋` TextBlock으로 교체해 버튼 크롬·패딩의 영향을 완전히 제거했다.
- 앱 UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `DF299503E22D2C3C9142FD37FF85DB05449659AA870AF6CFB0B8E6FA73DB627A`.
- Explorer 고정 레이어의 `HitTestDesktop`에서 기념일 항목을 일반 `PlannerItem`보다 먼저 분기한다. 한 번 클릭은 아무 상태도 바꾸지 않고, 더블클릭만 기념일 수정창을 열어 위쪽 선택 날짜·일정 카드가 바뀌지 않는다.
- 글자형 `＋` 컨트롤에 `open_anniversary` 태그를 부여하고 고정 레이어 히트테스트에 연결했다. WPF 이동 모드와 Explorer 고정 모드 모두 같은 등록 팝업을 연다.
- 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `2FF89D02E364F170EAEE4FE8C7742B79AFEFF0E27310CBD222A69FF6C3150862`.
- 고정 레이어에서 글자형 Border `＋`의 좌표 히트가 누락되는 환경을 확인해, 외형은 투명한 보라색 `＋`로 유지하면서 내부 컨트롤을 네이티브 프레임이 확실히 인식하는 WPF Button으로 교체했다.
- 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `A80C621DBA6E4801712272C22A029DB4B74EFC3A652EE43A34318CC023BB9594`.
- `+` 버튼의 공통 둥근 템플릿을 제거하고 ContentPresenter 하나만 가진 전용 무배경 템플릿으로 교체했다. 전각 `＋` 대신 Segoe UI의 일반 `+`를 사용해 작은 영역에서 글리프가 찌그러지는 문제를 제거하면서 실제 Button 히트테스트는 유지한다.
- 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `37C3A22E518E1DC698316A5BA552F3D23FCA5BEEB9ECFB08132DC66A3D3AB5A0`.
- 연·월 바로 이동 팝업을 재설계했다. 390px 폭, 설명이 포함된 헤더, 연도 입력 카드, 4×3 파스텔 월 선택 버튼, 공통 연분홍 닫기 버튼과 정렬된 취소/이동 버튼을 적용했다.
- 기본 ComboBox를 제거해 12개월을 한눈에 선택할 수 있게 했고 현재 월은 라벤더 배경과 보라 테두리로 강조한다. 기존 Enter 이동, Esc 취소, 1900~9998 검증은 유지한다.
- 앱 UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `5C70E3B0CBA50AB68877EEC25233993966073E7F04DD5C67DC41C4E5F39A05F3`.
- 달력의 전용 기념일 D+를 오늘 기준이 아니라 최초 기준일→해당 연도 발생일의 실제 경과일로 변경했다. 최초일은 `제목 D+0`, 다음 연도는 윤년을 포함한 실제 누적일로 표시한다.
- 기념일의 D-Day 표시를 끄면 달력에는 제목만 표시한다. 시험·마감 같은 일반 일정의 기존 오늘 기준 D-Day 계산은 변경하지 않았다.
- 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `27FF38BD69512CB936D94E63D3AC98AFFA05C0F91733E4D1D3D1AACD100307F3`.
- 오른쪽 `온하루 등록` 필터에 `기념일` 체크박스를 추가했다. 업무일정·개인일정과 동일한 방식으로 기념일 표시 여부를 즉시 전환하며 설정에 영구 저장한다.
- 기존 사용자 설정에서 기념일이 뜻하지 않게 숨겨지지 않도록 설정 버전을 14로 올리고 `AnniversaryVisible = true` 마이그레이션을 추가했다.
- 사용자 지정 주 범위 제목에 연도를 추가했다. 같은 연도는 `2026년 8월 9일 – 9월 5일`, 연도를 넘으면 시작일과 종료일에 각각 연도를 표시한다. 월 전체 제목 형식은 기존대로 유지한다.
- 앱 UI 구성, 기능팩, 설정 마이그레이션 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `0EC55CD4D498002D1305783FCFBD982BB8B12EA1E522EA789D4CD066106E55BB`.
- Google 읽기 전용 일정은 오른쪽 카드에서 한 번 클릭해도 즉시 수정 불가 안내를 표시하도록 고정 레이어와 WPF 이동 모드의 클릭 경로를 통일했다. 안내는 1.6초 후 자동으로 사라진다.
- 읽기 전용 안내가 선택 날짜를 해당 일정의 원래 시작일로 강제 변경하던 동작을 제거했다. 연속 일정의 8월 18일 카드를 눌러도 선택일이 7월 16일로 이동하지 않는다.
- 고정 레이어에서 안내를 표시하거나 지울 때 즉시 프레임을 다시 게시해 첫 클릭에도 화면에 반영되도록 했다.
- 설정 `표시 옵션`에 `오른쪽 하단에 기념일 표시`를 추가했다. 사이드바의 `기념일` 체크는 달력 일정 필터로 유지하고, 새 옵션은 오른쪽 하단 기념일 카드 영역만 독립적으로 표시하거나 숨긴다.
- 새 설정의 기존 사용자 기본값을 켜짐으로 보존하기 위해 설정 버전을 15로 올리고 마이그레이션을 추가했다.
- 앱 빌드, UI 구성, 기능팩, 마이그레이션 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `7D7C8C1B6C03DDC0802DDEAF66EDF59D78F5F470575A891741C0BB391BA9DDB5`.
- 설정의 표시 옵션 한 줄에 기념일 옵션을 추가해 오른쪽이 잘리던 배치를 폐기했다. `주차 표시 / 음력 표시`, `24절기 표시 / 오른쪽 하단 기념일 표시`의 2열 2행 구조로 기능 성격과 시작선을 맞췄다.
- 기존 섹션의 상하 여백은 늘리지 않고 각 행 높이를 23px로 고정해 설정이 늘어나도 읽기 쉽고 과도하게 길어지지 않도록 했다.
- UI 구성 검사도 새 SettingsWindow 생성자 인수를 직접 검증하도록 갱신했다. 앱 빌드, UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `0A79CE698A6F9DC4FBB634F8FC6DD8F084BE5B3AF6200A775BE810374A319807`.
- Google 일정의 일반 D-Day를 저장할 때 Google API 응답을 기다린 뒤 화면을 바꾸던 순서를 수정했다. 수정 결과를 로컬 데이터와 달력에 먼저 즉시 반영하고, Google 동기화 결과와 대기 상태는 비동기로 이어서 갱신한다.
- 동기화 시작 전 `PendingGoogleSync`를 저장해 앱이 중간에 종료되거나 네트워크가 느려도 변경 내용이 사라지지 않도록 했다.
- 앱 빌드, UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `9CEEAC98A7CE190BBC4406DF8562B19B4E50F415DA12966641FC4AC3B3B4544B`.
- 오른쪽 기념일 제목줄에 전체 접기/펼치기 화살표를 추가했다. 접으면 제목줄만 남고 펼치면 기본 5개 및 기존 `더보기` 기능이 그대로 나타난다.
- 제목, 접기/펼치기, 개수 배지, `+` 등록 버튼을 모두 24px 높이와 세로 중앙으로 맞췄다. 개수는 연한 라벤더 배지로 구분해 조작 버튼과 시각적으로 혼동되지 않게 했다.
- 고정 레이어에서도 접기/펼치기 버튼을 일반 Button 히트 경로로 처리해 한 번 클릭으로 동작한다.
- 앱 빌드, UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `F5D697FC529F7B69F1C453DE53686D57D15DCB4BBF63AD2A3D5A1501E66FD606`.
- 기념일 접기/펼치기의 화살표 글리프를 `접기`/`펼치기` 텍스트형 라벤더 버튼으로 교체했다. 폰트 자체 기준선 때문에 화살표가 중앙에서 어긋나 보이던 문제를 제거했다.
- 기본 Button 크롬과 내부 여백을 사용하지 않는 전용 ContentPresenter 템플릿을 적용해 버튼 박스와 글자가 정확히 중앙에 오도록 했다. 제목, 접기/펼치기, 개수 배지, `+`는 동일한 24px 행 안에서 세로 중앙 정렬된다.
- 앱 빌드, UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `567A0983DAE3574A1F9941DFCAE9E0E03EB3E58087B561F39A8F5CCA26AFC5F6`.
- 고정 레이어의 일정 히트 대상을 `달력 일정 막대`와 `오른쪽 상세 카드`로 분리했다. 달력의 연속 일정 막대는 막대 전체 시작일이 아니라 실제 클릭한 칸의 날짜를 계산해 오른쪽 선택일에 표시한다.
- 서로 다른 날짜의 연속 일정 막대를 차례로 클릭해도 이전 선택 날짜가 남지 않고 매번 새로 누른 날짜로 변경된다.
- 달력 일정 막대는 한 번 클릭 시 날짜 선택만 수행하고 두 번 클릭해야 수정 또는 읽기 전용 경고를 표시한다. 오른쪽 상세 카드의 읽기 전용 한 번 클릭 안내 동작은 유지했다.
- WPF 위치 조정 모드와 Explorer 고정 모드의 클릭 의미를 동일하게 맞췄다.
- 앱 빌드, UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `03651DCC1AF7011508FBECD506A46028EEF978D27EDE6D1ED7CA7D2F7BC09AC6`.
- 연속 일정의 첫 클릭에서 달력 전체를 재구성해 더블클릭의 두 번째 입력이 폐기된 컨트롤을 참조하던 먹통 원인을 수정했다.
- 한 번 클릭은 일정 막대를 유지한 채 날짜 셀 강조와 오른쪽 상세만 빠르게 갱신한다. 두 번 클릭은 현재 화면에서 찾은 대상을 우선 사용해 수정창을 연다.
- 좌표 변환 대상이 예외적으로 사라진 경우에도 앱이 멈추지 않도록 안전 처리를 추가했다. WPF 위치 조정 모드와 Explorer 고정 모드에 같은 방식으로 적용했다.
- 앱 빌드, UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `30F25D30FB07D49E2F6DB18330FC67199C7687479FFDB9C99C65E7BE3235F378`.
- 오른쪽 상세 일정 카드도 달력 일정 막대와 동일하게 한 번 클릭에는 동작하지 않고 더블클릭에만 수정 동작을 수행하도록 통일했다.
- Google 읽기 전용 상세 카드는 더블클릭했을 때만 `설정에서 수정 가능을 선택해주세요.` 안내가 나타난다. 이동 모드와 Explorer 고정 모드 모두 동일하다.
- 시험본 교체 중 기존 LayerHost 종료 이벤트가 발견되지 않아 고립 프레임 방지를 위해 Explorer를 재시작했다. 이 종료 절차는 별도 안정화가 필요하다.
- 앱 빌드, UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `B304BEB262764270D4999827032B5A4CEFB379CB61F0A37694FCB053D7558F48`.
- 설정의 `오른쪽 하단에 기념일 표시` 옵션을 제거했다. 사이드바 `온하루 등록 > 기념일` 체크박스 하나가 달력 기념일과 오른쪽 기념일 영역을 함께 표시하거나 숨긴다.
- 중복 옵션 제거 후 표시 옵션의 주차·음력·24절기 체크박스를 한 줄에 균형 있게 재배치했다.
- 기존 설정 파일 호환을 위해 설정 버전 15는 유지하되 사용하지 않는 `ShowAnniversaryPanel` 데이터 필드는 제거했다.
- 실행 중이던 시험본의 숨김 창이 외부 정상 종료 요청에 응답하지 않아 앱과 LayerHost 종료 후 Explorer를 재시작해 고정 프레임을 정리했다. 이후 현재 전체 소스로 다시 빌드해 직전 클릭 수정까지 모두 포함된 파일을 배포했다.
- 앱 빌드, UI 구성 및 기능팩 검사를 통과했다. 최신 LocalTest 앱 SHA-256: `7202F1F3811747FF9D196341C9396045E7CF88136CF9B77EA081EC07133FE2C8`.
- 설정 표시 옵션 문구를 `주차 (Week) 표시`, `음력 표시`, `24절기 표시`로 간결하게 변경했다. 기존 기능과 한 줄 배치는 유지한다.
- UI 구성 검사를 통과한 후보 파일 `Release/BuildTemp/ONHARU-2.1-display-labels-test.exe`를 생성했다. 현재 LocalTest가 실행 중이어서 레이어 잔상을 유발하는 강제 종료는 하지 않고 공식 시험본 교체를 보류했다.
- 기념일 제목을 `기념일 (5개)` 형식으로 바꿔 별도 개수 배지를 제거했다. `+` 아이콘 버튼도 제거하고 같은 크기의 텍스트형 `접기/펼치기`, `만들기` 버튼으로 정리했다.
- 접기/펼치기는 라벤더, 만들기는 민트 계열로 구분하고 두 버튼 모두 45×24px 영역과 내부 여백 없는 중앙 정렬 템플릿을 사용한다.
- 설정 표시 옵션의 짧은 문구 변경까지 함께 포함한 후보 `Release/BuildTemp/ONHARU-2.1-anniversary-actions-test.exe`가 UI 구성 및 기능팩 검사를 통과했다. 현재 LocalTest 실행 중이므로 공식 시험본 교체는 보류했다.
- 앱 시작 시 `calendarMinimized`를 명시적으로 해제해 새 실행은 항상 화면에 표시되도록 했다.
- 기존 ONHARU가 트레이에 최소화된 상태에서 실행 파일을 다시 누르면 두 번째 프로세스가 조용히 끝나는 대신, 이름 있는 `ShowOnLaunch` 이벤트를 기존 프로세스에 보내 `화면에 보이기`를 실행한다.
- 설정 문구, 기념일 제목줄 변경 및 실행 시 항상 표시 기능을 모두 포함한 후보 `Release/BuildTemp/ONHARU-2.1-always-visible-launch-test.exe`가 UI 구성 및 기능팩 검사를 통과했다. 현재 LocalTest 실행 중이므로 공식 시험본 교체는 보류했다.
- 사용자가 ONHARU와 LayerHost가 모두 종료된 상태임을 확인한 뒤 최신 전체 후보를 LocalTest에 배포했다. 후보와 배포본 해시가 일치하며 자동 실행하지 않았다.
- 최신 LocalTest 앱 SHA-256: `DCD80E66E3F8F7ED7B4D43CD264937B9F525A3450F0E5C115A3C159647E1B476`.
- 전체 리팩토링을 시작하기 전 LocalTest를 기준으로 `check-v21.ps1`의 13개 품질 게이트를 모두 통과해 회귀 기준을 고정했다.
- `MainWindow`를 partial class로 전환하고 Explorer 클릭 좌표 전달 자료형을 `DesktopItemHitTarget.cs`로 분리했다.
- 종료 확인, 최소화, 재표시, 중복 실행 시 화면 복원, 트레이 메뉴 생성 코드를 `MainWindow.Tray.cs`로 이동했다.
- 다중 모니터·DPI 변경·화면 밖 위치 복구 계산을 `MainWindow.Placement.cs`로 이동했다.
- 기능팩 검사가 `MainWindow.cs` 단일 파일에 의존하지 않고 모든 `MainWindow*.cs` partial 파일을 합쳐 검사하도록 개선했다.
- 각 분리 단계마다 새 실행 파일을 빌드하고 전체 13개 품질 게이트를 다시 실행했다. 최신 리팩토링 후보 `Release/BuildTemp/ONHARU-2.1-refactor-step4.exe`가 모두 통과했다.
- 리팩토링은 진행 중이다. 다음 순서는 MainWindow 렌더링/클릭/Explorer 상호작용, 설정·팝업 공통 스타일, Google·저장·기념일·반복 일정 경계 정리다. 실행 중인 LocalTest는 강제 교체하지 않았다.
- BuildTemp 단독 후보에는 LayerHost가 없어 고정 프레임을 만들지 못하는데도 WPF 창을 숨겨 화면이 사라지는 문제를 확인했다. `LayerHostController.Start()`가 성공 여부를 반환하고 실패 시 WPF 화면을 숨기지 않는 안전장치를 추가했다.
- `window-position-check.ps1`이 반환값을 검사하는 새 LayerHost 호출 형태도 올바르게 검증하도록 수정했다. 전체 13개 품질 게이트를 통과했다.
- 최신 리팩토링 후보를 의존 파일이 함께 있는 LocalTest에 배포했다. SHA-256: `D7DA773B5E8415784038A7A0E4D9666A2E560ACBDE99B569869B1998DA631279`.
- 최초 확인 실행에서 도구의 숨김 창 옵션을 잘못 사용해 표시 검증을 방해했다. 숨김 옵션 없이 다시 실행했고 ONHARU 앱과 LayerHost가 모두 정상 응답 중임을 확인했다.
- 리팩토링 단계 5: Explorer 프레임 게시, 고정/이동 화면 전환, LayerHost 실패 시 WPF 폴백을 `MainWindow.ExplorerLayer.cs`로 분리했다. `window-position-check.ps1`도 모든 partial 파일을 검사한다.
- 리팩토링 단계 6: 고정 화면 액션 라우팅, 클릭 히트테스트, 연속 일정 클릭 날짜 계산, 투명도·상세 스크롤 처리를 `MainWindow.DesktopInput.cs`로 분리했다.
- 리팩토링 단계 7: 기념일 목록 렌더링, 만들기/수정, 10개 제한, D-Day 계산을 `MainWindow.Anniversary.cs`로 분리했다. 만들기와 접기 버튼에 중복되던 ContentPresenter 템플릿은 `ContentOnlyButtonTemplate()` 하나로 통합했다.
- 단계 5~7 각각에서 빌드와 `check-v21.ps1` 전체 13개 품질 게이트를 통과했다. 최신 후보는 `Release/BuildTemp/ONHARU-2.1-refactor-step7.exe`이며 BuildTemp 후보는 직접 실행용이 아니다.
- Claude 검증 패키지는 리팩토링 완료 후 `before`, `after`, 파일 대응표, 전체 검사 로그와 해시를 하나의 ZIP으로 생성할 예정이다. 현재 실행 중인 LocalTest는 교체하지 않았다.
- 리팩토링 단계 8: Google 계정 표시, 동기화 대기 문구, 계정명 흐름 애니메이션, 미동기화 목록 팝업을 `MainWindow.Google.cs`로 분리했다.
- 리팩토링 단계 9: Google 로그인, 수동/자동 동기화, Google 일정 저장, 오류 축약과 기본 캘린더 연결 조정 코드도 같은 partial로 이동했다. 중복된 로그인 취소 예외 처리는 `GoogleConnectFailed()` 하나로 통합했다.
- `sync-security-check.ps1`도 모든 `MainWindow*.cs`를 검사하도록 갱신했다. 단계 8과 9 각각에서 빌드 및 전체 13개 품질 게이트를 통과했다.
- 현재 MainWindow 계열은 본체 1,363줄과 Anniversary 158줄, DesktopInput 208줄, ExplorerLayer 53줄, Google 207줄, Placement 62줄, Tray 115줄로 분리됐다. 다음은 Calendar/Detail 렌더링 분리다.
- 리팩토링 단계 10: 월/주 범위 계산, 날짜 셀, 선택일 배경, 연속 일정 막대 렌더링을 `MainWindow.Calendar.cs`로 분리했다.
- 리팩토링 단계 11: 오른쪽 선택일·이번 주·다음 주 상세 목록과 탭 상태를 `MainWindow.Detail.cs`로 분리했다.
- 리팩토링 단계 12: 일정 등록·수정, 로컬 반복일정 생성 및 시리즈 갱신을 `MainWindow.Items.cs`로 분리했다. `recurrence-check.ps1`도 모든 partial 파일을 합쳐 검사하도록 수정했다.
- 리팩토링 단계 13: 자정 이월, 알림 타이머, 조용한 시간 판정을 `MainWindow.Reminders.cs`로 분리했다.
- 단계 10~13 각각에서 새 BuildTemp 후보를 빌드하고 전체 13개 품질 게이트를 통과했다. 최신 후보는 `Release/BuildTemp/ONHARU-2.1-refactor-step13.exe`이고 실행 중인 LocalTest는 교체하지 않았다.
- `MainWindow.cs` 본체는 최초 2,119줄에서 837줄로 줄었다. 분리 파일은 책임별 partial이며 새 프레임워크·인터페이스·의존성은 추가하지 않았다.
- 리팩토링 단계 14: 설정 저장/반영, 검색, 로컬 일정 가져오기·내보내기, Excel 내보내기 연결을 `MainWindow.Settings.cs`로 분리했다.
- 리팩토링 단계 15: 달력 범위 이동, 연·월 바로 이동, 크기 조절 진입, 중요한 날 색상 팝업을 `MainWindow.Navigation.cs`로 분리했다.
- 리팩토링 단계 16: 일정 표시/정렬/색상/주차 계산과 사이드바 필터 구축을 `MainWindow.Display.cs`로 분리했다.
- 단계 14~16도 각각 빌드 및 전체 13개 품질 게이트를 통과했다. 최신 후보는 `Release/BuildTemp/ONHARU-2.1-refactor-step16.exe`이다.
- 최종 `MainWindow.cs`는 431줄로, 필드·생성자·창 모드/다이얼로그 배치 같은 창 골격만 남았다. `SettingsWindow.cs`와 `AddItemWindow.cs`는 대부분 순차적인 WPF UI 조립 코드라 임의로 더 나누지 않았다.
- 정확한 리팩토링 직전 2.1 소스 스냅샷은 작업 폴더에 남아 있지 않다. Claude 비교용 `before`는 최종 partial 본문을 한 클래스에 기계적으로 다시 합친 소스 동등본으로 만들고, 이 사실을 검증 문서에 명시한다.
- Claude 검증용 문서와 재현 스크립트 `REFACTORING_CLAUDE_REVIEW.md`, `build-claude-refactor-review.ps1`을 추가했다. 패키지는 재결합 단일 파일 컴파일, 분리본 컴파일, MainWindow 멤버 표면 비교, 전체 13개 품질 게이트와 SHA-256 목록을 포함한다.
- 최종 Claude 검증 폴더와 ZIP을 `Review/Claude-Refactor-Validation-20260817` 및 같은 이름의 `.zip`으로 생성했다. 단일 파일과 partial 빌드의 소스 정의 MainWindow 필드·메서드 표면이 동일했고, partial 빌드는 전체 13개 품질 게이트를 통과했다.
- Claude 승인 후 최종 리팩토링 소스로 LocalTest 앱과 Explorer LayerHost/DLL을 다시 빌드했다. `Release/LocalTest/ONHARU-2.1-local-test.exe`가 전체 13개 품질 게이트를 통과했다.
- 같은 소스로 배포 스테이지 `Release/ONHARU-2.1.0`과 Inno Setup 설치판 `Release/Installer/ONHARU-2.1.0-Setup.exe`를 생성했다. 배포 스테이지 앱도 전체 13개 품질 게이트를 통과했다.
- 최종 SHA-256: LocalTest `73011B17292412AFCE442A30CFA095D2D81FA16358099D335B985E7613D897E9`, 배포 앱 `280E21B2DD5892D6D4AC2B3F2F4C74A5E19A50434A139B95221E243C927AD34F`, 설치판 `C0945AFE813DE5DF022EA8B8A2D19B3F4BFB4DC9E9ED0C2D46A74C42CE0D64BC`.
- 설치판은 Authenticode 코드 서명이 없는 상태다. 설치 기능에는 영향이 없지만 외부 일반 배포 시 Windows SmartScreen 경고 가능성이 있어 코드 서명은 별도 배포 준비 항목으로 남긴다.
- 일반 배포 1단계 코드 서명 감사를 완료하고 `Distribution/CODE_SIGNING_STEP1_AUDIT.md`에 기록했다. Windows SDK SignTool은 설치되어 있으나 현재 사용자 인증서 저장소에 코드 서명 인증서는 0개이며 설치판은 `NotSigned`다.
- 2026년 Microsoft 공식 안내상 EV도 OV와 동일하게 SmartScreen 평판을 쌓아야 하고, 한국 개인은 Microsoft Artifact Signing 개인 Public Trust 대상 지역에 포함되지 않는다. 공개 EXE 배포의 기본 경로를 공인 CA OV 코드 서명 인증서로 정했다.
- 배포 주체를 개인 명의 프리웨어로 확정하고 `Distribution/CODE_SIGNING_STEP2_INDIVIDUAL_ROUTE.md`를 작성했다. 프리웨어는 Open Source 인증서 대상이 아니므로 개인 발급을 지원하는 Certum Standard Code Signing을 후보로 선정했다.
- 물리 카드형과 클라우드형 중 배송·리더기·드라이버 관리가 없는 Certum Standard Code Signing in the Cloud(공식 시작 가격 약 EUR 209)를 권장 경로로 정했다. 실제 구매와 신원 확인은 사용자 승인이 필요한 외부 단계로 남아 있다.
- 사용자가 매년 비용을 부담하지 않기로 결정해 공인 코드 서명 구매를 보류했다. 현재 무서명 설치판과 SHA-256을 이용해 배포하며 SmartScreen 안내를 제공하는 방향이다.
- Google Cloud의 ONHARU 프로젝트를 읽기 전용으로 감사하고 `Distribution/OAUTH_STEP3_CURRENT_STATE.md`에 기록했다. 외부/테스트 상태, 테스트 사용자 3명, 민감한 범위 1개 등록, 인증 미제출 상태다.
- 제품 코드는 `calendar.events`와 `calendar.calendarlist.readonly`를 요청하지만 콘솔에는 전자만 등록되어 있어 공개 인증 전 범위 일치가 필요하다. 홈페이지·개인정보처리방침·약관·승인 도메인·로고도 아직 미설정이다.
- Google 공식 문서상 데스크톱 앱의 client secret은 기밀로 유지할 수 없는 public client 정보다. ONHARU는 PKCE S256과 state 검증을 사용하고 Google 진단에서도 보안 OAuth 흐름으로 확인됐다.
- Google Cloud 동의 화면에 제품 코드가 사용하는 `calendar.calendarlist.readonly` 범위를 추가하고 저장했다. 기존 `calendar.events`와 함께 코드·동의 화면 범위가 일치한다.
- 오른쪽 상세 영역에 일반 일정의 `ShowDday`만 모아 보여주는 별도 `D-Day` 카드를 추가했다. 기념일은 제외하며 기본 5개, 더보기/접기와 전체 영역 접기/펼치기를 지원한다.
- 신규·일정 수정창에 `기념일로 등록`을 추가했다. 일반 D-Day와 상호 배타적으로 동작하고, 저장 시 Google 일정 선택과 관계없이 ONHARU 로컬 연간 기념일로 전환된다. 이후 수정은 일반 일정창이 아니라 기념일 전용 창으로만 열린다.
- 기존 반복 일정이나 Google 일정을 기념일로 전환할 때 기존 시리즈를 정리하고 새 로컬 기념일 시리즈를 생성하도록 별도 경로를 두었다. 기념일 최대 10개 제한도 유지한다.
- 기념일 날짜 선택 달력에 일정 등록·수정창과 같은 달력 스타일을 적용했다. 오른쪽 `중요한 날` 버튼을 줄이고 두 자리 월·일에서도 날짜 제목 공간이 확보되도록 제목 크기와 말줄임 처리를 조정했다.
- 변경 소스로 BuildTemp 후보를 컴파일했고 신규 동작을 검사하는 기능팩 게이트를 보강한 뒤 13개 품질 게이트를 모두 통과했다. 실행 중 LocalTest와 배포 설치판은 사용자 확인 전 교체하지 않았다.
- 오른쪽 상세 일정이 `VisibleItems()`의 정렬 결과를 받은 뒤 다시 무조건 카테고리별로 묶여 `전체 시간순` 설정이 깨지던 문제를 수정했다. 카테고리 모드는 지정 순서로 묶고, 시간순 모드는 정렬된 순서를 유지하면서 인접한 동일 카테고리만 카드로 묶는다.
- 카테고리 순서 창에서 이동 후 목록을 다시 만들 때 정지한 마우스 아래의 새 버튼이 즉시 인식되지 않던 문제를 수정했다. 스크롤 유무와 무관하게 이동 카드를 찾고, 필요하면 보이는 영역으로 이동한 뒤 WPF 마우스 히트 상태를 즉시 동기화한다.
- 두 정렬 수정이 포함된 BuildTemp 후보가 전체 13개 품질 게이트를 통과했다. 첫 FeatureTest가 실행 중이어서 덮어쓰지 않고 `Release/FeatureTest/ONHARU-2.1-feature-test-r2.exe`로 별도 생성했다.
- 사이드바 `온하루 등록` 필터에 `D-Day` 항목을 추가했다. 업무일정·개인일정·기념일·D-Day를 폭에 따라 한 줄 우선, 부족할 때만 두 줄로 흐르게 배치했다.
- D-Day 체크는 일정 자체를 숨기지 않고 오른쪽 D-Day 카드만 표시하거나 숨긴다. 설정 버전을 16으로 올려 기존 사용자에게는 최초 마이그레이션 시 표시 상태가 기본으로 켜지며 이후 선택을 저장한다.
- D-Day 필터와 설정 v16 마이그레이션을 포함한 `Release/FeatureTest/ONHARU-2.1-feature-test-r3.exe`를 생성했고, 해당 실행 파일을 대상으로 전체 13개 품질 게이트를 통과했다.
- 고정 레이어에서 D-Day 체크 상태는 변경됐지만 `RenderDetail()` 뒤 새 프레임 게시가 예약되지 않아 화면상 클릭되지 않은 것처럼 보이던 문제를 수정했다. Explorer 고정 모드의 모든 체크박스 입력은 클릭 이벤트 처리 후 공통으로 프레임 재게시를 예약한다.
- D-Day·기념일 카드의 위쪽 외부 간격을 8px로 줄이고 내부 위아래 Padding을 각각 8px로 통일했다. 접힌 상태에서는 제목 행의 불필요한 아래 Margin을 제거해 위아래 공백이 같고, 펼친 상태에서도 카드 외곽 여백은 유지된다.
- 오른쪽 일정 카드의 종일 일정은 별도 `하루 종일` 줄을 없애고 제목 뒤 `(하루 종일)`로 표시한다. 예: `사내교육 (하루 종일)`, `팀회식 (하루 종일)`. 시간 지정 및 여러 날 시간 표시는 기존대로 유지한다.
- `온하루 등록` 필터 순서를 `업무일정 → 개인일정 → D-Day → 기념일`로 변경했다. 기념일 카드 제목줄의 조작 버튼도 `만들기`와 `접기/펼치기` 위치를 서로 교체했다.
- 종일 일정은 정보를 없애지 않고 제목 뒤 `· 하루 종일`을 작은 회색 보조 텍스트로 표시하도록 개선했다. 제목·중요 표시 색상은 유지하고 종일 표기만 시각적 우선순위를 낮췄다.
- 시작 직후 실제 달력 크기가 확정되기 전에 임시 이벤트 레인을 Explorer에 게시해 회색 일정이 한 줄로 몰려 보이고 약 0.5초 뒤 정상화되던 원인을 수정했다. Loaded 단계에서 실제 레이아웃을 확정한 뒤 다시 렌더링하고 완성 프레임만 게시한다.
- 신규·수정 일정에서 `기념일로 등록`을 선택하면 일반 D-Day와 UI상 상호 배타 상태를 유지하되, 생성되는 기념일의 `D-Day 표시` 기본값은 켜지도록 변경했다.
- 기념일 수정창에 `일정으로 변경` 버튼을 추가했다. 기념일 시리즈를 제거하고 최초 기준일의 로컬 개인 종일 일정으로 되돌리며, 기념일 창의 D-Day 체크가 켜져 있으면 일반 D-Day 일정으로 전환된다. 이후 수정은 일반 일정 수정창에서 수행한다.
- Google D-Day 일정을 기념일로 전환할 때 변환 블록에서 Google 속성을 지운 뒤 공통 캘린더 선택 코드가 Google ID·색상을 다시 넣던 순서 오류를 수정했다. 기념일 전환 시 공통 Google 대상 적용을 건너뛰어 항상 ONHARU 로컬 기념일로 저장한다.
- 이전 시험본에서 이미 잘못 변환되어 Google 색상이 남은 ONHARU 생성 기념일은 시작 시 Google ID·색상·읽기 전용·동기화 속성을 자동 제거하고 로컬 기념일로 복구한다.
- 고정 상태에서 종료 확인창을 열기 전에 숨겨진 WPF 달력 전체를 복원하고 Explorer 프레임을 내리던 흐름이 화면 번쩍임을 만들었다. 고정 프레임은 그대로 유지하고 종료 팝업만 작업표시줄 없이 최상단에 표시하도록 변경했으며, 취소 시 불필요한 달력 재게시도 제거했다.
- 선택일 테두리를 일정 막대 앞으로 보이게 하려고 별도 오버레이를 추가한 실험은 효과가 없고 테두리 잔상이 남아 즉시 원복했다. 연속 일정 클릭이 안정적인 기존 Z-order를 유지하며 이 문제는 다른 방식으로 재검토한다.
- 사용자가 승인한 r10 원복 상태를 기준으로 배포 스테이지와 Inno Setup 설치판을 다시 생성했다. 배포 앱을 대상으로 전체 13개 품질 게이트를 통과했다.
- 최종 SHA-256: 배포 앱 `9780A281D31BEF38EBACBD2DE43EC4454ECD48753AE89C54363F473E14ECA8EB`, 설치판 `AB3F844306466A1B787BA735A2C2127737E0FF8F8F6627EAD19396E2AD00026B`.
- 설치판은 계획대로 Authenticode 서명 없이 생성되어 `NotSigned` 상태이며, 일반 PC에서는 SmartScreen의 알 수 없는 게시자 안내가 나타날 수 있다.
- 문서 전체 감사를 실시해 초기 기념일 시안이 남아 있던 `README.md`, `V2.1_ROADMAP.md`, `V2.1_TEST_CHECKLIST.md`를 현재의 일반 D-Day/전용 기념일 분리 구조로 갱신했다. `PROJECT_INDEX.md`, `BUILD_AND_RELEASE.md`, `PRD.md`의 배포 상태·미서명 정책·partial 소스 구조·2.1 버전 정보도 최신화했다.
- `DEVELOPMENT_HISTORY.md`에 2.1 마감 변경을 요약하고 갱신된 README가 배포 스테이지와 설치판에 포함되도록 정식 빌드를 다시 수행했다. 루트/스테이지 README 해시 일치와 전체 13개 품질 게이트 통과를 확인했다.
- 문서 갱신 후 최종 재빌드 SHA-256: 배포 앱 `BB4D717F7E5A0F76D1F78071A2A6C8186E85A8CA25417EA5798EA987227F01D7`, 설치판 `638C1117362E20A1ED14CFC8A686743D6E3B1294DD6754E0026E7FC186713409`. 앞의 해시는 이전 빌드 이력이며 이 값이 현재 최종 파일 기준이다.
- 고정/이동 전환 때 이전 선택일이 잠깐 나타나고 전체 화면이 번쩍이던 문제를 프레임 단위로 조사했다. 원인은 WPF HWND와 Explorer 합성 프레임 사이에 실제 그리기 완료 확인 없이 한쪽을 먼저 내리던 비동기 교대였다.
- WPF 창을 고정 중 `Hide()`하지 않고 DWM cloak으로 유지해 논리 화면과 렌더 표면이 계속 최신 상태가 되도록 했다.
- 공유 프레임 게시자가 이름 있는 자동 재설정 이벤트 `Local\OnharuV3.DesktopFrame.Drawn`을 만들고, Explorer 훅이 새 세대를 실제 화면 DC에 그린 직후 신호하도록 네이티브 ACK 경로를 추가했다.
- 고정 전환은 ACK 뒤에만 WPF를 cloak하고, 이동 전환은 Explorer 프레임을 유지한 채 투명 WPF를 uncloak·렌더한 후 정상 투명도로 복원하고 Explorer 프레임을 내리는 2단계 교대로 변경했다. 같은 cloak 상태의 중복 DWM 호출도 차단했다.
- 화면 좌표 클릭 기반 자동 시험이 비활성 창에서 실제 버튼을 누르지 못할 수 있음을 발견했다. `Invoke-OnharuLockButton.ps1`을 UI Automation 기반으로 만들고 `Run-TransitionCapture.ps1`이 이를 사용하도록 교정했다.
- 고정 상태에서 선택일을 8월 21일에서 8월 19일로 바꾼 뒤 이동 모드로 돌아오는 실제 변경 시나리오를 60fps로 녹화했다. 최신 선택 테두리와 상세가 유지됐고, 이전 선택일 재노출·뒤쪽 창 노출·빈 프레임은 관찰되지 않았다.
- 후보별 바이너리와 영상은 `Tests/LocalTest/TransitionCandidates/01`~`06`에 순차 보존했고 결과 요약은 해당 `README.md`와 후보 06의 `RESULT.md`에 기록했다. 전체 13개 품질 게이트가 PASS했다.
- 현재 LocalTest 및 후보 06 해시: 앱 `E377EF4600266D0154B56B7CB26DDBAAB29F413E5CF46457BBE7CDBF3F2E3BCE`, LayerHost `FEB77350130F9483F82DB49550A5A6B42A8E544B819283CF2E9150F467B21353`, DesktopHook `7CBF1CB21AA59538D3F024B40CA38922F9F4517E1EB8DDABA4A9AB874E8B1F3F`.
- 서로 다른 빈 날짜 좌표를 사용한 고정→날짜 선택→이동 전환을 추가로 6회 연속 녹화했다. 모든 회차에서 앱이 이동 가능 상태로 복귀했으며 전환 구간에 빈 화면이나 과거 선택 테두리 재노출이 없었다. 영상은 후보 06의 `captures/stress`에 보존했다.
- 사용자 실기기 피드백에서 선택일은 정상이나 달력 전체 밝기 깜빡임이 남음을 확인했다. 원인은 이동 전환에서 WPF 투명도를 복원한 뒤 다음 Dispatcher turn까지 반투명 Explorer 프레임을 함께 유지해 같은 달력이 한 프레임 중복 합성되던 구조였다.
- 후보 07에서 WPF 투명도 복원과 Explorer 프레임 해제를 동일 Dispatcher turn으로 합치고 마지막에 한 번만 `DwmFlush`하도록 변경했다. 자동 60fps 영상의 상단 달력 영역 최대 연속 프레임 차이는 약 20.19에서 약 8.85로 감소했다.
- Explorer 네이티브 페인트 콜백 안에서 WPF HWND를 직접 cloak하는 후보 08도 별도 저장·시험했지만 수치상 추가 개선이 없어 소스와 LocalTest를 후보 07로 원복했다.
- 후보 07 전체 13개 품질 게이트가 PASS했다. 현재 LocalTest 해시는 앱 `25C7C97B59A42DFB3C1B88112303559E393B36FCA7A0A3AD20A9335C041FDA81`, LayerHost `F4BD3F56D5984D212C6042EB0233843A2F9E7F61727035D7F0807BC8E77CE255`, DesktopHook `C6EC639B90FEFCB8BBDDBFDE3573A28AB3434B4243D7F85365B1E1010BDF4CC2`이다.

## 2026-08-19 배포 직전 정리

- 전환 깜빡임 실험은 `Tests/LocalTest/TransitionCandidates/README.md`의 후보 01~15에 보존했다. 현재 소스는 `RDW_ERASE` 제거, 전환 경로의 명시적 `DwmFlush` 제거, 이동 전환 시 cloak 상태에서 WPF를 완성한 뒤 정상 투명도로 한 번에 uncloak하고 Explorer 프레임을 내리는 후보 15 구조다.
- 작은 화면 대응으로 헤더 제목은 17px 고정, 상단 작업 버튼은 28px 높이와 균일한 5px 간격으로 줄였다. 투명도 슬라이더는 브랜드 행으로 이동했고 위치 상태와 동작 아이콘을 서로 구분했다.
- Explorer 고정 좌표는 가상 화면 원점 추정 대신 `SysListView32`의 실제 클라이언트 좌표로 변환해 작은 해상도·다중 모니터에서 고정 시 옆으로 밀리는 문제를 교정했다.
- 일정 등록·수정창은 작은 노트북 화면에서 스크롤 가능하게 높이를 제한했다. 제목 입력 높이는 유지하고 상단 저장 버튼을 추가했으며 알림 선택은 `없음`, `정시`, `직접 선택`으로 정리했다. 기념일 등록은 일반 일정창에서 제거하고 전용 기념일 창으로 일원화했다.
- 실행되지 않는다는 신고를 재현한 결과, 이전 LocalTest 프로세스가 창 없이 남아 있고 두 번째 실행은 단일 실행 mutex에 따라 즉시 종료된 상태였다. 잔류 시험 프로세스를 종료하고 전체 재빌드한 뒤 첫 실행 유지와 중복 실행 전달을 확인했다.
- 배포 전 정적 확인에서 App/ExplorerLayer 소스 69개에 병합 충돌 표식이 없음을 확인했고, `check-v21.ps1 -Build`의 13개 품질 게이트가 모두 PASS했다. 당시 LocalTest SHA-256은 `0286E5F2675FB82DEC1C17202952E6D1FD3218E74C55A372726C3E4C55044BA3`이다.
- 최신 배포 스테이지 `Release/ONHARU-2.1.0`을 다시 생성하고 그 안의 `ONHARU.exe`를 대상으로도 13개 품질 게이트를 모두 통과했다. 배포 앱 SHA-256은 `3004C2714A91C55A3A60F250DDE4118392E847671B5CB5F3ABC54A4FF1458F10`이다.
- 현재 Codex 작업 권한에서는 작업폴더 밖의 Inno Setup `ISCC.exe` 실행이 거부되어 설치 EXE 단계만 사용자 터미널 실행이 필요하다. `finalize-distribution.ps1`을 추가해 정식 빌드, Inno 설치판 생성, 설치 안내와 해시를 포함한 `ONHARU-2.1.0-Installer.zip` 생성을 한 번에 수행하도록 했다.
- 깜빡임 후보 16을 적용했다. WPF를 `Opacity=0`으로 먼저 uncloak하여 DWM 합성 트리에 참여시키고, 다음 Render turn에서 정상 투명도를 복원하면서 Explorer 프레임을 해제한다. 후보 15에 남아 있던 wallpaper-only 경쟁 구간을 줄이는 목적이며 명시적 `DwmFlush`와 `RDW_ERASE`는 계속 사용하지 않는다.
- 후보 16 전용 빌드가 실제 UI Dispatcher에서 고정↔이동 왕복 1,000회(렌더 전환 2,000회)를 263.742초 동안 수행했다. 실패 0건, 최종 이동 가능 상태, 종료 코드 0을 확인했다. 결과와 바이너리·전후 소스·해시는 `Tests/LocalTest/TransitionCandidates/16-transparent-wpf-prewarm`에 보존했다.
- 백그라운드 셸의 Windows 화면 캡처는 `screen grab failed`로 거부되어 픽셀 기반 깜빡임 수치는 만들지 못했다. 1,000회 시험은 멈춤·충돌·상태 누락 검증이며 최종 미세 깜빡임은 사용자 화면에서 육안 확인이 필요하다.
- 후보 16 전체 소스로 LocalTest와 배포 스테이지를 재생성했고, 두 실행 파일 모두 전체 13개 품질 게이트를 통과했다. 최신 LocalTest 앱 SHA-256은 `08154C1543E2D72FB54D38B7FD7CBC13642777085C1C469D75BE55B6D6EEC1C3`이다.
- GitHub 업로드용 `Release/GitHubUpload`을 생성했다. Portable ZIP, 공개 소스 ZIP(로컬 OAuth 자격 파일 제외), 릴리스 노트와 SHA-256 목록이 있다. Portable ZIP SHA-256은 `FEB925C08F8A32B532EBC4F104D0DCFD6C8E9F951CD86DB5E94CF10D4C940E7D`, Source ZIP은 `B12CCB61DA02016BF3CEA0A6FCE1218CF064D196DB7CFC78591A5636232A844D`다.
- GitHub CLI의 `cowpigpapa` 토큰은 만료됐고 연결된 GitHub 앱은 다른 계정이었지만, 앱 내 브라우저에서는 `cowpigpapa/onharu` 로그인과 기존 `v2.1.0` 릴리스를 확인했다. 최신 4개 자산을 업로드하려는 시점에 브라우저 보안 확인이 사용자 거부 상태로 반환되어 전송은 수행하지 않았다. 릴리스 편집 탭을 다음 작업용으로 유지했다.
- 정식 Inno Setup 설치 EXE는 작업폴더 밖 `ISCC.exe` 실행 권한 거부로 아직 후보 16으로 갱신하지 못했다. 사용자가 `finalize-distribution.ps1`을 실행한 뒤 설치 ZIP을 GitHub 릴리스 자산에 추가해야 한다.
- 스트레스 시험용 임시 작업공간은 결과 보존 후 정리했다. `Tests/Stress1000Workspace`만 백신 검사 복제 프로세스가 EXE를 점유해 삭제되지 않았으며 제품·배포 파일에는 포함되지 않는다.

## 2026-08-19 일정 배치·Google 필터·Explorer 호환성 보완

- 일정 표시 설정으로 정렬된 목록이 시간순이 아닐 때 기존 `laneEnds` 방식이 실제로 겹치지 않는 일정도 새 줄에 배치해 빈칸을 만들 수 있었다. 주간 각 레인의 실제 날짜 점유 구간을 검사해 첫 비충돌 레인에 배치하도록 바꿨다. 설정한 시간순/카테고리순과 연속 일정 우선순위는 그대로 유지한다.
- 셀 높이를 넘는 일정에 표시하던 주간 전체 폭 `⌄ +N개 일정` 막대를 제거했다. 표시 가능한 줄까지만 일정 막대를 그리며 상세 패널에서는 전체 일정을 계속 확인할 수 있다.
- Google 캘린더 체크박스가 StackPanel 폭 전체로 늘어나 고정 레이어 히트맵도 빈 오른쪽 영역까지 체크 대상으로 잡던 문제를 수정했다. 체크박스를 내용 너비로 제한해 체크 표시나 글자 위에서만 토글된다.
- Google 캘린더가 4개 이상이고 가장 긴 표시명이 15자 이하일 때만 두 개의 동일 폭 열을 사용한다. 항목 수의 절반을 기준으로 왼쪽/오른쪽 열을 나눠 WrapPanel처럼 임의 위치에서 흐르지 않는다. 긴 이름이 하나라도 있으면 한 열을 유지한다.
- 회사 노트북처럼 `SHELLDLL_DefView`가 `Progman`이 아니라 `WorkerW` 아래에 있는 Explorer 구성도 지원하도록 창 검색을 최상위 창 열거 방식으로 변경했다. C# 프레임 좌표·재그리기와 네이티브 LayerHost·DesktopHook이 동일한 실제 `SysListView32`를 찾고, 후크도 그 DefView 소유 스레드에 설치한다.
- 간헐적 미세 깜빡임은 이번 기능 수정과 분리해 후보 16 전환 구조를 유지했다. Explorer가 아이콘 선택·이동·배경 새로고침으로 ListView를 무효화한 시점과 DWM 합성 프레임이 엇갈리면 한 프레임 밝기 차이가 생길 수 있다. 1,000회 상태 시험은 충돌/누락을 검증하지만 픽셀 수준의 단발성 셸 재합성을 완전히 증명하지는 못한다.
- 실행 중인 `Tests/LocalTest/ONHARU-2.1-local-test.exe`가 출력 파일을 점유해 기본 `-Build` 덮어쓰기는 수행하지 않았다. 대신 `Tests/BuildTemp/ONHARU-2.1-check.exe`로 C#을 컴파일하고 네이티브 LayerHost/DLL도 빌드했다. PowerShell 7에서 전체 13개 품질 게이트가 PASS했다.
- 고정↔이동 토글 직후 포인터를 움직이기 전까지 이전 HWND의 화살표 커서와 히트 상태가 남아 첫 클릭이 무시되는 문제를 수정했다. DWM cloak 전환이 끝난 Input 단계에서 WPF `Mouse.Synchronize()`를 실행하고, 현재 포인터 아래의 실제 HWND에도 좌표를 바꾸지 않는 `WM_MOUSEMOVE`·`WM_SETCURSOR`를 보내 즉시 새 레이어의 히트테스트를 활성화한다.
- 공통 상단 버튼 템플릿에 hover 90%, pressed 62%의 짧고 분명한 눌림 피드백을 추가했다. Explorer 고정 레이어 클릭은 숨겨진 WPF의 `IsPressed`가 보이지 않으므로 130ms 피드백 프레임을 별도로 게시한 뒤 원래 밝기로 복귀한다.
- 글꼴별 모양과 중심이 달랐던 `⚙` 문자 설정 버튼을 15×15 벡터 톱니 아이콘으로 교체했다. 버튼 안에서 수평·수직 중앙 정렬되며 오른쪽으로 치우치지 않는다.
- D-Day는 업무·개인·Google 일정의 별도 종류가 아니라 일반 일정의 `ShowDday` 표시 속성으로 유지한다. `D-Day` 필터가 켜져 있으면 업무·개인·Google 캘린더 필터가 꺼져 있어도 명시적으로 D-Day 지정한 항목은 오른쪽 카드에 보이며, 해당 항목이 하나도 없으면 카드 자체를 만들지 않는다.
- 오른쪽 상세 Todo 카드는 체크박스뿐 아니라 제목·시간·메모 영역을 한 번 클릭해도 완료 상태가 토글된다. 230ms 동안 두 번째 클릭을 판별해 더블클릭은 완료 상태를 바꾸지 않고 기존 일정 수정만 연다. Explorer 고정 레이어에도 동일한 단일/더블클릭 규칙을 적용했다.
- 신규·수정 일정창의 위·아래 저장 버튼 캡션을 모두 `✓ 일정 저장`으로 통일했다.
- 여러 날 일정의 반복 간격이 일정 기간보다 짧아 회차가 서로 겹치지 않도록 기간별 허용 규칙을 추가했다. 7일 이하는 `반복 없음·매주`, 8일 이상은 `반복 없음·매월`만 선택할 수 있다. 종료 날짜를 바꾸는 즉시 선택 가능 상태를 다시 계산하며 불가능해진 기존 선택은 `없음`으로 복귀한다.
- 사이드바 필터를 `온하루 등록(업무·개인)`과 `Special Day(D-Day·기념일)` 두 그룹으로 재배치했다. 두 제목은 같은 작은 회색 서체를 사용하며, 그룹 사이에는 제목과 체크 행을 관통하는 1px 세로 구분선을 두어 일반 일정과 특별일 표시의 성격을 구분한다.
- 8일 이상 여러 날 일정의 매월 상세 옵션은 `매월 N일부터 같은 기간`, `매월 N번째 요일부터 같은 기간`으로 문맥을 명확히 했다. `매월 마지막 날부터 같은 기간`은 시작일이 실제 해당 월의 마지막 날인 경우에만 표시하며, 그 외 날짜에서는 부적절한 마지막 날 옵션을 숨긴다.
- Special Day 첫 조정에서 일반 일정 그룹을 82px로 과도하게 줄여 `개인`이 잘리는 회귀가 생겼다. 일반 일정 폭을 100px, 구분선 열을 17px로 재조정하고 Special Day가 남은 폭을 쓰게 해 두 그룹의 체크 문구를 모두 보존했다.
- 단일 일정의 매월 반복에서도 `매월 마지막 날`은 선택일이 실제 그 달의 마지막 날일 때만 표시한다. 월말이 아닌 날짜에서 과거 `monthly_last` 선택이 남아 있으면 `매월 N일`로 안전하게 전환한다.
- 일정창의 `자동 이월`은 `이월`로 줄이되 요청하지 않았던 상단 이동을 원복해 `분` 선택 아래에 배치했다. 한 줄 옵션은 `없음 · 다음날 · 다음주 같은 요일 · 다음 평일`이며, `다음 평일`은 다음 날부터 주말과 현재 동기화된 대한민국 휴일을 건너뛴다.
- 알림의 `직접 선택` 라디오, 숫자 입력, `분 전/시간 전/일 전` 드롭다운을 모두 27px 높이와 수직 중앙 정렬로 맞췄다. 숫자 입력에도 다른 입력란과 같은 둥근 테두리 템플릿을 적용하고 내부 Padding을 중앙 기준으로 보정했다.
- 여러 날 일정과 반복의 종료 캡션을 모두 `종료날짜`로 통일했다. 여러 날 날짜 버튼은 118→96px, 반복 종료 날짜 버튼은 104→92px로 줄이고 캡션과 버튼 간격을 4px로 맞췄다.
- 알림을 독립된 무테두리 영역에서 시간 카드 내부의 한 줄로 옮겼다. 시간 모드·여러 날·시/분·이월·알림이 하나의 일정 시간 설정 카드 안에서 순서대로 보인다.
- 반복 설정을 독립 영역에서 `온하루 로컬/Google 동기화` 일정 등록 카드 안으로 옮기고 얇은 구분선으로 소스 선택과 구분했다. 신규·수정은 같은 `AddItemWindow`를 사용하므로 양쪽에 동일 적용된다.
- 매년 반복에 `매년 같은 날짜`와 `매년 같은 주·요일` 두 모드를 추가했다. 후자는 같은 월의 N번째 또는 마지막 요일을 유지하며 로컬 다음 회차 계산과 Google RRULE의 `BYMONTH/BYDAY`까지 함께 지원한다. 기존 yearly 데이터는 `매년 같은 날짜`로 호환한다.
- Google 전송 본문을 재검토했다. `Special Day`는 UI 그룹명일 뿐 전송되지 않고, 일정 제목·메모에도 D-Day/중요 접두어를 붙이지 않는다. 중요와 D-Day 상태는 각각 Google private extended property `onharuImportant`, `onharuDday`로 동기화되며 전용 기념일은 계속 ONHARU 로컬 전용이다.

## 2026-08-19 신규·수정 일정 반복 카드 디자인 정리

- 사용자가 현재 집중 검토 중인 신규 일정 화면에서 반복 설정을 일정 소스/카테고리 카드와 다시 분리했다. `반복 일정` 체크박스가 있는 독립 테두리 카드로 구성하고, 체크하지 않은 상태에서는 세부 옵션을 접어 화면 밀도를 낮춘다.
- 반복을 켜면 현재 기간 규칙에 맞는 첫 반복 단위를 자동 선택한다. 단일 일정은 기본적으로 매일, 7일 이하 여러 날 일정은 매주, 8일 이상 여러 날 일정은 매월처럼 실제 활성화된 첫 옵션을 사용한다. 기존 일정 수정 시에는 저장된 반복 단위와 종료 방식이 그대로 복원된다.
- 반복 종료 방식을 사각 체크박스가 아닌 `횟수`와 `종료날짜` 원형 라디오 선택으로 통일했다. 횟수 숫자 입력과 종료 날짜 버튼은 선택된 방식에 맞춰 활성화된다.
- 공통 둥근 버튼 템플릿이 `BorderBrush`와 `BorderThickness`를 무시하던 결함을 수정했다. 반복 종료 날짜를 포함해 테두리를 지정한 날짜 버튼이 흰 배경에 묻히지 않는다.
- 직접 알림 숫자 입력 폭을 48px, 단위 선택 폭을 80px로 확보하고 내부 여백과 수직 중앙 정렬을 보정해 `30분` 계열 표시가 잘리지 않게 했다.
- 신규와 수정은 동일한 `AddItemWindow`를 사용하므로 위 디자인과 동작은 양쪽에 동일 적용된다. 전용 후보 `Tests/BuildTemp/RecurrenceCardPolish`를 만들었고 최신 EXE 기준 전체 13개 품질 게이트가 PASS했다.
- 후속 정렬 검토에서 여러 날 일정과 반복 일정의 `종료날짜` 캡션을 동일한 11px 회색(`#64748B`) 계열로 맞췄고, 반복의 `횟수`도 같은 계층의 회색 글자로 통일했다. 횟수 입력은 공통 둥근 입력 템플릿과 중앙 정렬을 적용해 네모난 임시 입력처럼 보이던 문제를 제거했다.
- 시간 카드의 `분·이월·알림` 세 행은 모두 34px 라벨 열, 동일한 3px 상하 행간을 사용한다. 각 라디오/입력 옵션은 두 번째 열의 같은 X 좌표에서 시작하고 알림 컨트롤의 불필요한 하단 여백을 제거했다. 후보 실행 파일을 v2로 갱신했으며 전체 13개 품질 게이트가 다시 PASS했다.
- 시간 모드 디자인을 두 줄로 재구성했다. 첫 줄은 체크박스 모양의 `하루 종일 · 여러 날 일정 · 종료날짜`, 둘째 줄은 체크박스 모양의 `오전 · 오후`이며, 시각적 균형을 위해 체크박스를 사용하되 내부 선택은 기존 라디오와 동일하게 상호배타적이다.
- `오전` 또는 `오후`를 선택하면 하루 종일과 여러 날 일정이 해제되고 시·분·이월이 활성화된다. `하루 종일`을 선택하면 오전·오후가 해제되고 여러 날 일정이 활성화된다. 활성 모드를 사용자가 다시 해제해 세 모드가 모두 꺼질 상황에는 안전하게 하루 종일로 복귀한다. 이 상호 로직을 UI 생성 검사에 추가했고 전체 13개 품질 게이트가 PASS했다.
- 분 선택을 기존 15분 간격 4개에서 `0·10·20·30·40·50분` 10분 간격 6개로 변경했다. 시간 선택과 동일한 6열 구조라 좌우 정렬이 맞으며, 기존 일정 및 사용자 기본 시작 분을 불러올 때도 가장 가까운 10분 항목을 선택한다. 전체 13개 품질 게이트가 PASS했다.
- `분·이월·알림` 옵션 영역은 모두 같은 34px 라벨 열을 사용했지만 분 라디오에만 2px 왼쪽 Margin이 남아 있었다. 분 라디오의 왼쪽 여백을 0으로 통일하고 각 6열 안에서 왼쪽 정렬해 세 행의 첫 선택 원과 텍스트 시작선을 맞췄다.
- 사이드바의 `온하루 등록` 그룹 폭을 100px에서 102px로 미세 조정해 `개인` 체크 문구의 우측 약 2px 잘림을 해소했다. 구분선과 Special Day 그룹은 함께 2px 오른쪽으로 이동하며 전체 품질 게이트를 통과했다.
- 오른쪽 선택 날짜 상세에서 마지막 일정 카드와 `+ 이 날짜에 추가` 버튼 사이의 상단 Margin을 14px에서 3px로 줄였다. 마지막 카드 자체의 5px 하단 Margin과 합쳐 일반 카드 사이와 비슷한 8px 간격이 된다.
- 상세 탭의 `선택일` 캡션을 `선택 날짜`로 변경했다.
- 검색창은 열릴 때 동기적으로 전체 결과를 만들지 않고 창을 먼저 표시한 뒤 Background Dispatcher 차례에 결과를 렌더링한다. 검색 기간·사용자 날짜 변경도 메뉴 선택 상태와 접기/펼치기를 먼저 반영한 후 최신 요청만 계산해, 전체 일정↔날짜 범위 전환의 체감 지연을 줄였다.
- 검색 기간 ComboBox를 설정창과 같은 둥근 드롭다운으로 교체하고 사용자 지정 DatePicker도 흰 배경·인디고 테두리·둥근 모서리·정렬된 화살표를 쓰는 전용 템플릿으로 변경했다. 검색 결과 ScrollViewer에는 공통 10px 파스텔 스크롤바를 적용했다.
- 검색 결과 카드의 내부 Grid에 좌우 12px·상하 5px 실제 Margin을 적용했다. 공통 버튼 템플릿에서 Button Padding이 표시되지 않아 `지난·오늘·예정` 상태가 테두리에 붙어 보이던 문제를 직접 해결했다. 전체 13개 품질 게이트가 PASS했다.
- 검색창 2차 디자인 정리에서 별도 `검색 기간` 캡션 행을 제거했다. 본문 첫 줄은 `검색어 입력 · 기간 드롭다운 · 검색`의 단일 작업 흐름으로 묶고, `오늘`은 검색 조건과 분리해 제목 행 오른쪽의 작은 빠른 이동 버튼으로 옮겼다.
- 사용자 지정 날짜 범위는 해당 기간을 선택했을 때만 검색 행 아래 오른쪽에 7px 간격으로 나타난다. 제목만 드래그 영역으로 지정해 제목 행의 오늘 버튼을 누를 때 창 이동이 시작되지 않게 했다. 전체 13개 품질 게이트가 PASS했다.
- 사용자 지정 DatePicker의 펼침 달력에도 일정 등록창과 같은 ONHARU 스타일을 적용했다. 아이보리 배경, 살구색 셀 테두리, 민트색 오늘, 코랄 선택일, 둥근 날짜·월 버튼을 사용한다.
- 사용자 지정 날짜 행의 정렬 목표를 재확인했다. 일정 카드 끝선이 아니라 바로 위 기간 드롭다운(`사용자 지정`) 열의 오른쪽 끝이 기준이다. 검색 버튼 열 82px만큼 오른쪽 Margin을 두어 날짜 범위 입력의 오른쪽 끝을 기간 드롭다운 오른쪽 끝과 정확히 맞췄다.
- `오늘` 빠른 이동 버튼은 제목 행의 먼 오른쪽에서 제거하고 `일정 검색` 제목 바로 오른쪽 12px 지점에 배치했다. 68×28px 보조 버튼으로 줄여 제목과 한 기능 묶음처럼 보이도록 했다. 전체 13개 품질 게이트가 PASS했다.
- 달력 일정 막대의 `□/✓` 영역을 직접 클릭해 오른쪽 상세 카드와 동일한 완료 상태를 토글할 수 있게 했다. 체크 영역 밖의 한 번 클릭은 기존 날짜 선택, 더블클릭은 일정 수정 동작을 유지하며 Explorer 고정 레이어에서도 같은 규칙을 적용한다.
- 설정의 기존 혼합형 `표시 옵션`을 두 카드로 재구성했다. `표시 옵션`에는 주차·음력·24절기·연속 일정 우선·완료 Todo 아래 이동·24시간제 체크박스를 모았고, `화면과 동작`에는 시작 요일·완료 일정·시작 화면·시작 위치 상태·닫기 버튼 동작·선택 날짜 표시·글자 크기를 묶었다.
- `주차 (Week) 표시`를 체크할 때만 `주차 방식` 행이 펼쳐진다. 방식은 `ISO 방식 (월요일이 첫째 주)`과 `일반 (일요일이 첫째 주)`로 표시한다.
- 달력 열의 `시작 요일`을 주차 계산 방식과 분리해 `월요일 시작/일요일 시작`을 독립 선택하게 했다. 기존 설정에는 종전 ISO/일반 값을 기준으로 동일한 시작 요일을 자동 이관하며, 주차 번호 계산은 ISO/일반 정의를 그대로 사용한다. 전체 13개 품질 게이트가 PASS했다.
- 주차 방식 토글 위치를 표시 옵션 하단에서 `주차 (Week) 표시` 바로 아래로 옮겼다. 켜면 18px 들여쓰기된 `주차 방식` 행이 즉시 나타나고, 음력·24절기 및 나머지 체크 옵션은 그 아래에 이어져 종속 관계가 명확하다.
## 2026-08-19 — xDiary/ONHARU 기능 비교와 2.1 후속 설계

- 설치된 xDiary 한국어 리소스와 ONHARU 2.1 실제 소스를 대조한 `XDIARY_ONHARU_FEATURE_COMPARISON.md`를 추가했다.
- xDiary 우위인 인쇄, 요일별 휴무 설정, 제품 정보, 메모·시간표·자동 업데이트와 ONHARU 우위인 Google 양방향 동기화, Todo 이월, 반복, D-Day·기념일, 범위 설정을 구분했다.
- `시작 요일`과 `쉬는 날`은 독립 설정으로 설계하고, 금·토 휴무가 달력 시작 요일을 자동으로 바꾸지 않도록 결정했다.
- 이동/고정 전환의 잔여 번쩍임은 WPF 표면에만 80~120ms 페이드 완충을 적용하는 별도 시험본으로 먼저 검증하며, Explorer bitmap 다중 재게시는 피하도록 구현 원칙을 기록했다.
- 다음 순서는 페이드 시험 → 쉬는 날 → 인쇄 → 제품 정보 → 기능 소개 HTML이다.

## 2026-08-19 — 전환 완충·쉬는 날·인쇄·제품 정보·소개 페이지

- 이동 전환은 WPF 표면을 준비하고 Explorer 프레임을 해제한 뒤 105ms `EaseOut`, 고정 전환은 Explorer 프레임 게시 후 WPF 표면만 90ms `EaseIn`으로 사라지도록 했다. Explorer bitmap 자체를 여러 프레임 재게시하지 않아 기존 아이콘 안정 경로를 유지한다.
- 설정 데이터 버전을 17로 올리고 `RestDays`를 추가했다. 기존 설정은 토·일 휴무로 마이그레이션하며 월~일을 복수 선택할 수 있다. 시작 요일은 별도 설정으로 유지한다.
- 쉬는 날은 요일 헤더와 날짜 숫자에 차분한 휴무색으로 표시하며 Todo의 `다음 평일` 이월도 사용자 휴무일과 대한민국 공휴일을 함께 건너뛴다.
- 설정 상단에 `인쇄`와 `제품 정보`를 추가했다. 인쇄는 바탕화면을 포함하지 않은 현재 ONHARU 화면을 미리보기로 만든 뒤 Windows 인쇄 대화상자에 전달한다.
- `ProductInfoWindow.cs`에 버전·데이터 저장 원칙·주요 기능·프리웨어 정보를 담은 ONHARU 스타일 팝업을 추가했다.
- `Distribution/ONHARU-FEATURES.html`에 반응형 단일 HTML 소개 페이지를 추가했다. 외부 라이브러리나 네트워크 자원 없이 열 수 있으며 향후 웹사이트에 그대로 연결할 수 있다.
- `check-v21.ps1 -Build`로 앱·x64 Explorer 레이어를 다시 빌드했고 13개 자동 품질 검사를 모두 통과했다.

## 2026-08-19 — 페이드·시작 요일·인쇄 전면 표시·투명도 손잡이 보완

- 이동 전환 페이드는 105ms에서 190ms, 고정 전환은 90ms에서 170ms로 늘려 표면 교대가 번쩍임보다 부드러운 전환으로 인식되게 조정했다.
- `시작 요일`을 월요일/일요일 2개에서 월~일 7개 라디오 버튼으로 확장했다. 월 전체 시작 셀, 사용자 지정 주 범위, 오른쪽 이번 주 계산, Explorer 고정 레이어 날짜 입력 좌표가 모두 동일한 시작 요일을 사용한다.
- 인쇄 미리보기 창은 고정 레이어 복귀 타이밍에 뒤로 밀리지 않도록 처음 열릴 때 전면 활성화하고 모달 작업 중 유지한다. 실제 프린터 선택창을 띄울 때만 Topmost를 잠시 해제한다.
- 인쇄·닫기 버튼을 둥근 템플릿으로 바꾸고, 메인 헤더 투명도 슬라이더의 트랙과 손잡이를 ONHARU 색상의 원형 손잡이로 교체했다.
- 실행 중인 기존 `ONHARU-2.1-local-test.exe`는 덮어쓰지 않고 `ONHARU-2.1-ui-followup-test.exe`로 별도 빌드했다. 새 후보를 대상으로 자동 품질 검사 13개를 모두 통과했다.

## 2026-08-19 — 공통 팝업 헤더와 인쇄 여백

- 제품 정보창 전체에 걸려 있던 `DragMove`를 제거했다. 제목 헤더만 이동 영역으로 사용하고 닫기 버튼은 입력을 우선 소비해, 버튼과 실제 클릭 위치가 어긋나거나 hover가 반전되던 문제를 없앴다.
- `OnharuPopupChrome.cs`를 추가해 공통 둥근 버튼, 닫기 버튼, 이동 가능한 헤더, 강조 팝업 셸을 한 곳에서 제공한다. 제품 정보와 인쇄 미리보기에 우선 적용했으며 이후 다른 팝업도 같은 양식으로 이전할 수 있다.
- 인쇄 미리보기 헤더를 드래그해 창을 이동할 수 있게 하고 인쇄·닫기 버튼을 공통 양식으로 교체했다.
- 인쇄 옵션에 `표준 여백 15mm`, `좁은 여백 7mm`, `여백 없음`을 추가했다. 기본은 15mm이며 이미지 전체를 `Uniform`으로 맞춰 A4 인쇄 가능 영역에서 잘리지 않게 한다.
- 별도 후보 `Tests/LocalTest/ONHARU-2.1-print-popup-test.exe`를 빌드하고 자동 품질 검사 13개를 모두 통과했다.

## 2026-08-19 — 선택일 색상+테두리와 투명도 폭

- 선택일 표시 방식에 `색상 + 테두리`를 추가했다. 기존 배경색과 테두리색 선택 버튼을 함께 사용하며 셀 내부 내용이 움직이지 않도록 기존 음수 margin/Z-index 테두리 경로를 그대로 사용한다.
- 설정 로드 검증이 `both` 값을 정상 설정으로 보존하도록 확장했다.
- 메인 헤더 투명도 슬라이더 폭을 64px에서 94px로 늘렸다. 세로 크기, 원형 손잡이, 실제 투명도 범위는 변경하지 않았다.
- `Tests/LocalTest/ONHARU-2.1-selection-both-test.exe`를 빌드하고 자동 품질 검사 13개를 모두 통과했다.

## 2026-08-19 — 페이드 원복·설정 유지 인쇄·오늘 색상·Claude 검토팩

- 이동/고정 전환의 170~190ms fade animation을 완전히 제거하고, WPF surface prewarm → Explorer frame 해제 → opacity 즉시 복구의 이전 교대 방식으로 원복했다.
- 설정창 인쇄 버튼을 저장/닫기 흐름에서 분리했다. 작은 프린터 아이콘을 누르면 설정창은 그대로 유지되고 제품 정보처럼 자식 인쇄 미리보기만 열린다.
- 인쇄 여백 ComboBox에 설정창과 동일한 ONHARU 둥근 템플릿, 화살표, hover/선택 색상을 적용했다.
- 오늘 표시는 복잡한 방식 선택 없이 중요한 날처럼 파스텔 배경색만 선택하도록 추가했다. 선택일은 색상/테두리/색상+테두리 세 방식을 유지한다.
- `Review/CLAUDE_LAYER_TRANSITION_REVIEW_20260819.md`에 현재 전환 구조, 실패 이력, 유지 조건과 외부 검토 질문을 정리했다.
- `Tests/LocalTest/ONHARU-2.1-no-fade-print-inline-test.exe`를 빌드하고 자동 품질 검사 13개를 모두 통과했다.

## 2026-08-19 — 일정창 상단 저장 버튼 통일

- 신규·수정 일정창 상단 `일정 저장` 버튼의 단색 배경을 제거하고 하단 저장 버튼과 동일한 파랑→보라 그라데이션을 공유하도록 변경했다.
- 상단 버튼의 높이·폭과 동작은 유지하고 글자 굵기를 하단과 같은 Bold로 맞췄다.
- `Tests/LocalTest/ONHARU-2.1-gradient-top-save-test.exe`를 빌드하고 자동 품질 검사 13개를 모두 통과했다.
## 2026-08-19 시간표 독립 모듈 1차 구현

- 설정 `표시 옵션`에 `시간표 기능` 체크박스를 추가했다. 기본값은 꺼짐이며 설정 버전을 18로 올렸다.
- 활성화하면 상단 `오늘` 왼쪽에 `▦` 시간표 버튼이 표시된다.
- 시간표 팝업은 월~토, 10교시 구조이며 시간·과목·장소를 직접 편집하고 저장할 수 있다.
- 시간표 데이터는 일반 일정과 Google 일정에서 완전히 분리된 `%LOCALAPPDATA%\OnharuV3\timetable.json`에 저장한다.
- 독립 파일: `TimetableData.cs`, `TimetableWindow.cs`, `MainWindow.Timetable.cs`. 기능 제거 시 이 파일과 설정/버튼 연결부만 제거하면 된다.
- 검증 후보: `Tests\LocalTest\ONHARU-2.1-timetable-test.exe`
- `check-v21.ps1`의 13개 품질 게이트 전체 통과.

### 시간표 팝업 내 설정·스크롤 보강

- 팝업 우측 상단에 접이식 `시간표 설정` 버튼을 추가했다.
- 표시 요일은 월~일 중 자유롭게 복수 선택할 수 있다. 아무 요일도 선택하지 않으면 월요일을 안전 기본값으로 복구한다.
- 교시 수는 1~12개, 시작 시각은 직접 입력, 수업 시간과 쉬는 시간은 ONHARU 스타일 드롭다운으로 설정한다.
- `적용` 시 `시작~종료` 교시 시간이 자동 계산되고, 기존 과목 데이터는 요일·교시 키 기준으로 보존된다.
- 시간 칸은 자동 생성 후에도 사용자가 직접 수정할 수 있다.
- 세로·가로 스크롤을 모두 지원하며 공통 스크롤 스타일이 방향을 올바르게 인식하도록 보강했다.
- 검증 후보: `Tests\LocalTest\ONHARU-2.1-timetable-settings-test2.exe`; 13개 품질 게이트 전체 통과.

### 시간표 ONHARU 디자인 정리

- 헤더에 파스텔 아이콘 카드와 ONHARU 청록→보라 제목 그라데이션을 적용했다.
- 시간표 설정을 둥근 파스텔 카드로 구성하고 요일별 주말 색상, 입력 높이와 정렬을 통일했다.
- 표 전체에 둥근 외곽선, 교차 행의 미세한 배경 차이, 요일별 파스텔 헤더를 적용했다.
- 시간 열에 `1교시`와 `시작~종료`를 계층적으로 표시하되 시간은 계속 직접 수정할 수 있다.
- 저장 버튼은 다른 일정 저장 버튼과 같은 파랑→보라 그라데이션으로 맞췄다.
- 검증 후보: `Tests\LocalTest\ONHARU-2.1-timetable-style-test.exe`; 13개 품질 게이트 전체 통과.

### Claude 레이어 전환 검토 대조

- `CLAUDE_LAYER_TRANSITION_REVIEW_RESULT_20260819.md`의 핵심 진단인 WPF/DWM 비동기 표면과 Explorer/GDI 동기 표면의 교대 간극은 현재 코드와 일치한다.
- 가장 낮은 위험의 다음 실험은 고정→이동 경로에서 `Opacity = intendedOpacity`를 `explorerFrame.Disable()`보다 먼저 실행하는 순서 반전이다.
- 이동→고정의 추가 Render turn 지연은 입력 지연 회귀 가능성이 있어 실험 A 결과 이후 별도 후보에서만 검증한다.
- `DwmFlush`, `RDW_ERASE`, 페이드, 네이티브 합성 캐시 변경은 과거 회귀 이력 때문에 재도입하지 않는다.

### 후보 17 — Claude 실험 A 적용

- 고정→이동 `ShowPreparedWpf`의 마지막 Render turn에서 `Opacity = intendedOpacity`를 먼저 지정하고 `explorerFrame.Disable()`을 뒤에 실행하도록 두 줄의 순서만 변경했다.
- 이동→고정 경로, 포인터 갱신, 아이콘 합성, 네이티브 캐시는 변경하지 않았다.
- 품질 게이트도 새 의도 순서를 회귀 조건으로 검사하도록 갱신했다.
- 후보: `Tests\LocalTest\TransitionCandidates\17-claude-opacity-before-disable\ONHARU-2.1-transition-A-test.exe`
- 설명/원복법: 같은 폴더의 `README.md`; 13개 품질 게이트 전체 PASS.

### 후보 18 — WPF 렌더 경계 게이트

- 후보 17 사용자 실측: 고정 전환은 안정적이고, 이동 전환은 대부분 개선됐으나 간헐적 깜빡임이 남았다.
- 정상 Opacity 복구 후 `CompositionTarget.Rendering` 두 번을 통과한 뒤 Explorer 프레임을 제거하도록 고정→이동 경로만 변경했다.
- 새 전환 시작·취소·재고정 시 Rendering 이벤트를 해제하여 잔류 콜백을 방지했다.
- 후보: `Tests\LocalTest\TransitionCandidates\18-wpf-render-boundary\ONHARU-2.1-transition-render-boundary-test.exe`
- Claude 재검토 문서: `Review\CLAUDE_LAYER_TRANSITION_EXPERIMENT_RESULT_20260819.md`
- 13개 품질 게이트 전체 PASS. 최종 육안 평가는 사용자 실기기 확인이 필요하다.

### 후보 18 실패 및 후보 19 복원

- 사용자 실측에서 이동→고정은 안정적이었으나 고정→이동 깜빡임이 후보 17보다 더 자주 발생했다.
- `CompositionTarget.Rendering`은 DWM present ACK가 아니며 대기 중 두 표면의 중복과 스케줄링 경쟁을 늘린 것으로 판정했다.
- Rendering 핸들러와 게이트를 전부 제거하고 후보 17의 동일-turn `Opacity 복구 → Explorer Disable` 순서로 원복했다.
- 복원본: `Tests\LocalTest\TransitionCandidates\19-candidate17-restored\ONHARU-2.1-transition-stable-restored.exe`; 13개 품질 게이트 PASS.

### ONHARU 2.1 공식 전환 방식 확정

- 후보 17을 2.1 공식 최신 소스로 확정했다. 후보 19는 후보 17 소스를 깨끗하게 복원해 검증한 바이너리다.
- 후보 17 선택 이유, 후보 18 실패 결과, 재도입 금지 항목, 알려진 제한과 향후 구조적 연구 방향을 `Docs\Architecture\LAYER_TRANSITION_FINAL_2.1.md` 및 `BUILD_AND_RELEASE.md`에 상세 기록했다.
- 잔여 고정→이동 미세 깜빡임은 알려진 제한으로 남기되 앞으로 별도 후보에서 계속 개선한다. 공식 소스의 안정적인 아이콘 합성·클릭 경로는 직접 실험 대상으로 사용하지 않는다.

### 2026-08-19 ONHARU 2.1.0 최종 설치판 생성

- 후보 17 공식 소스와 시간표 최종 UI를 포함해 `check-v21.ps1 -Build`를 실행했고 App/ExplorerLayer 빌드 및 13개 품질 게이트가 모두 통과했다.
- `build-release.ps1`로 최적화 App, x64 DesktopHook/LayerHost, Inno Setup 6.7.3 설치판을 재생성했다.
- 최종 설치 파일: `Release\Installer\ONHARU-2.1.0-Setup.exe` (2,311,406 bytes)
- SHA-256: `DF0CE8E1124C241625C725606B8848A855F2CD25BD0A037BC335631B321349C2`
- `Release\ONHARU-2.1.0\SHA256SUMS.txt`의 ONHARU.exe, DesktopHook.dll, LayerHost.exe, README.md를 실제 파일과 재검증해 모두 일치했다.
- 릴리스 설명: `Release\Installer\RELEASE_NOTES-2.1.0.md`

### 후보 19B — cloak-only + Rendering 1회 독립 시험

- Claude 후속 의견에 따라 WPF Opacity를 0으로 내리지 않고 정상값으로 유지한 채 uncloak하고, `CompositionTarget.Rendering`을 한 번 받은 뒤 Explorer 프레임을 제거하는 후보를 제작했다.
- 기존 후보 19가 후보 17 복원 검증 번호이므로 새 시험은 `19B-cloak-only-one-render`로 구분했다.
- 시험본: `Tests\LocalTest\TransitionCandidates\19B-cloak-only-one-render\ONHARU-2.1-transition-19B-test.exe`
- 빌드 및 13개 품질 게이트 PASS. 최종 설치판은 변경하지 않았다.
- 시험본 생성 후 활성 프로젝트 소스는 공식 후보 17로 즉시 원복한다. 사용자 실측 승인 전에는 19B를 공식 빌드에 반영하지 않는다.

### 후보 19B 최초 시험 무효 및 패키지 보정

- 최초 후보 폴더에 App EXE만 있고 LayerHost와 DesktopHook DLL이 없어 Explorer 고정 레이어가 시작되지 않았다.
- 사용자가 확인한 `깜빡임 없음 + 아이콘을 달력 위로 놓을 수 없음`은 WPF 창만 표시된 폴백 상태였으므로 전환 성공 결과가 아니다.
- `Tests\LocalTest`에서 동일 빌드의 `OnharuV3.LayerHost.exe`, `OnharuV3.DesktopHook.dll`을 후보 19B 폴더에 복사해 완전한 3파일 시험 세트로 보정했다.
- 공식 설치판과 공식 후보 17 소스는 변경하지 않았다.

### 2026-08-20 시간표·Google 계정·색상 선택 UX 보강

- 시간표 기본 창 높이를 9교시 표의 실제 높이에 맞추고, 외곽 Border 자체를 자르던 클립을 내부 ScrollViewer에만 적용해 네 모서리 테두리 잘림을 제거했다.
- 시간표 설정에 작게/보통/크게 글자 크기를 추가했다. 과목뿐 아니라 왼쪽 교시·시간에도 비례 적용하며 `timetable.json`에 저장한다.
- `나의 시간표` 글자에 한정됐던 이동 영역을 상단 빈 영역 전체로 확장하되 버튼·입력 컨트롤은 이동 시작에서 제외했다.
- 메인 시간표 아이콘은 34px/19px로 키워 식별성을 높였다.
- 설정의 Google 계정 작업창에 현재 연결 계정명을 표시한다.
- 오른쪽 Google 계정 카드를 눌러 수동 동기화하면 카드가 보라색 `동기화 중` → 초록색 `동기화 완료` 또는 분홍색 오류 상태로 바뀐 뒤 정상 계정 표시로 돌아온다.
- 색상 편집 체크는 최대 2개까지만 가능하며 2개가 선택되면 나머지를 비활성화한다. 하나를 해제하면 즉시 다시 선택할 수 있다.
- 색상 교환 버튼을 `☑ ⇄ ☑` 형태로 변경해 두 선택 색상의 교환이라는 의미를 명확히 했다.
- 로컬 시험본: `Tests\LocalTest\ONHARU-2.1-local-test.exe`; App 빌드 및 13개 품질 게이트 전체 PASS.

### 2026-08-20 시간표 치수·색상 라벨·고정 투명도 후속 보정

- 메인 달력의 시간표 버튼은 기존 30px/13px로 원복하고, 시간표 팝업의 `나의 시간표` 옆 아이콘만 42px 카드·22px 글리프로 확대했다.
- 시간표 설명 문구를 제거하고 기본 창을 568px로 조정해 9교시에서 세로 스크롤이 생기지 않도록 했다.
- 시간표 외곽 Padding을 제거하고 마지막 행·열의 중복 경계선을 생략해 내부 표와 둥근 외곽선 사이의 틈·겹침을 정리했다.
- 색상명 자체를 CheckBox Content로 통합해 체크박스뿐 아니라 옆 글자를 눌러도 선택된다. 교환 버튼은 `☑ ⇄ ☑ 색상 교환`으로 의미를 함께 표시한다.
- 고정 상태 투명도 변경 시 숨겨진 WPF 창의 Opacity까지 동시에 바꾸던 경로를 제거했다. Explorer 캐시 알파 갱신은 32ms 단위로 합쳐 과도한 repaint를 줄이고, 포인터를 놓을 때 최종 알파와 프레임을 한 번 확정한다.
- `Tests\LocalTest\ONHARU-2.1-local-test.exe` 재빌드 및 13개 품질 게이트 전체 PASS.

### 2026-08-20 시간표 틀 고정·Google 상태 게시·투명도 손잡이 히트 보정

- 시간표가 10교시 이상이어서 스크롤될 때 첫 행의 요일과 첫 열의 교시·시간이 화면에 고정되도록 ScrollViewer offset을 역보정한다.
- 시간표 스크롤바는 기존 ONHARU 둥근 스크롤 스타일을 유지한다.
- 메인 시간표 버튼은 32px 안에서 Segoe UI Symbol 글리프를 중앙 정렬했고, 시간표 팝업 아이콘은 제목 글자 높이에 맞춘 32px 카드·16px 글리프로 축소했다. 헤더와 표 간격도 우측 버튼 아래 간격과 맞췄다.
- 색상 교환 버튼 폭을 154px로 늘려 `☑ ⇄ ☑ 색상 교환` 문구가 잘리지 않게 했다.
- 고정 레이어 투명도는 네이티브 hit map의 슬라이더 전체가 아니라 현재 투명도에 해당하는 손잡이 중심 ±10px에서만 drag capture를 시작한다. 트랙 클릭은 한 번의 값 변경만 허용한다.
- Google 계정 카드의 `동기화 중/완료/오류` 상태를 바꿀 때 고정 Explorer 프레임도 즉시 다시 게시하여 실제 화면에서 상태가 보이도록 했다.
- App/네이티브 레이어 재빌드 및 13개 품질 게이트 전체 PASS.

### 2026-08-20 고정 레이어 상태 Visual 강제 게시

- 고정 모드의 Google 카드와 투명도 손잡이는 WPF 상태만 바뀌고 Explorer 캐시에는 이전 Visual이 남을 수 있었다.
- 일반 `SchedulePublish`와 별개로 `ScheduleFixedVisualRefresh`를 추가해 상태성 UI가 바뀌면 Render 우선순위에서 현재 Visual, opacity, hit map, action sink를 한 세트로 다시 게시한다.
- Google 카드의 동기화 시작·완료·오류·정상 복귀에서 강제 프레임을 게시한다.
- 고정 투명도는 32ms마다 캐시 알파와 새 손잡이 Visual을 함께 게시하고, 놓을 때 최종 프레임을 다시 확정한다.
- 시간표 `나의 시간표` 제목을 2px 내려 아이콘과의 수직 균형을 맞췄다.
- App/네이티브 빌드와 13개 품질 게이트 전체 PASS. 최신 시험본과 LayerHost가 함께 실행됨.

### 2026-08-20 설정창 잔류 종료 오류 수정

- 설정·검색 등 중앙 팝업은 고정 레이어 호환을 위해 MainWindow Owner가 없는 독립 WPF 창이므로 메인 `Close()`만으로는 함께 닫히지 않았다.
- 모든 종료 진입점(종료 확인창, 닫기 우클릭 메뉴, 트레이 종료)을 `ExitApplication`으로 통합했다.
- 종료 확정 시 `Application.Current.Windows`의 ONHARU 보조 창을 먼저 닫고 메인 창을 닫는다. MainWindow Closing에서도 같은 정리를 방어적으로 수행한다.
- 취소·최소화 분기는 기존 상태 복구 동작을 그대로 유지하며 보조 창을 닫지 않는다.
- 시험본 재빌드 및 13개 품질 게이트 전체 PASS.

### 2026-08-20 고정 Google 전용 입력·투명도 이중 주기 개선

- 고정 Google 카드 클릭을 일반 WPF 좌표 재히트에 맡기지 않고 hit map Kind 5 → 네이티브 action 108 → `GoogleClick`으로 직접 연결했다.
- Google 동기화 시작·완료·오류 상태는 Dispatcher 예약 대신 현재 Visual을 동기 캡처·게시한다. 완료 카드는 1.8초간 유지한다.
- 고정 투명도는 네이티브 캐시 알파를 16ms 주기로 갱신하고, 무거운 WPF 손잡이 Visual은 4회마다 갱신한다. 투명도 변화는 부드럽게 유지하면서 전체 프레임 캡처 부하는 줄인다.
- 손잡이를 놓으면 알파와 손잡이 Visual을 최종 한 번 동기화한다.
- App/네이티브 레이어 빌드와 13개 품질 게이트 전체 PASS.

### 고정 투명도 후보 02 — 슬라이더 영역 부분 게시

- 사용자 평가 80점의 안정본 3파일을 `Tests\LocalTest\OpacityCandidates\01-stable-80point`에 보존했다.
- 전체 WPF 달력을 다시 캡처하던 경로 대신, 현재 Visual에서 투명도 슬라이더의 94×18px 영역만 RenderTargetBitmap으로 잘라 공유 비트맵의 비활성 슬롯에 기록한다.
- 두 공유 슬롯은 전체 게시 시 동기화하고, 부분 게시에서는 슬라이더 영역만 갱신한 뒤 슬롯과 generation을 원자적으로 전환한다.
- hit map reserved 값을 부분 갱신 표식으로 사용해 네이티브 합성기가 깨끗한 바탕화면 캐시를 불필요하게 다시 수집하지 않도록 했다.
- 후보 폴더: `Tests\LocalTest\OpacityCandidates\02-slider-region-patch`; App/네이티브 빌드 및 13개 품질 게이트 전체 PASS.
- 사용자 실측 전까지 공식 설치판은 변경하지 않는다. 회귀 시 후보 01의 3파일 또는 해당 시점 소스로 원복한다.

### 후보 02 실패·80점 안정본 원복 및 Claude 패키지

- 사용자 실측 결과 후보 02는 80점 안정본보다 투명도 조작감이 나빠 실패로 판정했다.
- `UpdateRegion`, 공유 슬롯 동기화, 부분 generation/base-cache 예외를 활성 소스에서 모두 제거했다.
- 활성 소스는 16ms constant-alpha 갱신 + 4회마다 WPF thumb 갱신 + release 최종 게시 방식으로 원복했으며 App/네이티브 빌드와 13개 품질 게이트가 모두 PASS했다.
- Claude 요청 문서와 안정/실패 소스를 `Review\Claude-Fixed-Opacity-Review-20260820`에 구성했다.
- 공유 ZIP: `Review\CLAUDE_FIXED_OPACITY_REVIEW_20260820.zip`.
- 검토 핵심은 native thumb/background 분리, Explorer paint cadence, cache invalidation, 최소 위험 실험 순서다.

### 고정 투명도 후보 03 — QPC 병목 계측

- 안티그래비티 검토 결과 중 안전한 1단계인 QPC 계측만 적용했다. 합성·캐시·입력 동작은 80점 안정본과 동일하다.
- opacity-only frame에서 전체 Base BitBlt, GDI AlphaBlend, 최종 BitBlt 시간을 각각 마이크로초로 측정한다.
- 60표본마다 `%TEMP%\ONHARU-opacity-qpc.log`에 패널 크기, 평균 및 최대 시간을 기록한다. 파일 쓰기는 계측 구간 밖에서 60표본마다 한 번만 수행한다.
- 후보 폴더: `Tests\LocalTest\OpacityCandidates\03-qpc-instrumentation`.
- App/네이티브 빌드 및 13개 품질 게이트 전체 PASS. 수치 확보 후 계측 코드는 제거한다.

### 후보 03 실측 결과 및 고정 커서 hit map 보정

- 사용자 왕복 조작 360표본(60표본×6구간), 패널 1157×1032 실측 결과:
  - Base BitBlt 평균 0.48~0.64ms, 최대 1.29ms
  - GDI AlphaBlend 평균 3.30~6.76ms, 최대 11.47ms
  - Final BitBlt 평균 0.29~0.50ms, 최대 0.97ms
  - 전체 평균 4.07~7.89ms
- AlphaBlend가 전체 비용과 변동의 대부분을 차지한다는 병목 가설이 실제 환경에서 확인됐다.
- 고정 상태 빈 헤더 구역이 손모양이 되던 원인은 최신 hit map보다 과거의 넓은 좌표 추정 fallback을 먼저 적용했기 때문이다.
- 날짜·일정 Tag도 정확한 hit map에 포함하고, 유효한 현재 generation hit map이 있으면 실제 레코드만 커서 근거로 사용한다. 넓은 좌표 추정은 hit map이 없을 때만 사용한다.
- App/네이티브 재빌드 및 13개 품질 게이트 전체 PASS.

### 고정 투명도 후보 04 — AVX2 DIB 합성

- 후보 03 실측에서 GDI AlphaBlend가 평균 3.30~6.76ms, 최대 11.47ms로 전체 합성 병목임을 확인한 뒤 별도 후보로 구현했다.
- `g_baseDC`와 `g_finalDC`의 비트맵을 32bit top-down DIBSection으로 변경해 raw BGRA 픽셀 접근을 가능하게 했다.
- AVX2 지원 CPU에서는 Pbgra32 source color/alpha와 constant opacity를 4픽셀 단위 16bit 벡터 연산으로 합성한다.
- AVX2 미지원 또는 DIB 초기화 실패 시 기존 GDI AlphaBlend 경로로 자동 fallback한다.
- 아이콘 계층, base-cache 무효화, generation, hit map, 후보 17 전환 순서는 변경하지 않았다.
- QPC 로그에 `backend=avx2|gdi`를 기록해 기존 수치와 직접 비교한다.
- 후보 폴더: `Tests\LocalTest\OpacityCandidates\04-avx2-compositor`; App/네이티브 빌드 및 13개 품질 게이트 전체 PASS. 사용자 색상·합성 육안 확인 전 공식 채택 금지.
- 실기기 계측 결과(1157x1032): AVX2 합성 평균 `71.1~71.7ms`, 최대 `77.3ms`로 기존 GDI `3.3~6.8ms`보다 약 10배 이상 느렸다.
- 판정: **실패 및 폐기**. 현재 네이티브 빌드 조건과 16-bit lane 변환 방식에서는 메모리 왕복·픽셀 변환 비용이 GDI 하드웨어 최적화 경로보다 훨씬 컸다.
- 조치: 실험 산출물은 `Tests/LocalTest/OpacityCandidates/04-avx2-compositor`에 보존하고, 활성 소스는 후보 03 QPC 계측본과 완전히 동일하게 원복했다. 공식 안정본/배포본으로 승격하지 않는다.

### 고정 투명도 후보 05 — 네이티브 슬라이더 오버레이

- 목적: 드래그 중 4틱마다 수행하던 전체 WPF `RenderTargetBitmap` 재생성을 제거하면서 손잡이는 즉시 따라오게 한다.
- 구현: `DesktopHook.cpp`가 hit-map의 Slider 영역을 찾아 Explorer paint 결과 위에 트랙과 원형 손잡이를 직접 렌더링한다. 마우스 이동 때 해당 작은 영역만 즉시 다시 그린다.
- C# 변경: 고정 모드 드래그 중에는 상수 알파만 16ms 간격으로 갱신하고 전체 WPF visual refresh는 하지 않는다. 버튼을 놓을 때 기존 WPF 슬라이더와 최종 프레임을 한 번 복원한다.
- 보존 범위: Explorer 아이콘 우선 입력, 캐시 generation, 후보 17 레이어 전환, 저장 로직은 변경하지 않았다.
- 산출물: `Tests/LocalTest/OpacityCandidates/05-native-slider-overlay`
- 검증: 네이티브/C# 빌드 성공, ONHARU 2.1 품질 게이트 13개 통과. 실기기 시각 검증 전이므로 공식 안정본이 아닌 실험 후보이다.
- 실기기 판정: 사용자 확인 결과 기존 디자인과 동작 감각을 해쳐 **폐기**했다. 후보 폴더만 분석 기록으로 보존한다.
- 조치: 활성 소스는 후보 05의 네이티브 오버레이 및 C# refresh 억제를 모두 제거하고, 사용자 평가 80점의 후보 03 방식으로 복구했다. 투명도 추가 최적화 실험은 중단한다.

## 2026-08-21 사용자 테스트 정리 및 안정화

- 중복된 사용자 테스트 메모를 `Docs/Audits/TEST_REVIEW_AND_ROADMAP_20260821.md`로 통합하고, 즉시 수정과 후속 개발을 분리했다.
- Explorer 고정 상태의 `중요한 날` 팝업은 WPF 화면으로 잠시 전환하여 표시하고 닫힌 뒤 고정 레이어로 복귀하도록 수정했다.
- 설정창이 모달로 열린 상태에서도 알림 확인·미루기가 가능하도록 알림창을 독립 STA UI 스레드에서 실행하고 결과만 메인 Dispatcher로 전달한다.
- 기념일 등록·수정 또는 일정→기념일 전환 후 현재 달과 선택 날짜를 강제로 바꾸지 않도록 수정했다.
- 기념일 창에 검증된 `YYYYMMDD` 빠른 입력을 추가했다. 8자리 실제 날짜와 1900년 이후 값만 허용한다.
- 원하는 달 이동은 연도 직접 입력 대신 `±1년`, `±10년` 클릭식 이동으로 변경했다.
- 데이터 관리 버튼의 작업 플래그를 상호 초기화해 내보내기와 가져오기 동작이 섞이지 않도록 보강했다.
- 백업 복원은 로컬 일정만 교체하고 기존 Google 일정은 보존하며, 완료 안내에도 이를 명시한다.
- 하루 종일 일정에서는 시간 입력을 숨기고 오전·오후 선택 시에만 시·분 입력을 표시한다.
- 프로 경기 일정은 향후 독립 모듈용 야구 아이콘과 준비 중 안내만 추가했다. API 연동은 아직 하지 않았다.
- 혼합 DPI 다중 모니터, 0% 완전 투명, 오늘/선택일 복합 표시, 일기장, ICS, Google Tasks, 스포츠 API, 모바일, 자동 업데이트·다국어는 부분 수정하지 않고 `TODO_NEXT.md`에 우선순위별로 분리했다.
- 수동 확인 대상: 고정 상태 중요한 날, 설정창 위 알림 입력, 네 가지 데이터 관리 버튼, 기념일 빠른 입력과 화면 유지, 연도 이동 버튼, 조건부 시간 입력.

### 고정 상태 검은 프레임·중요한 날 입력 회귀 수정

- 초기 진단에서는 WPF 팝업 전환과 `SchedulePublish()` 경쟁을 의심했으나, 전환 차단만으로는 해결되지 않았다. 해당 임시 변경은 원복했다.
- 최종 해결은 아래 후보 02의 레이어 내부 floating palette 및 PREPAINT 완성 프레임 캐시 방식이다.
- 야구 아이콘은 일반 텍스트 버튼의 좌우 콘텐츠 여백을 상속하지 않도록 전용 `IconButton`을 추가했다. 아이콘 내부 여백은 0이며 상하좌우 중앙 정렬한다.
- C# 빌드 성공 및 ONHARU 2.1 품질 게이트 13개 전체 PASS.
- 실기기 확인 대상: 시작부터 검은 패널이 없는지, 고정 상태에서 `중요한 날` 단일 클릭으로 팔레트가 열리는지, 색상 선택·닫기 후 다시 정상 고정되는지.

### 고정 회색 배경 근본 원인 확인 및 직접 PREPAINT 합성 후보

- 전체 화면을 직접 캡처해 약 63% 투명도인데도 달력 영역이 벽지 대신 균일한 진회색으로 합성되는 현상을 확인했다.
- 원인: `SysListView32`의 중간 paint DC를 배경으로 캐시했지만, 해당 DC에는 DWM 벽지가 없고 list-view fallback 색만 들어오는 경우가 있다. 이 캐시 위에 반투명 달력을 합성하면서 큰 회색 사각형이 만들어졌다.
- 수정 후보: `CDDS_PREPAINT`가 제공한 현재 paint DC 위에 캐시된 WPF Pbgra32 프레임을 직접 `AlphaBlend`한다. 불완전한 `g_baseDC/g_finalDC` 배경 캐시를 최종 출력에 사용하지 않는다.
- 실제 화면 재캡처 결과 벽지가 달력 아래로 정상적으로 비치며 회색 패널이 제거된 것을 확인했다.
- `중요한 날`은 고정 레이어에서 숨은 WPF `Popup`을 열지 않는다. 메인 visual tree에 포함된 inline 팔레트를 펼치고 Explorer hit map으로 색상 버튼을 직접 처리한다.
- 야구 아이콘은 글꼴 이모지 대신 원·솔기 Path로 만든 16×16 벡터 아이콘을 사용하며 내부 여백 0, 상하좌우 중앙 정렬한다.
- 후보·전후 소스·화면 캡처: `Tests/LocalTest/FixedLayerCandidates/01-direct-prepaint-alpha`.
- 네이티브/C# 빌드 성공 및 품질 게이트 13개 전체 PASS. 바탕화면 아이콘 이동 시 깜빡임은 사용자 실기기 확인 후 공식 안정본 채택 여부를 결정한다.

### 고정 레이어 후보 02 — PREPAINT 최초 합성 + 완성 프레임 캐시

- 후보 01의 직접 AlphaBlend는 회색 배경을 해결했지만 아이콘 이동 paint 때마다 전체 알파 합성을 반복해 깜빡임이 재발했다.
- 후보 02는 새 generation·투명도에서만 Explorer의 정상 PREPAINT DC에 직접 합성하고 그 완성 화면을 `g_finalDC`에 저장한다. 같은 화면의 아이콘 이동·선택 repaint는 캐시된 완성 프레임을 `BitBlt` 한 번으로 복원한다.
- `중요한 날` 팔레트는 레이아웃 아래로 밀어내지 않고 메인 frame 최상단에 겹치는 floating overlay로 배치한다. 고정 레이어 visual/hit map 안에 있으므로 별도 WPF Popup 없이도 클릭 가능하다.
- 후보 폴더: `Tests/LocalTest/FixedLayerCandidates/02-prepaint-final-cache`.
- 직접 화면 캡처에서 벽지 투명 합성, 달력 위치, 야구 벡터 아이콘 중앙 정렬을 확인했다. 네이티브/C# 빌드 및 품질 게이트 13개 전체 PASS.
- **실기기 최종 판정: 성공·안정본 채택.** 사용자가 바탕화면 아이콘을 달력 위에 놓고 이동해도 깜빡이지 않음을 확인했다.
- 유지 불변식: 새 generation·투명도에서만 PREPAINT DC에 직접 AlphaBlend하고, 이후 아이콘 관련 repaint는 완성된 `g_finalDC`를 BitBlt한다. `SysListView32` 중간 DC를 별도 배경으로 캐시하거나 매 repaint마다 AlphaBlend하는 방식으로 되돌리지 않는다.
- 후속 UI 보정: `중요한 날` floating palette를 일반 Grid margin 배치에서 별도 Canvas overlay로 변경했다. 팔레트 위치가 부모 레이아웃의 측정값을 다시 바꾸며 아래·왼쪽으로 튀던 피드백 루프를 제거했다. Canvas 좌표는 버튼 우측 하단을 기준으로 한 번 계산하며 달력 본문 배치를 밀지 않는다.

### 2026-08-21 입력·백업·표시 안정화

- 원하는 달 이동창은 `±1년`, `±10년` 클릭과 4자리 연도 직접 입력을 함께 지원한다. 직접 입력은 Tab, 월 선택, 포커스 이탈 시 검증·적용한다.
- 기념일창은 체크박스형 빠른 입력을 제거하고 `기념일자` YYYYMMDD 입력과 달력 아이콘을 한 줄에 배치했다. `다가오는 날짜를 D-Day로 표시` 옵션을 사용한다.
- 백업 복원창에 실제 `%LOCALAPPDATA%\OnharuV3\backups` 경로와 폴더 열기 버튼, 목록 패딩·둥근 컨테이너를 추가했다. 자동 백업은 저장 때 당일 파일을 갱신하고 다음 날 새 파일을 만든다.
- 오늘 표시를 `없음/색상/테두리/색상+테두리`로 확장했다. 선택일과 오늘이 동시에 테두리를 쓰지 않도록 설정 단계에서 상호 배제하며 화면에서는 선택일 테두리를 우선한다.
- 투명도 범위를 0%까지 확장하고 트레이에 `투명도 복구 (70%)`를 추가했다. 네이티브 고정 레이어도 alpha 0을 유효값으로 처리한다.
- Google 상태 문구를 오른쪽 아래의 조용한 오버레이로 이동했다.
- 이월 기능 사용 설정을 추가하되 기존 데이터와 저장 형식은 보존하고 등록·수정 UI만 숨긴다.
- 혼합 DPI 위치 이동 회귀를 줄이기 위해 모니터 장치명과 Win32 물리 픽셀 위치·크기를 함께 저장·복원한다. 모니터가 빠진 경우 주 모니터 작업영역 안으로 복구한다.
- 설정 데이터 버전은 21이다. C# 빌드 및 기존 품질 게이트 13개 전체 PASS. 실제 확인 대상은 기념일/연도/백업 UI, 0% 복구, 오늘·선택일 조합, 100/125/150% 혼합 DPI 재실행이다.

### 2026-08-22 입력 흐름·백업·동기화 표시 보정

- 기념일 이름에서 Tab을 누르면 `기념일자` 입력칸으로 이동하고 기존 YYYYMMDD 전체를 선택해 즉시 덮어쓸 수 있게 했다.
- 0% 투명은 달력의 존재와 조작 가능성을 사용자가 인지할 수 없어 실사용 하한을 10%로 되돌렸다. `alpha 0 Explorer frame`은 향후 레이어 전환의 무표시 완충 실험 아이디어로만 보류하며 공식 동작에는 사용하지 않는다.
- 백업 복원창 안내를 `복원할 백업 날짜를 선택하세요`로 고치고 불필요한 `기본 백업 폴더` 제목을 제거했다. 폴더 카드 높이·목록 간격·폴더 아이콘 크기와 중앙 정렬을 보정했다.
- 자동·수동 Google 동기화 모두 오른쪽 아래에 `동기화 중…`과 녹색 `동기화 완료`를 잠시 표시한다. MADE BY 기준선처럼 오른쪽 끝과 아래 기준선을 맞췄다.
- 설정의 `표시 옵션`을 `표시 설정`으로 변경하고 `이월 기능`을 `미완료 Todo 자동 이월`로 명확히 했다. 완료 Todo 배치 옵션 옆에 두어 관련 설정을 묶었다.
- C# 빌드 성공 및 기존 품질 게이트 13개 전체 PASS. 혼합 DPI 실제 모니터 조합 검증은 환경 확보 시까지 미완료로 유지한다.
- 사용자 재검토에 따라 설정 캡션은 `표시 옵션`, `시간표`로 확정했다. 백업 날짜 카드는 좌우 여백과 큰 복원 아이콘을 가진 한 줄 구성으로 보정했다. Google 상태는 사이드바 안에서 제거하고 MADE BY와 같은 하단 오버레이 행의 오른쪽 끝으로 이동했다.
- 백업 날짜 카드의 바깥 좌우 Margin은 원복하고 내부 왼쪽 Padding만 18px로 늘렸다. 새 일정·일정 수정 모두 초기 로딩 직후 시간 모드를 명시적으로 적용하여 `하루 종일`이면 첫 화면부터 1~12시·분 입력 영역을 숨긴다.
- 일반 D-Day 카드 규칙을 `미래 D-n / 당일 D-0 / 지난 뒤 D+1~D+7`로 확정했다. 8일 이상 지난 항목은 오른쪽 D-Day 카드에서만 제외하며 원본 일정, 달력 표시, Google/로컬 저장 데이터는 변경하거나 삭제하지 않는다.
- 당일 표기는 최종적으로 `D-Day`로 변경했다. 일반 D-Day 당일 행은 연한 하늘색 배경·파란 테두리·Bold, 기념일 당일 행은 연한 분홍 배경·분홍 테두리·Bold로 강조한다. 미래 `D-n`, 지난 뒤 `D+1~D+7` 규칙은 유지한다.
- 중대 회귀 수정: 고정 레이어의 WPF 릴레이 창은 전환 중 임시 `Opacity=0`을 사용할 수 있는데, 필터 체크박스의 `SaveWindowSettings()`가 이 창 값을 사용자 투명도로 저장하고 있었다. 이제 투명도 슬라이더 값만 저장한다. 오염된 시험 설정 `0.418895...`는 사용자 의도에 따라 `0.98`로 복구했으며 원본은 `%LOCALAPPDATA%\OnharuV3\settings.pre-opacity-fix-20260822.json`에 보존했다.
- 백업 날짜 카드는 사용자 피드백에 따라 원래 한 줄 버튼 형태로 복구하고, 공통 둥근 버튼 템플릿이 `Padding`을 실제 콘텐츠에 전달하도록 수정해 내부 왼쪽 여백 20px를 적용했다. 카드 바깥 좌우 여백은 없다.
- 자동 백업 위치는 `%LOCALAPPDATA%\Onharu\backups`로 변경했다. 기존 `%LOCALAPPDATA%\OnharuV3\backups` 파일은 삭제하지 않고 새 위치에 없는 파일만 자동 복사한다. 나머지 2.0 호환 데이터·토큰 경로는 변경하지 않는다.
- 새 일정·수정창 폭을 460→480px, 기본 스크롤 허용 높이를 최대 620→690px로 조정해 일반 등록 상태에서는 스크롤이 생기지 않도록 했다. 작은 화면에서는 기존처럼 자동 스크롤을 유지한다.
- 공통 `UiRound.StyleTextBox`를 추가하고 일정 알림·반복 횟수, 기념일 YYYYMMDD, 연도 직접 입력, 시간표 시작 시간 등 팝업의 숫자 입력칸을 둥근 ONHARU 입력 디자인으로 통일했다.
- 백업 날짜 카드는 클릭 즉시 복원하지 않고 선택 강조만 한다. 하단 `선택한 날짜로 복원` 버튼은 선택 전 비활성화되며, 사용자가 명시적으로 누른 경우에만 기존 복원 절차로 넘어간다.
- 공통 단일행 TextBox 템플릿은 상하 Padding을 0으로 정규화하고 내부 ScrollViewer를 세로 중앙에 놓도록 보정했다. 제목·연도·YYYYMMDD·알림 값·반복 횟수 등 글자가 위로 밀리거나 잘리는 문제를 함께 수정했으며, 여러 줄 메모 입력의 Padding은 유지한다.

### 2026-08-22 사용자 실기기 승인

- 업무·개인 필터 반복 변경 시 투명도 유지: PASS
- 백업 날짜 선택과 실제 복원 확인 단계 분리: PASS
- 새 일정·수정 기본창 무스크롤 표시: PASS
- 제목·숫자 입력 중앙 정렬 및 잘림 없음: PASS
- 일반 D-Day·기념일 당일 강조: PASS
- 고정 상태 중요한 날·일정 등록/수정·필터 입력: PASS
- 수동 동기화 하단 오른쪽 상태 표시: PASS
- 위 7개는 사용자 실기기 확인으로 승인됐다. 남은 환경 의존 검사는 혼합 DPI 다중 모니터이며, 다음 배포 단계는 최종 코드 감사→설치/업그레이드/제거 시험→GitHub 2.1 Release 갱신 순서다.

### 2026-08-22 변경분 리팩토링 감사

- 오늘 수정 파일의 책임 경계를 재검토했으며 대규모 재분리는 필요하지 않다고 판정했다.
- Google 상태 표시의 지역 타이머 중복을 단일 `googleStatusTimer`로 바꿔 과거 타이머가 최신 상태를 숨기는 경쟁을 제거했다.
- 구 백업 경로 마이그레이션은 프로세스당 한 번만 실행하도록 잠금·완료 플래그를 추가했다.
- 백업 폴더 열기 실패의 빈 catch를 개인정보 없는 오류 로그로 교체했다.
- 상세 감사: `Docs/Audits/REFACTOR_AUDIT_20260822.md`. 빌드 및 품질 게이트 13개 PASS.

### 2026-08-22 일정 데이터 작업 분기 회귀 수정

- 일정 가져오기·내보내기·백업·Excel 작업이 여러 bool 플래그로 표현되어 있었고 초기화 목록에서 `ImportLocalItems`가 빠져 있었다. 다섯 작업을 `SettingsDataAction` 단일 enum으로 교체해 한 번에 하나의 작업만 실행되도록 구조적으로 보장했다.
- JSON·CSV 저장 대화상자에 바탕화면 초기 위치와 `RestoreDirectory`를 지정해 Windows가 느리거나 끊긴 이전 셸 위치를 먼저 탐색하는 지연을 줄였다.
- 가져오기는 ID 기준 `신규`와 `내용 변경` 후보를 보여주는 선택적 병합이다. 현재 목록에서 삭제된 일정이 파일에 있으면 신규 후보로 나타나지만, 파일에 없는 일정은 현재 목록에서 자동 삭제하지 않는다.
- 기능 회귀 검사를 추가하고 C# 빌드 및 품질 게이트 13개 PASS.

### 2026-08-22 해상도 변경 후 이동·고정 좌표 불일치 계측 및 후보 01

- 사용자 허가 아래 Windows 표시 설정을 3840×2160 125% ↔ 2560×1440 100%로 실제 왕복하며 두 전환 방향을 직접 계측했다. 시험 후 원래 표시 설정으로 복원했다.
- 근본 원인: Explorer 고정 중 cloaked WPF 창이 이전 표시 좌표계를 유지하고, 이동 모드로 uncloaking된 뒤에 지연된 WPF 좌표 보정을 적용해 화면이 이미 전환된 다음 HWND가 움직였다.
- 수정: WPF를 `Opacity=0`으로 먼저 DWM에 복귀시키고, `GetDpiForWindow`·`PresentationSource`·`GetWindowRect`가 목표 고정 프레임에 2회 연속 안정된 뒤에만 WPF를 표시하고 Explorer 프레임을 해제한다.
- 이동→고정 로직은 `LockCurrentPlacement()` 하나로 합쳐 메인 버튼·트레이·진단 시험이 동일한 저장·게시 순서를 사용한다.
- 실측: 고정→확대→이동과 이동→축소→고정 모두 최종 물리 좌표가 일치했다. 1000회 빠른 토글에서 500회 고정, 464회 완전한 이동 전환이 모두 동일한 `(1190,50,1058×883)`을 유지했고 누적 편차는 0px였다. 나머지 36회는 다음 입력이 120ms 안에 도착해 진행 중 전환을 정상 취소한 경우다.
- 진단은 `ONHARU_PLACEMENT_TRACE=1`일 때만 기록하며 일반 실행에는 파일 I/O가 없다. 후보·소스·로그·품질 게이트는 `Tests/LocalTest/PlacementCandidates/01-dpi-settled-handoff`에 보존했다.
- C# 빌드 및 품질 게이트 13개 PASS. 150% 노트북·혼합 DPI 듀얼 모니터는 실제 장비 확인이 남았다.

### 후보 01 사용자 재검증 실패 및 외부 검토 전환

- 사용자가 정상 실행·실제 버튼으로 해상도 변경 후 고정↔이동을 다시 시험한 결과, 위치뿐 아니라 크기도 달라지는 현상이 그대로 재현됐다. 후보 01은 **실패**이며 공식 안정본이나 설치판으로 승격하지 않는다.
- 자동 시험은 특정 순간의 `GetWindowRect` 일치만 확인했으며 최종 DWM 화면, 정상 시작 DPI context, 실제 버튼 타이밍을 충분히 검증하지 못했다. 과거 자동 PASS 기록은 구현 성공 근거가 아니라 자동 계측 한계의 증거로만 보존한다.
- 추가 확인 결과 `App/build.ps1`에 DPI awareness manifest가 없고 `Program.cs`에도 PMv2 초기화가 없다. Windows UI는 125%인데 후보 로그가 `GetDpiForWindow/PresentationSource=96/96`을 기록한 구간이 있어 좌표 가상화가 강한 원인 후보다.
- 현재 코드를 더 수정하지 않고 `Review/External-Resolution-Mode-Review-20260822`에 관련 C#/native 소스, 전후 로그, 실패 문서, 검토 질문을 구성했다. OAuth credential, 토큰, 일정, 사용자 설정 원본은 제외했다.
- 다음 구현은 외부 검토 후 `PMv2 + 단일 physical RECT` 최소안과 `XDiary식 이동 전용 overlay` 구조안 중 하나를 선택해 별도 후보로 진행한다.

### 후보 02A — PMv2 manifest 계측 전용 준비

- Claude Code와 Antigravity 외부 검토를 현재 소스와 대조했다. 공통 결론은 현재 WPF가 System DPI Aware 계열로 실행되며 `GetWindowRect`·WPF 좌표·Explorer frame이 같은 물리 좌표를 보장하지 않는다는 것이다.
- 활성 `App`과 설치판은 변경하지 않고 `Tests/LocalTest/PlacementCandidates/02A-pmv2-manifest-instrumentation`에 별도 후보를 만들었다.
- manifest에 `PerMonitorV2, PerMonitor`와 구버전 fallback `true/PM`을 선언하고 후보 빌드에만 `/win32manifest`로 내장했다. `Switch.System.Windows.DoNotScaleForDpiChanges`는 원인 분리를 위해 02A에 넣지 않았다.
- 관찰 전용 hook으로 `WM_DPICHANGED`의 DPI·suggested RECT, `WM_DISPLAYCHANGE`, `WM_WINDOWPOSCHANGED`, thread/window DPI context, `GetDpiForWindow`, WPF transform, `GetWindowRect`, `DWMWA_EXTENDED_FRAME_BOUNDS`를 기록한다. 모든 메시지는 `handled=false`로 통과하며 위치 계산은 바꾸지 않는다.
- `SavePhysicalPlacement` 로그에는 `CallerMemberName`을 추가해 `SaveWindowSettings`를 포함한 실제 저장 호출자를 식별한다.
- 후보 EXE 빌드와 embedded manifest 추출 검증을 통과했다. SHA-256은 `FE9483EEF26EF258AD015F9285A616735D2B5D236B04F1D9404D9AECB10E8C83`이다.
- 현재 일반 테스트본이 고정 상태로 실행 중이며 정상 종료 UI를 자동으로 찾지 못했다. 사용자 데이터 저장 lifecycle을 건너뛰는 강제 종료는 하지 않았고, 후보 실기기 실행은 기존 앱 정상 종료 후로 보류했다.

### 후보 02A/02B 실측 및 후보 03 자동 검증

- 후보 02A에서 manifest 기반 PMv2를 적용하자 125% 환경의 네이티브 HWND는 120 DPI를 인식했지만 WPF `TransformToDevice`는 96 DPI에 머물렀다. manifest 단독으로는 해결되지 않는다.
- 후보 02B에 `Switch.System.Windows.DoNotScaleForDpiChanges=false`만 추가해 HWND/WPF가 120/120으로 일치함을 확인했다. 이어 150%에서 WPF `MinWidth=820`, `MinHeight=560` DIP가 물리 1230×840px 최소 크기로 확대되어 1058×708px Explorer 고정 프레임을 재현하지 못하는 두 번째 원인을 확인했다.
- 후보 03은 최소 크기를 820×560 물리 px로 역산하고, `WM_DPICHANGED` 직전 물리 RECT를 다음 Render turn에 복원하며, 시작 시 저장된 물리 RECT를 첫 게시 전에 적용한다. 활성 `App`과 설치판은 변경하지 않았다.
- 실제 표시 설정을 100%·125%·150%로 바꾸며 고정→이동, 이동→고정, 재시작을 계측했다. 네이티브 action sink 1,000회 전환(이동 500·고정 500)에서 1,434회 좌표 검사가 모두 동일 frame/DPI를 기록했고 `ready=False`, DPI 불일치, HWND/DWM 불일치, 예외는 각각 0건이었다.
- 기존 UI Automation 기반 1,000회 도구는 고정 모드에서 cloaked WPF 버튼을 찾지 못해 시험 도구 실패로 폐기하고, 실제 앱 입력 경로인 `OnharuV3.ActionSink`로 대체했다.
- 시험 뒤 Windows를 1920×1080 100%로, 달력을 시험 전 물리 위치 862,23,1058×883으로 복원했다. 후보 03은 고정 상태로 남겨 사용자 육안 확인이 가능하다.
- 정식 승격 조건은 사용자 화면에서 네 모서리·글씨 크기·첫 프레임이 동일함을 확인하는 것이다. 혼합 DPI 듀얼 모니터는 실제 장비 확인 전까지 미완료로 둔다.
- 상세 결과와 전체 로그: `Tests/LocalTest/PlacementCandidates/03-pmv2-physical-rect-authority/CANDIDATE_03_RESULT.md`, `placement-transition-full.log`.

### 후보 03 공식 소스 승격

- 사용자가 후보 03 화면을 확인하고 공식 소스 이식을 승인했다.
- 공식 `App`에는 후보 계측 코드를 넣지 않고 검증된 최소 구성만 이식했다: `app.manifest`의 PMv2, `app.config`의 WPF DPI switch, 820×560 물리 px 최소 크기, `WM_DPICHANGED` 직전 물리 RECT 복원.
- DPI 처리는 `MainWindow.DpiPlacement.cs`로 분리해 기존 partial 책임 경계를 유지했다.
- `App/build.ps1`은 manifest를 EXE에 내장하고 출력 EXE 옆에 `.config`를 복사한다. `build-release.ps1`과 Inno Setup도 `ONHARU.exe.config`를 배포 파일에 포함한다.
- 위치 품질 게이트에 PMv2 manifest, WPF switch, 물리 최소 크기와 DPI hook 검사를 추가하고 릴리스 검사에 `.config` 포함 여부를 추가했다.
- 공식 로컬 테스트 빌드 성공, 기존 품질 게이트 13개 전체 PASS. 다음 단계는 공식 로컬 테스트본의 사용자 육안 확인이며 혼합 DPI 듀얼 모니터는 실제 장비 검증 전까지 미완료다.

### 2026-08-22 일기장 1차 구현

- `DiaryStore.cs`, `DiaryWindows.cs`, `MainWindow.Diary.cs`로 일정·Google 코드와 분리했다. 날짜별 일기 1개를 `%LOCALAPPDATA%\OnharuV3\diary.json`에 저장하고 `%LOCALAPPDATA%\Onharu\backups\diary-yyyyMMdd.json`에 일별 안전 백업을 남긴다.
- 빈 날짜 셀 더블클릭은 일기 작성/수정으로 연결했다. 일정 막대 더블클릭은 기존 일정 수정, 오른쪽 `이 날짜에 추가`는 기존 새 일정 등록을 그대로 유지한다.
- 헤더의 시간표 옆에 일기장 아이콘을 추가했다. 설정 `표시 옵션 > 일기장`으로 표시 여부를 선택하며 기존 사용자 설정은 데이터 버전 22 마이그레이션에서 일기장을 기본 활성화한다.
- 일기장 리더는 날짜·제목 목록, 제목/내용/날짜 검색, 이전/다음 이동, `목록 보기`와 `한 장 보기`를 제공한다. 일기는 Google 계정과 무관한 이 PC 로컬 데이터다.
- 고정 Explorer 레이어의 날짜 더블클릭도 같은 일기 편집 경로를 사용한다. 일정 이벤트와 상세 카드의 더블클릭 경로는 변경하지 않았다.
- 로컬 빌드와 품질 게이트 13개를 모두 통과했다. `feature-pack-check`와 `ui-construction-check`에 일기 타입·설정·메인 입력 연결·팝업 생성 검사를 추가했다.
- 일기 작성·리더 상단 전체를 드래그 영역으로 유지하고, 작성 날짜를 `YYYYMMDD` 직접 입력 또는 ONHARU 달력으로 변경할 수 있게 했다. 잘못된 날짜와 기존 일기 날짜 충돌은 저장 전에 차단한다.
- 리더 목록에 최신순/오래된순 정렬을 추가하고 날짜·제목을 한 줄 카드로 정리했다. 이전/다음은 표시 정렬과 무관하게 실제 날짜의 이전/다음으로 이동하며 저장 버튼은 작성창 오른쪽 아래에 고정했다.
- 사용자 검토 후 일기 진입 영역을 날짜 숫자로 한정했다. 날짜 숫자 더블클릭은 일기, 셀 빈공간 더블클릭은 새 일정, 일정 막대 더블클릭은 일정 수정이며 WPF와 Explorer 고정 입력 모두 같은 규칙을 사용한다.
- 일기 날짜 입력은 본문 위의 상시 입력칸을 제거하고 제목 아래 날짜 문구 옆 달력 아이콘으로 이동했다. 아이콘 팝업에서 `YYYYMMDD` 또는 ONHARU 달력을 선택한다. 최신순/오래된순은 둥근 선택 버튼으로 교체하고 이전/다음은 왼쪽 한 그룹으로 고정했다.
- 날짜 변경 팝업에서 달력 날짜를 고르는 행위는 후보 날짜만 바꾸며 `적용`을 눌러야 작성창 날짜가 확정된다. 바깥 클릭이나 팝업 닫기는 후보를 버린다. 일기 진입 범위는 날짜 숫자·음력 날짜·작성 표시점으로 확장하되 오늘/절기/휴일 문구와 셀 빈공간은 제외했다.
- WPF Calendar가 날짜 선택 후 내부 마우스 캡처를 유지해 `적용`의 첫 클릭이 캡처 해제에 소비되던 근본 원인을 수정했다. Calendar의 마우스 업 처리가 끝난 다음 turn에 전역 캡처를 해제하고 적용 버튼으로 포커스를 넘기며, 적용 자체는 표준 `Click` 한 번으로 처리한다.
- 일기장 실시간 검색 입력칸 오른쪽 안쪽에 ONHARU 포인트 색상의 돋보기 아이콘을 추가했다. 장식 아이콘은 입력 히트테스트를 막지 않으며 텍스트가 겹치지 않도록 오른쪽 내부 여백을 확보했다.
- `OnharuCalendarStyle.cs`를 공통 팝업 달력 기준으로 추가했다. 일기 달력의 크기를 기준으로 일정 시작일·종료일·반복 종료일, 기념일, 검색 기간 달력에 남아 있던 112%/120% 개별 배율과 별도 스타일을 제거했다.
- 공통 달력은 날짜·연월 헤더·요일을 13px로 통일하고 요일과 헤더를 SemiBold로 보강한다. 날짜 셀 크기·색상·오늘/선택 강조·비활성 날짜 투명도도 모든 팝업에서 동일하다.
- 설정의 `일기장` 체크를 아이콘 표시 옵션이 아닌 `일기장 기능` 전체 토글로 변경했다. 끄면 헤더 아이콘, 작성 표시점, 날짜 숫자·음력의 일기 더블클릭 경로가 모두 사라지고 셀 더블클릭은 새 일정 등록으로 동작한다. 기존 `diary.json`과 일기 백업은 보존하며 다시 켜면 그대로 복구한다.
- 일기 리더의 이전/다음을 `← 이전`, `다음 →` ONHARU 버튼으로 정리했다. 현재 일기보다 오래되거나 새로운 일기가 없는 방향은 버튼·화살표를 회색으로 표시하고 클릭을 비활성화한다.
- 새 일정·일정 수정의 시간 선택을 24시간 값으로 통일했다. 오전은 `00~11시`, 오후는 `12~23시`를 표시하며 선택된 숫자를 별도 오전/오후 환산 없이 그대로 저장·복원한다. `24시`는 다음 날 `00시`와 날짜가 겹치므로 제공하지 않는다.
- `ui-construction-check.ps1`에서 오전 첫/마지막 값 `0/11`, 오후 첫/마지막 값 `12/23`을 실제 UI 생성 후 검사하며 전체 품질 게이트 13개가 PASS했다.

### 2026-08-22 Rainlendar·FAQ·Google Tasks 분리

- Rainlendar 2.24 계열과 ONHARU를 비교해 ICS 교환, 빠른 일정 추가, 일정 템플릿, 모니터 연결 변경 대응을 유효 후보로 정리했다. 자유 스킨·Lua 플러그인은 ONHARU 제품 방향과 유지비를 고려해 우선순위를 낮췄다.
- JSON은 ONHARU 완전 백업, ICS는 달력 앱 간 교환, CSV는 Excel 보고서라는 역할을 확정했다. 별도 Excel 아이콘은 향후 `일정 내보내기` 형식 선택 팝업으로 합칠 수 있으나 CSV 기능 자체는 유지한다.
- `Distribution/ONHARU-FAQ.html`에 13개 FAQ, 실시간 검색, 분류 필터, 접기·펼치기를 구현하고 기존 `ONHARU-FEATURES.html`과 양방향 메뉴로 연결했다. ICS·특수 Google 이벤트처럼 미완료 기능은 준비 중으로 표시했다.
- `GoogleTasksService.cs`를 추가해 Tasks API를 Calendar API와 분리했다. 날짜가 있는 Task는 전용 `tasks:` 소스와 하루 종일 Todo로 표시하며 완료·미완료는 Tasks API PATCH로 전송한다.
- 일반 Google Calendar 시간 일정의 체크박스는 유지하지만 완료 여부는 로컬에만 저장한다. `onharuCompleted`를 Calendar 메타데이터에 새로 쓰거나 원격 값으로 덮어쓰지 않는다.
- `birthday`, `fromGmail`, `focusTime`, `outOfOffice`, `workingLocation` 같은 Google 특수 이벤트는 `eventType`을 보존하고 읽기 전용·체크박스 없음으로 처리한다.
- OAuth 범위에 Tasks를 추가하고 새 로그인에서만 권한 마커를 기록한다. 기존 토큰 사용자는 Calendar 동기화를 계속 사용하며 Tasks는 계정 재연결 안내만 표시한다. Google Cloud의 Tasks API 활성화와 실제 계정 왕복 시험은 수동 확인이 필요하다.
- Google Task 날짜 파싱, 소스 경계, 완료 PATCH, 일반 Calendar 완료 로컬 경계를 `sync-security-check.ps1`에 추가했다. 빌드와 전체 품질 게이트 13개 PASS.

### 2026-08-22 소개·FAQ 웹 자료 디자인 정리

- `Distribution/ONHARU-FEATURES.html`과 `Distribution/ONHARU-FAQ.html`의 기본 콘텐츠 폭을 1120px 기준으로 통일했다. FAQ 본문은 읽기 편하도록 내부 900px 폭을 유지한다.
- 소개 첫 문구 `하루의 모든 순간을`을 한 줄 단위로 고정하고 화면 폭별 글자 크기를 조정해 `순간 / 을`처럼 어색하게 갈라지는 줄바꿈을 제거했다.
- 소개 자료를 현재 2.1 기능 기준으로 갱신했다. 시간표, 일기장, Todo, 반복 일정, D-Day·기념일, 표시 설정, 검색·백업·내보내기를 반영했다.
- 소개 하단에 기능 영역과 FAQ로 이동하는 ONHARU 스타일 버튼을 추가했다.
- 공개 전 사용자 작업은 한 번에 하나씩 안내한다. 첫 단계는 Google Cloud 프로젝트에서 Google Tasks API를 활성화하는 것이다.

### 2026-08-22 Google Tasks 실계정 검증과 문서화

- Google Tasks OAuth 범위 등록·계정 재연결 후 실계정 목록과 할 일 2건을 정상 수신했다. 완료 상태도 ONHARU 데이터에 반영되는 것을 확인했다.
- Google Tasks API 원문 응답을 직접 비교한 결과 반복 할 일은 공개된 현재 리소스만 반환됐고, 미래 반복 회차와 반복 규칙은 제공되지 않았다.
- Google Tasks API의 `due`는 날짜만 반환하고 Google 화면에서 지정한 시각은 제공하지 않아 ONHARU에서는 하루 종일 Todo로 표시한다.
- 임의 반복 생성이나 임의 시간 추정은 중복·오표시 위험이 있어 하지 않는다. 정확한 시간·미래 반복 회차가 필요한 경우 Google Calendar 또는 ONHARU 반복 일정을 안내한다.
- `README.md`, 배포본 `README.md`, `Distribution/ONHARU-FAQ.html`에 지원 범위와 Google 자체 제한을 같은 기준으로 기록했다.

### 2026-08-22 Google Tasks 명시적 활성화와 등록 경계

- `PlannerSettings.ShowGoogleTasks`를 버전 23 설정으로 추가했다. 기존 사용자는 기본 꺼짐으로 마이그레이션되며, 꺼진 동안 Tasks API를 호출하지 않고 Task 일정과 오른쪽 필터·수정 권한을 표시하지 않는다.
- 설정의 표시 옵션에 `Google Tasks 표시·동기화`를 추가했다. 켤 때 ONHARU 스타일 확인창에서 반복 규칙·미래 회차·마감 시간 API 제한, 외부 생성 Task의 완료 전용 정책, Google Calendar/ONHARU 일정 사용 권장을 설명하고 동의한 경우에만 활성화한다.
- 기능을 켜면 Tasks 목록이 오른쪽 Google 필터와 `Google 일정 수정 권한`에 나타난다. 경고에 동의해 처음 활성화할 때 ONHARU 등록 Task 수정 권한도 함께 켜며, 이후에는 목록별로 다시 끌 수 있다.
- 설정창에서 체크하고 경고를 승인하는 즉시 `Google 일정 수정 권한` 안의 기존 Task 목록을 임시로 표시한다. 저장 전에는 원본 설정을 바꾸지 않으므로 설정창을 취소하거나 닫으면 완전히 원복되고, 오른쪽 메인 목록과 실제 동기화는 설정 저장 후에만 반영된다.
- 표시 옵션의 `24시간제로 시간 표시`와 Google Tasks 체크박스 사이에 18px 간격을 추가해 서로 다른 기능이 붙어 보이지 않게 정리했다.
- ONHARU에서 만든 Google Task는 Tasks API POST/PATCH/DELETE로 제목·날짜·메모·완료·삭제를 동기화한다. 등록창에서는 시간·여러 날·알림·이월·반복 카드를 숨기고 저장 데이터에서도 반복과 알림을 제거한다.
- Google에서 만든 Task는 반복 여부를 공개 API로 판별할 수 없으므로 계속 완료 상태만 변경한다. ONHARU에서 만든 Task는 `OnharuManaged/CreatedInOnharu` 경계를 보존해 수정 가능 여부를 구분한다.
- Explorer 레이어 소스는 변경하지 않았다. 빌드와 13개 전체 품질 게이트가 PASS했다.
- 간헐적 `127.0.0.1 ERR_CONNECTION_REFUSED` 대응으로 OAuth 대기시간을 1분에서 5분으로 늘리고 성공·취소·시간초과 모두 `finally`에서 현재 listener만 정리하도록 변경했다. 다만 2026-08-24 새 Google 계정 시험에서 5분 이내에도 같은 오류가 발생했고 재시도 후 로그인은 성공했다. 따라서 원인을 시간초과로 확정하지 않으며, 중복 입력·브라우저 재요청·listener 조기 종료·보안 프로그램 개입을 다음 재현에서 구분한다.

# 2026-08-24 회사 보안망 다운로드 차단 관찰

- 회사의 Skyhigh Security 환경에서 ONHARU 무서명 EXE와 EXE가 포함된 ZIP 다운로드가 모두 차단됐다. 모든 실행 파일을 일괄 차단하는 환경은 아니므로 현재 ONHARU 파일 유형·서명·평판 또는 배포 도메인 정책의 영향으로 기록한다.
- 정상 대응 우선순위는 Microsoft Store 배포, 향후 코드 서명, `onharu.app` URL·SHA-256을 이용한 회사 보안 관리자 허용 요청이다. 파일 확장자 변경·이중 압축·암호화 압축 같은 보안 우회는 제품 배포 방법으로 사용하지 않는다.
# 2026-08-22 로그인 후 로그아웃 상태 일정 가져오기

- Google 동기화로 계정 식별이 완료된 뒤 `local` 저장소에 아직 가져오지 않은 일정이 있으면 안내한다.
- `일정 선택`을 누르면 기존 `LocalImportWindow`에서 원하는 일정만 현재 계정으로 이동한다.
- `지금은 안 함`은 현재 실행에서만 안내를 닫는다. 가져온 항목은 `local`에서 제거하므로 반복 제안되지 않는다.
- 계정별 일정 파일 분리 원칙은 유지한다.
- 향후 `onharu.net` 메일 발송 기반이 준비되면 로컬 일정·기념일·D-Day 파일을 사용자 이메일로 보내 다른 PC의 `일정 가져오기`에서 선택 복원하는 기능을 추가한다. Google 원본 일정은 제외한다.
- 일정 내보내기에 `내 PC에 저장 / 내 메일로 보내기` 선택 메뉴를 추가했다. 메일은 서버 기능 준비 전까지 `구현 중` 안내만 표시한다.
- 계정 전환 직후 대상 계정의 기존 로컬 파일을 읽지 않고 Google 결과만 저장해 기념일이 사라지는 버그를 수정했다. 연결된 계정 키를 먼저 활성화하고 기존 로컬·기념일을 로드한 뒤 Google 동기화를 병합한다.
- 혼합 DPI 듀얼 모니터 실기기 검증은 현재 작업 대상에서 제외했다. 사용자가 테스트 환경을 확보하고 문제를 재현할 때만 다시 진행한다.
- 일정 관리를 `백업 복원 / 가져오기 / PC 저장 / 메일 보내기 / 일정 삭제` 5개로 재구성했다. 로그아웃 상태 일정 가져오기는 백업 복원 메뉴에 통합했다. JSON·ICS는 로컬 일정, Excel CSV는 Google 포함 전체 일정임을 형식 선택 화면에 표시한다. 현재 JSON 가져오기·내보내기와 CSV 내보내기는 동작하며 ICS·CSV 가져오기·메일 전송은 구현 중 안내를 표시한다.
# 2026-08-22 배포 웹페이지 정리

- `Distribution/ONHARU-FEATURES.html`과 `ONHARU-FAQ.html`의 공통 메뉴에 다운로드·개인정보처리방침을 연결했다.
- 실제 2.1.0 설치판·포터블 파일과 SHA-256을 안내하는 `Distribution/ONHARU-DOWNLOAD.html`을 추가했다.
- 앱의 로컬 저장, Google Calendar/Tasks OAuth 범위, DPAPI 토큰 보호, 오류 로그 마스킹 동작을 기준으로 `Distribution/ONHARU-PRIVACY.html`을 추가했다.
- 네 HTML 문서의 로컬 링크 26개와 다운로드 대상 파일 존재 여부를 자동 확인했으며 끊어진 링크는 없었다.
- `check-v21.ps1`의 13개 품질 검사를 실행해 모두 통과했다.
- 구매 도메인을 `onharu.app`으로 반영하고, 웹 루트에 바로 압축 해제할 수 있는 `Publish/ONHARU-Web-Publish.zip`을 생성했다. 홈페이지, 소개·다운로드·FAQ·개인정보 페이지, 공통 CSS, 설치판과 포터블 파일을 포함하며 내부 링크와 두 배포 파일의 SHA-256을 재검증했다.
# 2026-08-22 KBO 일정 사용성 및 캐시 개선

- Parse.bot에서 받은 KBO 월별 일정은 `%LOCALAPPDATA%\OnharuV3\sports-kbo-parse-YYYYMM.json`에 보관한다. 이미 받은 월은 서버 호출 없이 즉시 표시한다.
- 수동 새로고침은 기존 캐시와 새 응답을 경기 키로 비교하여 `새 일정 / 변경 / 취소` 건수를 표시하고 월 캐시를 교체한다.
- 사용자가 명시적으로 누르는 `시즌 저장`은 해당 연도의 3~11월 중 캐시가 없는 월만 요청한다. 숨은 자동 선조회로 Parse.bot 크레딧을 소비하지 않는다.
- KBO 달력은 실제 월의 5주/6주 수에 맞춰 행 수를 유동 구성하고, 경기 행을 16px로 압축해 하루 최소 5경기가 보이도록 했다.
- 오늘은 녹색 파스텔 배경과 테두리, `오늘` 문구로 강조한다.
- 공식 구단 로고 파일을 복제하지 않고 ONHARU 자체 색상 팀 배지를 사용한다.
- 응원팀을 저장하고 현재 월의 응원팀 경기를 한 번에 선택한 뒤 기존 `선택 경기 ONHARU 등록` 안전 단계를 거치도록 했다.
- 후속 디자인 조정에서 작은 알약형 팀 표시는 폐기하고, 구단색 원형 심볼과 팀명을 분리했다. 응원팀 드롭다운도 `심볼 + 팀명` 형식으로 통일했다.
- 6주 달력에서도 다섯 경기 행이 들어가도록 창의 기본 높이와 경기 행 높이를 다시 조정했다.
- 별도 `시즌 저장` 버튼은 제거했다. 월 최초 조회와 수동 새로고침 결과는 기존처럼 자동으로 월별 캐시에 저장된다.
- 야구 창을 1120×820으로 확대하고 경기 행을 18px로 구성해 팀 로고·팀명과 하루 5경기를 함께 읽을 수 있게 했다.
- 10개 구단 PNG 로고는 최초 사용 시 `%LOCALAPPDATA%\OnharuV3\kbo-team-logos-v1`에 내려받아 이후 로컬 캐시로 표시한다. 일반 공개 배포 전 구단 상표 사용 지침 확인이 필요하다.
- 야구 일정이 저장·재로딩·가져오기·수정창을 거쳐도 `야구` 카테고리를 유지하도록 정규화 경로를 수정했다.
- 현재 Parse.bot 일정 API에는 점수/결과가 없다. 향후 `away_score`, `home_score`, `game_status`가 추가될 경우 점수와 취소선을 즉시 표시하도록 호환 필드를 준비했다.
- 로고 다운로드 실패 원인은 .NET Framework HTTPS 프로토콜 협상으로 확인하여 TLS 1.2를 명시했다. 달력 셀은 `시간 + 원정 로고 vs 홈 로고`만 표시하고 팀명은 상세 보기와 응원팀 드롭다운에서만 보여준다.
- 야구 창은 1220×900으로 확대했다. 기본 셀은 최대 5경기, 초과 경기는 `+N경기 더보기`로 표시하고 날짜 숫자 또는 더보기를 누르면 그날 전체 경기를 큰 로고와 함께 선택할 수 있다.
- 응원팀 ComboBox에 공통 `SettingsWindow.StyleComboBox`를 적용해 ONHARU 둥근 드롭다운 디자인으로 통일했다.
- 야구 창은 세로 920px를 유지하면서 가로를 1100px로 줄이고 셀 로고를 22px로 키웠다.
- 보기 선택은 사용자 피드백에 따라 `월간 보기 / 4주 보기` 두 토글 버튼으로 단순화했다. 표시 범위가 두 달에 걸치면 필요한 월 캐시를 함께 읽는다.
- 4주 보기는 표시 범위만 4주로 유지하고 좌우 화살표는 한 주씩 이동하도록 수정했다. 월간 보기만 한 달씩 이동한다.
- 야구 창의 날짜·요일·시간·로고·경기행·당일 확대창 크기는 메인 설정의 글자 크기(작게/보통/크게)를 같은 비율로 적용한다. 창 크기도 작업 영역 안에서 함께 조정해 큰 글씨에서 다섯 경기 행이 잘리지 않도록 했다.
- 실사용 피드백에 따라 날짜 13.5px, 경기 시간 11.5px, 로고 24px를 새 기준으로 한 단계 더 확대했다.
- 보기 버튼 순서는 `4주 보기 / 월간 보기`로 변경하고 연월·기간은 인디고 파스텔 배지 안에 표시한다.
- 응원팀 드롭다운에 `미선택`을 추가했다. 응원팀 선택 시 해당 팀 경기를 노란 파스텔 배경과 테두리로 즉시 강조한다.
- `SportsApi`에 프로세스 내 월별 메모리 캐시를 추가하고 로고 캐시 확인도 최초 한 번으로 제한했다. 같은 월 안에서 주 이동 시 JSON·로고 파일을 반복해서 읽지 않는다.
- 응원팀 드롭다운은 10개 구단이 한 번에 보이도록 최대 높이를 380px로 지정했다.
- 일정 삭제창에 업무·개인·야구·기념일·Google D-Day 카테고리 선택을 추가했다. 카테고리를 체크하면 해당 묶음의 개별 일정이 선택되며 기존 최종 확인·백업 절차를 그대로 거친다.
- KBO 창 세로 높이를 944px 기준으로 소폭 확대해 큰 글씨·6주 월간 보기의 마지막 경기 행 잘림을 보완했다.
- 연월/기간 표시는 별도 카드 배경을 제거하고 메인 ONHARU 날짜 제목과 어울리는 19px 인디고 텍스트로 단순화했다.
- 응원팀 선택 영역은 `응원팀` 캡션과 드롭다운을 좌우로 정렬하고 빈 선택 이름을 `응원팀 없음`으로 변경했다.
- 응원팀 드롭다운 최대 높이를 480px로 늘려 `응원팀 없음 + 10개 구단`을 한 번에 표시한다. 작은 화면 등에서 스크롤이 불가피하면 공통 ONHARU 스크롤바 스타일을 적용한다.
- 월간 보기에서 하루 여섯 경기 이상이면 별도 행을 소비하지 않고 날짜 옆에 `+N경기`를 표시한다. 4주 보기에서는 기존 `+N경기 더보기` 행과 당일 확대 선택창을 유지한다.
- KBO 달력의 기본 화면을 `4주 보기`로 변경했다. 오늘이 포함된 주는 두 번째 행에 놓여 앞 주의 결과와 다음 일정을 함께 볼 수 있다.
- 4주 보기 좌우 버튼은 한 주, 월간 보기 좌우 버튼은 한 달을 먼저 조회한다. 대상 표시 범위에 경기가 하나도 없으면 이동하지 않고 현재 화면을 유지하며 이전/다음 경기 없음 상태를 안내한다.
- 응원팀은 최대 두 팀까지 선택할 수 있도록 두 개의 ONHARU 드롭다운으로 확장했다. 동일 팀 중복 선택은 막고 기존 단일 팀 설정값도 그대로 호환한다.
- `응원경기 선택`은 두 응원팀의 경기를 함께 선택하며 `전체 선택 취소`로 현재 선택한 경기 전체를 즉시 해제할 수 있다.
- KBO 창 최초 표시 시 월 캐시를 읽은 직후 달력을 먼저 렌더링하고, 구단 로고 다운로드/확인은 뒤에서 완료한 후 화면만 갱신한다. 로고 네트워크 대기로 첫 화면 전체가 늦게 뜨던 경로를 제거했다.
- 일정 삭제창의 카테고리 선택을 일괄 체크 기능에서 목록 필터로 변경했다. 기본 `전체 카테고리`와 업무·개인·야구·기념일·Google D-Day 필터를 제공한다.
- 필터 변경 시 숨겨진 기존 선택을 해제하고 해당 카테고리만 목록에 표시한다. 이후 `전체 선택`은 현재 보이는 항목에만 적용되어 다른 카테고리가 실수로 삭제되지 않는다.
- 삭제창의 카테고리 행과 전체 선택 행 사이 간격을 늘리고, 왼쪽 체크 시작선을 목록 카드와 맞췄다.
- 사용자 실기기 검토에 따라 두 번째 응원팀 드롭다운은 제거하고 단일 응원팀 선택으로 복귀했다. 저장돼 있던 복수 값이 있으면 첫 번째 팀만 안전하게 사용한다.
- 버튼 문구를 `응원팀 경기 선택`으로 명확히 하고 `전체 선택 취소`는 인디고 파스텔 배경으로 대비를 높였다.
- `응원팀 없음` 항목 앞의 불필요한 원형 아이콘을 제거했다. 공통 ComboBox 팝업의 가로 스크롤을 비활성화해 팀 목록에 좌우 스크롤바가 생기지 않도록 했다.
- KBO 경기 일정의 보기 크기를 메인 달력 `글자 크기` 설정과 분리했다. 창은 항상 `중간`으로 시작하며 하단의 `작게 / 중간 / 크게` 버튼으로 즉시 변경한다.
- 야구 보기 크기를 바꾸면 날짜·경기 글씨·구단 로고·행 높이와 팝업 전체 크기가 함께 비례 조정된다. 버튼은 상단 탐색 영역을 복잡하게 만들지 않도록 하단 상태와 등록 버튼 사이에 배치했다.
- 야구 보기 크기 전환은 창의 왼쪽 위가 아닌 `하단 중앙`을 기준으로 150ms EaseOut 애니메이션을 적용한다. 크기 버튼이 마우스 아래에서 다른 버튼 위치로 밀리는 현상과 갑작스러운 창 점프를 완화했다.
- KBO 탐색 버튼을 `≪  ‹  오늘  ›  ≫` 구조로 확장했다. 4주 보기에서 단일 화살표는 1주, 이중 화살표는 4주를 이동한다. 월간 보기에서는 단일 화살표가 1개월을 이동하고 이중 화살표는 회색 비활성화된다.
- `API 설정`은 복잡한 상단에서 하단 왼쪽 상태 영역으로 이동했다. 상시 `PC 저장 일정` 문구는 제거하고 조회 중·오류·해당 월 경기 수처럼 필요한 상태만 표시한다.
- 하단 `보기 크기` 캡션은 간결하게 `보기`로 변경했다.
- KBO 상단 옵션을 별도 첫째 줄 오른쪽에 `4주 보기 / 월간 보기 · 글씨 크기: 작게 / 중간 / 크게`로 재배치했다. 날짜 탐색과 응원팀 작업은 둘째 줄에 분리해 버튼 겹침을 제거했다.
- 글씨 크기 전환의 창 기준점을 사용자 피드백에 따라 왼쪽 위 모서리 고정으로 변경했다. Left/Top은 움직이지 않고 Width/Height만 EaseOut으로 변하므로 크기별 위치가 중구난방으로 이동하지 않는다.
- 하단은 `API 설정 + 상태`와 `선택 경기 ONHARU 등록`만 남겨 역할을 단순화했다.
- KBO 일정 조회·기간 이동·새로고침 중 달력 중앙에 ONHARU 스타일 `경기 일정을 준비하고 있어요…` 오버레이를 즉시 표시한다. 일정과 로고 준비가 끝나면 완성된 달력을 한 번에 노출해 단계별 채움의 어수선함을 없앴다.
- 설정창 체감 지연 원인 분리를 위해 `SETTINGS data-ready`와 `SETTINGS ui-created` 시간을 PlacementTrace에 기록한다. 데이터 I/O와 WPF UI 구성 중 실제 병목을 확인한 뒤 최적화할 수 있게 했다.
- 메인 달력 탐색도 KBO 창과 동일한 `≪  ‹  오늘  ›  ≫` 규칙으로 통일했다. 사용자 지정 주 보기에서 단일 화살표는 1주, 이중 화살표는 설정된 전체 주 수만큼 이동한다.
- 메인 달력 월 전체 보기에서는 단일 화살표가 한 달씩 이동하고 이중 화살표는 회색 비활성화된다. 설정에서 표시 범위를 바꾸면 버튼 상태도 즉시 갱신한다.
- KBO `크게` 보기에서 화면 작업 영역 때문에 창 높이를 더 늘릴 수 없는 경우에도 다섯 번째 경기가 잘리지 않도록 경기 행과 구단 로고의 세로 확대는 중간 크기에서 제한하고, 날짜·경기 글씨와 가로 공간은 크게 보기 비율을 유지한다.
- KBO 월간 보기 렌더링 때마다 제목을 현재 기준 월의 `yyyy년 M월`로 다시 계산한다. 월 이동 후 캡션이 이전 달에 머물던 오류와 기준일이 월 1일이 아닐 때 주 수가 잘못 계산될 가능성을 함께 수정했다.
- KBO 월간 보기는 항상 최대 5주 행으로 고정했다. 여섯 번째 주가 필요한 달은 다음 달 월간 화면에서 이어서 확인한다.
- 월간 화면의 첫 표시 주가 이전 달에 걸치거나 마지막 표시 주가 다음 달에 걸치면 해당 인접 월 캐시도 함께 읽어 실제 표시 범위 35일의 경기를 모두 보여준다. 월 이동 가능 여부도 이 35일 범위 기준으로 판단한다.
- KBO 상단에 `전체 보기 / 응원팀 경기만` 필터를 추가했다. 기본은 전체 보기이며 응원팀 전용 보기에서는 선택한 팀이 원정 또는 홈으로 포함된 경기만 표시한다.
- 응원팀이 없는 상태에서 전용 보기를 누르면 필터를 바꾸지 않고 안내한다. 전용 보기 중 응원팀을 해제하면 자동으로 전체 보기로 안전하게 복귀한다.
- 단일 응원팀 ComboBox를 드롭다운형 체크 목록으로 교체했다. 10개 구단의 로고·팀명·체크박스를 표시하며 최대 2팀까지 선택할 수 있다.
- 선택 결과는 닫힌 버튼에 `LG · 한화`처럼 즉시 표시되고 `전체 해제 / 완료`를 제공한다. 세 번째 팀 선택은 거부하고 하단 상태로 최대 2팀 제한을 안내한다.
- 복수 응원팀은 `|` 구분값으로 기존 설정 필드에 저장한다. 응원팀 전용 보기·경기 강조·응원팀 경기 일괄 선택은 두 팀 모두에 적용된다.
- 응원팀 목록은 현재 조회 월과 무관하게 KBO 10개 구단 전체를 사용해 포스트시즌·비시즌 이동 시 저장 선택이 사라지지 않는다.
- KBO 상단 옵션을 `표시 / 기간 / 크기` 세 그룹으로 재구성했다. 각 그룹은 옅은 배경·테두리·둥근 모서리로 구분하고 버튼 높이·간격·테두리를 통일해 활성/비활성 상태가 안정적으로 보이게 했다.
- 응원팀 체크 목록의 각 행을 30px로 고정하고 체크박스·22px 로고·팀명을 수직 중앙 정렬했다. 닫힌 응원팀 선택 버튼도 30px 높이와 중앙 정렬을 적용했다.
- 응원팀 선택 버튼과 팝업 폭·기준점을 고정하고, 열린 상태에서 같은 버튼을 다시 누르면 닫히도록 PreviewMouseDown 토글을 적용했다.
- 체크박스와 로고 사이에 7px 간격을 추가했다. 두 팀 선택 시 선택된 팀만 활성 상태로 유지하고 나머지 팀 체크박스를 비활성화하며, 한 팀 해제 시 즉시 다시 활성화한다.
- 닫힌 응원팀 선택 버튼에 선택된 구단의 18px 로고와 팀명을 함께 표시한다.
- KBO 상단 첫째 줄을 `KBO 경기 일정 + 새로고침`(왼쪽), `표시 / 기간 / 크기 + 닫기`(오른쪽) 한 기준선으로 통합했다. 닫기 버튼은 크게 버튼 오른쪽에 배치했다.
- 표시·기간·크기 그룹은 각각 민트·블루·퍼플 파스텔 배경/테두리로 구분하고 캡션과 버튼 글씨를 11.5px로 통일했다.
- 응원팀 선택 변경 때 열린 Popup 전체를 재생성하던 로직을 제거했다. 기존 체크박스 인스턴스의 체크/활성 상태만 갱신해 두 팀 중 한 팀 해제 시 팝업이 오른쪽으로 재배치되는 현상을 수정했다.
- KBO 상단 문구를 `표시: 전체 경기 / 응원팀 경기`, `보기: 4주 / 월`, `크기: 작게 / 중간 / 크게`로 간결하게 정리했다.
- KBO 제목 영역 자체에 창 드래그 처리를 연결했다. 내부 버튼이 마우스 이벤트를 점유해 창이 움직이지 않던 회귀를 제거했다.
- 새 일정·일정 수정 팝업의 일반 화면 최대 콘텐츠 높이를 690px에서 720px로 소폭 늘렸다. 일반 해상도에서는 불필요한 스크롤을 줄이고, 작은 노트북에서는 작업 영역 기반 자동 제한과 스크롤을 그대로 유지한다.
- 위 변경 후 App 빌드, feature-pack-check, ui-construction-check를 모두 통과했다.
- 메인 헤더의 `≪ / ‹ / 오늘 / › / ≫` 버튼 폭과 높이를 소폭 줄여 공간을 정리했다.
- 메인 달력에 슬라이딩형 `기간 / 월간` 임시 보기 토글을 추가했다. `월간`은 설정을 변경하지 않고 월 전체 화면만 잠시 보여주며, `기간`을 누르면 전환 전 날짜 위치와 사용자가 설정한 주 수 보기로 돌아간다.
- 임시 월간 보기에서도 단일 화살표는 월 이동, 이중 화살표는 비활성화되며 버튼 활성색이 현재 모드를 즉시 표시한다.
- 위 변경 후 App 빌드, feature-pack-check, ui-construction-check를 다시 통과했다.
- KBO 상단 그룹 명칭을 사용 의미에 맞게 `보기: 전체 경기 / 응원팀 경기`, `기간: 4주 / 월`로 정리했다.
- 보기·기간·크기 선택지를 흰 캡슐 안에 빈틈없이 연결된 세그먼트 버튼으로 변경했다. 선택된 항목만 색이 채워져 슬라이딩 토글처럼 인식되며, 기존 필터·기간·크기 동작은 그대로 유지한다.
- KBO 경기 일정 창이 움직이지 않던 회귀를 수정했다. 공통 팝업 드래그 도우미를 handled 이벤트도 받는 PreviewMouseDown 방식으로 보강하고, KBO 상단 전체 영역에 연결했다. 버튼·콤보박스 위에서는 클릭 동작을 보존하고 제목·빈 영역에서는 창을 이동한다.
- KBO 상단 Grid에 투명 HitTest 배경을 지정했다. 이제 `KBO 경기 일정` 글자뿐 아니라 상단의 빈 공간을 잡아도 다른 ONHARU 팝업처럼 창을 이동할 수 있다.
- Google Tasks 실제 왕복과 KBO 기능 완료 상태를 `TODO_NEXT.md`에 반영하고, 후보 03·ICS 관련 오래된 미완료 표기를 정리했다.
- `check-v21.ps1 -Build`의 전체 품질 검사 13종이 모두 통과했다.
- 최신 정식 설치판을 빌드하고 별도 AppId·별도 폴더의 InstallTest에서 설치→필수 파일 5개 확인→제거→폴더 제거를 완료했다.
- 정식 설치판을 기존 `%LOCALAPPDATA%\Programs\ONHARU`에 덮어써 업그레이드했다. 설치된 App/config/LayerHost/DesktopHook 네 파일이 Release staging과 SHA-256까지 일치하고 실행되는 것을 확인했다.
- GitHub `cowpigpapa/onharu` main을 기존 1.x 단일 파일에서 실제 2.1 모듈 소스 141개 파일로 갱신했다. 공개 소스와 Source ZIP에서 `OAuthCredentials.local.cs` 및 모든 생성 바이너리를 제외했다. 커밋은 `7897f3f84b6742992b0e7fbf55922c5df4c75c44`이다.
- GitHub `v2.1.0` Release의 오래된 Setup.zip을 제거하고 새 설치판·Portable ZIP·Source ZIP·SHA256SUMS로 교체했다. 릴리스 노트에는 일기장·Google Tasks·ICS/CSV·KBO·DPI 개선을 반영했다.
- 최신 배포 SHA-256: Setup `7BA236685D07D1097709CE3BE2E179B6B64DB908B4C7B41C63D4C06768638A99`, Portable `C4F0EF73E375276AD8D6FE108FE94AAA2F5A5146A16380B9D57FA5B69CCB8093`, Source `847980A534DD7BF678519C85F60F7FAA6BD9B03BE738A0E2AE1C92EE3F9B275B`.
- `Publish/ONHARU-Web`의 설치판·포터블·다운로드 페이지·배포 ZIP을 최신화했다. 실제 `onharu.app`에서 두 파일을 다시 내려받아 크기와 SHA-256이 로컬 릴리스와 모두 일치함을 확인했다.
- 소스 정리로 과거 UI 시험 EXE·오래된 테스트 ZIP·이전 설치 ZIP/안정본 ZIP·InstallTest 생성물·구 해시 등 재생성 파일 23개를 삭제했다. 사용자 데이터와 실험 문서·영상·스크린샷은 유지했다.
- 공개 소개 페이지에 2.1의 최신 기능을 반영했다. ICS·CSV 가져오기, 선택 삭제 전 안전 백업, 프로야구 경기 선택 등록, 달력 인쇄, 독립 시간표, 개선된 반복 일정을 새 기능 카드에 정리했다.
- 공개 FAQ에 프로야구 API 설정과 로컬 등록 범위, ICS·CSV 가져오기의 Google 원본 비변경 원칙, 삭제 전 안전 백업, 다중 모니터 위치 복구, 시간표 병행 사용, 달력 인쇄 안내를 추가했다.
- 공식 지원 이메일을 `support@onharu.app`으로 확정하고 공개 지원·문의 페이지를 추가했다. 문의 시 버전·Windows 환경·재현 순서를 안내하고 비밀번호·토큰·API 키 등 민감정보를 보내지 않도록 명시했다.
- 소개 페이지 첫 화면 오른쪽의 축약 달력 목업을 개인정보 없는 ONHARU 전체 앱 홍보 이미지(`onharu-app-preview.png`)로 교체했다. 월간 달력과 오른쪽 일정 상세 영역을 한 화면에서 보여주며 반응형 비율을 유지한다.
- 공식 소개 페이지 풋터에 개인정보 비수집형 방문 카운터를 추가했다. 페이지가 열릴 때 정적 `visit.gif`를 요청하고 AWS의 `update-visitor-counter.py`가 Nginx 로그에서 해당 요청과 검색봇을 구분해 누적/오늘 횟수만 `counter.json`으로 만든다. 외부 카운터 서비스·쿠키·사용자 식별 데이터는 사용하지 않으며 서버 cron이 1분마다 갱신한다.
- 다운로드 페이지에도 같은 집계 구조를 확장했다. 설치판과 포터블 버튼 클릭 시 `download-hit.gif?file=...` 요청을 남기고 `counter.json.downloads`의 개별 클릭 횟수를 각 카드에 표시한다. 실제 파일 전송 재시도 대신 명확한 사용자 버튼 클릭을 기준으로 집계한다.
- 사용자 결정에 따라 다운로드 횟수 공개 표시는 제거했다. 설치판·포터블 클릭 집계는 계속하며 날짜별 `visits/setup/portable` 숫자를 서버 비공개 `/home/ubuntu/onharu-stats-history.json`에 0600 권한으로 보존한다. 공개 `counter.json`에는 누적/오늘 방문 수만 포함하고, 다운로드 통계는 요청 시 SSH로 기간별 표·CSV를 산출한다.
- 공개 릴리스 노트 페이지를 추가해 ONHARU 2.1.0의 기능, 화면·사용성, 데이터·안정성, 설치 주의사항을 제품 언어로 정리했다. 모든 웹 페이지 풋터에서 지원·문의, 릴리스 노트, 개인정보처리방침으로 이동할 수 있다.
# 2026-08-23 저장 경로 통합

- 일정·설정·Google 토큰·일기·시간표·KBO 캐시·로그·자동 백업의 공식 루트를 `%LOCALAPPDATA%\Onharu`로 통일했다.
- `AppDataPaths.cs`를 단일 경로 정의로 추가하고 각 저장소가 이를 사용하게 했다.
- 업데이트 후 최초 실행 시 `%LOCALAPPDATA%\OnharuV3`의 기존 파일을 새 폴더로 복사하되, 새 위치의 파일을 덮어쓰거나 기존 폴더를 삭제하지 않는다.
- V1 `%LOCALAPPDATA%\FamilyPlanner` 가져오기 대상도 새 `Onharu` 루트로 변경했다.
- `OnharuV3.*` 네이티브 파일명과 IPC/mutex 이름은 사용자 데이터 경로가 아니며 레이어 호환성을 위해 유지했다.

## 최종 정정

- 아직 일반 배포 전이므로 위 호환안은 폐기했다. `%LOCALAPPDATA%\OnharuV3` 자동 복사 코드를 제거했다.
- 사용자 데이터·백업·로그는 `%LOCALAPPDATA%\Onharu`만 사용한다.
- 네이티브 파일명, 프로세스명, mutex, 이벤트, 공유 메모리, Explorer 메시지·속성도 모두 `Onharu.*`로 변경했다.
- V1 `%LOCALAPPDATA%\FamilyPlanner` 데이터 이관만 `LegacyMigration.cs`로 유지한다.

## 설치 후 Google 동기화 오류 수정

- 버전 26 설정을 27로 올릴 때 `LastUpdateCheckUtc=DateTime.MinValue`가 한국 시간대에서 UTC 변환 범위를 벗어나 Google 동기화 후 설정 JSON 저장을 실패시키는 원인이었다.
- 자동업데이트 최초 확인 기준을 UTC 2000-01-01로 변경하고, 로드·저장 양쪽에서 1900년 이전 값을 정규화한다.
- Google 토큰이나 일정 데이터의 손상은 아니며 동기화 마지막 설정 저장 단계의 직렬화 오류였다.

## 배포용 제거 정책

- 제거 시작 시 `사용자 데이터 보관(권장) / 모든 사용자 데이터 삭제 / 제거 취소`를 선택하게 했다.
- 기본 보관 시 일정·설정·일기·시간표·백업·암호화 토큰과 API 키는 유지하고, 로그·KBO 경기/로고 캐시는 자동 정리한다.
- 전체 삭제는 복구 불가 경고를 한 번 더 확인한 뒤 `%LOCALAPPDATA%\Onharu` 전체를 제거한다.
- 무인 제거는 데이터 손실 방지를 위해 사용자 데이터를 보관한다.

## 개발 사용자 데이터 정리

- `%LOCALAPPDATA%\Onharu`에서 구형 Google 토큰, V2 계정 파일, 손상·시험 일정, 수동 복구 폴더, 과거 오류 로그와 설정 사본을 삭제했다.
- 활성 계정별 일정, 로컬 일정, 설정, 일기, 시간표, Google v3 인증, Tasks 범위, KBO API 키와 정상 백업만 유지했다.
- V1 이관은 빈 `Onharu` 폴더에서 `settings.json`과 `items.json`만 복사하도록 제한하고, 활성 설정이나 계정 일정이 이미 있으면 실행하지 않는다.
# 2026-08-23 저장명 통일·메일 백업 클라이언트

- 활성 저장명을 `google-token.dat`, `google-account.dat`, `google-tasks-scope.dat`, `kbo-api-key.dat`, `kbo-team-logos`로 통일했다. 기존 개발 PC 파일도 새 이름으로 이동하여 로그인과 API 설정을 보존했다. `v1`/`v3`는 제품 버전이 아니라 과거 내부 포맷 표식이었으며 정식 배포 전 제거했다.
- 일정 관리의 `메일 보내기`를 실제 흐름으로 연결했다. JSON/ICS는 로컬 일정만, CSV는 Google을 포함한 전체 일정으로 생성하며, 연결된 Google 계정 확인 → 임시 첨부 생성 → HTTPS 요청 → 임시 파일 삭제 순서다.
- 발신자는 `ONHARU <noreply@onharu.app>`, 클라이언트 대상 주소는 `https://onharu.app/api/v1/backup-email`, 첨부 상한은 1MB다.
- 앱 빌드와 기존 13개 품질 게이트 및 `email-backup-check.ps1`가 통과했다.
- 당시 차단점이었던 AWS 콘솔 로그인은 해결됐다. SES production access는 계속 검토 중이다.
- 후속 완료: AWS 서울 리전에 `onharu-email-backup` Lambda, 최소 권한 `onharu-email-backup-lambda-role`, HTTP API `gva624k60e`를 생성했다. API Gateway 기본 제한은 초당 1회/버스트 2회다. `onharu.app` nginx의 정확한 `POST /api/v1/backup-email` 경로만 API Gateway로 프록시하며 본문은 1.5MB로 제한했다. 기존 nginx 설정은 `/etc/nginx/sites-available/onharu.before-email-api-20260823`에 보존했다.
- 시험 allowlist는 `juan.hjlee@gmail.com`, `hngreen1982@gmail.com`, `juan.lee@lxpantos.com` 세 주소뿐이다. 임의 주소 요청이 `403`으로 차단되는 것을 확인했다. Lambda 역할에는 MIME 첨부 발송에 필요한 `ses:SendEmail`과 `ses:SendRawEmail`만 허용했다. SES는 아직 sandbox(일 200통, 초당 1통)다.
- `juan.hjlee@gmail.com` 수신 주소를 SES에서 인증한 뒤 빈 JSON 첨부(`ONHARU-test-20260823.json`)를 실제 발송했고 API 응답 `200 {"message":"sent"}`를 확인했다. 나머지 두 allowlist 주소는 sandbox 기간에는 별도 수신 인증이 필요하다.
- 메일 백업 수신자는 사용자가 입력하지 않고 현재 연결된 Google 계정 주소로 자동 고정한다. 다른 주소 전송은 UI에서 허용하지 않으며, Google 로그아웃 상태에서는 발송을 중단하고 계정 연결 안내를 표시한다. 앱 빌드와 메일 경계 검사가 통과했다.
- 메일 형식에 Excel CSV를 추가했다. 발송 화면에 Google 포함 전체 일정임을 명시하고, Lambda도 `text/csv`와 `.csv`만 정확히 짝지어 허용한다. 1행 시험 CSV를 `juan.hjlee@gmail.com`로 실제 발송해 `200 sent`를 확인했다.
# 2026-08-23 메일 보안·자동 업데이트·배포 절차

- 메일 날짜 우클릭 일정 등록은 현실성과 제품 범위가 맞지 않아 로드맵에서 제외했다.
- 메일 백업 요청에 Google ID 토큰을 추가하고 AWS Lambda가 OAuth 대상 앱, 이메일 인증 여부, 연결 Gmail과 수신자 일치를 검증하도록 보강했다. Calendar access token과 refresh token은 서버에 전송하지 않는다.
- AWS `onharu-email-backup` 함수와 `GOOGLE_CLIENT_ID` 환경 변수를 갱신했다. 인증 토큰 없는 직접 요청이 HTTP 401로 거절되는 것을 확인했다. 시험 allowlist는 SES sandbox 기간 동안 추가 방어선으로 유지한다.
- 기존 GitHub Release 기반 자동 업데이트가 하루 1회 비차단 확인, 사용자 승인, Setup EXE 다운로드, `SHA256SUMS.txt` 검증을 이미 구현하고 있음을 재확인했다. `App/update-check.ps1`을 추가해 회귀 게이트에 포함했다.
- 시험 배포와 공식 배포를 구분한 `Docs/Release/RELEASE_PLAYBOOK.md`를 작성했다. 버전, 설치판, GitHub Release, 홈페이지, SHA-256을 한 묶음으로 갱신하는 절차를 명시했다.
- 설치 프로그램은 이미 한국어·영어를 지원한다. 앱 본문은 저장 데이터의 한국어 카테고리 ID와 다수 하드코딩 UI 때문에 단순 문자열 교체가 아니므로 2.1 안정화 중에는 구조를 급히 바꾸지 않는다.
- 최신 전체 소스로 LocalTest를 재빌드했고 기존 13개에 메일 보안·자동 업데이트 검사를 더한 15개 품질 게이트가 모두 PASS했다. 빌드 첫 시도는 실행 중 시험본이 파일을 점유해 실패했고 정상 종료 후 재실행하여 통과했다.
- GitHub 최신 정식 Release가 `v2.1.0`, draft/prerelease가 아니며 Setup EXE, Portable ZIP, Source ZIP, `SHA256SUMS.txt` 네 자산을 모두 갖춘 것을 실제 API로 확인했다.
- 사용자는 약 1주 동안 기능 실사용 시험을 계속한다. Google 공개 OAuth 검증과 새 공식 Release는 이 안정화 기간 뒤에 진행한다.
- 메일 중계 예외를 반영해 `Distribution`과 `Publish/ONHARU-Web`의 개인정보처리방침을 갱신했다. 일정 파일·수신 Gmail·단기 ID 토큰의 일시 처리, 비보관, Calendar access/refresh token 미전송을 명시했다. 홈페이지 실제 게시는 별도 홈페이지 작업에서 수행한다.

# 2026-08-23 홈페이지 서비스 이용약관

- 무료 Windows 소프트웨어와 `onharu.app`에 적용되는 `Distribution/ONHARU-TERMS.html`을 추가했다. 외부 서비스, 로컬 데이터, 이용자 책임, 지식재산권, 서비스 변경·중단, 법정 권리를 침해하지 않는 책임 범위, 약관 변경과 문의처를 명시했다.
- 소개·다운로드·FAQ·지원·릴리스 노트·개인정보처리방침 페이지의 공통 하단에 `이용약관` 링크를 추가하고 `Publish/ONHARU-Web` 및 실제 웹사이트와 동기화했다.
- 기본 기능은 현재 무료이지만 향후 Google 연동을 포함한 일부 기능의 유료 전환 또는 유료 요금제 추가가 가능함을 약관에 명시했다. 대상·가격·시행일·결제·해지·환불 조건은 사전 안내하며, 명시적 결제 동의 없이 청구하지 않는다.

# 2026-08-23 상단 탐색·투명도·2.2 테마 결정

- 메인 날짜 제목의 양옆에 이전/다음 이동 화살표를 배치해 `‹ 날짜 ›`가 하나의 탐색 단위로 보이게 했다.
- `오늘` 버튼은 `기간/월간` 보기 전환 바로 옆으로 이동했다. 기존 한 기간 이동 `≪/≫` 동작은 보존했다.
- 달력 투명도 범위를 10~98%에서 10~100%로 변경하고 로드·고정 레이어 입력의 상한도 100%로 통일했다. 10% 미만은 달력 발견성과 조작성이 크게 떨어져 최저값을 유지했다.
- 2.2는 기능 복제 없이 `기본/블랙/컬러` 세 디자인 스킨을 공통 의미 팔레트로 교체하는 방향으로 결정했다. 현재 색상 하드코딩이 많아 2.1 안정화와 분리하여 진행한다.
- 오늘 추가된 메일 백업과 자동 업데이트는 각각 `EmailBackupService`, `UpdateService`, `MainWindow.Updates`로 이미 분리돼 있다. `MainWindow.cs`도 132줄의 공통 상태·생성자 역할만 유지하므로 추가 리팩토링은 하지 않았다.
- 중복 문서 `V2.1_ROADMAP.md`, `REFACTORING_CLAUDE_REVIEW.md`, 구형 xDiary 비교문서, 구형 OAuth 상태문서와 일회성 Claude 검증 빌드 스크립트를 제거했다. 최신 기준은 `TODO_NEXT.md`, `Docs/Architecture`, `Docs/Audit`, `Docs/Release`에 유지한다.

# 2026-08-23 공통 팝업 상단 이동 규칙

- 임시 `Popup`·`ContextMenu`를 제외한 실제 팝업 창은 상단 60px의 빈 영역 또는 제목 영역을 끌어 이동할 수 있도록 통일했다.
- `OnharuPopupChrome.EnableTopDrag()`가 버튼, 입력칸, 체크박스·라디오 버튼, 목록, 슬라이더 등 조작 컨트롤을 판별해 창 드래그와 클릭이 충돌하지 않게 한다.
- 공통 `OnharuPopupChrome.Shell` 사용 창은 로드 시 자동 적용하고, 메인 달력에서 여는 창은 `PlaceCalendarDialog()`에서 보완한다. 별도 경로의 알림·카테고리 순서 창도 같은 규칙에 연결했다.
- 기존 제목 전용 `EnableDrag()`도 같은 입력 컨트롤 판별 로직을 공유한다. `popup-policy-check.ps1`에 공통 이동 규칙 회귀 검사를 추가했다.
- LocalTest 재빌드와 17개 2.2 품질 게이트가 모두 PASS했다.

# 2026-08-23 팝업 표시 중 메인 날짜 반전 수정

- 설정 등 차단 팝업을 열 때 메인 창 전체를 `IsEnabled=false`로 만들면서 WPF 비활성 스타일이 날짜 헤더와 체크박스에 적용돼 반전·퇴색처럼 보이는 회귀를 확인했다.
- 팝업 중복과 메인 입력 차단은 유지하되, 메인 달력의 시각 상태를 바꾸지 않는 `IsHitTestVisible=false`로 교체했다. Explorer 고정 레이어 입력은 기존 `blockingDialogDepth` 경계가 계속 차단한다.
- `popup-policy-check.ps1`에 비활성 렌더링 재도입 방지 검사를 추가했다.

# 2026-08-23 설정 첫 표시·스크롤 전환 개선

- 설정 창의 스크롤 스타일을 `Loaded` 이후 Dispatcher 작업으로 미뤄 Windows 기본 스크롤이 한 프레임 보인 뒤 온하루 스타일로 바뀌던 원인을 제거했다.
- 설정 본문은 첫 로드 동안 투명하게 유지하고, 실제 모니터 높이·스크롤 템플릿·상단 저장 버튼 표시를 같은 로드 단계에서 확정한 다음 한 번에 표시한다.
- 팝업 정책 검사에 설정 스크롤 스타일의 지연 적용 재도입 방지 게이트를 추가했다.

# 2026-08-23 전체 팝업 첫 프레임 스타일 통일

- 설정 창에 적용한 첫 프레임 스크롤 규칙을 모든 실제 ONHARU 팝업으로 확대했다. `EnableTopDrag()` 등록 시 공통으로 첫 `Loaded` 단계에서 내부 스크롤바를 온하루 스타일로 확정한다.
- 일정 등록·수정과 일정 검색에 남아 있던 `Dispatcher.BeginInvoke` 기반 지연 스크롤 교체를 제거했다. 검색 결과 계산만 다음 Dispatcher 작업으로 유지한다.
- 임시 `Popup`·`ContextMenu`는 대상에서 제외한다. 품질 게이트가 지연된 `SoftenScrollBars` 호출의 재도입을 검사한다.

# 2026-08-23 공통 hover 피드백 규칙

- `온하루 · ONHARU` 아래의 연·월 날짜 버튼이 기본 WPF 버튼 템플릿 때문에 마우스를 올릴 때 반전되던 현상을 제거했다.
- 일반 버튼과 아이콘 버튼의 기본 hover 투명도 변화도 제거했다. 기본 피드백은 손 모양 커서이며 실제 클릭 순간의 눌림 피드백은 유지한다.
- 선택·활성 상태, 슬라이딩 토글, 메뉴·목록 탐색, 위험 경고처럼 의미 전달이 필요한 경우만 색상 강조를 허용한다. 이 원칙을 `AGENTS.md`와 2.2 테마 문서에 기록했다.
- `theme-check.ps1`가 날짜 제목의 중립 템플릿과 기본 헤더 버튼의 hover 반전 미사용을 검사한다.

# 2026-08-23 아이콘·슬라이딩 버튼·브랜드 규칙

- 테마 팔레트에 `Icon` 의미색을 추가했다. 기본·컬러 스킨은 검정 계열, 블랙 스킨은 흰색이며 설정·검색·시간표·일기·야구·날짜 이동·사이드바 아이콘에 공통 적용한다. 닫기 등 의미색 버튼은 예외다.
- 아이콘 전용 버튼은 Padding 0과 가로·세로 중앙 정렬을 공통 `IconButton()` 경로에서 보장한다. 블랙 테마에서 회색이던 설정 톱니바퀴도 흰색으로 바뀐다.
- 슬라이딩 버튼은 좌우 Padding 8px를 동일하게 사용하고 `20 + 글자 수 × 10px`를 최소 폭으로 적용한다. 호출부가 더 넓은 폭을 명시한 경우에는 기존 배치를 보존한다.
- 브랜드명과 메인 연·월 제목은 테마 변경과 무관하게 청록→보라 ONHARU 브랜드 그라데이션으로 고정했다. 세 테마의 세부 기준색 결정은 다음 작업으로 분리한다.

# 2026-08-23 헤더 아이콘 가시성 보강

- 테마별 아이콘 색 통일만으로는 작은 화면에서 이동·검색·시간표·일기 아이콘의 식별력이 부족한 것을 확인했다.
- `« ‹ › »`, 검색, 시간표, 일기 아이콘을 글꼴 기호 대신 17px 벡터 경로와 1.8px 둥근 획으로 교체했다. 버튼 외곽 크기와 헤더 배치는 유지해 기존 정렬을 보존했다.
- 야구와 설정 아이콘도 17px로 키우고 획을 보강했다. `오늘` 버튼은 폭 44px, 11.5px SemiBold로 조정했다.
- 앞으로 아이콘 규칙은 색상·중앙 정렬뿐 아니라 100% DPI에서 최소 16px 유효 도형과 1.7px 이상 획으로 가시성을 확보하는 것을 포함한다.
- `오늘` 버튼은 12.5px SemiBold로 한 단계 더 키웠다. 배경색은 설정의 `오늘 표시 > 날짜 원형` 색상을 실시간으로 공유하고 글자는 흰색으로 고정해 설정과 메인 헤더의 의미를 연결했다.
- 오른쪽 상단 버튼 묶음과 사이드바 정렬선을 맞추기 위해 `4주/전체월`, `이동/고정`의 각 세그먼트를 1px씩 줄였다. 전체 폭만 4px 줄이고 벡터 아이콘 크기와 가시성은 유지했다.
- 위치 상태 표시에 2.1에서 사용하던 `↔ 이동 가능`, `📌 고정됨` 아이콘을 복원하고 글자를 11.5px로 키웠다. 모든 공통 슬라이딩 버튼도 `오늘`보다 1px 작은 11.5px SemiBold로 통일했다.

# 2026-08-24 메인 헤더 최종 아이콘·정렬 기준

- 최신 확정값은 `Docs/Design/MAIN_HEADER_UI_STANDARD.md`로 분리했다. 이 기준이 위 2026-08-23 기록의 과거 8px Padding·11.5px 글자 기준보다 우선한다.
- 슬라이딩 버튼은 12.5px SemiBold, 실측 글자 폭 + 좌우 2px, 외곽 27px·내부 23px로 확정했다.
- 오른쪽 헤더는 310px 기준으로 패널 접기 버튼을 메인 달력 오른쪽 선, 투명도·기간 버튼을 상세 패널 왼쪽 선, 이동·고정을 상세 패널 오른쪽 선에 맞췄다. 파스텔·블랙은 남은 공간의 정중앙에 둔다.
- 오른쪽 상세 패널 Padding을 18px에서 9px로 줄이고 `선택 날짜 / 이번 주 / 다음 주`를 좌·중앙·우 끝 기준과 동일 간격으로 배치했다.
- 선택 상태와 기간·투명도는 현재 프리셋 대표색을 공유한다. 파스텔·블랙 스위치의 파스텔 선택 면만 대표색을 파스텔 톤으로 변환한다.

# 2026-08-23 사용자 지정·월 전체 프리셋 분리

- 기존 `CalendarRangeMode` 하나로 월 전체와 사용자 지정을 배타 저장해, 설정에서 월 전체 5주를 고르면 헤더는 `3주`로 남고 실제 화면만 5주가 되는 불일치를 수정했다.
- 설정에서 `월전체 (1일부터)`의 자동·5주·6주와 `사용자 지정`의 1~6주를 각각 독립 선택·저장한다. 헤더 왼쪽 버튼은 사용자 지정 주 수, 오른쪽 `월 전체` 버튼은 월 전체 프리셋만 사용한다.
- 현재 헤더에서 선택한 보기 상태도 `UseMonthView`로 별도 보존한다. 기존 설정은 버전 29 마이그레이션에서 현재 모드와 월 전체 프리셋으로 안전하게 분리한다.
- 신규 설치 기본값은 `4주 보기`, 이번 주 `위에서 2번째`, 월 전체 `자동 (4~6주)`로 통일했다. 이 값들은 `settings.json`이 없을 때만 사용하며, 업데이트·덮어쓰기에서는 기존 보기·위치·크기를 그대로 유지한다. 기존 사용자 값을 강제로 바꾸는 설정 마이그레이션은 추가하지 않았다.
- 신규 설치의 나머지 기본값도 `Docs/Design/DEFAULT_SETTINGS_PROFILE.md`에 한 번에 고정했다. 2026-08-24 사용자가 직접 구성한 설정을 기준으로 이동 가능, 중앙 1120×700, 파스텔·오션블루, 보통 글자 12, 투명도 95%, 일요일 시작·ISO 주차, 음력·24절기, 선택일 테두리, 오늘 원형 아이콘, 5분 동기화, Todo 자동 이월을 사용한다. 기존 설정이 있으면 이 프로필을 적용하지 않는다.

# 2026-08-23 스킨별 팔레트·내 설정 흐름 정리

- 사용자 표시 이름을 `파스텔`, `블랙`, `컬러`로 확정하고 설정 설명을 각 스킨의 채도·대비 목적에 맞게 수정했다.
- 스킨마다 추천 팔레트 5개를 제공한다. 파스텔은 저채도, 블랙은 네온·원색 계열, 컬러는 중간 채도로 구성하며 스킨 선택 시 추천 목록도 즉시 바뀐다.
- RGB 조정과 색상 교환만으로 선택 프리셋이 바뀌지 않게 했다. `<선택 프리셋 이름> 변경`을 눌렀을 때만 그 프리셋을 덮어쓰며, `내 설정으로 저장`은 프리셋 선택을 그대로 둔 채 별도 내 설정 슬롯에 복사한다. Google 기본은 계속 읽기 전용이다.
- 업무·개인 외에 야구·D-Day·기념일·휴일 색상 카드와 영구 설정을 추가했다. 설정 버전 30에서 기존 사용자에게 기본색을 안전하게 채운다.
- 일정 렌더링은 파스텔=부드러운 배경+거의 검정 글자, 블랙=선명한 배경+동계열의 아주 밝은 글자, 컬러=중간 채도+동계열 진한 글자 규칙으로 분리했다.
- 추천 프리셋 선택 상태와 수정된 팔레트 값을 별도로 보존한다. 자동 `내 설정` 전환은 제거했으며, 스킨을 바꾸면 해당 스킨의 추천 5개는 기본값으로 다시 구성하고 저장된 내 설정은 유지한다.
- 휴일·야구·D-Day·기념일 색상 카드는 일반·Google 색상 아래 `Special Day 색상` 구역에 배치하고, 메인에서 해당 항목을 표시 중일 때만 설정에 나타난다. 설정 버전은 31이다.
- 색상 작업 흐름은 `프리셋 선택 → 프리셋 변경/내 설정 저장/색상 교환 → RGB 조절` 순서로 재배치했다. 프리셋 변경 버튼은 강조색으로 가시성을 높였고, RGB 조절과 색상 초기화는 같은 높이·아이콘 규칙의 도구 버튼으로 묶었다. RGB 버튼은 왼쪽 정렬과 고정 폭을 사용해 펼침·접힘 문구가 바뀌어도 위치가 흔들리지 않는다.
- 추천 프리셋이 복잡해 보이지 않도록 8개에서 대표 5개로 줄였다. `프리셋 변경/내 설정 저장/색상 교환/색상 초기화`는 라디오 버튼 위 한 행에 두고, 아래 행에는 `내 설정/Google 기본`과 RGB 펼침 버튼만 남겼다. 기존 8번 내 설정과 9번 Google 선택값은 새 5번·6번 위치로 호환 변환한다.
- 프리셋 라디오는 대표 5개와 `내 설정/Google 기본`을 합쳐 한 줄에 표시하고 RGB 조절은 구역 제목 오른쪽으로 옮겼다. 프리셋을 막 선택했을 때 변경 버튼은 이름만 표시하며, RGB 조정이나 색상 교환이 발생한 뒤에만 `변경` 문구가 붙는다. 내 설정 저장의 하트는 제거하고 색상 교환 여백과 초기화 강조색도 보완했다.
- 프리셋 이름은 공백 없는 5글자 이하로 정리하고 `내설정`, `Google`을 사용한다. 첫 라디오는 왼쪽 끝, 마지막 라디오는 오른쪽 끝에 맞추며 가운데 항목은 균등 정렬한다. 각 프리셋 내부 6색은 중복하지 않고 휴일 기본값은 모든 스킨에서 `#EF4444`로 통일하되 색상 편집은 허용한다.
- 색상 편집 카드는 스킨에 따라 글자·RGB 채널·숫자·배경·테두리를 함께 바꾼다. 파스텔은 부드러운 배경과 동계열 진한 글자, 블랙은 어두운 색상 배경과 흰 글자, 컬러는 더 선명한 배경과 동계열 진한 글자를 사용하며 스킨 변경 즉시 열린 설정 화면에도 반영한다.
- 프리셋 아래 미니 색상칩과 프리셋 이름 자체를 일정 카드로 만드는 시도는 모두 사용 의도와 달라 폐기하고 라디오는 단순 이름으로 복원했다. 실제 변경 대상은 아래 색상 편집 카드이며, 블랙에서는 진한 계열 배경 위에 흰색 고정 글자가 아니라 같은 색 계열의 밝은 글자를 표시한다. 파스텔은 거의 검정, 컬러는 동계열 진한 글자를 사용한다.
- 블랙 색상 편집 카드의 배경을 선택색의 28%로 어둡게 누르던 처리를 제거했다. 핑크는 진한 핑크, 시안은 진한 시안처럼 선택 색상 자체를 카드 배경으로 사용하고 같은 계열의 밝은 글자를 올린다.
- 파스텔·블랙·컬러의 15개 추천 프리셋을 다시 구성해 휴일 기본 빨강을 제외한 75개 색상이 전체에서 한 번씩만 나오도록 했다. 설정 버전 32 마이그레이션은 기존 시험 중 덮어쓴 추천 5개 슬롯만 비우고 `내설정` 색은 보존해 새 기본 팔레트가 즉시 보이게 한다.
- 설정창 생성 전에 체크된 프리셋 이벤트가 지나가 실제 편집기에는 예전 중복색이 남던 초기화 순서를 수정했다. 설정을 처음 열 때도 선택 프리셋을 색상 카드에 적용하며, 업무·개인·야구·D-Day·기념일·휴일과 Google 캘린더를 함께 비교해 실제 표시색이 겹치면 Google 쪽 후보색을 자동으로 다른 색으로 바꾼다. Google 원본색을 그대로 쓰는 `Google` 프리셋만 이 자동 분리의 예외다.
- 팔레트의 최종 기준을 `스킨별로 서로 다른 색을 재정의`하는 방식에서 `고유 원색 + 스킨별 톤 변환` 방식으로 바꿨다. 추천 프리셋은 업무·개인·Google 색을 대비가 큰 순서로 섞어 배치하고, 야구 `#16A085`, D-Day `#38A7D8`, 기념일 `#A78BFA`, 휴일 `#EF4444`는 프리셋과 무관하게 의미색을 유지한다. 파스텔·컬러·블랙 렌더러가 같은 원색의 채도·밝기·글자 대비만 바꾼다. 설정 버전 33에서 추천 슬롯만 새 기본값으로 갱신하며 내 설정은 보존한다.
- 최종 팔레트 출처를 2.1로 다시 확정했다. 블랙은 2.1 상단의 `오션·핫핑크·라임블루·선셋`, 파스텔은 하단의 `로즈밀크·라벤더·민트·피치스카이` 조합과 카테고리 순서를 그대로 사용한다. 컬러는 상단 원색 조합을 중간 톤으로 표현한다. 각 스킨의 다섯 번째 항목만 기존 네 조합과 겹치지 않는 보조 프리셋이며, 설정 버전 34에서 추천 슬롯만 갱신한다.
- 색상 변경의 공식 기준으로 `Docs/Design/COLOR_PALETTE_STANDARD.md`를 추가했다. 2.1 팔레트 출처, 카테고리 의미색, 스킨별 톤 변환, 유사색 판정, 수동·자동 검증과 금지 사항을 한 문서에 고정했다.
- 사용자 승인에 따라 블랙 기준 원색 팔레트를 최종 확정했다. 다섯 프리셋은 대표색 하나와 대비되는 여러 색 계열로 구성하고 세 스킨이 같은 원색 배열을 공유한다. Special Day는 휴일 `#FF2A2A`, 야구 `#00FF66`, D-Day `#FF5722`, 기념일 `#FF1744`를 사용한다. 컬러의 흰색 혼합률은 58%에서 36%로 낮춰 중간 채도를 살리고, 블랙 표면은 `#121212`·`#1A1A1A` 계열로 조정했다. 설정 버전 35는 기존 기본 Special Day 값과 추천 슬롯만 새 기준으로 갱신하며 사용자 지정색은 보존한다.
- 밝은 네온 녹색·노란색 위의 글자 대비가 낮다는 실기기 피드백에 따라 형광에 가까운 기준색을 모두 중간 명도 색으로 교체했다. 첫 프리셋 이름도 `네온오션`에서 `오션블루`로 변경했다. Special Day는 휴일 `#E53E3E`, 야구 `#38A169`, D-Day `#DD6B20`, 기념일 `#E52E71`로 낮췄다. 블랙 일정과 설정 색상 카드는 상대 명도가 0.58 이상이면 짙은 `#111827`, 그보다 어두우면 밝은 동계열 글자를 자동 선택한다. 설정 버전은 36이다.
- 사용자 RGB 조정에도 글자색이 자동 대응하도록 `CategoryColorSystem.cs`를 분리했다. 파스텔·블랙·컬러의 실제 일정 배경, 동계열 밝은·진한 글자 후보, 설정 카드 테두리를 이 모듈 한곳에서 계산한다. WCAG 상대 명도 대비율 4.5:1을 만족하지 못하면 `#111827` 또는 흰색으로 대체한다. `MainWindow.Theme.cs`와 `SettingsWindow.cs`의 중복 계산식을 제거했으며 `theme-check.ps1`은 모든 기본 프리셋×세 스킨 조합의 실제 대비율을 검사한다.
- 첫 대비 검사에서 블랙 핫핑크 `#D62976`과 `#111827`이 4.23:1에 그치는 경계 사례를 발견했다. 안전 대체 순서를 동계열 후보 → `#111827`/흰색 → 순수 검정/흰색 비교로 보강해 어떤 사용자색도 최종 4.5:1 이상이 되도록 수정했고 전체 품질 게이트를 통과했다.
# 2026-08-23 파스텔 공통 표현 보정

- KBO 경기 일정의 `작게·중간·크게` 선택이 창을 닫으면 중간으로 초기화되던 문제를 수정했다. 선택 즉시 `SportsCalendarScale`에 저장하고 다음 창 생성 시 복원한다. 설정 버전은 39로 올렸으며 기존 사용자는 중간 크기로 안전하게 초기화한다.
- 색상 책임 리팩토링을 완료했다. `OnharuColorPresets.cs`로 5개 프리셋·기본 카테고리색을, `OnharuStateColors.cs`로 오른쪽 탭·중요한 날 상태색을 이동했다. `SettingsWindow`, `MainWindow.cs`, `MainWindow.Detail.cs`는 더 이상 해당 색상 정책이나 HEX 배열을 직접 소유하지 않는다. 사용자 저장 형식과 실제 색상값은 변경하지 않았다.
- 설정의 프리셋 라디오는 `라디오 원 + 캡션`을 하나의 묶음으로 취급해 오션블루부터 Google까지 7개의 동일 폭 열에 배치했다. 묶음 내부 좌우 여백을 제거해 12px `시안나이트`가 잘리지 않도록 했다.
- 실기기 확인 결과 동일 폭 열은 글자 길이에 따라 묶음 사이의 실제 빈 간격이 달라 보였다. 7개 묶음은 내용 너비 `Auto`, 사이의 6개 공백은 동일한 `*` 너비를 쓰도록 바꿨다. 긴 `시안나이트`도 잘리지 않으며 모든 묶음 사이의 빈 공간이 동일하다.
- 설정 프리셋 라디오를 동일 폭으로 바꾸고 색상 편집 카드의 체크박스·제목·미리보기 칩을 수직 중앙 정렬했다. 블랙은 메인 일정·설정·오른쪽 카드 글자를 흰색으로 통일하며, 밝은 기준색은 흰 글자 4.5:1 대비가 나올 때까지만 자동으로 어둡게 한다. 설정 카드 끝 칩은 원색 대신 파스텔 변환색을 표시한다. 오른쪽 날짜 범위 탭은 미선택 중립/선택 강한 인디고로 분리했고 중요한 날은 두 스킨 모두 같은 사용자색 변환 로직을 사용한다.
- 중요한 날 버튼은 일반 카테고리 카드가 아니다. 미설정은 흰색, 설정됨은 선택한 파스텔 배경과 진분홍 글자·연분홍 테두리를 사용하는 기존 상태 표현으로 복원했다. 블랙에서도 같은 의미 표현을 유지한다.
- 실기기에서 중요한 날의 설정·미설정 상태가 모두 너무 흐리다는 피드백을 반영했다. 설정됨은 일반 일정의 80%가 아니라 58% 백색 혼합 배경과 더 선명한 진분홍 글자·테두리, 미설정은 연회색 배경과 명확한 회색 글자·테두리를 사용한다.
- Special Day 프리셋 원칙을 보완했다. 휴일은 모든 기본 프리셋에서 선명한 레드 `#DC2626`, 야구는 현재 필드 그린 `#38A169`을 유지한다. D-Day와 기념일은 더 이상 고정색이 아니며 오션블루·핫핑크·라임펄스·바이올렛·시안나이트 라디오 선택에 따라 각각 다른 조화색으로 즉시 변경된다. 사용자의 RGB 편집 가능성은 유지한다.
- 프리셋 라디오 이름은 설정의 일반 옵션과 같은 12px 글자로 통일했다.
- 오른쪽 상세 카드가 메인 일정 막대보다 진해 보인다는 실기기 피드백에 따라 `DetailBackground`와 `DetailForeground`를 메인 `Background`·`Foreground`의 직접 별칭으로 바꿨다. 두 영역은 이제 계산값까지 완전히 동일하다. 필터 체크박스만 상태 가시성을 위해 파스텔 기준색의 백색 혼합률을 58%로 낮춘 한 단계 진한 색을 사용한다.
- 설정 색상 카드와 오른쪽 상세 카드가 메인 달력보다 진하고 글자색도 제각각이던 문제를 수정했다. `CategoryColorSystem`에서 파스텔 배경의 백색 혼합률을 80%로 통일하고, 기준색을 단계적으로 어둡게 하면서 WCAG 4.5:1을 만족하는 가장 부드러운 동계열 글자를 자동 선택한다. 메인 달력·설정·오른쪽 일반 일정·야구·D-Day·기념일이 같은 계산 경로를 사용하며 전체 17개 품질 게이트를 통과했다.
# 2026-08-23 2.2 스킨·부가기능 디자인 정리

- 색상 프리셋, 카테고리 대비, 오른쪽 상태색을 각각 `OnharuColorPresets`, `CategoryColorSystem`, `OnharuStateColors`로 분리했다.
- 시간표·일기장·KBO 독립 창은 `OnharuPopupChrome.FeatureTitle`, `PrimaryButton`, `OnharuSegmentedSwitch`를 재사용하도록 정리했다.
- 메인 부가기능 아이콘과 팝업 아이콘을 `▦`, `✎`, `⚾`로 통일하고 메인에서는 21px로 확대했다.
- `Docs/Audits/REFACTOR_AND_RELEASE_AUDIT_20260823.md`와 `Docs/Release/AUTO_UPDATE_TEST_GUIDE.md`에 감사 및 시험 절차를 기록했다.
- 전체 17개 품질 게이트를 통과했으며 외부 배포는 사용자 확인 전까지 보류했다.

# 2026-08-24 홈페이지 2.2.1 게시

- `Release/GitHubUpload221`의 설치판과 포터블판을 웹 배포본 `Publish/ONHARU-Web/downloads`에 반영했다.
- 다운로드·소개·FAQ·릴리스 노트·이용약관·개인정보처리방침의 표시 버전과 안내를 2.2.1 기준으로 갱신했다.
- 2.1·2.2.0 사용자는 자동 업데이트 통신 보완을 적용하기 위해 2.2.1을 한 번 직접 설치해야 한다는 안내를 추가했다.
- 설치판 SHA-256은 `0C3C668AF03B5B481E4434F4DBD6A50BC2090599B42B7E08AB4D8C75374709E3`, 포터블판은 `B11603149182A17D92781E23BDFA88F9A834FBF2C41E9065CECD573294A3556F`이다.
- `pwsh -NoProfile -File ./check-v22.ps1`로 전체 17개 품질 게이트 통과를 확인했다.
- AWS 게시 전 `/home/ubuntu/onharu-web-backups/site-before-2.2.1-20260824-001401.tar.gz`로 기존 `/var/www/html` 전체를 백업했다.
- `onharu.app`의 9개 공개 페이지가 모두 HTTP 200으로 응답하고, 다운로드 페이지의 파일명·해시와 공개 파일의 바이트 크기가 원본과 일치함을 확인했다.

# 2026-08-24 Microsoft Store 다운로드 선택지 준비

- 웹 다운로드 영역을 설치판·포터블·Microsoft Store의 3개 카드 구조로 정리했다. 기존 설치판과 포터블 링크·파일명·SHA-256·다운로드 집계 동작은 변경하지 않았다.
- Microsoft Store 카드는 ONHARU 파스텔 카드 디자인을 유지하면서 `준비 중` 상태로 표시했다. Store 주소가 확정되기 전에는 클릭이나 이동이 발생하지 않도록 링크가 아닌 `aria-disabled` 요소로 구성했다.
- 카드 영역은 데스크톱 3열, 900px 이하 2열, 680px 이하 1열로 전환한다.
- `Publish/ONHARU-Web/ONHARU-DOWNLOAD.html`과 `Distribution/ONHARU-DOWNLOAD.html`을 동일하게 맞췄다.
- 카드 수, 실제 다운로드 링크 2개, Store 비활성 상태, 반응형 분기, 다운로드 파일 존재와 두 웹 원본의 해시 일치를 정적 검사했다. `pwsh -NoProfile -File ./check-v22.ps1`의 전체 17개 품질 게이트도 통과했다.
- 운영 서버에는 게시하지 않았다. 로컬 `file://` 페이지의 브라우저 자동 표시는 보안 정책으로 차단되어 시각 검증은 수행하지 못했다.

# 2026-08-24 릴리스 노트 이전 버전 톤 구분

- 릴리스 노트에서 첫 번째 현재 버전 카드는 그대로 유지하고, 뒤이어 배치되는 이전 버전 카드의 배경·테두리·그림자·본문·배지를 낮은 채도의 중립색으로 조정했다.
- 인접 형제 선택자 `.release + .release`를 사용해 앞으로 이전 릴리스 카드가 추가되어도 같은 표현이 자동 적용된다.
- `Publish/ONHARU-Web/ONHARU-RELEASES.html`과 `Distribution/ONHARU-RELEASES.html`을 동일하게 맞췄으며 운영 서버에는 게시하지 않았다.
- 사용자 승인 후 다운로드와 릴리스 노트 변경을 운영 서버에 게시했다. 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-store-release-style-20260824-235525.tar.gz`이다.
- 공개 페이지 HTTP 200, Store `준비 중`·비링크 상태, 이전 버전 톤 선택자, 현재·이전 배지, 기존 설치판·포터블 링크와 파일 HTTP 200을 포함한 10개 운영 검증을 통과했다.

# 2026-08-25 이전 릴리스 접기

- 현재 버전은 항상 펼친 상태를 유지하고 두 번째 이후 `.release` 카드는 이전 버전으로 자동 분류해 기본 접힘 상태로 표시한다.
- 각 이전 버전 헤더에 `펼치기/접기` 버튼을 추가하고, 릴리스 목록 오른쪽 위에 `전체 펼치기/전체 접기` 버튼을 배치했다. 개별 조작 후에도 전체 버튼 문구가 실제 상태에 맞게 갱신된다.
- 기존 카드 마크업을 복제하지 않고 CSS와 짧은 네이티브 JavaScript로 처리해 이후 이전 버전 카드가 늘어나도 자동 적용된다.
- `Publish/ONHARU-Web/ONHARU-RELEASES.html`과 `Distribution/ONHARU-RELEASES.html`을 동일하게 유지한다. 운영 서버에는 아직 게시하지 않았다.
- 사용자 승인 후 접기 기능을 운영 서버에 게시했다. 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-release-fold-20260825-000325.tar.gz`이다.
- 공개 페이지 HTTP 200, 기본 접힘 CSS, 개별 펼치기·접기, 전체 펼치기·접기, 현재·이전 버전 표시를 포함한 6개 운영 검증을 통과했다.

# 2026-08-26 소개·Free · Plus 통합

- 별도 `Free · Plus` 페이지의 기능 비교와 운영 원칙을 소개 페이지 기능 설명 뒤에 자연스럽게 통합했다. Free 기본 기능과 준비 중인 Plus의 Google 연결 기능을 2열 비교하고 모바일 760px 이하에서는 1열로 전환한다.
- 전체 웹 페이지 헤더에서 `Free · Plus` 메뉴를 제거해 `소개` 단일 진입점으로 정리했다.
- 기존 `ONHARU-PLUS.html` 주소는 삭제하지 않고 `ONHARU-FEATURES.html#free-plus`로 즉시 이동하는 안내 페이지로 유지했다.
- `index.html`과 소개 페이지, `Publish/ONHARU-Web`과 `Distribution`의 10개 HTML을 서로 맞췄다. 운영 서버에는 아직 게시하지 않았다.
- 사용자 승인 후 10개 HTML을 운영 서버에 게시했다. 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-features-plus-merge-20260826-183803.tar.gz`이다.
- 기존 다운로드·릴리스 노트 파일만 `root` 소유여서 최초 직접 덮어쓰기가 거부됐으나, 임시 업로드 후 `sudo cp`로 10개 문서를 일괄 교체하고 모든 HTML 소유권을 `ubuntu:ubuntu`로 통일했다.
- 공개 10개 페이지 HTTP 200, 헤더 Plus 제거, 소개 메뉴 단일화, 비교 구역·준비 중 문구·모바일 1열, 기존 Plus 주소 리디렉션·수동 링크, index/소개 일치를 포함한 9개 운영 검증을 통과했다.

# 2026-08-28 홈페이지 2.2.5 사전 갱신

- 사용자 확인에 따라 2.2.5 정식 확정 전 홈페이지의 제품 설명을 먼저 갱신했다. 소개·FAQ·릴리스 노트에 KBO 로컬/Google 선택 등록과 중복 방지, 선택형 Google Tasks, 고정 상태 주차·인쇄, 업데이트 위치 복원, 설치 전 LayerHost 종료를 반영했다.
- 릴리스 노트의 2.2.5는 `출시 예정`, 2.2.4는 `현재 정식 버전`으로 구분했다. 다운로드 페이지도 2.2.4 정식판과 2.2.5 시험판을 명확히 분리하며 시험판이 자동 업데이트 대상이 아님을 유지한다.
- 최신 시험판 설치 파일과 포터블 파일을 웹 배포 폴더에 동기화하고 표시 SHA-256을 원본과 맞췄다.
- 소개와 index, 이용약관·개인정보처리방침 적용 버전을 2.2.5 기준으로 갱신하고 `Publish/ONHARU-Web`과 `Distribution`의 HTML을 동기화했다. 운영 서버에는 아직 게시하지 않았다.
- 사용자 승인 후 10개 HTML과 최신 2.2.5 시험판 설치·포터블 파일을 운영 서버에 게시했다. 게시 전 백업은 `/home/ubuntu/onharu-web-backups/site-before-2.2.5-preview-20260828-004118.tar.gz`이다.
- 서버 SHA-256이 원본과 일치하고, 공개 10개 페이지 HTTP 200, 소개·다운로드·릴리스·정책 상태, 시험판 두 파일 HTTP 200과 바이트 크기를 포함한 9개 운영 검증을 통과했다.

# 2026-08-24 ONHARU Free · Plus 공개 안내

- `Publish/ONHARU-Web/ONHARU-PLUS.html`을 추가했다. Free 로컬 기능, Plus의 Google Calendar 양방향 동기화, 하나의 앱·설치 파일, 연간 구독 준비 방향, 구독 종료 후 로컬 데이터 보존, 공식 웹과 Microsoft Store 병행 계획을 일반 사용자 문구로 정리했다.
- 가격·결제 사업자·출시일은 확정하지 않았으며 구매 버튼 없이 `준비 중`으로 표시했다. 결제사 후보와 수수료 등 내부 운영 조사 내용은 공개하지 않았다.
- 기존 9개 페이지의 공통 헤더에 `Free · Plus` 링크를 추가하고 모바일 가로 스크롤 규칙을 유지했다.
- 게시 전 `/var/www/html` 전체를 `/home/ubuntu/onharu-web-backups/site-before-plus-20260824-004517.tar.gz`로 백업했다.
- `https://onharu.app/ONHARU-PLUS.html`과 기존 9개 페이지 모두 HTTP 200, Plus 링크, viewport 선언을 확인했다. 390×844 모바일 검증에서 본문 가로 넘침은 0이고 비교 카드는 1열, 헤더는 가로 스크롤로 동작했다.
# 2026-08-24 설정 스킨·색상 영역 재정리

- 설정의 `상단 스킨 전환 버튼 표시`와 저장 필드는 삭제하되, 메인 헤더의 `파스텔·블랙` 빠른 전환 자체는 항상 표시한다.
- 설정 맨 위에 디자인 스킨을 배치하고, 파스텔 선택 카드는 현재 프리셋 대표색의 파스텔 변환색을 사용한다.
- 색상 작업 순서는 `프리셋 라디오 → RGB 펼치기 / 내 설정으로 저장 / 색상 교환 / 색상 설정 초기화 → 색상 카드`로 통일했다.
- 추천 프리셋을 직접 덮어쓰던 `선택 프리셋 변경` 버튼과 관련 로직은 삭제했다.
- 추천 색상 조합·도구·색상표를 하나의 섹션 테두리로 묶고 `색상 설정 초기화`를 제거했다.
- 색상 선택 제한을 제거했다. 선택 순서의 첫 두 카드는 `업무일정 ⇄ 개인일정 교환`처럼 교환 대상이 되며 교환만으로 저장하지 않는다.
- 하나 이상 선택 후 `내 설정으로 저장`을 누르면 선택 항목만 내 설정에 갱신한다. `<프리셋명> 변경` 버튼은 현재 전체 색상을 선택한 프리셋에 저장하며 Google은 변경할 수 없다.
- 내 설정 부분 저장은 색상 하나부터 가능하도록 완화하고, 색상 교환은 정확히 두 개가 선택된 경우에만 활성화했다.
- 일정 정렬에 `중요 일정 우선` 설정을 추가했다. 실제 우선순위는 `완료 상태 → 중요 일정 → 하루종일 → 카테고리/시간`이며 하루종일 일정은 별도 체크 없이 항상 시간 일정 위에 둔다.
- 주차 표시와 ISO/일반 주차 방식 라디오를 한 줄에 배치하고, 주차 표시가 꺼지면 방식 라디오만 숨긴다. RGB 버튼은 왼쪽 정렬 `RGB ▼ / RGB ▲`로 단순화했다.
- 추천색 초기화를 다시 추가하되 기본 다섯 프리셋만 복원하고 내 설정은 보존한다. 색상 교환 버튼은 `업무일정 ⇄ 개인일정`처럼 카테고리명만 표시하고 교환 직후 모든 체크를 해제한다.
- 색상 도구는 좌우 1px 여백을 기준으로 폭을 압축했다. 프리셋 변경은 네 글자 프리셋명과 `변경`이 들어가는 96px 기준이며, RGB는 섹션 왼쪽 끝에 맞췄다.
- 오른쪽 상세의 `선택 날짜 / 이번 주 / 다음 주` 버튼은 88×31로 조정해 두 사이 간격을 약 14px 확보했다. 생성 후 72/60/60px로 다시 덮어쓰던 중복 레이아웃 코드는 제거 상태를 유지한다.
- 프리셋 변경 버튼은 현재 대표색의 파스텔 배경과 동계열 진한 글자를 사용한다.
# 2026-08-25 ONHARU 2.2.2 배포 기준

- Assembly·화면·설치 스크립트·배포 파일명을 `2.2.2`로 통일했다.
- 스킨·팔레트·상태색·오른쪽 카드·메인 헤더·부가 팝업의 최신 개선을 포함한다.
- 음력은 파스텔에서 짙은 회색, 블랙에서 밝은 회색을 사용하고 24절기와 오늘은 테마 의미색을 사용하도록 변경했다.
- 배포 후 1920×1080 125% 및 혼합 DPI 다중 모니터 실제 장비 결과를 받아 필요한 보정은 `2.2.3`에서만 진행한다.
- 17개 자동 품질 게이트를 모두 통과했고 Inno Setup 설치판·포터블 ZIP·설치 ZIP·SHA-256을 생성했다.
- 공개 소스 커밋은 `195755d6cb01461b9131ed0adbc48a8dee3efd81`이며 GitHub `v2.2.2` Release를 최신 정식 버전으로 게시했다.
- GitHub에서 4개 배포 자산을 다시 내려받아 로컬 원본과 SHA-256이 모두 일치함을 확인했다.
- 2.2.2 배포 후 확인된 후속 수정: 행동 강조색이 Google/업무 카테고리색을 우선하여 프리셋을 바꿔도 녹색이 남는 회귀를 수정했다. 선택 날짜·주/월 범위·투명도 강조색은 현재 프리셋의 고정 대표색만 사용한다. 이 수정은 2.2.3 후보에 포함한다.
- 2.2.2 배포 후 확인된 후속 수정: 로컬 일정 파일이 없는 첫 실행에서 코드 내장 샘플 4개를 반환하던 경로를 제거했다. 신규 설치는 빈 일정 목록으로 시작하며, 기존 사용자 파일은 수정하지 않는다. 이 수정은 2.2.3 후보에 포함한다.
# 2026-08-25 2.2.3 다른 PC 시험 메모 정리

- 혼합 DPI/해상도 모니터 경계에서 이동 창이 양쪽 좌표계 사이를 왕복해 드래그 자체가 불가능한 현상이 실제 장비에서 확인됐다. 같은 해상도·같은 배율에서는 정상이고, 100%→125% 또는 낮은 해상도→높은 해상도에서 재현된다.
- 2.2.3 최우선 진단은 드래그 중 `WM_DPICHANGED`, 화면 안쪽 보정, placement 복원/저장이 서로 재진입하는지 확인하는 것이다. 드래그 중 좌표 권위를 하나로 제한하고 종료 후 한 번만 정규화하는 방향을 우선한다.
- 고정 상태 상세 패널 접기 입력 실패와 최소 폭 헤더 겹침도 2.2.3 회귀 항목으로 확정했다.
- 전체 사용자 메모는 `TODO_NEXT.md`의 `2026-08-25 다른 PC 시험 결과 — 2.2.3 범위`에 P0/P1/후속 범위로 정리했다. 아직 이 메모에 따른 제품 코드는 변경하지 않았다.
# 2026-08-26 설정·중요한 날·알림 정리

- 설정에서 더 이상 존재하지 않는 `× 버튼 동작` UI와 저장 필드를 제거했다.
- 상세 날짜 앞에는 테두리 없는 5각 별만 표시하며, 클릭 시 중요한 날 색상 팔레트가 열린다.
- 알림 설정은 하나의 카드로 합쳤다. 전역 `알림 사용`을 끄면 일정별 알림 값은 유지하지만 팝업과 소리는 발생하지 않는다.
- 혼합 DPI 실기기 시험과 사용 통계·팝업 대표색·주간/일간/일정 화면·Windows 셸 연동·Free/Plus 권한 구조는 `TODO_NEXT.md`의 추후 확인으로 남겼다.
- 달력 범위 조작을 설정에서 메인 화면으로 이동했다. 주 보기 버튼은 동일 버튼을 다시 눌러도 1~6주 선택 목록을 열며, 월 전체는 항상 필요한 4~6개 행을 자동 계산한다. 이번 주 행은 1~2주일 때 첫 행, 3~6주일 때 두 번째 행으로 고정한다.
- 별도 숨기기·종료 버튼은 헤더 혼잡을 피하기 위해 보류했다. 로고 메뉴 최초 1회 안내와 향후 도움말 아이콘·상세 사용법 화면을 `TODO_NEXT.md`에 기록했다.

# 2026-08-26 상세 카드 순서 변경

- 이동할 수 없는 일정 카드의 제한 사유 툴팁은 제거하고 금지 커서만 유지했다.
- 오른쪽 상세 화면이 `카테고리` 정렬일 때 카테고리 카드 제목을 위아래로 드래그해 순서를 바꿀 수 있다. 사용자가 정한 순서는 설정에 저장되며 메인 달력의 일정 정렬에는 영향을 주지 않는다.
- `시간순` 정렬은 시간 의미가 흐트러지지 않도록 수동 카드 순서 변경을 적용하지 않는다.
- 로컬 테스트본 빌드와 `check-v22.ps1` 전체 품질 게이트를 통과했다.
- 일정의 `드래그하여 이동` 툴팁을 제거했다. 상세 카테고리 제목은 손가락 커서를 사용하며 클릭하면 D-Day·기념일 카드처럼 접히거나 펼쳐진다.
- 일반 일정·D-Day·기념일 상세 카드의 내부 여백은 모두 `10, 8, 10, 7px`로 동일함을 재확인했다.
- 일반 일정 카드 제목 영역도 24px로 통일해 접힌 카드의 실제 높이를 D-Day·기념일과 맞췄다. 모서리 반경도 12px로 통일했다.
- 상세 카드 순서 드래그 중 카드 사이 여백은 유효한 정렬 영역으로 처리한다. 상세 영역을 벗어나거나 메인 달력 위로 이동하면 금지 커서를 표시해 일정 이동·복사와 혼동되지 않게 했다.
- 일반 일정 카드의 기존 접힌 높이를 기준으로 되돌리고, D-Day·기념일의 접힌 제목 높이만 15px로 줄였다. 접힌 기념일 카드에서는 `만들기` 버튼을 숨겨 세 카드 높이를 맞춘다.
- 상세 스크롤 영역 전체를 카드 정렬 드롭 구역으로 지정해 카드 사이 여백에서도 금지 커서가 나오지 않게 했다.
- `시간순` 보기는 카테고리별로 잘게 나누지 않고 `하루 종일`과 `시간 일정` 두 카드로 묶는다. 두 카드도 제목 드래그로 위아래 순서를 바꿀 수 있고 별도 순서로 저장한다.
- 메인 달력 일정 막대의 드래그 안내 툴팁을 제거했다. 기념일·읽기 전용 일정처럼 편집은 가능하지만 이동은 불가능한 항목은 평상시 손가락 커서를 사용하고, 실제 드래그 임계 거리를 넘겼을 때만 금지 커서를 표시한다.
- 상세 카드 정렬은 대상 카드의 위·아래 절반을 맞출 필요 없이 카드 영역 어디에 놓아도 이동 방향에 따라 대상 카드 앞이나 뒤로 배치한다.
- `시간순`의 `하루 종일`·`시간 일정` 카드는 흰색 배경과 중립 테두리를 공통 사용하고, 각 일정 글씨는 해당 달력 카테고리의 색상 계열을 사용한다.
- 시간순 카드의 일정 글씨는 별도 명도 변환 없이 메인 달력과 동일한 `EventTextBrush`를 사용한다. Google 하루 종일 일반 일정에는 카테고리 구분용 표시 전용 체크박스를 추가하되 클릭·완료 저장·Google 동기화는 하지 않는다.
- 중요 일정 기본색과 임시 팔레트는 `중아 OB 모임 (홍어랑민어랑)`의 `#FFF1F7` 배경·`#F20D7A` 글씨를 기준으로 밝은 조합으로 복원했다. 배경색을 처음 선택해도 검정 글씨를 자동 지정하지 않고 같은 위치의 선명한 동계열 글씨색을 짝으로 적용한다.
- 상세 일정 표기는 메인 달력과 동일하게 `시간 → ★ → 일정명` 순서를 사용한다.
- 카테고리 보기의 오른쪽 카드 색은 첫 일정의 중요 여부를 참조하지 않고 항상 원래 카테고리색으로 계산한다. `DetailBackground`와 `DetailForeground`는 메인 달력의 `Background`·`Foreground`를 그대로 호출하므로 두 영역의 실제 채움색과 글씨색은 동일하다. 사용자가 요청한 흰색 `시간순` 카드만 예외다.
- 카드 전체 배경은 카테고리색을 유지하되, 카드 안의 중요 일정 제목·부가 글씨는 사용자가 일정 등록/수정에서 선택한 중요 글씨색을 그대로 사용한다. 중요 배경색으로 행 전체를 칠하지 않는다.
- 예외적으로 `중요 일정 우선`이 켜져 중요 일정이 동일 카테고리의 일반 일정과 별도 카드로 분리되면, 그 중요 카드 전체는 달력의 중요 일정과 동일한 사용자 선택 배경색·글씨색을 사용한다. 섞여 있는 카드는 계속 카테고리색을 유지한다.
- 중요 일정 우선으로 분리된 중요 카드와 일반 카드는 화면 제목이 같아도 내부 접기·정렬 식별값을 별도로 사용한다. 따라서 한쪽 제목을 눌러도 다른 카드가 함께 접히지 않는다.
- `중요 일정 우선`의 중요 카드는 항상 최상단 고정이며 카드 정렬 대상에서 제외한다. 중요 카드를 끌거나 일반 카드를 중요 카드 위에 놓으려 하면 금지 커서를 표시하고, 중요 카드 아래의 일반 카드끼리만 순서를 변경한다.
- D-Day와 기념일 요약 카드도 일반 상세 카드와 같은 드래그 순서 목록에 포함했다. 제목 클릭 접기·펼치기는 유지하며, 중요 일정 고정 카드 아래에서 일반 카테고리 카드와 서로 순서를 바꿀 수 있고 선택 순서는 정렬 모드별 설정에 저장된다.

# 2026-08-26 색상 체계 분석·대비 안전 보정

- 중요 일정의 사용자 지정 배경·글씨가 공통 계산을 우회해 흰 배경에 흰 글씨가 사라질 수 있던 원인을 수정했다. 선호 글씨색은 대비율 4.5:1 이상일 때 유지하고 부족할 때만 안전색으로 보정한다.
- `Docs/Design/COLOR_ORCHESTRATION_RULES.md`에 파스텔·블랙의 색 사용량, 채도·네온 제한, 대표색 반복 억제, 사용자 지정색 안전 규칙과 결정 순서를 정리했다.
- 공식 문서의 오래된 `시안나이트` 표기를 실제 코드의 `앰버선셋`과 맞췄다. 프리셋 원색은 변경하지 않았다.

# 2026-08-28 메인 조작부 색상 절제

- 최소화·최대화·닫기 버튼의 파랑·초록·빨강 조합을 제거하고 두 스킨의 공통 중립 버튼 스타일로 통일했다.
- 달력 범위, 스킨, 이동·고정, 상세 정렬, 주 개수, 투명도와 팝업 테두리를 프리셋 대표색에서 분리했다. 프리셋 색은 일정·카테고리 표현에 집중한다.
- `오늘` 버튼의 시안·사용자색·보라 3색 그라데이션을 제거하고 파스텔은 연한 인디고, 블랙은 어두운 인디고 단색 조합으로 낮췄다.

# 2026-08-28 Soft Workspace 내 설정 기준

- 사용자 제공 이미지에서 블루 `#AECBFA`, 옐로 `#FEEFC3`, 그린 `#CEEAD6`, 코랄 `#FAD2CF`, 보조 블루 `#E8F0FE`, 중립 `#EAEBED`를 추출해 `Docs/Design/SOFT_WORKSPACE_COLOR_REFERENCE.md`에 톤·명도·채도 원칙을 기록했다.
- 이미지의 완성 면색을 그대로 저장하면 ONHARU 파스텔 변환에서 지나치게 희어지므로 같은 색조의 기준색 12개를 `SoftWorkspacePalette()`로 정의했다.
- 새 사용자와 아직 `내설정`을 저장하지 않은 기존 사용자에게만 이 배열을 적용한다. 이미 저장된 사용자 팔레트는 보존한다.

# 2026-08-28 웹 소개 실제 달력 화면 교체

- 소개 페이지 오른쪽의 합성 달력 그림을 ONHARU 2.2.5가 실제 렌더링한 전체 달력 화면으로 교체했다.
- 캡처에는 별도 격리 데이터 폴더의 임의 일정만 사용했으며 실제 사용자 일정이나 Google 계정 데이터는 포함하지 않았다.
- 이미지 확대·회전·잘라내기를 없애고 달력 본체 전체가 데스크톱과 모바일에서 비율을 유지해 보이도록 조정했다.
- `Publish/ONHARU-Web`과 `Distribution`의 소개 페이지, 시작 페이지, 미리보기 이미지를 동기화했다. 운영 서버에는 아직 배포하지 않았다.
# 2026-08-28 팔레트 랩 보기 확장

- 기능별 팔레트 랩에 `파스텔 + 블랙 / 파스텔만 / 블랙만` 보기 전환을 추가했다.
- 개별 색상 선택과 별도로 기준색·파스텔·블랙을 계열 단위로 비교하는 `계열 팔레트 묶음` 보기를 추가했다.
- 모든 후보를 H값 기준 `빨강·주황·노랑·초록·파랑·남색·보라`로 재분류하는 색상환 팔레트 보기를 추가했다.
- 비슷한 채도·명도의 서로 다른 계열색을 7색씩 묶은 조합 팔레트와 묶음 선택을 추가했으며, 모든 보기에서 개별 색상 선택이 가능하다.
- 현재 앱의 오션블루 프리셋과 중요 일정 6색은 사용자 선호 기준으로 확정해 실제 코드 HEX 그대로 조합 팔레트에 추가했다. 취소용 중립 회색은 중요색 조합에서 제외한다.
- 조합 팔레트에 색별 HSL과 조합 전체의 평균 채도·명도 및 범위를 표시해 색감 요청을 수치로 전달할 수 있게 했다. 여러 색상환 구간을 섞는 조합은 H 평균이 왜곡되므로 H는 색별로만 표시한다.
- 팔레트랩 상단에 배경·글씨 독립 선택, 실제 카드 미리보기, WCAG 대비율과 AA/AAA 판정을 실시간 표시하는 조합 실험기를 추가했다.
- 팔레트 색 또는 묶음을 선택하면 현재 스킨 기준의 배경·글씨 쌍이 조합 실험기에 자동 반영된다.
- 팔레트랩 설명은 접기 가능하게, 실제 적용 위치는 상단 고정으로, 보기 옵션·색 조합 실험은 왼쪽 고정 패널로, 팔레트는 오른쪽 스크롤 영역으로 재배치했다.
- 설명을 고정 팝업으로 바꾸고 팔레트 옵션도 왼쪽에 배치했다. 배경·글씨 선택 모드를 분리했으며 HEX 문자열은 선택 동작과 분리해 복사 중 재렌더링되지 않게 했다.
- 팔레트 클릭은 항상 후보 한 색만 선택하고, 검토한 배경·글씨 쌍을 왼쪽 선택색 모음에 명시적으로 추가하는 흐름으로 변경했다. 업무·개인·야구 및 Special Day는 각각 세트 대상이다.
- 기본 5계열은 유지하고, 옐로·골드는 기본 프리셋이 아닌 비교 후보로만 추가했다.
# 2026-08-30 검색 팝업 헤더·외곽선 정리

- 검색 팝업의 닫기 버튼을 별도 오버레이에서 헤더 안으로 옮겨 슬레이트 헤더가 오른쪽 끝까지 하나의 행으로 이어지게 했다.
- `UiRound.EmphasizePopup()`이 공통 팝업 셸의 중립 테두리를 인디고로 덮어쓰던 문제를 수정했다. 호출자가 지정한 테두리는 보존하고, 미지정 시에만 중립 슬레이트를 사용한다.
- `check-v22.ps1` 17개 품질 게이트를 모두 통과했다.
- 슬레이트 헤더와 분리돼 보이던 공통 닫기 버튼 및 검색창의 `오늘` 버튼을 같은 슬레이트 계열로 통일했다. 주 실행인 `검색` 버튼만 블루를 유지한다.
- 헤더 제목을 검색 입력과 같은 11px 텍스트 기준선에 맞추고, 검색 입력·결과 카드·날짜 선택기의 보라 계열 선을 공통 슬레이트 테두리로 통일했다.
- 세부 달력처럼 여러 저채도 색이 역할별로 조화를 이루도록 팝업 공통 팔레트를 재구성했다. 구조는 중립 슬레이트, 주 실행은 틸, 선택·세그먼트는 보색인 피치 코랄을 사용하며 공통 헤더 모서리를 10px로 부드럽게 처리한다.
- 일정 등록·수정과 설정 창의 상단 저장 버튼을 제거해 하단 저장만 남겼다. 두 창의 최종 저장 버튼은 파스텔 카드와 구분되는 중간 채도 틸 단색으로 통일하고, 닫기는 공통 팝업 스타일을 사용한다.
- 일정 등록·수정 및 설정 헤더의 닫기 버튼에 상하 4px·오른쪽 6px 내부 여백을 확보하고, 제목 시작선은 11px로 맞춰 버튼과 제목이 헤더 중앙에 정렬되도록 했다.
- 공통 닫기 버튼을 피치 포인트에서 진한 슬레이트 회색(`#475569`) 바탕과 흰색 글씨로 변경해 중립 팝업 헤더와 선명하게 구분되도록 했다.
- 검색 결과 선택 후 다음 편집창을 UI 큐에서 열도록 지연해 검색창만 닫히는 팝업 전환 충돌을 방지했다.
- 검색 팝업의 파스텔은 피치 `오늘`과 틸 `검색` 두 계열만 유지하고, 입력·결과 카드·날짜 선택 테두리는 연한 중립 회색(`#D5D8DE`)으로 낮췄다.
- 검색 상단 입력·범위·검색 버튼 행에 결과 스크롤바 점유 폭과 같은 26px 오른쪽 여백을 적용해 결과 카드의 오른쪽 끝선과 정렬했다.
- 검색 결과 500개 제한을 단순 과거 날짜순 절단에서 `오늘과 가까운 순으로 500개 선택 후 날짜순 재정렬`로 변경해 과거 일정이 많은 경우 미래 일정이 모두 탈락하던 문제를 해결했다.
- 사용자 지정 검색의 날짜 선택 폭을 130px에서 142px로 넓혀 마지막 날짜 숫자 잘림을 없앴고, 검색 행 오른쪽 여백을 실측 20px로 보정해 결과 카드 끝선과 맞췄다.
- 검색용 날짜 달력의 기본 날짜 테두리를 제거하고 중립 흰색 바탕으로 정리했다. 오늘은 틸, 선택 날짜는 피치만 사용하며 월 선택은 웜그레이로 맞춰 검색 팝업과 같은 2색 포인트 체계를 사용한다.
- 검색 전용 달력 변형은 제거하고 모든 팝업 달력이 `OnharuCalendarStyle`과 공통 `PopupHost`를 사용하도록 통일했다. 일정 등록·수정, 기념일, 일기, 검색 달력의 기존 크기와 패딩은 유지한다.
- 검색 결과 스크롤은 세부 일정과 같은 라벤더 손잡이(`#B7ACE8`)와 옅은 트랙(`#F1F5F9`)을 사용한다. `오늘` 버튼 위치는 제목 기능으로 적절해 유지한다.
- 공통 `StyleComboBox`가 포커스 후 두 번째 클릭에서 열리던 문제를 수정해 첫 클릭에서 직접 토글한다. 검색·설정·일정의 공통 화살표를 11.5px로 키우고, 드롭다운 선은 중립 회색, 선택은 피치, 호버는 웜그레이로 통일했다.
- 첫 클릭 보정 이벤트가 드롭다운의 `ComboBoxItem` 클릭까지 가로채던 문제를 수정했다. 항목 컨테이너 내부 클릭은 기본 선택 처리에 맡겨 옵션 선택과 `SelectionChanged`가 정상 동작한다.
- 검색 결과 영역을 기본 370px→400px, 사용자 지정 336px→366px로 늘려 하단의 과도한 빈 공간을 줄이고 기존 팝업 수준의 여백만 남겼다.
- 시간표의 요일별 파스텔 헤더는 유지하고, 표 외곽선만 중립 회색(`#D5D8DE`)으로 낮췄다. 시간표 스크롤도 세부 일정·검색과 같은 라벤더 손잡이(`#B7ACE8`)와 옅은 트랙(`#F1F5F9`)을 사용한다.
- 도구 팝업 제목 아이콘은 공통으로 중립 표면과 검정 슬레이트 아이콘을 사용한다. 시간표 설정·적용은 중립색으로 낮추고, `시간표 저장`만 공통 틸 파스텔 주 실행 버튼으로 유지했다.
- 시간표 설정 토글은 톱니 기능 버튼 대신 드롭다운 형태의 `⌄ 설정 펼치기` / `⌃ 설정 접기`로 변경했다.
- 공통 `DisclosureButton`을 추가해 펼침 토글은 항상 제목을 왼쪽, 중립 슬레이트 셰브론을 오른쪽에 둔다. 시간표에는 상태 문구 대신 `설정`과 위·아래 셰브론만 표시한다.
- 시간표 펼침 토글은 실제 설정 콤보박스와 맞춰 흰 바탕·중립 테두리·11.5px 우측 `▾`/`▴` 화살표로 재정리하고, 표기는 `시간표 설정`으로 바꿨다.
- 펼침 토글의 우측 화살표 여백을 5px 더 확보했다. 시간표 저장은 설정 저장·일정 저장과 같은 공통 `ActionButton`(진한 틸, 흰 글씨)으로 통일했다.
- 공통 닫기 버튼에 `Padding(0)`과 가로·세로 콘텐츠 중앙 정렬을 명시했다. 기존에는 공통 템플릿상 중앙이었지만 시간표에는 패딩 0이 명시되지 않았고, 이제 모든 팝업이 같은 규칙을 사용한다.
- 일기장은 아이콘·입력선을 중립 슬레이트로 맞추고, `새 일기`·`일기 저장`은 설정 저장과 같은 공통 틸 `ActionButton`으로 통일했다. 정렬/이전·다음/왼쪽 선택 일기는 라벤더, `수정`은 피치로 역할을 분리했다. 공통 닫기는 글꼴 여백 영향 없이 실제 중심에 오도록 독립 `TextBlock` 콘텐츠로 교체했다.
- 일기장 읽기는 참고 시안처럼 본문을 넓게 두고, 하단 `최근 일기`를 가로 카드로 배치했다. 카드는 라일락·베이지·민트를 순환하고, `새 일기`는 소프트 민트, 저장은 공통 진한 틸, 수정은 피치, 정렬/이동은 라일락으로 역할을 분리했다. 일기 데이터에는 태그 필드가 없어 가짜 태그 칩은 추가하지 않았다.
- 사용자 피드백으로 하단 최근 일기 카드 레이아웃은 원복하고 기존 `왼쪽 목록 + 오른쪽 본문` 구조로 복구했다. 일기장 포인트는 소프트 바이올렛/인디고 한 계열로 통일해 새 일기, 최신/오래된순, 목록/한 장 보기, 수정, 이전/다음, 선택 목록에 적용했다.
- 메인 달력 창 제어와 같은 26px 선형 X를 쓰는 `ToolCloseButton`을 추가해 검색·시간표·일기·야구·달력 인쇄·날짜 일정 목록에 적용했다. 설정과 일정 편집은 기존 닫기 규칙을 유지한다. 일기장 선택 탭·새 일기·수정은 인디고 바탕/흰 글씨로 강화하고 본문 날짜 아래 여백을 5px 확보했다.
- 일기장 전용 최종 팔레트는 `#6C5CE7` 바이올렛, `#2B2D42` 딥 차콜, `#8D99AE` 쿨그레이, `#FAF8F5` 크림, 흰 카드로 정리했다. 연필은 흰 칩, 선택 토글·새 일기·수정은 바이올렛/흰 글씨이며 왼쪽 목록 날짜 13px·제목 14.5px Bold·카드 높이 48px로 조정했다.
- 일기장 헤더의 글자 토글을 제거했다. 정렬은 검색 아래 `일기 목록` 행 우측의 `▽ 최신순 / △ 오래된순` 아이콘으로 옮기고, 목록/한 장 보기는 메인 달력 사이드바처럼 `‹/›` 셰브론 하나로 접고 펼치도록 변경했다.
