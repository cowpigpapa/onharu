namespace FamilyPlanner
{
    // 이 파일을 OAuthCredentials.local.cs로 복사한 뒤 Google Desktop OAuth Client Secret을 입력합니다.
    // OAuthCredentials.local.cs는 .gitignore에 포함되어 공개 저장소에 올라가지 않습니다.
    static class OAuthCredentials
    {
        public const string ClientSecret = "YOUR_DESKTOP_OAUTH_CLIENT_SECRET";
    }
}
