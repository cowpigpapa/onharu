# ONHARU project instructions

이 파일은 `Onharu_v2.2` 전체 작업에 적용한다.

## 두 지침 파일의 역할과 동기화

- `AGENTS.md` = Codex 진입점, `CLAUDE.md` = Claude 진입점. 제품·코드·디자인·팝업·검증 규칙은 두 파일이 같은 내용을 담는다.
- 공통 규칙을 한쪽에서 고치면 **같은 작업에서 다른 쪽도 같은 문장으로** 고친다. 한쪽만 갱신한 작업은 완료로 처리하지 않는다.
- 각 파일 고유 영역은 도구별 운영 방법뿐이다. Claude 고유 영역은 `CLAUDE.md`의 `Claude 세션 운영`이다.
- 두 파일이 어긋나 있으면 먼저 `HANDOFF.md`의 최신 기록으로 어느 쪽이 최신인지 확인하고, 판단이 서지 않으면 사용자에게 확인한 뒤 양쪽을 맞춘다.

## 작업 시작과 문서 동기화

- 모든 새 채팅과 작업자는 코드나 문서를 변경하기 전에 `START_HERE.md`, `CURRENT_STATUS.md`, 이 파일, `HANDOFF.md`의 최신 기록을 순서대로 읽는다.
- 코드·설정·사용자 동작·버전·배포 상태를 변경하면 `START_HERE.md`의 문서 동기화 표에 따라 관련 문서를 같은 작업에서 반드시 갱신한다. 문서가 갱신되지 않은 기능 변경은 완료로 처리하지 않는다.
- 사용자가 확인한 실기기 결과와 `CURRENT_STATUS.md`를 오래된 TODO보다 우선한다. 완료된 검사를 미완료로 다시 보고하지 않는다.
- 버전별 과거 기록은 보존하되 현재 상태 문서와 사용자 설명에 오래된 버전이 현재값처럼 남지 않게 한다.
- 작업 원칙·현재 상태·핵심 구조를 변경하면 `START_HERE.md`에 지정된 ONHARU 옵시디언 요약 노트도 같은 작업에서 갱신한다. 옵시디언을 개발 원본으로 사용하거나 전체 소스를 복제하지 않는다.

## 제품 기준

- ONHARU의 기존 기능, 사용자 데이터, Explorer 아이콘 아래 레이어 안정성을 보존한다.
- 디자인과 기능 개선은 계속하되 승인된 레이어 전환 기준선을 임의로 변경하지 않는다.
- 실험적인 레이어 코드는 정식 소스에 바로 넣지 말고 `Tests/LocalTest/TransitionCandidates`의 별도 시험본으로 검증한다.

## 코드 책임 분리

- `MainWindow.cs`에는 공유 필드, 생성자 골격, 최상위 조립만 둔다.
- 새 기능을 `MainWindow.cs` 또는 하나의 대형 파일에 몰아넣지 않는다.
- MainWindow 기능은 기존 partial 책임에 배치하고 맞는 경계가 없을 때만 명확한 새 partial을 만든다.
- UI 구성은 `MainWindow.Layout.cs`, 시작·종료 수명주기와 데이터 보정은 `MainWindow.Startup.cs`, 팝업 배치는 `MainWindow.Dialogs.cs`, 위치 상태는 `MainWindow.PositionMode.cs`, 색상·크기 도우미는 `MainWindow.Theme.cs`에서 유지한다.
- Google, ExplorerLayer, 반복 일정, 저장·마이그레이션, 시간표, 향후 Localization·Updates·Licensing은 서로 독립된 서비스나 모듈로 유지한다.
- 기존 파일을 분리할 때 먼저 기능 변화 없는 기계적 이동을 하고 빌드·품질 게이트를 통과한 뒤 로직 개선을 별도 단계로 수행한다.
- 한국어 표시 문자열을 영구 데이터 식별자로 새로 추가하지 않는다. 향후 다국어화를 위해 내부 ID와 표시 문자열을 분리한다.

## ONHARU 디자인 원칙

- 2.2의 기능·디자인·배치 기준선은 완성된 형제 프로젝트 `../Onharu_v2.1`이다. 추가 개발 전에 반드시 대응하는 2.1 소스와 실제 배치를 먼저 비교한다.
- 사용자가 별도로 변경을 요청하지 않은 폭·높이·Margin·Padding·정렬·글자 크기·클릭 영역은 2.1 값을 그대로 유지한다.
- 스킨 작업은 색상·브러시·필요한 시각 표현만 교체한다. 스킨 적용을 이유로 2.1의 레이아웃 치수나 기능 동작을 임의 변경하지 않는다.
- 새로운 UI가 필요하면 가장 가까운 2.1 화면의 구성·간격·공통 컨트롤을 재사용하고, 차이가 필요한 이유를 `HANDOFF.md`에 남긴다.
- 화면 변경 후 2.1과 끝선, 첫 행 기준선, 세로 중앙, 문자열 잘림, 스크롤 발생 여부를 비교한다.
- 새 화면·팝업·버튼은 반드시 기존 ONHARU 스타일과 조화를 이루게 만든다.
- 파스텔 색상, 둥근 모서리, 얇은 테두리, 공통 버튼 반응, 정돈된 여백을 기본으로 사용한다.
- 팝업은 가능하면 `OnharuPopupChrome`과 기존 공통 스타일을 재사용한다.
- 같은 역할의 버튼은 높이·모서리·내부 여백을 통일한다.
- 좌우 끝선, 라벨과 입력칸의 시작선, 행 안의 세로 중앙 정렬을 확인한다.
- 과한 공백을 피하고 섹션 간격과 행 간격을 일관되게 유지한다.
- 작은 노트북 해상도와 한국어·영어의 문자열 길이를 고려해 잘림과 불필요한 스크롤을 확인한다.
- 디자인 판단이 제품 경험을 크게 바꾸거나 기존 화면과 충돌하면 임의 확정하지 말고 사용자에게 한 가지씩 확인한다.
- 기본 클릭 요소의 hover는 색상·배경 반전을 사용하지 않고 손 모양 커서만 제공한다. 눌림 순간의 짧은 피드백과 선택·활성·경고처럼 상태 전달이 필요한 경우에만 색상 변화를 허용한다.
- 목록 선택, 메뉴 항목, 드롭다운 후보처럼 현재 위치를 구분해야 하는 탐색 UI의 hover 강조는 예외로 허용하되, 선택 상태와 혼동되는 강한 반전은 피한다.
- 아이콘 전용 버튼은 내부 여백을 0으로 두고 아이콘을 가로·세로 중앙에 배치한다. 파스텔 테마 아이콘은 검정 계열, 블랙 테마 아이콘은 흰색으로 통일한다. 닫기·삭제·경고처럼 의미색이 필요한 아이콘만 예외다.
- 아이콘은 색상 통일보다 먼저 가시성을 확보한다. 헤더의 주요 동작 아이콘은 100% DPI 기준 최소 16px의 유효 도형과 약 1.7px 이상의 획을 사용하며, 얇은 글꼴 기호 대신 가능한 한 벡터 도형을 사용한다.
- 메인 상단의 부가기능 아이콘과 해당 팝업 제목 아이콘은 같은 기호를 사용한다. 시간표 `▦`, 일기장 `✎`, 야구 `⚾`는 메인 버튼과 팝업 제목 모두 21px를 기준으로 하며 버튼 크기·간격을 바꾸지 않고 아이콘만 중앙 확대한다.
- 팝업 제목은 `OnharuPopupChrome.FeatureHeading`으로 통일한다. 24px 글리프 칸과 21px 제목을 쓰고 아이콘 박스나 테두리를 두지 않는다. 검색·설정·시간표·KBO가 같은 규격이다.
- 아이콘 도형은 `App/OnharuIcons.cs`가 단일 기준이다. 메인 헤더의 `HeaderGlyph`와 팝업 제목의 `FeatureHeading`이 모두 여기서 도형을 받아 같은 그림을 그린다. 새 아이콘은 글꼴 기호로 두지 말고 이 파일에 벡터 경로를 먼저 추가한다. 획은 1.6px 둥근 끝을 기본으로 한다.
- 팝업 제목 아이콘은 상자가 아니라 획을 왼쪽 기준선에 세운다. `FeatureHeading`의 글리프는 24px 칸에 가운데 정렬되므로 글리프 폭에 따라 헤더 왼쪽 여백을 보정한다. 현재 값은 `⌕` 14, `▦` 17이며 근거를 각 창의 주석에 남긴다. 컨트롤은 테두리를, 글자는 글자를 같은 기준선에 세운다.
- 새 부가기능은 메인 진입 버튼과 팝업 제목 아이콘을 동시에 정의하고, `OnharuPopupChrome.FeatureHeading`과 메인 `HeaderGlyph`의 크기·색·중앙 정렬 규칙을 따른다. 두 곳 모두 `OnharuIcons`의 같은 도형을 쓰므로 화면마다 서로 다른 대체 아이콘이 생기지 않는다.
- 슬라이딩 버튼과 메인 `오늘` 버튼은 12.5px SemiBold를 사용한다. 활성·비활성 상태 모두 작은 화면에서 즉시 읽혀야 한다.
- 스킨은 `파스텔`, `블랙` 두 개만 제공한다. 파스텔은 밝고 생동감 있는 배경과 같은 계열의 짙은 글자, 블랙은 선명한 배경과 대비가 보장된 글자를 사용한다. 프리셋 미리보기도 실제 일정 막대와 같은 배경·글자 조합을 보여준다.
- 추천 팔레트는 `OnharuColorPresets`의 5개 기본값을 사용한다. 설정 색상 영역은 `디자인 스킨 → 프리셋 → 색상 도구 → 색상 카드` 순서를 유지한다. 추천 프리셋을 직접 덮어쓰지 않으며, `내 설정으로 저장`은 현재 색을 별도 내 설정 슬롯에 복사한다. Google 기본 팔레트는 변경하지 않는다.
- 색상 카드는 제한 없이 선택할 수 있다. 정확히 두 개를 선택했을 때만 교환 버튼이 활성화되며 교환 자체는 저장하지 않는다. 하나 이상 선택한 뒤 `내 설정으로 저장`을 누르면 체크된 항목만 내 설정 슬롯에 갱신한다.
- 일정 표시 순서는 `중요 일정(옵션) → 하루종일(항상) → 카테고리별/전체 시간순`이다. 하루종일 우선은 별도 옵션으로 만들지 않는다.
- 추천 프리셋은 `<프리셋명> 변경` 버튼으로 현재 전체 색상을 명시적으로 덮어쓴다. Google 프리셋은 변경할 수 없다.
- `추천색 초기화`는 기본 다섯 프리셋만 복원하고 `내설정`은 보존한다. 색상 교환 후에는 체크를 모두 해제한다.
- 메인 헤더의 파스텔·블랙 빠른 전환은 기본으로 표시하되 설정에서 숨길 수 있다(`ShowThemeSwitch`). 설정창의 디자인 스킨과 같은 값을 사용한다.
- 프리셋 표시 이름은 공백 없이 5글자 이내로 유지한다. 팔레트는 선명한 고유 원색을 기준으로 하고 파스텔·블랙 스킨은 색상 정체성을 바꾸지 않은 채 톤과 글자 대비만 변환한다. 야구와 휴일은 공통 의미색을 유지하고 D-Day와 기념일은 프리셋별 조화색을 사용하며, 모두 사용자가 RGB 조정·색상 교환·내 설정 저장으로 변경할 수 있다.
- 추천 프리셋 적용 시 정의 배열뿐 아니라 현재 계정의 실제 로컬·Google 색상 카드까지 중복을 검사한다. Google 원본색 보존 모드를 제외하면 서로 다른 일정 출처가 같은 최종 표시색을 갖지 않게 한다.
- 추천색의 최종 기준은 `Docs/Design/COLOR_PALETTE_STANDARD.md`의 블랙 원색 배열이다. 두 스킨은 같은 다섯 프리셋과 원색 정체성을 공유하고 톤만 바꾼다. 새 조합을 임의 난수처럼 만들지 않는다.
- 작은 일정 글자의 가독성을 색의 화려함보다 우선한다. 발광에 가까운 네온색은 기본 팔레트에서 제외하고, 블랙 스킨 일정·설정·오른쪽 카드의 글자는 흰색으로 통일하며 배경을 필요한 만큼 어둡게 해 대비율을 확보한다.
- 사용자 카테고리색의 배경·글자·테두리는 `CategoryColorSystem`에서만 계산한다. 설정 미리보기와 실제 달력이 같은 모듈을 사용하고 모든 글자는 WCAG 대비율 4.5:1 이상을 만족해야 한다.
- 선택·미선택·중요한 날 같은 상태색은 `OnharuStateColors`에서만 계산하며 UI 파일에 상태 HEX 값을 직접 추가하지 않는다.
- 슬라이딩 버튼은 모든 항목의 좌우 내부 여백을 동일하게 유지하고 글자 수에 따라 최소 폭을 계산한다. 두 글자와 세 글자 항목을 억지로 같은 폭으로 만들지 않는다.
- `온하루 · ONHARU` 브랜드 글자와 메인 연·월 날짜 색상은 테마가 바뀌어도 공통 ONHARU 브랜드 그라데이션을 유지한다.
- 각 창의 대표 실행 버튼 하나는 `OnharuPopupChrome.BrandGradientBrush()`와 흰 글자를 사용한다. 창마다 다른 대표색을 만들지 않는다. 브랜드 그라데이션은 이 용도와 `온하루 · ONHARU` 글자, 연·월 날짜 제목, 오른쪽 상세의 Google 계정 카드에만 쓴다. 투명도 슬라이더 같은 조작 컨트롤에는 쓰지 않는다.
- 메인 헤더의 기능 아이콘 줄은 **오른쪽이 창 조작 버튼 왼쪽에 고정**이다. 아이콘을 하나씩 켤 때마다 왼쪽으로 늘어나고, **전부 켰을 때만** 맨 왼쪽 아이콘의 테두리가 기간 스위치의 왼쪽 테두리와 같은 선에 선다. 보이는 첫 아이콘을 기준으로 맞추지 않는다. 아래 줄의 스위치를 숨기면 기준선이 크게 밀리므로 보정은 아이콘 한 칸 폭까지만 허용하고 그보다 크면 고정 위치를 지킨다.
- 메인 헤더의 정확한 아이콘 크기, 버튼 높이, 기준선과 간격은 `Docs/Design/MAIN_HEADER_UI_STANDARD.md`를 단일 기준으로 사용한다.

## 팝업 규칙

- 새 창을 추가하거나 표시 방식을 바꾸기 전에 `Docs/Architecture/POPUP_POLICY.md`의 독립 도구·메인 차단·자식 차단·임시 팝업 분류를 따른다.
- 메인 차단 팝업은 반드시 `ShowBlockingDialog()`, WinForms 파일 창은 `ShowBlockingFileDialog()`를 사용한다.
- 시간표·일기장 읽기·KBO 경기 일정은 종류별 단일 인스턴스 독립 창으로 유지한다.
- 차단 팝업이 열려 있을 때 Explorer 고정 레이어의 특정 버튼만 예외적으로 작동하게 만들지 않는다.

## 검증과 기록

- 색상·프리셋·스킨 톤을 변경할 때는 `Docs/Design/COLOR_PALETTE_STANDARD.md`를 공식 기준으로 사용한다.
- 코드 변경 후 관련 자동 검사와 `check-v22.ps1` 품질 게이트를 실행한다.
- Explorer 레이어, 이동·고정, 입력, DPI처럼 자동화가 어려운 변경은 수동 시험 항목도 남긴다.
- 변경 파일과 이유, 검증 결과를 `HANDOFF.md`에 기록한다.
- 장기 제품 방향은 `TODO_NEXT.md`, 빌드·배포 방식은 `BUILD_AND_RELEASE.md`, 구조 설명은 `Docs/Architecture`와 일치시킨다.
- 시험 실행 파일은 `Tests`, 정식 배포 파일은 `Release`에만 둔다.
