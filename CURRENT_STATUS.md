# ONHARU 현재 상태

최종 갱신: 2026-09-03

## 버전과 배포

- **현재 공개 정식 버전: `2.2.5`.** 2026-09-03 GitHub Latest(`v2.2.5`)와 onharu.app에 게시했다.
- 직전 정식판은 `2.2.4`다. 2.2.4 이하 사용자는 앱의 자동 업데이트 안내로 올라온다.
- 개발 중 쓰던 `2.2.6-test` 번호는 배포되지 않았다. 2.2.5 시험판 설치자가 없어 사용자 결정으로 정식 번호를 2.2.5로 되돌렸다.
- onharu.app의 `ONHARU-2.2.5-Test-*`·`ONHARU-2.2.6-Test-*` 시험판 네 파일은 게시와 함께 서버에서 지웠다. 앞으로 웹 시험판은 2.2.6으로 다시 올린다.
- 릴리스 자산 SHA-256: 설치판 `36F59E0AA9647933E39C49CC205D9EE89C8B8774BF4940FE4B041E0F859228F4`, 포터블 `879A2BAE13475A41307AA26C0D6780DCA7064124DFEA6444BB00B7A80965215A`, 소스 `E386AE6B877D36992C0F237C3FC35C2F4FBE35304795301AE4E85756AA9D5FBD`.

## 소스 관리

- 프로젝트 루트는 로컬 Git `main` 브랜치로 관리한다.
- 이 로컬 저장소에는 공개 원격을 연결하지 않았다.
- 공개 GitHub 저장소는 `cowpigpapa/onharu`이며 정식 배포 때만 별도 공개 스테이지로 올린다. 현재 개발 이력의 기준은 이 로컬 저장소다.
- OAuth 로컬 자격 증명, 사용자 데이터, 실행·설치 파일, `Release`, `Publish`는 Git에서 제외한다.

## 2.2.5 검증 상태

- 추천 프리셋은 검증된 36개 색상 쌍을 유지하면서 로컬 역할을 `업무 블루·개인 로즈·야구 그린·D-Day 골드·기념일 바이올렛·휴일 레드`로 재배치했다.
- C# `2.2.5` EXE 빌드와 버전 메타데이터 검사: PASS(`FileVersion 2.2.5.0`, `ProductVersion 2.2.5`)
- 변경 항목 소스 검사(국경일 인덱스·완료 불투명도·더보기 상태색): PASS
- `check-v22.ps1` 자동 품질 게이트 **18개 전부 PASS**(2026-09-02 복구 완료). 검사 목록은 18개이며 이전 기록의 `17개`는 잘못된 수다.
  - 8월 말 디자인 작업 이후 게이트가 멈춰 있었다. 원인은 두 갈래였다. 게이트가 두 검사만 `pwsh`(PowerShell 7)로 호출하는데 개발 PC에 `pwsh`가 없었고, 개별 검사에 구현된 적 없는 토큰과 이미 의도적으로 바뀐 구조를 요구하는 단언이 남아 있었다.
  - 복구 기준은 `검사를 코드에 무조건 맞추지 않는다`이다. 각 단언을 `구현된 적 없음` / `의도적 변경` / `판단 보류`로 나눠 처리했고 근거를 검사 파일 주석과 `HANDOFF.md`에 남겼다.
  - 표준 시험본 `Tests\LocalTest\ONHARU-2.2-local-test.exe`를 최신 소스로 다시 만들었고 게이트도 이 파일로 통과했다. 검증용 임시 빌드는 정리했다.
- 메인 헤더 상단 아이콘 줄의 왼쪽이 기간 스위치 왼쪽과 어긋나던 문제를 고쳤다. 기준을 아이콘 획에서 **버튼 테두리**로 바꿨고 125% DPI 실측으로 확인했다. 실기기 최종 확인 대기.
- 아이콘 도형을 다시 그렸다. 설정 톱니는 실제 이빨 여섯을 가진 톱니바퀴, 알람은 둥근 종과 다리를 가진 알람시계, 시간표는 머리줄과 왼쪽 시간열을 가진 표다. 인쇄·정보도 `OnharuIcons` 벡터로 옮겼다. 이전에는 설정창·시간표·KBO의 톱니가 글꼴 기호로 떨어져 혼자 작고 얇았다.
- 설정창을 전체 검토해 정렬·역할색·순서를 정리했다. 라벨 열 폭 다섯 종류를 120 하나로, 다열 체크박스를 3열 균등 격자로 통일했고, 카드 순서를 매직 인덱스에서 배열 한 곳으로 옮겼다. 실기기 최종 확인 대기.
- 상세 스크롤바의 과거 저장 라벤더 오버라이드는 설정 버전 48 마이그레이션에서 제거되며, 파스텔은 중립 슬레이트 손잡이·트랙을 사용한다.

## 2.2.5 검증 기준선

- `check-v22.ps1` 자동 품질 게이트 17개: PASS
- 고정 상태 1~6주 선택: 실기기 PASS
- 실제 프린터 인쇄 완료: 실기기 PASS
- 좁은 화면과 125% DPI 상단 버튼: 실기기 PASS
- 혼합 DPI 듀얼 모니터 이동·고정: 실기기 PASS
- 설치 중 기존 `Onharu.LayerHost.exe` 자동 종료와 교체: 실기기 PASS

위 결과는 사용자가 2026-08-28 직접 확인한 현재 기준이다. 과거 TODO나 HANDOFF에 같은 항목이 미완료로 남아 있더라도 이 문서를 우선한다.

## 현재 핵심 기능

- Explorer의 바탕화면 아이콘 아래에 고정되는 월간·1~6주 달력
- 고정 상태 날짜·일정·스크롤·설정 입력과 아이콘 클릭·드래그
- 로컬 업무·개인 Todo, 반복·연속 일정, 완료·이월·알림
- Google Calendar 양방향 동기화와 선택형 Google Tasks
- D-Day·기념일, 일기장, 시간표, KBO 일정
- 검색, 인쇄, 백업·복원, JSON·ICS·CSV 가져오기·내보내기
- 파스텔·블랙 스킨, 사용자 색상과 접근성 대비
- 사용자 승인형 자동 업데이트와 설치·제거

## 2026-09-02 확정된 디자인 판단

사용자가 `현재 기준을 적용`으로 확정해 두 항목 모두 기준 문서 쪽으로 코드를 맞췄다.

- **투명도 슬라이더:** 인터페이스 강조색 `OnharuStateColors.ActionAccent`(파스텔 `#6D5CC6`, 블랙 `#7462CF`). 브랜드 그라데이션은 `온하루 · ONHARU` 글자와 연·월 제목 전용이다. 생성 시점과 테마 적용 시점 두 곳을 모두 맞췄고 `theme-check.ps1`이 둘 다 검사한다.
- **일정 저장 버튼:** `OnharuPopupChrome.BrandGradientBrush()`. 알람 `시작`, 시간표 `저장`·`적용`, KBO `선택 경기 등록`, 검색 `오늘`과 같은 브러시다. `feature-pack-check.ps1`이 다섯 창의 대표 실행 버튼을 한 번에 검사한다.
- `OnharuStateColors.OpacityControl`은 이 확정으로도 호출부가 없다. 인터페이스 강조색을 쓰기로 했으므로 리팩토링 단계 정리 대상으로 남는다.

## 다음 제품 과제

- 2.2.5 정식본의 색상·가독성 변경 실기기 확인
- Google OAuth 공개 검증
- Windows 코드 서명 또는 배포 신뢰 경로 확정
- 다수 사용자 장기 운영과 다양한 Windows 보안 환경의 피드백 축적
- 2.3 다국어 작업 전 저장 ID와 표시 문자열 분리

## 기준 문서

- 시작 규칙: `START_HERE.md`
- 개발 지침: `AGENTS.md`
- 최신 작업 이력: `HANDOFF.md`
- 다음 작업: `TODO_NEXT.md`
- 사용자 설명: `README.md`
- 빌드·배포: `BUILD_AND_RELEASE.md`
- 구조: `PROJECT_INDEX.md`, `Docs/Architecture`
