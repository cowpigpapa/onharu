# ONHARU TO-DO NEXT

## 2026-08-26 설정·중요한 날·알림 정리

### 완료

- [x] 메인 화면에 `×` 버튼이 없으므로 설정의 `× 버튼 동작` 항목과 저장 필드를 제거했다.
- [x] 상세 날짜 앞의 중요한 날 표시를 테두리 없는 5각 별 아이콘으로 변경했다. 별을 누르면 기존 색상 팔레트가 열린다.
- [x] 알림 설정을 하나의 카드로 통합하고 `알림 사용`, `소리 사용`, 조용한 시간, 알림 위치를 함께 배치했다.
- [x] `알림 사용`을 끄면 일정별 알림 설정은 보존하되 알림 팝업과 소리를 모두 발생시키지 않는다.
- [x] 설정의 `달력 표시 범위`와 `이번 주 위치`를 제거했다. 메인 상단의 주 버튼을 누르면 1~6주 목록이 펼쳐지고 즉시 변경된다.
- [x] 월 전체는 별도 선택 없이 항상 `자동 (4~6주)`로 표시한다.
- [x] 이번 주 위치는 1~2주 보기에서 첫 번째 행, 3~6주 보기에서 위에서 두 번째 행으로 자동 고정한다.

### 추후 확인

- [ ] 익명 활성 사용자·Google 연동 사용자 집계 실제 구현
- [ ] 팝업 테두리와 선택한 대표색 연계
- [ ] 주간·일간·일정 화면
- [ ] Windows 우클릭 `ONHARU 일정 등록`
- [ ] Free/Plus 7일 체험·리딤코드·결제 권한 구조
- [ ] 혼합 DPI 듀얼 모니터 실기기 시험은 2026-08-27 사용자 환경에서 진행한다.
- [ ] 별도 종료 버튼은 당장 추가하지 않는다. 최초 실행 시 한 번만 `ONHARU 로고를 누르면 숨기기와 종료 메뉴를 사용할 수 있습니다` 안내를 보여주는 방안을 검토한다.
- [ ] 상단에 도움말 아이콘을 추가하고, 주요 버튼·이동/고정·로고 메뉴·설정·Google 연결·백업 등 ONHARU 사용법을 자세히 확인할 수 있는 도움말 화면을 만든다. 좁은 화면의 헤더 공간과 웹 FAQ 연계 방식을 함께 검토한다.

### 제외

- 일정별 개별 색상은 추가하지 않는다. 카테고리 색상과 Special Day 기능으로 유지한다.

## 2026-08-24 멀티모니터·상단 UI 실기기 테스트

### 최우선 오류

1. [ ] 1920×1080 125% 모니터와 다른 모니터 사이에서 이동할 때 창이 왕복하며 실질적으로 이동되지 않는 현상을 재현·계측한다. DPI 변경 중 저장 좌표와 OS suggested RECT가 서로 되먹임되지 않도록 전환 기준을 하나로 통일한다.
2. [ ] 1920×1080 125%에서 상단의 `>`, `>>`, 날짜, 오늘 버튼이 잘리지 않도록 헤더 최소 폭과 반응형 축소 우선순위를 정의한다. 아이콘을 숨겨 문제를 가리지 말고 필수 조작부가 전부 보이는 최소 창 크기를 확보한다.
3. [ ] 블랙 스킨 오른쪽 일정 카드의 체크박스가 배경과 구분되도록 흰색 계열 외곽선·체크 표시를 적용한다.

### 상단 구조 재설계 후보

- [ ] 추가 기능 아이콘(시간표·일기·KBO 등)을 ONHARU 로고 위쪽 또는 별도 상단 행으로 옮겨 좁은 화면의 주 조작부와 분리한다.
- [ ] 투명도 슬라이더의 위치를 재검토하고 로고·기간·이동 버튼과 겹치지 않는 전용 영역을 정한다.
- [ ] `이동 가능/고정됨` 상태 문구 삭제 여부를 결정한다. 상태를 없앨 경우 이동·고정 토글 자체만으로 현재 상태를 명확히 표현해야 한다.
- [ ] Google 계정 카드를 설정 밖 메인 화면으로 옮길 위치와 동기화 상태 표시를 함께 설계한다.
- [ ] ONHARU 로고 클릭을 `오늘로 이동`으로 변경하고 기존 오늘 아이콘을 제거하는 안을 검토한다.
- [ ] 오늘 버튼을 투명도 조절 옆으로 옮기고 오른쪽 상세 영역에도 오늘 버튼을 추가하는 안은 중복 조작 여부를 확인한 뒤 확정한다.

### 날짜 이동 동작 후보

- [ ] 주 보기에서 좌우 이동 버튼 단일 클릭은 1주 이동, 더블클릭은 다음/이전 일정으로 이동하도록 구분하는 안을 검토한다.
- [ ] 월 전체 보기에서는 단일·더블클릭 모두 월 이동으로 동일하게 유지한다.
- [ ] 더블클릭 판정 지연 때문에 단일 클릭 반응이 느려지지 않도록 별도 버튼 또는 보조키 방식과 비교한다.

### Google 최초 연결 안내

- [ ] OAuth loopback의 `127.0.0.1 연결 거부`가 브라우저 복귀 시점·로컬 수신기 종료·시간초과 중 무엇인지 로그로 구분한다.
- [ ] 2026-08-24 새 Google 계정 시험에서는 5분 이내에 `127.0.0.1` 오류가 나타났으나 재시도 후 로그인과 동기화는 성공했다. 따라서 시간초과 하나로 단정하지 않고, 오류 발생 시점·중복 입력·listener 종료·보안 프로그램 개입을 다음 재현에서 함께 기록한다.
- [ ] 최초 Google 연결은 브라우저 동의와 초기 동기화에 시간이 걸릴 수 있음을 안내하고, 인증 대기 제한을 최대 5분 범위에서 검토한다.
- [ ] 단순히 5분을 기다리게 하지 말고 `브라우저 인증 대기 중 / 계정 확인 중 / 최초 일정 동기화 중` 단계 상태와 재시도 안내를 표시한다.

### 운영·웹 후속

- [ ] 활성 사용자 또는 Google 동기화 사용 인원을 집계하려면 개인정보를 최소화한 익명 설치 ID·집계 목적·동의·보존기간을 먼저 설계한다. Google 계정 이메일이나 일정 내용은 통계로 전송하지 않는다.
- [ ] 홈페이지 다운로드 버튼은 사용자가 말한 Notion 참고 디자인을 전달받은 뒤 웹 관리 작업에서 반영한다.
- [ ] 회사 Skyhigh Security 환경에서 ONHARU의 무서명 EXE와 EXE가 포함된 ZIP만 차단되는 사례를 확인했다. 모든 실행 파일이 차단되는 환경은 아니다. 코드 서명·Microsoft Store 배포·`onharu.app` 허용 목록 요청을 정상 해결 경로로 검토하며, 확장자 변경이나 암호화 압축으로 보안 정책을 우회하지 않는다.

원칙: 위 항목은 한 번에 헤더를 임의 재배치하지 않는다. 멀티모니터 이동 불능과 최소 크기를 먼저 해결하고, 상단 와이어프레임을 확정한 뒤 기능 버튼을 이동한다.

## 현재 기준과 다음 배포

- 공개 최신 버전: `2.2.2` — 스킨·색상·헤더·부가 팝업 품질 개선
- 다음 개발·배포 버전: `2.2.3`
- 공개한 버전 번호는 다시 사용하지 않는다. 내부 시험본은 `2.2.2-local-test`, 정식 공개는 `2.2.2`로 구분한다.
- 2026-08-24 이후 변경은 모두 2.2.2에 포함한다.

### 2.2.2에 반영 완료

- [x] 신규 설치 기본 보기를 4주, 이번 주 2번째 행, 월 전체 자동으로 확정했다.
- [x] 사용자가 직접 구성한 현재 설정을 신규 설치 기본 프로필로 반영했다. 이동 가능, 중앙 1120×700, 파스텔·오션블루, 보통 글자, 95% 투명도, 일요일 시작·ISO 주차, 음력·24절기, 선택일 테두리, 오늘 원형 아이콘, 완료 일정 흐리게, 5분 동기화, Todo 자동 이월을 사용한다.
- [x] Google 계정·캘린더 ID, 토큰, API 키, 개인 경로, 화면 절대 좌표는 배포 기본값에서 제외했다.
- [x] 기존 `%LOCALAPPDATA%\Onharu\settings.json`이 있으면 보기·위치·크기·스킨·글자·투명도 등 기존 설정을 그대로 보존한다.
- [x] 신규 기본 프로필 자동 회귀검사를 추가하고 로컬 빌드 PASS를 확인했다.

### 2.2.3 다음 작업

1. [ ] 1920×1080 125% 및 서로 다른 DPI의 다중 모니터에서 이동·고정 위치, 크기, 헤더 잘림을 실제 장비로 확인한다.
2. [ ] 위 장비 시험에서 확인된 DPI·배율 문제만 2.2.3 후보에서 보정한다.
3. [ ] 2.2.2에서 새 버전 감지·SHA-256 검증·사용자 승인 설치 전체 흐름을 확인한다.
4. [ ] Google OAuth 127.0.0.1 오류 재현 시 단계별 로그로 listener 종료·중복 입력·보안 프로그램 개입을 구분한다.

### 배포 이후 또는 시간을 두고 진행

- [x] Google Keep 공식 연동 가능성을 검토했다. 일반 개인 Gmail 앱용 API가 아니고 기존 메모 갱신도 지원하지 않아 직접 연동은 제외한다. ONHARU 일기·메모는 로컬 기능으로 유지한다.
- [ ] Google OAuth 공개 검증은 약 1주 실사용 안정화와 홈페이지·개인정보처리방침·서비스 약관·로고·시연 영상 확정 후 제출한다. Calendar 2개 범위와 선택형 Tasks 범위는 증분 동의로 분리한다.
- [ ] SES production access 승인 후 Google ID token 검증, 수신자 고정, 사용자별 발송 한도, bounce/complaint 감시, WAF·비용 경보까지 공개 사용자 운영 경계를 완성한다.
- [ ] 한국어·영어 다국어는 `2.3` 대표 작업으로 진행한다. 표시 문자열과 저장 ID를 먼저 분리하고 한국어 기본·영어 추가 순서로 구현한다.
- 보류: Parse.bot KBO 더블헤더·취소·시간 변경은 공급 API가 관련 필드를 제공할 때만 재개한다.
- [x] Free/Plus 제품 경계를 확정했다. 로컬·부가 기능은 Free, Google Calendar 연결과 양방향 동기화 전체는 Plus로 둔다. 선택형 Google Tasks는 당분간 Free 부가 연동으로 유지한다.
- 보류: 혼합 DPI 듀얼 모니터 실기기 검증은 2026-08-25 사용자 회사 장비 시험 후 필요할 때 재개한다.
- 별도 작업: `onharu.app` 홈페이지는 홈페이지 전용 작업에서 관리한다. 앱 정식 배포 직후 웹 다운로드 버전·SHA-256·변경사항도 갱신하도록 사용자에게 알린다.
- 조사 보고서: `Docs/Research/NIGHT_RESEARCH_KEEP_OAUTH_SES_PLUS_20260824.md`

## ONHARU 2.2 디자인 스킨

- [x] 2.1과 분리된 `Onharu_v2.2` 개발 작업공간을 만들었다.
- [x] `classic`, `dark`, `colorful` 3종 팔레트와 언어 독립 스킨 ID를 구현했다.
- [x] 메인 외곽, 달력, 일정 막대, 오른쪽 상세, 필터 체크박스에 스킨을 적용했다.
- [x] 설정 저장·마이그레이션·잘못된 값 fallback과 자동 검증을 추가했다.
- [ ] 실제 화면에서 세 스킨을 순차 전환해 색 대비, 일정 가독성, 고정·이동 전환을 사용자 확인한다.
- [ ] 메인 화면 승인 후 설정·일정·기념일·시간표·야구 등 공통 팝업을 같은 의미 기반 팔레트로 확장한다.
- [ ] 2.2 배포 직전 스킨별 스크린샷과 수동 회귀 체크리스트를 만든다.

원칙: 스킨 때문에 MainWindow나 Explorer 레이어를 복제하지 않는다. 기능·입력·레이어 코드는 공유하고 렌더링 의미 색상만 교체한다.

## 프로야구 경기 일정 — Parse.bot BYOK·KBO 달력 구현 및 실제 키 시험 완료 (2026-08-22)

- KBO 전체 일정·팀·점수 지원 여부를 확인해 API-SPORTS Baseball을 1차 공급자로 정했다.
- 서버 없이 가볍게 동작하는 원칙에 따라 사용자가 자신의 Parse.bot KBO Schedule API 키를 연결하는 선택형 BYOK 방식으로 교체했다.
- 설정을 `달력 내 표시`와 `상단 기능 아이콘`으로 나누고 `프로야구 일정`을 추가했다. 발급 안내, 연결 확인, DPAPI 암호화 저장, 변경·삭제 흐름을 구현했다.
- KBO 월간 팝업, 이전·오늘·다음 달, 새로고침, 경기 선택, 중복 방지, ONHARU `야구` 로컬 일정 등록과 오른쪽 필터를 구현했다.
- 실제 사용자 키 연결과 응답을 확인했고, 10개 구단 로고·최대 2개 응원팀·전체/응원팀 필터·4주/월간 보기·크기 선택·캐시·ONHARU 야구 일정 등록을 구현했다.
- 경기 데이터는 일반/Google/로컬 일정에 저장하지 않고 별도 Sports 모듈과 캐시에만 둔다. 기능 제거 시 메인 달력 데이터에 영향이 없어야 한다.
- 공급자 API 키는 소스·일반 설정·로그에 넣지 않고 사용자 Windows 계정에만 암호화 저장한다.

최종 갱신: 2026-08-22  
상태: 향후 제품 방향으로 확정  
공식 도메인: `onharu.net`

## 2026-08-22 요청 통합 정리

같은 내용이 반복된 항목은 하나로 합쳤다.

### 완료

- [x] 후보 03의 PMv2·WPF DPI 갱신·물리 최소 크기·`WM_DPICHANGED` RECT 복원을 공식 소스와 빌드·설치 경로에 이식했다. 빌드와 품질 게이트 13개 PASS.
- [x] 연·월 제목 클릭으로 이동창을 열고 `±1년`, `±10년`, 직접 연도 입력, 월 클릭을 함께 지원한다.
- [x] 기념일자는 `YYYYMMDD` 직접 입력과 달력 선택을 함께 제공하고 형식을 검증한다.
- [x] 기념일 저장·카드 클릭이 메인 달력을 등록 기준일로 이동시키지 않게 했다.
- [x] 고정 상태의 중요한 날 팔레트 입력을 복구하고 빠른 클릭도 단일 클릭으로 처리했다.
- [x] 백업 복원·일정 가져오기·로컬 JSON 내보내기·전체 Excel CSV 내보내기를 서로 다른 작업으로 분리했다.
- [x] 백업 복원은 기존 Google 일정 객체를 보존하고 로컬 일정만 교체한다. Google 서버 자료를 수정하거나 삭제하지 않는다.
- [x] 자동 백업은 일정 저장 시 계정별로 당일 파일을 갱신하고 최근 30일을 보관한다.
- [x] Google 동기화 상태를 MADE BY와 같은 하단 오른쪽에 표시한다.
- [x] 설정창이 열린 상태에서도 알림 확인 버튼을 누를 수 있도록 알림창을 별도 STA UI 스레드로 분리했다.
- [x] 새 일정·수정에서 하루 종일이면 시간 입력을 숨기고 오전·오후를 선택했을 때만 표시한다.
- [x] 이월 기능은 삭제하지 않고 설정의 `미완료 Todo 자동 이월`로 노출 여부를 선택하게 했다.
- [x] 오늘·선택일 모두 색상/테두리/색상+테두리/표시 안 함을 지원하며 두 테두리가 겹치지 않게 한다.
- [x] 야구 아이콘은 실제 API 연결 전 준비 중 진입점으로만 추가했다.
- [x] 완전 투명은 조작 불능 위험 때문에 제품 기능으로 채택하지 않고 최소 10%를 유지한다.

### 오늘 확인·마무리

- [x] 후보 03이 이식된 공식 로컬 테스트본에서 해상도 변경 후 고정→이동, 이동→고정, 재실행을 육안 확인했다.
- [ ] 설정창을 띄운 상태에서 실제 알림의 `확인/다시 알림` 버튼을 실기기로 확인한다.
- [ ] 백업 복원·가져오기·JSON 내보내기·Excel 내보내기 버튼이 각각 올바른 창만 여는지 최종 회귀 확인한다.
- [ ] 월 이동창의 직접 연도 입력, `±1/±10년`, 월 클릭 및 기념일 `YYYYMMDD` 입력을 한 번씩 확인한다.
- [ ] 오늘 변경분과 DPI 배포 파일을 포함한 최종 코드 감사를 마친 뒤 설치판 갱신 여부를 결정한다.

### 시간을 두고 진행

- [x] 신규 설치 달력 기본값을 `4주 보기 / 이번 주 2번째 행 / 월 전체 자동`으로 통일했다. 기존 `settings.json`이 있으면 보기·위치·크기를 덮어쓰지 않는다. 다음 패치 배포에서 신규 설치·덮어쓰기 양쪽을 회귀 확인한다.

- [x] 독립 `DiaryStore`·작성 팝업·날짜 더블클릭·상단 아이콘·검색 가능한 북리더를 구현했다. 일정/Google 데이터와 분리하고 목록/한 장 보기 2가지 읽기 모드를 제공한다.
- 보류: 서로 다른 DPI의 듀얼 모니터 실기기 검증은 현재 개발 대상에서 제외한다. 사용자가 환경을 확보해 재요청할 때만 진행한다.
- [x] Rainlendar 2.24 계열 기능과 ONHARU를 비교하고 ICS·빠른 등록·템플릿·모니터 대응을 우선 후보로 정리했다. 상세 보고서는 `Docs/Audits/RAINLENDAR_DATA_EXCHANGE_GOOGLE_TASKS_REVIEW_20260822.md`에 있다.
- [x] 기존 제품 소개 HTML과 왕복 연결되는 검색·분류형 FAQ 초안을 `Distribution/ONHARU-FAQ.html`로 만들었다. 다운로드·개인정보처리방침·실제 onharu.net 배포는 계속 진행한다.
- [x] Google Calendar 일반 일정과 Google Tasks를 분리했다. 일반 일정 완료는 로컬에만 저장하고, 날짜가 있는 실제 Task와 완료 상태는 별도 `GoogleTasksService`에서 동기화한다.
- [x] Google Cloud에서 Tasks API와 OAuth 범위를 활성화하고 실제 계정의 Task 목록·완료 상태 수신을 확인했다.
- [x] `Google Tasks 표시·동기화`를 명시적 선택 기능으로 분리하고 활성화 경고, 오른쪽 필터, 수정 권한, ONHARU Task 등록·수정·삭제 경계를 추가했다.
- [x] ONHARU에서 만든 Google Task의 생성→수정→완료→삭제 왕복을 실제 계정으로 최종 확인했다.
- [x] Google Keep 공식 연동을 검토하고 제외했다. 개인 Gmail 공개 앱에 맞지 않는 Workspace 관리자용 API이며 정상적인 메모 양방향 갱신도 지원하지 않는다.
- [x] 스포츠 API provider를 독립 모듈로 만들고 Parse.bot BYOK 방식의 KBO 일정·팀 선택·캐시·로컬 등록을 구현했다.
- [x] ICS 가져오기·내보내기와 사전 비교 화면을 구현했다. JSON은 복원, ICS는 달력 교환, CSV는 Excel 보고서 역할을 유지한다.
- 제외: 메일 날짜 우클릭 일정 등록은 현실성과 제품 범위가 맞지 않아 로드맵에서 제거했다.
- [ ] Android·iOS는 Windows Explorer 레이어를 재사용하지 않고 공통 데이터 규격과 Google 동기화 서비스만 공유하는 별도 앱으로 설계한다.
- [ ] 다국어, 자동 업데이트, 공개 Google OAuth, 향후 Plus 권한 경계를 기존 장기 로드맵 순서대로 진행한다.
- [ ] 다른 날짜 입력 UI도 필요할 때마다 `직접 입력 + 달력 선택 + 검증` 패턴으로 통일한다.
- [ ] 제품 소개 문구는 `공유가 필요한 일정은 Google로, 개인 로컬 일정은 내 컴퓨터에 저장하는 바탕화면 달력`을 기준으로 다듬는다.

## 2026-08-22 데이터 관리 확인

- [ ] 해상도 변경 뒤 고정·이동 전환 시 현재 보이는 위치·크기를 그대로 유지한다. 후보 03은 자동 계측을 통과했으며 사용자 화면 확인 뒤에만 완료 처리한다.
- [x] 작은 해상도→큰 해상도와 큰 해상도→작은 해상도에서 고정→이동 및 이동→고정을 각각 독립 계측했다.
- [x] 앱 시작 전 PerMonitorV2 선언과 process/thread/window DPI awareness를 계측하고 단일 physical RECT 후보를 검증했다.
- [x] 투명 WPF DPI 재인식 후보는 실기기에서 모드 간 차이가 남아 실패로 판정하고 제거했다.
- [ ] XDiary 방식과 같이 현재 고정 프레임을 이동 편집의 유일한 기준으로 삼는 이동 전용 overlay 후보를 확인한다.
- [ ] 후보 03에서 이동 상태 해상도 변경→고정 시 실제 화면의 네 모서리와 글씨 크기가 그대로인지 사용자 확인한다.
- [x] 후보 02A에서 125%의 HWND=PMv2/120 DPI, WPF=96 DPI를 확인했다.
- [x] 후보 02B에 WPF DPI switch를 추가해 HWND/WPF=120/120을 확인하고, 150%에서 고정 DIP 최소 크기가 물리 1230×840px로 커지는 추가 원인을 확인했다.
- [x] 후보 03에서 물리 최소 크기와 `WM_DPICHANGED` 전 물리 RECT 복원을 적용하고 네이티브 경로 1,000회 전환을 완료했다. 1,434회 좌표 검사에서 DPI·HWND·DWM 불일치와 예외는 0건이다.
- [x] 후보 03 사용자 화면 확인 후 승인된 manifest/config/물리 최소 크기/RECT 복원을 활성 `App`에 최소 이식했다.
- [x] 삭제 전 안전 백업 파일을 정상 날짜·시간과 용도로 표시하도록 백업 파일명 파서를 보완했다.
- [x] 일정 관리 5개 버튼의 글자 크기와 1px 내부 여백을 통일했다.
- [x] 모든 성공 안내가 `일정 가져오기`로 보이던 공통 팝업 제목 고정 버그를 제거했다.
- [x] 일정 관리 5개 작업을 같은 줄에 배치하고 삭제창의 버튼 잘림·스크롤·카드 디자인을 보완했다.
- [x] 가져오기/로컬 JSON 내보내기/전체 Excel CSV 내보내기를 단일 enum 분기로 분리하고 자동 검사로 고정했다.
- [x] 데이터 파일 대화상자가 마지막 성공 폴더를 기억하도록 했다.
- [x] 로컬 일정·기념일·Google D-Day 로컬 설정을 선별 관리하는 안전 삭제 팝업을 추가했다.
- [x] 기념일을 100년치 실데이터 대신 기준 레코드 1개+표시 시 계산 방식으로 전환했다.
- [ ] 실기기에서 네 데이터 버튼이 각각 백업 복원/가져오기/JSON 저장/CSV 저장만 여는지 확인한다.
- [ ] 선택 삭제에서 로컬 일정, 반복 시리즈, 기념일, Google D-Day 설정을 각각 시험하고 Google 원본이 유지되는지 확인한다.

## 1. 장기 목표

ONHARU를 개인 PC용 실험 프로그램이 아니라 누구나 내려받아 사용할 수 있는 Windows 데스크톱 달력으로 공개한다.

### 2026-08-23 공개 배포 진행 순서

1. 로컬 일정 메일 발송 서버의 production access 승인과 공개 사용자 인증 설계
2. Google OAuth 공개 검증 제출 (브랜딩 저장·프로덕션 게시 완료, 인증 센터 제출 남음)
3. [완료] 사용자 승인형 자동 업데이트 구현 및 GitHub v2.2.0 Release 경로 검증
4. 한국어·영어 다국어

배포 현황: GitHub 소스와 `v2.2.0` 정식 Release는 게시 완료했다. 실제 `onharu.app`은 아직 2.1이며 `Publish/ONHARU-Web-Publish-2.2.0.zip`을 서버에 게시해야 한다. Google 동기화는 현재 외부·프로덕션 앱이므로 Google 계정으로 접근할 수 있지만, 민감 범위 공개 검증 전에는 미검증 경고와 프로젝트 수명 전체 100명 신규 사용자 상한 때문에 `누구나 안정적으로 사용 가능`한 상태로 판정하지 않는다.

### 2.2 다음 수정 후보

- [x] 2.1 배포 EXE의 GitHub 업데이트 확인 실패를 동일 .NET Framework 조건에서 재현했다. 기본 `Ssl3, Tls`로는 GitHub가 TLS 채널 생성을 거부했으며 `Tls12`를 명시하자 HTTP 200을 확인했다. 2.2 `UpdateService`와 자동검사에 TLS 1.2를 고정했다. 기존 2.1 설치본은 자체 업데이트 코드가 이미 실패하므로 2.2로 최초 한 번은 수동 업데이트가 필요하다.
- [ ] Google 동기화 실패 원인을 인증 오류와 일시적 통신 오류로 구분한다.
  - `invalid_grant`, 토큰 취소·만료, 인증 관련 401/403이면 기존 로컬 일정은 유지하고 `Google 연결 인증이 만료되었거나 취소되었습니다. 다시 로그인해 주세요.` 안내와 재로그인 동작을 제공한다.
  - 인터넷 단절, DNS, 타임아웃, Google 서버 5xx이면 재로그인을 요구하지 않고 `네트워크 연결을 확인한 뒤 다시 시도해 주세요.`로 안내한다.
  - 원문 토큰·응답 본문·개인정보는 화면과 로그에 노출하지 않고 진단용 오류 종류만 기록한다.
- [ ] 앱 시작 시 Google 동기화 완료를 기다린 뒤 업데이트를 확인하는 순서를 제거하고, 자동 업데이트 확인과 Google 동기화를 서로 독립적으로 실행한다.
- [ ] `Loaded` 시작 절차를 단계별 안전 실행으로 분리한다. Google 초기 동기화가 예외로 끝나도 자동 업데이트, 자동 동기화 타이머, 알림, 트레이 및 나머지 초기화가 계속 실행되어야 한다.
- [ ] 실행 중 재로그인으로 Google 연결이 복구되면 동기화만 성공하고 중단됐던 시작 절차는 재개되지 않는 회귀를 추가한다. 재로그인 여부와 관계없이 업데이트 확인은 앱 시작 시 독립적으로 완료되어야 한다.

메일 발송은 Amazon SES 서울 리전과 `ONHARU <noreply@onharu.app>` 발신자로 확정했다. Lambda/API Gateway 중계 API와 실제 JSON 첨부 시험은 완료했다. 다음은 SES production access 승인과 공개 사용자가 임의 수신 주소를 악용하지 못하도록 하는 인증 경계를 확정하는 일이다.

홈페이지 소스와 배포는 별도 작업에서 관리한다. 이 프로젝트에서는 앱·서버 계약·OAuth 제출 자료만 관리한다.

- 기본 로컬 기능은 완성도 높은 프리웨어로 제공한다.
- 한국어·영어를 시작으로 다국어를 지원한다.
- 앱에서 새 버전을 확인하고 사용자의 승인을 받아 안전하게 업데이트한다.
- `onharu.net`을 공식 소개, 다운로드, 도움말, 개인정보처리방침, 업데이트 안내의 기준 주소로 사용한다.
- 향후 Google Calendar 연동은 ONHARU Plus 유료 기능으로 전환할 수 있게 경계를 준비한다.
- 기능·디자인 개선은 계속하되 핵심 레이어, 다국어, 업데이트, 라이선스 코드가 서로 얽히지 않게 유지한다.
- 고정 레이어는 `PREPAINT 최초 직접 합성 + 완성 프레임 캐시` 안정본을 유지한다. 아이콘 이동 깜빡임과 회색 배경이 실기기에서 해결된 상태이며 이전 배경 캐시 방식으로 되돌리지 않는다.

## 2. 확정된 제품 범위

### ONHARU Free

- 바탕화면 아이콘 아래 달력
- 로컬 일정, Todo, 반복·여러 날 일정
- D-Day와 기념일
- 시간표
- 검색, 백업·복원, 가져오기·내보내기, 인쇄
- 표시 범위, 쉬는 날, 시작 요일, 색상과 디자인 설정

### 향후 ONHARU Plus

- Google Calendar 계정 연결
- 다중 Google 캘린더
- 양방향·자동 동기화
- 미동기화 일정 관리와 재시도
- 향후 여러 PC 설정 동기화 검토

유료 기능이 만료되더라도 로컬 일정과 사용자 데이터는 삭제하지 않는다. Google 동기화만 중단하고 읽을 수 있는 상태와 명확한 안내를 제공한다.

### 범위에서 제외

- 독립 스티커 메모는 ONHARU 제품 목표와 무관하므로 xDiarys 비교와 개발 우선순위에서 제외한다.
- Windows 배경화면을 교체하는 벽지 기능은 현재 핵심 철학과 충돌하므로 우선순위가 낮다.

## 3. P0 — 현재 완성도 유지

### 2026-08-22 실기기 승인 완료

- [x] 업무·개인 필터를 반복 변경해도 사용자 투명도가 유지된다.
- [x] 백업 날짜 클릭은 선택만 하며 하단 확인 버튼에서만 복원한다.
- [x] 일반 화면에서 새 일정·수정창이 스크롤 없이 열린다.
- [x] 제목과 숫자 입력이 둥근 입력칸 중앙에 표시되고 잘리지 않는다.
- [x] 일반 D-Day와 기념일 당일 카드의 색상·테두리·Bold 강조를 확인했다.
- [x] 고정 상태의 중요한 날, 일정 등록·수정, 필터 체크박스 입력을 확인했다.
- [x] 수동 Google 동기화 상태가 MADE BY와 같은 하단 오른쪽에 표시된다.

### 계속 유지할 회귀 기준

- [ ] 공식 레이어 전환 후보 17을 기준선으로 유지한다.
- [ ] 전환 실험은 `Tests/LocalTest/TransitionCandidates`의 별도 시험본에서만 수행한다.
- [ ] 디자인·기능 변경마다 이동/고정, 아이콘 클릭·드래그, 바탕화면 범위 선택을 회귀 검사한다.
- [ ] Explorer 재시작, 절전 복귀, DPI·해상도·모니터 변경 수동 검사를 누적한다.
- [ ] 디자인에서는 좌우 끝선, 세로 중앙, 동일 역할 버튼의 크기·간격을 우선 검토한다.
- [x] 혼합 DPI 이동 시 현재 DPI를 모든 화면에 재사용하지 않도록 물리 픽셀 위치·크기와 모니터 장치명을 함께 저장·복원한다.
- 보류: 100/125/150% 실제 모니터 조합 시험은 사용자 환경이 준비된 뒤 재개한다.
- [x] 후보 01 실패 원인을 후보 02A/02B 계측으로 분리했다: DPI 가상화, WPF DPI 지연, 고정 DIP 최소 크기의 물리 확대가 함께 작용했다.
- [x] 네이티브 action sink 1,000회 토글과 독립 `GetWindowRect`/DWM extended frame 계측을 완료했다. 자동 불일치 0건이며 사용자 화면 판정만 남았다.
- [x] 앱 시작 전 PerMonitorV2 선언과 thread/window DPI awareness context를 후보에서 확정했다.
- [x] 고정·이동이 공유하는 단일 canonical physical RECT 후보를 구현했다. 정식 이식은 사용자 승인 후 진행한다.
- [ ] XDiary처럼 기능 입력이 없는 이동 전용 overlay/snapshot 창으로 분리할지 외부 검토 후 결정한다.
- [ ] 150% 노트북 및 서로 다른 DPI의 듀얼 모니터에서 후보 03의 전환·재시작 위치를 사용자 실기기로 확인한다.
- [x] 오늘·선택일 표시 모드를 통합하고 테두리 중복 우선순위를 적용한다.
- [x] 완전 투명과 트레이 복구 명령을 시험한 뒤, 조작성 인지가 어려워 실사용 최소값을 10%로 확정한다. alpha 0은 레이어 전환 실험 후보로만 보존한다.
- [x] 오른쪽 하단에 Google 동기화 상태를 조용하게 표시한다.
- [x] 이월 기능은 데이터 호환을 유지한 채 설정으로 등록·수정 UI 노출을 제어한다.

### 다음 배포 전 작업

- [ ] 약 1주 동안 사용자 실사용 기능 시험을 계속하고 발견 사항을 회귀 목록에 누적한다.
- [ ] 2.1 최종 소스 감사와 불필요한 시험 산출물 정리를 수행한다.
- [ ] 설치판 빌드 후 신규 설치·2.0 덮어쓰기·제거·재설치를 확인한다.
- [ ] GitHub ONHARU 2.1 Release의 설치 파일·ZIP·SHA-256·설명서를 갱신한다.
- [ ] 100/125/150% 혼합 DPI 환경이 준비되면 모니터 이동→고정→종료→재실행을 확인한다.
- [ ] 로컬 일정 일괄 관리는 별도 목록에서 선택 삭제를 우선 검토한다. `전체 삭제`는 Google과 완전히 분리하고 백업 생성·2단계 확인을 갖춘 위험 구역 기능으로만 제공할지 결정한다.

## 3-1. P1 — 사용자 기능 확장

- [x] 로컬 일기장, 편집기, 북리더, 검색을 독립 파일로 구현했다.
- [x] 빈 날짜 더블클릭은 일기 쓰기, 일정 막대 더블클릭은 기존 일정 수정으로 분리했다. 새 일정은 오른쪽 `이 날짜에 추가` 동작을 유지한다.
- [x] ICS 가져오기·내보내기와 사전 비교 화면을 구현했다.
- [x] Google Calendar 일정과 Google Tasks API를 분리하고 선택형 표시·동기화 경계를 구현했다.
- [x] 독립 Sports provider와 KBO BYOK 기능을 구현했다. 축구 API는 후속 범위로 남긴다.
- [ ] FAQ 검색 페이지를 `onharu.net` 공개 범위에 포함한다.

## 4. P1 — 한국어·영어 다국어

### 설치 프로그램

- [x] Inno Setup에 한국어와 영어 설치 언어를 등록했다.
- [ ] 설치 언어 선택, 바로가기, 자동 실행, 제거 문구를 두 언어로 실기기 확인한다.

### ONHARU 앱

- [ ] `App/Localization`을 만들고 `ko-KR`, `en-US` 리소스를 분리한다.
- [ ] 설정에 `시스템 언어 / 한국어 / English`를 추가한다.
- [ ] 버튼·팝업·툴팁·알림·오류·트레이 메뉴의 직접 작성 문자열을 리소스 키로 교체한다.
- [ ] `업무일정`, `개인일정`, `기념일` 같은 저장 식별자를 `work`, `personal`, `anniversary` 같은 언어 독립 ID로 분리한다.
- [ ] 기존 한국어 데이터의 ID 마이그레이션과 되돌리기용 백업을 제공한다.
- [ ] 한국어·영어에서 잘림, 줄바꿈, 버튼 폭, 정렬을 각각 화면 검증한다.

언어 작업은 단순 번역이 아니라 데이터 ID와 표시 문자열을 분리하는 구조 작업부터 시작한다.

## 5. P2 — 자동 업데이트

- [x] GitHub Release의 최신 정식 릴리스를 업데이트 원본으로 사용한다.
- [ ] manifest에 버전, 설치 파일 URL, SHA-256, 최소 지원 버전, 변경사항 URL을 기록한다.
- [x] 앱 실행 후 UI를 막지 않고 지연된 백그라운드 확인을 수행한다.
- [x] 하루 한 번 확인하고 설정에서 자동 확인을 끌 수 있다.
- [x] 새 버전이 있으면 사용자 승인형 ONHARU 팝업을 표시한다.
- [x] 임시 폴더 다운로드와 Release SHA-256 검증 후 설치 프로그램을 실행한다.
- [x] 현재 Inno Setup `AppId`를 유지해 기존 설치를 덮어쓰고 데이터와 Google 토큰을 보존한다.
- [x] 다운로드 실패, 해시 불일치, 설치 취소, 오프라인 오류를 차단·안내한다.
- [ ] 설정의 수동 `업데이트 확인` 버튼과 설치 시작 직전 자동 안전 백업을 추가할지 최종 UX를 확인한다.
- [ ] 코드 서명 도입 전에는 미서명 경고 가능성과 SHA-256 확인 방법을 다운로드 페이지에 알린다.

자동 업데이트는 사용자의 명시적 승인 없이 강제로 설치하지 않는다.

## 6. P3 — `onharu.net` 공개 배포 기반

- [ ] 제품 소개와 주요 기능 페이지
- [ ] Windows 설치 파일 다운로드와 현재 버전 표시
- [ ] 릴리스 노트와 SHA-256
- [ ] 사용자 설명서와 자주 묻는 질문
- [ ] 개인정보처리방침
- [ ] Google Calendar 데이터의 읽기·쓰기·저장 범위 설명
- [ ] 이용약관과 지원 이메일
- [ ] 제거 방법과 데이터 보존·삭제 방법
- [ ] GitHub Release와 공식 사이트 다운로드 링크의 버전 일치 자동 확인
- [x] Google OAuth 동의 화면의 홈페이지·개인정보처리방침·승인 도메인을 `onharu.app` 기준으로 연결
- [x] 외부 앱을 프로덕션 단계로 게시
- [x] ONHARU 120×120 앱 로고를 OAuth 브랜딩에 등록
- [ ] Google 인증 센터에서 공개 OAuth 검증 제출 및 승인 완료
- 보류: 공개 OAuth 검증 제출은 사용자 실사용 안정화가 끝난 뒤 진행한다.

## 7. P4 — Google 유료 기능 경계

결제부터 만들지 않고 Google 기능을 하나의 권한 경계로 먼저 분리한다.

- [ ] `IFeatureEntitlement` 또는 동등한 독립 서비스로 `GoogleSync` 권한을 조회한다.
- [ ] UI에서 Google 버튼 표시, 연결, 수동·자동 동기화를 같은 권한 검사로 통일한다.
- [ ] 개발·현재 배포에서는 권한 제공자를 `모두 허용`으로 두어 기존 기능을 보존한다.
- [ ] 향후 라이선스 서버는 사용자에게 Plus 권한이 있는지만 확인한다.
- [ ] Google OAuth 토큰과 일정 내용은 계속 사용자 PC에 저장하고 라이선스 서버에 보내지 않는다.
- [ ] 오프라인 유예 기간을 두고 서버 일시 장애가 기존 사용자 일정을 막지 않게 한다.
- [ ] 구독 만료·환불·계정 변경 시 로컬 데이터 보존 정책을 명시한다.
- [ ] 결제 제공자와 가격은 일반 공개 배포와 Google 검증이 안정된 후 결정한다.

로컬 설정 파일의 `Paid=true` 같은 값만으로 유료 권한을 판정하지 않는다.

## 7-1. P3 — PC 간 로컬 일정 전달

`onharu.net`의 메일 발송 기반이 준비된 뒤 기존 파일 내보내기를 확장한다.

- [ ] 내보내기에서 `내 PC에 저장`과 `내 이메일로 보내기`를 제공한다.
- [x] JSON/ICS 메일에는 계정별 로컬 일정·기념일·D-Day만 포함한다. CSV 메일은 사용자 선택에 따라 Google 포함 전체 일정을 제공하고 이를 발송 전에 안내한다.
- [ ] 이메일 주소를 확인하고 전송 전 포함 항목 수와 데이터 범위를 보여준다.
- [ ] 다른 PC에서는 기존 `일정 가져오기` 목록으로 파일 내용을 검토하고 선택해서 가져온다.
- [ ] 기존 일정과 ID·내용을 비교해 `신규`와 `변경된 일정`을 구분한다.
- [ ] 메일 서버에는 일정 파일을 영구 저장하지 않고 발송 완료 후 즉시 폐기한다.
- [ ] 전송 구간 암호화와 짧은 만료 시간의 일회성 다운로드 링크 방식을 우선 검토한다.
- [ ] 발송 횟수 제한, 실패 재시도, 잘못된 주소 안내와 개인정보처리방침을 마련한다.

초기 버전은 사용자가 직접 첨부 파일을 보관하고 가져오는 현재 JSON 방식을 유지한다. 서버 메일 기능은 PC 간 이동을 편리하게 만드는 선택 기능이며 계정별 로컬 저장 원칙을 바꾸지 않는다.

## 8. 코드 분리 원칙

새 기능은 `MainWindow.cs`에 다시 몰아넣지 않는다.

```text
App/Localization        언어 리소스와 번역 조회
App/Updates             버전 확인·다운로드·검증·설치 인계
App/Licensing           Free/Plus 권한과 향후 서버 연결
App/Google              기존 Google 서비스와 권한 연결 경계
App/MainWindow.*.cs     화면 조정만, 네트워크·결제 구현 금지
Installer               설치·업그레이드·설치 언어
Website                 향후 onharu.net 공개 자료
```

- UI, 데이터, 서비스, 네이티브 Explorer 레이어의 책임을 섞지 않는다.
- 새 설정에는 버전과 마이그레이션을 함께 둔다.
- 기능마다 독립 자동 검사를 추가하고 기존 13개 품질 게이트를 유지한다.
- 실험 코드와 정식 코드를 분리하고 승인된 기준선만 설치판에 포함한다.
- 디자인 공통 요소는 `OnharuPopupChrome`처럼 재사용 가능한 공통 스타일로 관리한다.

## 9. 권장 실행 순서

1. 현재 2.1 안정성 유지와 디자인·기능 보완
2. 언어 독립 데이터 ID 도입
3. 한국어·영어 앱 리소스와 설치 언어 적용
4. `onharu.net` 소개·다운로드·개인정보처리방침 공개
5. GitHub Release 기반 업데이트 알림·설치 구현
6. Google OAuth 일반 공개 검증
7. Google 권한 경계 도입하되 기존 사용자는 계속 허용
8. 충분한 무료 공개 운영 후 ONHARU Plus 결제·라이선스 연결

## 10. 완료 기준

- 신규 사용자가 `onharu.net`에서 설치 파일과 설명서를 찾을 수 있다.
- 한국어·영어 Windows에서 설치와 앱 전체 UI가 자연스럽고 잘리지 않는다.
- 새 버전이 나오면 ONHARU 안에서 알림을 받고 승인 후 업데이트할 수 있다.
- 기존 일정, 설정, 시간표, Google 토큰이 업데이트 후 보존된다.
- 일반 사용자가 Google OAuth 테스트 사용자 등록 없이 연결할 수 있다.
- Google 기능을 유료로 전환하더라도 Free 기능과 로컬 데이터는 영향을 받지 않는다.
- 디자인·기능 보완이 계속되어도 Localization, Updates, Licensing, ExplorerLayer가 독립적으로 유지된다.

## 11. ONHARU 2.2 디자인 스킨

기능과 데이터 구조는 2.1을 그대로 유지하고 외형만 선택 가능하게 한다.

- [ ] `기본`: 현재 ONHARU 파스텔 디자인
- [ ] `블랙`: 어두운 배경, 충분한 명암비, 일정 색상 식별 유지
- [ ] `컬러`: 홈페이지 시안처럼 컬러 체크박스와 일정 배경보다 진한 동계열 글자 사용
- [ ] 화면별 색상 하드코딩을 의미별 공통 팔레트(창, 카드, 선, 본문, 보조 글자, 선택, 일정)로 통합
- [ ] 한 설정값으로 메인 달력·사이드바·팝업이 함께 전환되고 기능 코드와 렌더링 레이어는 복제하지 않음
- [ ] 세 테마 모두 이동/고정, 투명도, Explorer 아이콘 표시 회귀 시험

2.1 안정화 중에는 대규모 색상 치환을 하지 않고, 2.2 디자인 전용 작업으로 진행한다.
# KBO 후속 확인

- [x] 월별 로컬 캐시 및 캐시 월 즉시 이동
- [x] 새로고침 시 추가·변경·취소 경기 비교
- [x] 하루 최소 5경기 표시, 5/6주 유동 레이아웃, 오늘 강조
- [x] ONHARU 자체 팀 색상 배지와 응원팀 경기 일괄 선택
- [x] 조회·새로고침 결과 자동 월별 저장 (`시즌 저장` 별도 버튼 제거)
- [ ] 실사용 데이터에서 더블헤더·우천 취소·시간 변경 비교 결과 확인
- [ ] 이미 ONHARU에 등록한 경기의 시간/구장 변경을 사용자가 승인해 갱신하는 기능 검토
- [ ] Parse.bot에 KBO 결과·점수·취소 상태 endpoint/field 확장 요청 후 실데이터 검증
- [ ] 일반 공개 전 10개 구단 로고 상표 사용 지침과 출처 검토
- [진행 중] `noreply@onharu.app` SES 중계 API와 앱의 JSON/ICS/CSV 메일 발송 UI를 완료했다. 고정 allowlist, 속도 제한, 1MB 제한 및 `juan.hjlee@gmail.com` 실제 JSON·CSV 첨부 발송을 확인했다. SES production access 승인, 공개 사용자 인증 경계, 두 번째 실제 메일함 시험이 남았다.
- [완료] 앱의 백업 수신 주소를 현재 연결된 Google 계정으로 자동 고정했다. 다른 이메일 직접 입력은 제거했고 로그아웃 상태에서는 사용할 수 없다.

## 12. 2026-08-25 다른 PC 시험 결과 — 2.2.3 범위

### P0 — 반드시 먼저 해결

- [x] 혼합 DPI·해상도 모니터 사이에서 이동 창이 양쪽으로 왕복하며 실제 이동이 불가능한 문제의 재진입 원인을 수정했다. 실제 혼합 DPI 장비 재시험은 필요하다.
  - 같은 해상도·같은 배율 사이 이동은 정상이다.
  - 같은 해상도 100% → 125% 이동은 실패한다.
  - 낮은 해상도 → 높은 해상도 이동도 실패한다.
  - 모니터 경계에서 위치 보정과 `WM_DPICHANGED` suggested RECT가 서로 재진입하는지 우선 계측한다.
  - 드래그 중에는 위치 저장·화면 안쪽 보정·DPI restore 중 하나만 좌표 권위를 갖게 하고 나머지는 드래그 종료 후 한 번만 실행한다.
- [x] 고정 상태에서 상세 패널 접기 `>`가 실제 UI 토글 경로를 타도록 수정했다.
- [x] 최소 논리 폭 720을 확보하고 접기 버튼 폭과 브랜드 간격을 조정했다.
- [x] 2.1 형식 실제 백업 4개와 자동 호환 fixture를 현재 가져오기 코드로 검증했다.

### P1 — 2.2.3 UI 안정화

- [x] 로고/브랜드 간격을 줄이고 `< 오늘 >` 묶음을 왼쪽으로 이동했다.
- [x] 접기 `>` 폭을 날짜 이동 버튼과 같은 20px로 맞췄다.
- [x] 상단 슬라이딩 버튼을 26/22px 체계로 한 단계 줄였다.
- [x] Google 계정·G 설정·기간 탭·추가 버튼·슬라이딩 버튼 높이를 27px 안팎으로 통일했다.
- [x] Google 캘린더 목록을 항상 2열과 중앙 구분선으로 구성했다.
- [x] Special Day 카드의 내부 여백을 일반 상세 일정 카드와 맞췄다.
- [ ] 팝업 테두리·상태색은 현재 대표색과의 연계를 검토하되 가독성 우선 원칙을 유지한다.
- [x] 일기는 날짜 숫자와 일기 표시점에서만 열고 음력 날짜·일정·빈 공간은 제외했다.
- [x] 중요 일정에 배경색·글자색 팔레트를 추가하고 달력·상세·Google 비공개 속성에 보존했다.
- [x] 오른쪽 상세 일정에 독립 `카테고리/시간순` 토글을 추가했다.
- [x] 카테고리 순서 이동 시 스크롤을 보정해 선택 행의 화면 위치를 유지했다.
- [x] 야구 기능이 꺼져 있으면 등록·수정과 온하루 등록 필터에서 야구를 숨긴다.

### 확인 후 결정

- [x] 알림 위치를 `사용 화면 중앙` 또는 `온하루 위`로 설정에서 선택하도록 추가했다.
- [ ] 활성 사용자·Google 동기화 사용자 수는 개인정보 내용 없이 익명 설치 ID, 명시적 안내, 최소 보존기간으로 설계한다. 2.2.3에는 넣지 않는다.
- [x] 색상 접근성 원칙과 팝업 대표색 적용 경계를 `Docs/Audits/COLOR_ACCESSIBILITY_AUDIT_2.2.3.md`에 정리했다.

### 후속 버전 기능

- [ ] 주간·일간·일정 탭을 검토한다. 주간 탭에는 시간표, 주간 스터디 계획, 학사일정을 함께 보여주는 구성을 우선 시안으로 만든다.
- [ ] 카테고리 색 외에 일정별 색상 재정의를 선택 기능으로 검토한다.
- [ ] 로고 메뉴만으로 숨기기·종료를 제공하면 발견성이 낮으므로 별도 창 동작 버튼 또는 최초 안내를 검토한다.
- [ ] Windows Explorer 우클릭 메뉴의 `ONHARU 일정 등록`은 시스템 셸 확장 범위가 크므로 2.2.3에서 제외하고 별도 기술 검토 후 결정한다.
- [ ] Free/Plus 시험은 한 빌드와 권한 서비스로 운영한다. 최초 7일 전체 기능 체험, 이후 Google 연동 제한, 결제 전 리딤코드 활성화 가능성을 검토한다. 단순 로컬 플래그만으로 Plus를 판정하지 않는다.

### 2.2.3에서 제외

- Windows 셸 우클릭 일정 등록
- Free/Plus 결제·리딤코드 실제 구현
- 주간·일간·일정 탭
- 일정별 개별 색상
- 활성 사용자 통계 서버 구현
