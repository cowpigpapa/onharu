# ONHARU 2.1 개발 목록

기준: ONHARU 2.0의 안정적인 Explorer 레이어·Google 동기화·V1 동일 기능은 유지한다. 2.1은 편의 기능을 추가하되 2.0 사용자 일정과 설정을 손상시키지 않는 소규모 업그레이드다.

## P0 — 기반과 회귀 방지

- [x] 2.0 소스에서 독립된 `Onharu_v2.1` 개발 폴더 생성
- [x] 제품/설치 파일 버전을 2.1.0으로 변경
- [x] 2.0 설정을 읽는 전진 호환 마이그레이션 추가
- [x] 앱·x64 Explorer 레이어 빌드와 자동 회귀 검사 13종 통과
- [x] 실제 Explorer 레이어·아이콘 상호작용 기본 수동 검사 통과
- [ ] 일반 사용자 PC에서 Google OAuth 게시 후 최종 왕복 동기화 검사

## P1 — 일정 등록 편의

- [x] 맞춤 알림: 없음·정해진 시간 외에 사용자가 분/시간/일 단위 값을 지정
- [x] 일정 등록 기본값: 기본 캘린더, 종일 여부, 시작 시간, 기본 소요 시간, 기본 알림
- [x] 새 일정 창은 기본값을 적용하고 기존 일정 수정에는 저장된 값을 우선

완료 기준: 설정 저장 후 다음 새 일정부터 즉시 반영되고, 기존 일정 수정값은 변하지 않는다.

## P1 — 달력 탐색과 표시

- [x] 제목을 눌러 연·월로 바로 이동
- [x] 완료 일정 표시 방식: 그대로 표시 / 흐리게 / 숨김
- [x] 검색 기간 선택: 전체 / 과거 1년 / 앞으로 1년 / 사용자 지정 시작·종료일
- [x] 시작 시 표시 범위: 오늘 / 마지막으로 본 날짜

완료 기준: 월·주 표시 모드 모두에서 이동과 복귀가 정확하고 Google/로컬 일정 필터가 동일하게 적용된다.

## P1 — 반복과 기념일

- [x] 반복 종료 방식에 날짜 외 `N회 후 종료` 추가
- [x] 기념일·D-Day 표시 추가
- [x] D-Day/D-n/D+n 표기와 기존 매년 반복 옵션 연계
- [x] 일반 D-Day 카드와 전용 기념일 카드 분리, 각각 접기·펼치기와 기본 5개 표시
- [x] 일정 등록·수정에서 로컬 기념일 전환 및 기념일에서 일반/D-Day 일정으로 역변환
- [x] 기념일 전용 등록·수정, 최대 10개 제한, 누적 일수와 다음 기념일 표시

완료 기준: 윤년·월말·매월 n번째 요일 반복과 함께 기존 반복 일정 회귀가 없다.

## P2 — 생활 편의

- [x] 알림 소리 사용 여부와 조용한 시간
- [x] 시작 시 위치 고정/조정 상태 선택

## 제외 또는 후순위

- 자체 클라우드·팀 계정·VIP 체계: Google Calendar가 현재 역할을 충족하므로 2.1에서 제외
- 메모·시간표·인쇄 엔진: 범위가 커서 2.2 이후 별도 검토
- Explorer 합성 구조 재작성: 2.0에서 안정화된 경로를 유지

## 개발 원칙

1. `Onharu_v2`는 2.0 동결본이므로 수정하지 않는다.
2. 모든 변경은 이 폴더의 `HANDOFF.md`에 기록한다.
3. 사용자 데이터 형식은 추가 필드만 허용하고 기존 필드 의미를 바꾸지 않는다.
4. 기능별 최소 자동 검사와 App 빌드를 통과한 뒤 다음 항목으로 이동한다. 전체 검사는 `check-v21.ps1 -Build`로 실행한다.
5. 배포 설치본은 P0/P1 회귀 검증 완료 전 생성하지 않는다.
